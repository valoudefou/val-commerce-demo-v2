/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 34:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isCallable = __webpack_require__(4901);

module.exports = function (it) {
  return typeof it == 'object' ? it !== null : isCallable(it);
};


/***/ }),

/***/ 44:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(4782);
__webpack_require__(7495);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.SharedActionTracking = void 0;
__webpack_require__(2008);
__webpack_require__(2062);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _index = __webpack_require__(5588);
var _type = __webpack_require__(6421);
var _utils = __webpack_require__(6517);
var _Event = __webpack_require__(8796);
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
class SharedActionTracking {
  constructor({
    sdkConfig
  }) {
    (0, _defineProperty2.default)(this, "visitor", null);
    this.trustedNonces = {};
    this.sdkConfig = sdkConfig;
  }
  generateNonce() {
    if (!(0, _utils.isBrowser)() || !this.visitor || !this.visitor.hasConsented) {
      return undefined;
    }
    const nonce = Math.random().toString(36).substring(2) + Date.now().toString(36);
    this.trustedNonces[nonce] = false;
    return nonce;
  }
  initialize(visitor) {
    var _window2;
    this.visitor = visitor;
    this.initTimestamp = Date.now();
    if (!(0, _utils.isBrowser)()) {
      return;
    }
    if (this.onMessageReceived) {
      var _window;
      (_window = window) == null || _window.removeEventListener('message', this.onMessageReceived);
    }
    this.onMessageReceived = event => {
      this.handleMessage(event);
    };
    (_window2 = window) == null || _window2.addEventListener('message', this.onMessageReceived);
  }
  processHit(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this$visitor, _this$visitor2, _this$visitor3;
      if ((hit == null ? void 0 : hit.ec) !== _index.EventCategory.ACTION_TRACKING || !hit.ea) {
        (0, _utils.logDebugSprintf)(_this.sdkConfig, _FlagshipConstant.ACTION_TRACKING, _FlagshipConstant.ACTION_TRACKING_INVALID_HIT, hit);
        return;
      }
      const eventHit = new _Event.Event({
        category: hit.ec,
        action: hit.ea,
        label: hit.el,
        value: hit.ev,
        visitorId: (_this$visitor = _this.visitor) == null ? void 0 : _this$visitor.visitorId,
        anonymousId: (_this$visitor2 = _this.visitor) == null ? void 0 : _this$visitor2.anonymousId,
        isActionTrackingHit: true
      });
      (_this$visitor3 = _this.visitor) == null || _this$visitor3.addInTrackingManager(eventHit);
      (0, _utils.logDebugSprintf)(_this.sdkConfig, _FlagshipConstant.ACTION_TRACKING, _FlagshipConstant.ACTION_TRACKING_HIT_RECEIVED, hit);
    })();
  }
  handleMessage(event) {
    var _this$visitor4;
    if (!(event != null && event.data) || (event == null ? void 0 : event.origin) !== window.location.origin || !((_this$visitor4 = this.visitor) != null && _this$visitor4.hasConsented)) {
      return;
    }
    const payload = event.data;
    if (payload.action !== _type.SharedActionSource.ABT_TAG_TRACK_ACTION || !payload.nonce) {
      return;
    }
    const nonce = payload.nonce;
    if (this.trustedNonces[nonce] === undefined || this.trustedNonces[nonce]) {
      (0, _utils.logDebugSprintf)(this.sdkConfig, _FlagshipConstant.ACTION_TRACKING, _FlagshipConstant.ACTION_TRACKING_INVALID_NONCE, nonce);
      return;
    }
    this.trustedNonces[nonce] = true;
    const hits = payload.data;
    if (!hits || !hits.length) {
      return;
    }
    var _iterator = _createForOfIteratorHelper(hits),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        const hit = _step.value;
        this.processHit(hit);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
  }
  dispatchEventHits(hits) {
    if (!this.shouldProcessHits(hits)) {
      return;
    }
    const nonce = this.getNonce();
    if (!nonce) {
      return;
    }
    const hitsToDispatch = this.filterHitsToDispatch(hits);
    if (!hitsToDispatch.length) {
      return;
    }
    this.postHits(hitsToDispatch, nonce);
  }
  shouldProcessHits(hits) {
    return (0, _utils.isBrowser)() && !!this.visitor && !!hits && hits.length > 0;
  }
  getNonce() {
    var _window$ABTasty, _window$ABTasty$_getA;
    return (_window$ABTasty = window.ABTasty) == null || (_window$ABTasty = _window$ABTasty.api) == null || (_window$ABTasty = _window$ABTasty.internal) == null || (_window$ABTasty$_getA = _window$ABTasty._getActionTrackingNonce) == null ? void 0 : _window$ABTasty$_getA.call(_window$ABTasty);
  }
  filterHitsToDispatch(hits) {
    return hits.filter(hit => {
      var _this$visitor5, _this$visitor6;
      const isHitPostInit = this.initTimestamp && hit.createdAt >= this.initTimestamp;
      const isHitForVisitor = hit.visitorId === ((_this$visitor5 = this.visitor) == null ? void 0 : _this$visitor5.visitorId) || hit.visitorId === ((_this$visitor6 = this.visitor) == null ? void 0 : _this$visitor6.anonymousId);
      return hit.data.ec === _index.EventCategory.ACTION_TRACKING && isHitPostInit && isHitForVisitor;
    }).map(hit => hit.data);
  }
  postHits(hitsToDispatch, nonce) {
    var _window3;
    const payload = {
      action: _type.SharedActionSource.ABT_WEB_SDK_TRACK_ACTION,
      data: hitsToDispatch,
      nonce,
      timestamp: Date.now()
    };
    (_window3 = window) == null || _window3.postMessage(payload, window.location.origin);
    (0, _utils.logDebugSprintf)(this.sdkConfig, _FlagshipConstant.ACTION_TRACKING, _FlagshipConstant.ACTION_TRACKING_DISPATCHED, hitsToDispatch);
  }
}
exports.SharedActionTracking = SharedActionTracking;

/***/ }),

/***/ 79:
/***/ ((module) => {

function _arrayLikeToArray(r, a) {
  (null == a || a > r.length) && (a = r.length);
  for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
  return n;
}
module.exports = _arrayLikeToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 81:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var aCallable = __webpack_require__(9306);
var anObject = __webpack_require__(8551);
var tryToString = __webpack_require__(6823);
var getIteratorMethod = __webpack_require__(851);

var $TypeError = TypeError;

module.exports = function (argument, usingIterator) {
  var iteratorMethod = arguments.length < 2 ? getIteratorMethod(argument) : usingIterator;
  if (aCallable(iteratorMethod)) return anObject(call(iteratorMethod, argument));
  throw new $TypeError(tryToString(argument) + ' is not iterable');
};


/***/ }),

/***/ 82:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.ApiManager = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _DecisionManager = __webpack_require__(5232);
class ApiManager extends _DecisionManager.DecisionManager {
  getCampaignsAsync(visitor) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      return _this.getDecisionApiCampaignsAsync(visitor);
    })();
  }
}
exports.ApiManager = ApiManager;

/***/ }),

/***/ 280:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var getBuiltIn = __webpack_require__(7751);
var IS_PURE = __webpack_require__(6395);
var NativePromiseConstructor = __webpack_require__(550);
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(916).CONSTRUCTOR);
var promiseResolve = __webpack_require__(3438);

var PromiseConstructorWrapper = getBuiltIn('Promise');
var CHECK_WRAPPER = IS_PURE && !FORCED_PROMISE_CONSTRUCTOR;

// `Promise.resolve` method
// https://tc39.es/ecma262/#sec-promise.resolve
$({ target: 'Promise', stat: true, forced: IS_PURE || FORCED_PROMISE_CONSTRUCTOR }, {
  resolve: function resolve(x) {
    return promiseResolve(CHECK_WRAPPER && this === PromiseConstructorWrapper ? NativePromiseConstructor : this, x);
  }
});


/***/ }),

/***/ 283:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var fails = __webpack_require__(9039);
var isCallable = __webpack_require__(4901);
var hasOwn = __webpack_require__(9297);
var DESCRIPTORS = __webpack_require__(3724);
var CONFIGURABLE_FUNCTION_NAME = (__webpack_require__(350).CONFIGURABLE);
var inspectSource = __webpack_require__(3706);
var InternalStateModule = __webpack_require__(1181);

var enforceInternalState = InternalStateModule.enforce;
var getInternalState = InternalStateModule.get;
var $String = String;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;
var stringSlice = uncurryThis(''.slice);
var replace = uncurryThis(''.replace);
var join = uncurryThis([].join);

var CONFIGURABLE_LENGTH = DESCRIPTORS && !fails(function () {
  return defineProperty(function () { /* empty */ }, 'length', { value: 8 }).length !== 8;
});

var TEMPLATE = String(String).split('String');

var makeBuiltIn = module.exports = function (value, name, options) {
  if (stringSlice($String(name), 0, 7) === 'Symbol(') {
    name = '[' + replace($String(name), /^Symbol\(([^)]*)\).*$/, '$1') + ']';
  }
  if (options && options.getter) name = 'get ' + name;
  if (options && options.setter) name = 'set ' + name;
  if (!hasOwn(value, 'name') || (CONFIGURABLE_FUNCTION_NAME && value.name !== name)) {
    if (DESCRIPTORS) defineProperty(value, 'name', { value: name, configurable: true });
    else value.name = name;
  }
  if (CONFIGURABLE_LENGTH && options && hasOwn(options, 'arity') && value.length !== options.arity) {
    defineProperty(value, 'length', { value: options.arity });
  }
  try {
    if (options && hasOwn(options, 'constructor') && options.constructor) {
      if (DESCRIPTORS) defineProperty(value, 'prototype', { writable: false });
    // in V8 ~ Chrome 53, prototypes of some methods, like `Array.prototype.values`, are non-writable
    } else if (value.prototype) value.prototype = undefined;
  } catch (error) { /* empty */ }
  var state = enforceInternalState(value);
  if (!hasOwn(state, 'source')) {
    state.source = join(TEMPLATE, typeof name == 'string' ? name : '');
  } return value;
};

// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
// eslint-disable-next-line no-extend-native -- required
Function.prototype.toString = makeBuiltIn(function toString() {
  return isCallable(this) && getInternalState(this).source || inspectSource(this);
}, 'toString');


/***/ }),

/***/ 298:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* eslint-disable es/no-object-getownpropertynames -- safe */
var classof = __webpack_require__(4576);
var toIndexedObject = __webpack_require__(5397);
var $getOwnPropertyNames = (__webpack_require__(8480).f);
var arraySlice = __webpack_require__(7680);

var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
  ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function (it) {
  try {
    return $getOwnPropertyNames(it);
  } catch (error) {
    return arraySlice(windowNames);
  }
};

// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
module.exports.f = function getOwnPropertyNames(it) {
  return windowNames && classof(it) === 'Window'
    ? getWindowNames(it)
    : $getOwnPropertyNames(toIndexedObject(it));
};


/***/ }),

/***/ 350:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var hasOwn = __webpack_require__(9297);

var FunctionPrototype = Function.prototype;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;

var EXISTS = hasOwn(FunctionPrototype, 'name');
// additional protection from minified / mangled / dropped function names
var PROPER = EXISTS && (function something() { /* empty */ }).name === 'something';
var CONFIGURABLE = EXISTS && (!DESCRIPTORS || (DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable));

module.exports = {
  EXISTS: EXISTS,
  PROPER: PROPER,
  CONFIGURABLE: CONFIGURABLE
};


/***/ }),

/***/ 397:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var getBuiltIn = __webpack_require__(7751);

module.exports = getBuiltIn('document', 'documentElement');


/***/ }),

/***/ 421:
/***/ ((module) => {

"use strict";

module.exports = {};


/***/ }),

/***/ 436:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var IS_PURE = __webpack_require__(6395);
var IS_NODE = __webpack_require__(6193);
var globalThis = __webpack_require__(2195);
var call = __webpack_require__(9565);
var defineBuiltIn = __webpack_require__(6840);
var setPrototypeOf = __webpack_require__(2967);
var setToStringTag = __webpack_require__(687);
var setSpecies = __webpack_require__(7633);
var aCallable = __webpack_require__(9306);
var isCallable = __webpack_require__(4901);
var isObject = __webpack_require__(34);
var anInstance = __webpack_require__(679);
var speciesConstructor = __webpack_require__(2293);
var task = (__webpack_require__(9225).set);
var microtask = __webpack_require__(1955);
var hostReportErrors = __webpack_require__(3138);
var perform = __webpack_require__(1103);
var Queue = __webpack_require__(8265);
var InternalStateModule = __webpack_require__(1181);
var NativePromiseConstructor = __webpack_require__(550);
var PromiseConstructorDetection = __webpack_require__(916);
var newPromiseCapabilityModule = __webpack_require__(6043);

var PROMISE = 'Promise';
var FORCED_PROMISE_CONSTRUCTOR = PromiseConstructorDetection.CONSTRUCTOR;
var NATIVE_PROMISE_REJECTION_EVENT = PromiseConstructorDetection.REJECTION_EVENT;
var NATIVE_PROMISE_SUBCLASSING = PromiseConstructorDetection.SUBCLASSING;
var getInternalPromiseState = InternalStateModule.getterFor(PROMISE);
var setInternalState = InternalStateModule.set;
var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var PromiseConstructor = NativePromiseConstructor;
var PromisePrototype = NativePromisePrototype;
var TypeError = globalThis.TypeError;
var document = globalThis.document;
var process = globalThis.process;
var newPromiseCapability = newPromiseCapabilityModule.f;
var newGenericPromiseCapability = newPromiseCapability;

var DISPATCH_EVENT = !!(document && document.createEvent && globalThis.dispatchEvent);
var UNHANDLED_REJECTION = 'unhandledrejection';
var REJECTION_HANDLED = 'rejectionhandled';
var PENDING = 0;
var FULFILLED = 1;
var REJECTED = 2;
var HANDLED = 1;
var UNHANDLED = 2;

var Internal, OwnPromiseCapability, PromiseWrapper, nativeThen;

// helpers
var isThenable = function (it) {
  var then;
  return isObject(it) && isCallable(then = it.then) ? then : false;
};

var callReaction = function (reaction, state) {
  var value = state.value;
  var ok = state.state === FULFILLED;
  var handler = ok ? reaction.ok : reaction.fail;
  var resolve = reaction.resolve;
  var reject = reaction.reject;
  var domain = reaction.domain;
  var result, then, exited;
  try {
    if (handler) {
      if (!ok) {
        if (state.rejection === UNHANDLED) onHandleUnhandled(state);
        state.rejection = HANDLED;
      }
      if (handler === true) result = value;
      else {
        if (domain) domain.enter();
        result = handler(value); // can throw
        if (domain) {
          domain.exit();
          exited = true;
        }
      }
      if (result === reaction.promise) {
        reject(new TypeError('Promise-chain cycle'));
      } else if (then = isThenable(result)) {
        call(then, result, resolve, reject);
      } else resolve(result);
    } else reject(value);
  } catch (error) {
    if (domain && !exited) domain.exit();
    reject(error);
  }
};

var notify = function (state, isReject) {
  if (state.notified) return;
  state.notified = true;
  microtask(function () {
    var reactions = state.reactions;
    var reaction;
    while (reaction = reactions.get()) {
      callReaction(reaction, state);
    }
    state.notified = false;
    if (isReject && !state.rejection) onUnhandled(state);
  });
};

var dispatchEvent = function (name, promise, reason) {
  var event, handler;
  if (DISPATCH_EVENT) {
    event = document.createEvent('Event');
    event.promise = promise;
    event.reason = reason;
    event.initEvent(name, false, true);
    globalThis.dispatchEvent(event);
  } else event = { promise: promise, reason: reason };
  if (!NATIVE_PROMISE_REJECTION_EVENT && (handler = globalThis['on' + name])) handler(event);
  else if (name === UNHANDLED_REJECTION) hostReportErrors('Unhandled promise rejection', reason);
};

var onUnhandled = function (state) {
  call(task, globalThis, function () {
    var promise = state.facade;
    var value = state.value;
    var IS_UNHANDLED = isUnhandled(state);
    var result;
    if (IS_UNHANDLED) {
      result = perform(function () {
        if (IS_NODE) {
          process.emit('unhandledRejection', value, promise);
        } else dispatchEvent(UNHANDLED_REJECTION, promise, value);
      });
      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
      state.rejection = IS_NODE || isUnhandled(state) ? UNHANDLED : HANDLED;
      if (result.error) throw result.value;
    }
  });
};

var isUnhandled = function (state) {
  return state.rejection !== HANDLED && !state.parent;
};

var onHandleUnhandled = function (state) {
  call(task, globalThis, function () {
    var promise = state.facade;
    if (IS_NODE) {
      process.emit('rejectionHandled', promise);
    } else dispatchEvent(REJECTION_HANDLED, promise, state.value);
  });
};

var bind = function (fn, state, unwrap) {
  return function (value) {
    fn(state, value, unwrap);
  };
};

var internalReject = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  state.value = value;
  state.state = REJECTED;
  notify(state, true);
};

var internalResolve = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  try {
    if (state.facade === value) throw new TypeError("Promise can't be resolved itself");
    var then = isThenable(value);
    if (then) {
      microtask(function () {
        var wrapper = { done: false };
        try {
          call(then, value,
            bind(internalResolve, wrapper, state),
            bind(internalReject, wrapper, state)
          );
        } catch (error) {
          internalReject(wrapper, error, state);
        }
      });
    } else {
      state.value = value;
      state.state = FULFILLED;
      notify(state, false);
    }
  } catch (error) {
    internalReject({ done: false }, error, state);
  }
};

// constructor polyfill
if (FORCED_PROMISE_CONSTRUCTOR) {
  // 25.4.3.1 Promise(executor)
  PromiseConstructor = function Promise(executor) {
    anInstance(this, PromisePrototype);
    aCallable(executor);
    call(Internal, this);
    var state = getInternalPromiseState(this);
    try {
      executor(bind(internalResolve, state), bind(internalReject, state));
    } catch (error) {
      internalReject(state, error);
    }
  };

  PromisePrototype = PromiseConstructor.prototype;

  // eslint-disable-next-line no-unused-vars -- required for `.length`
  Internal = function Promise(executor) {
    setInternalState(this, {
      type: PROMISE,
      done: false,
      notified: false,
      parent: false,
      reactions: new Queue(),
      rejection: false,
      state: PENDING,
      value: null
    });
  };

  // `Promise.prototype.then` method
  // https://tc39.es/ecma262/#sec-promise.prototype.then
  Internal.prototype = defineBuiltIn(PromisePrototype, 'then', function then(onFulfilled, onRejected) {
    var state = getInternalPromiseState(this);
    var reaction = newPromiseCapability(speciesConstructor(this, PromiseConstructor));
    state.parent = true;
    reaction.ok = isCallable(onFulfilled) ? onFulfilled : true;
    reaction.fail = isCallable(onRejected) && onRejected;
    reaction.domain = IS_NODE ? process.domain : undefined;
    if (state.state === PENDING) state.reactions.add(reaction);
    else microtask(function () {
      callReaction(reaction, state);
    });
    return reaction.promise;
  });

  OwnPromiseCapability = function () {
    var promise = new Internal();
    var state = getInternalPromiseState(promise);
    this.promise = promise;
    this.resolve = bind(internalResolve, state);
    this.reject = bind(internalReject, state);
  };

  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
    return C === PromiseConstructor || C === PromiseWrapper
      ? new OwnPromiseCapability(C)
      : newGenericPromiseCapability(C);
  };

  if (!IS_PURE && isCallable(NativePromiseConstructor) && NativePromisePrototype !== Object.prototype) {
    nativeThen = NativePromisePrototype.then;

    if (!NATIVE_PROMISE_SUBCLASSING) {
      // make `Promise#then` return a polyfilled `Promise` for native promise-based APIs
      defineBuiltIn(NativePromisePrototype, 'then', function then(onFulfilled, onRejected) {
        var that = this;
        return new PromiseConstructor(function (resolve, reject) {
          call(nativeThen, that, resolve, reject);
        }).then(onFulfilled, onRejected);
      // https://github.com/zloirock/core-js/issues/640
      }, { unsafe: true });
    }

    // make `.constructor === Promise` work for native promise-based APIs
    try {
      delete NativePromisePrototype.constructor;
    } catch (error) { /* empty */ }

    // make `instanceof Promise` work for native promise-based APIs
    if (setPrototypeOf) {
      setPrototypeOf(NativePromisePrototype, PromisePrototype);
    }
  }
}

// `Promise` constructor
// https://tc39.es/ecma262/#sec-promise-executor
$({ global: true, constructor: true, wrap: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  Promise: PromiseConstructor
});

setToStringTag(PromiseConstructor, PROMISE, false, true);
setSpecies(PROMISE);


/***/ }),

/***/ 448:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.FSFlagCollection = void 0;
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(6033);
__webpack_require__(3362);
__webpack_require__(9479);
__webpack_require__(1415);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _utils = __webpack_require__(6517);
var _FsFlags = __webpack_require__(4707);
var _FlagshipConstant = __webpack_require__(2932);
/**
 * Represents a collection of flags.
 */
class FSFlagCollection {
  /**
     * Creates a new instance of FSFlagCollection.
     * @param param - The parameter object.
     * @param param.visitor - The visitor delegate.
     * @param param.flags - The initial flags.
     */
  constructor(param) {
    (0, _defineProperty2.default)(this, "_keys", new Set());
    const visitor = param.visitor,
      flags = param.flags;
    this._visitor = visitor;
    this._flags = flags || new Map();
    if (this._flags.size === 0) {
      this._keys = new Set(visitor == null ? void 0 : visitor.flagsData.keys());
      this._keys.forEach(key => {
        this._flags.set(key, new _FsFlags.FSFlag({
          key,
          visitor
        }));
      });
    } else {
      this._keys = new Set(this._flags.keys());
    }
  }

  /**
   * @inheritdoc
   */
  get size() {
    return this._keys.size;
  }

  /**
   * @inheritdoc
   */
  get(key) {
    const flag = this._flags.get(key);
    if (!flag) {
      var _this$_visitor;
      if ((_this$_visitor = this._visitor) != null && _this$_visitor.config) {
        (0, _utils.logWarningSprintf)(this._visitor.config, _FlagshipConstant.GET_FLAG, _FlagshipConstant.GET_FLAG_NOT_FOUND, this._visitor.visitorId, key);
      }
      return new _FsFlags.FSFlag({
        key
      });
    }
    return flag;
  }

  /**
   * @inheritdoc
   */
  has(key) {
    return this._keys.has(key);
  }

  /**
   * @inheritdoc
   */
  keys() {
    return this._keys;
  }

  /**
   * @inheritdoc
   */
  [Symbol.iterator]() {
    let index = 0;
    const keysArray = Array.from(this._keys);
    return {
      next: () => {
        if (index < keysArray.length) {
          const key = keysArray[index++];
          return {
            value: [key, this._flags.get(key)],
            done: false
          };
        } else {
          return {
            value: null,
            done: true
          };
        }
      }
    };
  }

  /**
   * @inheritdoc
   */
  filter(predicate) {
    const flags = new Map();
    this._flags.forEach((flag, key) => {
      if (predicate(flag, key, this)) {
        flags.set(key, flag);
      }
    });
    return new FSFlagCollection({
      visitor: this._visitor,
      flags
    });
  }

  /**
   * @inheritdoc
   */
  exposeAll() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield Promise.all(Array.from(_this._flags.values(), flag => flag.visitorExposed()));
    })();
  }

  /**
   * @inheritdoc
   */
  getMetadata() {
    const metadata = new Map();
    this._flags.forEach((flag, key) => {
      metadata.set(key, flag.metadata);
    });
    return metadata;
  }

  /**
   * @inheritdoc
   */
  toJSON() {
    const serializedData = [];
    this._flags.forEach((flag, key) => {
      const metadata = flag.metadata;
      serializedData.push({
        key,
        campaignId: metadata.campaignId,
        campaignName: metadata.campaignName,
        variationGroupId: metadata.variationGroupId,
        variationGroupName: metadata.variationGroupName,
        variationId: metadata.variationId,
        variationName: metadata.variationName,
        isReference: metadata.isReference,
        campaignType: metadata.campaignType,
        slug: metadata.slug,
        hex: (0, _utils.valueToHex)({
          v: flag.getValue(null, false)
        })
      });
    });
    return serializedData;
  }
  forEach(callbackfn) {
    this._flags.forEach((flag, key) => {
      callbackfn(flag, key, this);
    });
  }
}
exports.FSFlagCollection = FSFlagCollection;

/***/ }),

/***/ 509:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BucketingConfig = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _DecisionMode = __webpack_require__(8884);
var _FlagshipConfig = __webpack_require__(7999);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
class BucketingConfig extends _FlagshipConfig.FlagshipConfig {
  constructor(param) {
    var _param$pollingInterva;
    super(_objectSpread(_objectSpread({}, param), {}, {
      decisionMode: _DecisionMode.DecisionMode.BUCKETING
    }));
    this.pollingInterval = (_param$pollingInterva = param == null ? void 0 : param.pollingInterval) != null ? _param$pollingInterva : _index.DEFAULT_POLLING_INTERVAL;
    this.onBucketingUpdated = param == null ? void 0 : param.onBucketingUpdated;
    this.fetchThirdPartyData = param == null ? void 0 : param.fetchThirdPartyData;
  }
}
exports.BucketingConfig = BucketingConfig;

/***/ }),

/***/ 511:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isRegExp = __webpack_require__(788);

var $TypeError = TypeError;

module.exports = function (it) {
  if (isRegExp(it)) {
    throw new $TypeError("The method doesn't accept regular expressions");
  } return it;
};


/***/ }),

/***/ 533:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// https://github.com/tc39/proposal-string-pad-start-end
var uncurryThis = __webpack_require__(9504);
var toLength = __webpack_require__(8014);
var toString = __webpack_require__(655);
var $repeat = __webpack_require__(2333);
var requireObjectCoercible = __webpack_require__(7750);

var repeat = uncurryThis($repeat);
var stringSlice = uncurryThis(''.slice);
var ceil = Math.ceil;

// `String.prototype.{ padStart, padEnd }` methods implementation
var createMethod = function (IS_END) {
  return function ($this, maxLength, fillString) {
    var S = toString(requireObjectCoercible($this));
    var intMaxLength = toLength(maxLength);
    var stringLength = S.length;
    var fillStr = fillString === undefined ? ' ' : toString(fillString);
    var fillLen, stringFiller;
    if (intMaxLength <= stringLength || fillStr === '') return S;
    fillLen = intMaxLength - stringLength;
    stringFiller = repeat(fillStr, ceil(fillLen / fillStr.length));
    if (stringFiller.length > fillLen) stringFiller = stringSlice(stringFiller, 0, fillLen);
    return IS_END ? S + stringFiller : stringFiller + S;
  };
};

module.exports = {
  // `String.prototype.padStart` method
  // https://tc39.es/ecma262/#sec-string.prototype.padstart
  start: createMethod(false),
  // `String.prototype.padEnd` method
  // https://tc39.es/ecma262/#sec-string.prototype.padend
  end: createMethod(true)
};


/***/ }),

/***/ 537:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var NativePromiseConstructor = __webpack_require__(550);
var checkCorrectnessOfIteration = __webpack_require__(4428);
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(916).CONSTRUCTOR);

module.exports = FORCED_PROMISE_CONSTRUCTOR || !checkCorrectnessOfIteration(function (iterable) {
  NativePromiseConstructor.all(iterable).then(undefined, function () { /* empty */ });
});


/***/ }),

/***/ 550:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);

module.exports = globalThis.Promise;


/***/ }),

/***/ 564:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.TrackingManagerConfig = void 0;
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
class TrackingManagerConfig {
  constructor(param) {
    this.batchIntervals = param == null ? void 0 : param.batchIntervals;
    this.poolMaxSize = param == null ? void 0 : param.poolMaxSize;
    this._batchStrategy = this.getCacheStrategy(param == null ? void 0 : param.cacheStrategy);
  }
  getCacheStrategy(cacheStrategy) {
    if (typeof cacheStrategy === 'number') {
      return cacheStrategy;
    }
    return (0, _utils.isBrowser)() ? _index.CacheStrategy.CONTINUOUS_CACHING : _index.CacheStrategy.PERIODIC_CACHING;
  }
  get batchIntervals() {
    return this._batchIntervals;
  }
  set batchIntervals(v) {
    if (typeof v !== 'number' || v < 0 || v > 10800) {
      v = (0, _utils.isBrowser)() ? _index.DEFAULT_BROWSER_TIME_INTERVAL : _index.DEFAULT_SERVER_TIME_INTERVAL;
    }
    this._batchIntervals = v;
  }
  get poolMaxSize() {
    return this._poolMaxSize;
  }
  set poolMaxSize(v) {
    if (typeof v !== 'number' || v < 5) {
      v = (0, _utils.isBrowser)() ? _index.DEFAULT_BROWSER_POOL_MAX_SIZE : _index.DEFAULT_SERVER_POOL_MAX_SIZE;
    }
    this._poolMaxSize = v;
  }
  get cacheStrategy() {
    return this._batchStrategy;
  }
}
exports.TrackingManagerConfig = TrackingManagerConfig;

/***/ }),

/***/ 568:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.EventCategory = void 0;
let EventCategory = exports.EventCategory = /*#__PURE__*/function (EventCategory) {
  EventCategory["ACTION_TRACKING"] = "Action Tracking";
  EventCategory["USER_ENGAGEMENT"] = "User Engagement";
  return EventCategory;
}({});

/***/ }),

/***/ 597:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var wellKnownSymbol = __webpack_require__(8227);
var V8_VERSION = __webpack_require__(9519);

var SPECIES = wellKnownSymbol('species');

module.exports = function (METHOD_NAME) {
  // We can't use this feature detection in V8 since it causes
  // deoptimization and serious performance degradation
  // https://github.com/zloirock/core-js/issues/677
  return V8_VERSION >= 51 || !fails(function () {
    var array = [];
    var constructor = array.constructor = {};
    constructor[SPECIES] = function () {
      return { foo: 1 };
    };
    return array[METHOD_NAME](Boolean).foo !== 1;
  });
};


/***/ }),

/***/ 616:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-function-prototype-bind -- safe
  var test = (function () { /* empty */ }).bind();
  // eslint-disable-next-line no-prototype-builtins -- safe
  return typeof test != 'function' || test.hasOwnProperty('prototype');
});


/***/ }),

/***/ 655:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var classof = __webpack_require__(6955);

var $String = String;

module.exports = function (argument) {
  if (classof(argument) === 'Symbol') throw new TypeError('Cannot convert a Symbol value to a string');
  return $String(argument);
};


/***/ }),

/***/ 679:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isPrototypeOf = __webpack_require__(1625);

var $TypeError = TypeError;

module.exports = function (it, Prototype) {
  if (isPrototypeOf(Prototype, it)) return it;
  throw new $TypeError('Incorrect invocation');
};


/***/ }),

/***/ 687:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var defineProperty = (__webpack_require__(4913).f);
var hasOwn = __webpack_require__(9297);
var wellKnownSymbol = __webpack_require__(8227);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');

module.exports = function (target, TAG, STATIC) {
  if (target && !STATIC) target = target.prototype;
  if (target && !hasOwn(target, TO_STRING_TAG)) {
    defineProperty(target, TO_STRING_TAG, { configurable: true, value: TAG });
  }
};


/***/ }),

/***/ 741:
/***/ ((module) => {

"use strict";

var ceil = Math.ceil;
var floor = Math.floor;

// `Math.trunc` method
// https://tc39.es/ecma262/#sec-math.trunc
// eslint-disable-next-line es/no-math-trunc -- safe
module.exports = Math.trunc || function trunc(x) {
  var n = +x;
  return (n > 0 ? floor : ceil)(n);
};


/***/ }),

/***/ 757:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var getBuiltIn = __webpack_require__(7751);
var isCallable = __webpack_require__(4901);
var isPrototypeOf = __webpack_require__(1625);
var USE_SYMBOL_AS_UID = __webpack_require__(7040);

var $Object = Object;

module.exports = USE_SYMBOL_AS_UID ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  var $Symbol = getBuiltIn('Symbol');
  return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
};


/***/ }),

/***/ 788:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isObject = __webpack_require__(34);
var classof = __webpack_require__(4576);
var wellKnownSymbol = __webpack_require__(8227);

var MATCH = wellKnownSymbol('match');

// `IsRegExp` abstract operation
// https://tc39.es/ecma262/#sec-isregexp
module.exports = function (it) {
  var isRegExp;
  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : classof(it) === 'RegExp');
};


/***/ }),

/***/ 851:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var classof = __webpack_require__(6955);
var getMethod = __webpack_require__(5966);
var isNullOrUndefined = __webpack_require__(4117);
var Iterators = __webpack_require__(6269);
var wellKnownSymbol = __webpack_require__(8227);

var ITERATOR = wellKnownSymbol('iterator');

module.exports = function (it) {
  if (!isNullOrUndefined(it)) return getMethod(it, ITERATOR)
    || getMethod(it, '@@iterator')
    || Iterators[classof(it)];
};


/***/ }),

/***/ 916:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var NativePromiseConstructor = __webpack_require__(550);
var isCallable = __webpack_require__(4901);
var isForced = __webpack_require__(2796);
var inspectSource = __webpack_require__(3706);
var wellKnownSymbol = __webpack_require__(8227);
var ENVIRONMENT = __webpack_require__(4215);
var IS_PURE = __webpack_require__(6395);
var V8_VERSION = __webpack_require__(9519);

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var SPECIES = wellKnownSymbol('species');
var SUBCLASSING = false;
var NATIVE_PROMISE_REJECTION_EVENT = isCallable(globalThis.PromiseRejectionEvent);

var FORCED_PROMISE_CONSTRUCTOR = isForced('Promise', function () {
  var PROMISE_CONSTRUCTOR_SOURCE = inspectSource(NativePromiseConstructor);
  var GLOBAL_CORE_JS_PROMISE = PROMISE_CONSTRUCTOR_SOURCE !== String(NativePromiseConstructor);
  // V8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
  // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
  // We can't detect it synchronously, so just check versions
  if (!GLOBAL_CORE_JS_PROMISE && V8_VERSION === 66) return true;
  // We need Promise#{ catch, finally } in the pure version for preventing prototype pollution
  if (IS_PURE && !(NativePromisePrototype['catch'] && NativePromisePrototype['finally'])) return true;
  // We can't use @@species feature detection in V8 since it causes
  // deoptimization and performance degradation
  // https://github.com/zloirock/core-js/issues/679
  if (!V8_VERSION || V8_VERSION < 51 || !/native code/.test(PROMISE_CONSTRUCTOR_SOURCE)) {
    // Detect correctness of subclassing with @@species support
    var promise = new NativePromiseConstructor(function (resolve) { resolve(1); });
    var FakePromise = function (exec) {
      exec(function () { /* empty */ }, function () { /* empty */ });
    };
    var constructor = promise.constructor = {};
    constructor[SPECIES] = FakePromise;
    SUBCLASSING = promise.then(function () { /* empty */ }) instanceof FakePromise;
    if (!SUBCLASSING) return true;
  // Unhandled rejections tracking support, NodeJS Promise without it fails @@species test
  } return !GLOBAL_CORE_JS_PROMISE && (ENVIRONMENT === 'BROWSER' || ENVIRONMENT === 'DENO') && !NATIVE_PROMISE_REJECTION_EVENT;
});

module.exports = {
  CONSTRUCTOR: FORCED_PROMISE_CONSTRUCTOR,
  REJECTION_EVENT: NATIVE_PROMISE_REJECTION_EVENT,
  SUBCLASSING: SUBCLASSING
};


/***/ }),

/***/ 1034:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var hasOwn = __webpack_require__(9297);
var isPrototypeOf = __webpack_require__(1625);
var regExpFlags = __webpack_require__(7979);

var RegExpPrototype = RegExp.prototype;

module.exports = function (R) {
  var flags = R.flags;
  return flags === undefined && !('flags' in RegExpPrototype) && !hasOwn(R, 'flags') && isPrototypeOf(RegExpPrototype, R)
    ? call(regExpFlags, R) : flags;
};


/***/ }),

/***/ 1056:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var defineProperty = (__webpack_require__(4913).f);

module.exports = function (Target, Source, key) {
  key in Target || defineProperty(Target, key, {
    configurable: true,
    get: function () { return Source[key]; },
    set: function (it) { Source[key] = it; }
  });
};


/***/ }),

/***/ 1071:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;

/***/ }),

/***/ 1072:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var internalObjectKeys = __webpack_require__(1828);
var enumBugKeys = __webpack_require__(8727);

// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es/no-object-keys -- safe
module.exports = Object.keys || function keys(O) {
  return internalObjectKeys(O, enumBugKeys);
};


/***/ }),

/***/ 1088:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var call = __webpack_require__(9565);
var IS_PURE = __webpack_require__(6395);
var FunctionName = __webpack_require__(350);
var isCallable = __webpack_require__(4901);
var createIteratorConstructor = __webpack_require__(3994);
var getPrototypeOf = __webpack_require__(2787);
var setPrototypeOf = __webpack_require__(2967);
var setToStringTag = __webpack_require__(687);
var createNonEnumerableProperty = __webpack_require__(6699);
var defineBuiltIn = __webpack_require__(6840);
var wellKnownSymbol = __webpack_require__(8227);
var Iterators = __webpack_require__(6269);
var IteratorsCore = __webpack_require__(7657);

var PROPER_FUNCTION_NAME = FunctionName.PROPER;
var CONFIGURABLE_FUNCTION_NAME = FunctionName.CONFIGURABLE;
var IteratorPrototype = IteratorsCore.IteratorPrototype;
var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
var ITERATOR = wellKnownSymbol('iterator');
var KEYS = 'keys';
var VALUES = 'values';
var ENTRIES = 'entries';

var returnThis = function () { return this; };

module.exports = function (Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
  createIteratorConstructor(IteratorConstructor, NAME, next);

  var getIterationMethod = function (KIND) {
    if (KIND === DEFAULT && defaultIterator) return defaultIterator;
    if (!BUGGY_SAFARI_ITERATORS && KIND && KIND in IterablePrototype) return IterablePrototype[KIND];

    switch (KIND) {
      case KEYS: return function keys() { return new IteratorConstructor(this, KIND); };
      case VALUES: return function values() { return new IteratorConstructor(this, KIND); };
      case ENTRIES: return function entries() { return new IteratorConstructor(this, KIND); };
    }

    return function () { return new IteratorConstructor(this); };
  };

  var TO_STRING_TAG = NAME + ' Iterator';
  var INCORRECT_VALUES_NAME = false;
  var IterablePrototype = Iterable.prototype;
  var nativeIterator = IterablePrototype[ITERATOR]
    || IterablePrototype['@@iterator']
    || DEFAULT && IterablePrototype[DEFAULT];
  var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
  var anyNativeIterator = NAME === 'Array' ? IterablePrototype.entries || nativeIterator : nativeIterator;
  var CurrentIteratorPrototype, methods, KEY;

  // fix native
  if (anyNativeIterator) {
    CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));
    if (CurrentIteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
      if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
        if (setPrototypeOf) {
          setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
        } else if (!isCallable(CurrentIteratorPrototype[ITERATOR])) {
          defineBuiltIn(CurrentIteratorPrototype, ITERATOR, returnThis);
        }
      }
      // Set @@toStringTag to native iterators
      setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
      if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
    }
  }

  // fix Array.prototype.{ values, @@iterator }.name in V8 / FF
  if (PROPER_FUNCTION_NAME && DEFAULT === VALUES && nativeIterator && nativeIterator.name !== VALUES) {
    if (!IS_PURE && CONFIGURABLE_FUNCTION_NAME) {
      createNonEnumerableProperty(IterablePrototype, 'name', VALUES);
    } else {
      INCORRECT_VALUES_NAME = true;
      defaultIterator = function values() { return call(nativeIterator, this); };
    }
  }

  // export additional methods
  if (DEFAULT) {
    methods = {
      values: getIterationMethod(VALUES),
      keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
      entries: getIterationMethod(ENTRIES)
    };
    if (FORCED) for (KEY in methods) {
      if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) {
        defineBuiltIn(IterablePrototype, KEY, methods[KEY]);
      }
    } else $({ target: NAME, proto: true, forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME }, methods);
  }

  // define iterator
  if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) {
    defineBuiltIn(IterablePrototype, ITERATOR, defaultIterator, { name: DEFAULT });
  }
  Iterators[NAME] = defaultIterator;

  return methods;
};


/***/ }),

/***/ 1103:
/***/ ((module) => {

"use strict";

module.exports = function (exec) {
  try {
    return { error: false, value: exec() };
  } catch (error) {
    return { error: true, value: error };
  }
};


/***/ }),

/***/ 1120:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.VisitorProfileCache = void 0;
class VisitorProfileCache {
  constructor(sdkConfig) {
    this._sdkConfig = sdkConfig;
  }
  saveVisitorProfile() {
    //
  }
  loadVisitorProfile() {
    //
    return null;
  }
}
exports.VisitorProfileCache = VisitorProfileCache;

/***/ }),

/***/ 1156:
/***/ ((module) => {

function _iterableToArrayLimit(r, l) {
  var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (null != t) {
    var e,
      n,
      i,
      u,
      a = [],
      f = !0,
      o = !1;
    try {
      if (i = (t = t.call(r)).next, 0 === l) {
        if (Object(t) !== t) return;
        f = !1;
      } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
    } catch (r) {
      o = !0, n = r;
    } finally {
      try {
        if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
      } finally {
        if (o) throw n;
      }
    }
    return a;
  }
}
module.exports = _iterableToArrayLimit, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 1181:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var NATIVE_WEAK_MAP = __webpack_require__(8622);
var globalThis = __webpack_require__(2195);
var isObject = __webpack_require__(34);
var createNonEnumerableProperty = __webpack_require__(6699);
var hasOwn = __webpack_require__(9297);
var shared = __webpack_require__(7629);
var sharedKey = __webpack_require__(6119);
var hiddenKeys = __webpack_require__(421);

var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var TypeError = globalThis.TypeError;
var WeakMap = globalThis.WeakMap;
var set, get, has;

var enforce = function (it) {
  return has(it) ? get(it) : set(it, {});
};

var getterFor = function (TYPE) {
  return function (it) {
    var state;
    if (!isObject(it) || (state = get(it)).type !== TYPE) {
      throw new TypeError('Incompatible receiver, ' + TYPE + ' required');
    } return state;
  };
};

if (NATIVE_WEAK_MAP || shared.state) {
  var store = shared.state || (shared.state = new WeakMap());
  /* eslint-disable no-self-assign -- prototype methods protection */
  store.get = store.get;
  store.has = store.has;
  store.set = store.set;
  /* eslint-enable no-self-assign -- prototype methods protection */
  set = function (it, metadata) {
    if (store.has(it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    store.set(it, metadata);
    return metadata;
  };
  get = function (it) {
    return store.get(it) || {};
  };
  has = function (it) {
    return store.has(it);
  };
} else {
  var STATE = sharedKey('state');
  hiddenKeys[STATE] = true;
  set = function (it, metadata) {
    if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    createNonEnumerableProperty(it, STATE, metadata);
    return metadata;
  };
  get = function (it) {
    return hasOwn(it, STATE) ? it[STATE] : {};
  };
  has = function (it) {
    return hasOwn(it, STATE);
  };
}

module.exports = {
  set: set,
  get: get,
  has: has,
  enforce: enforce,
  getterFor: getterFor
};


/***/ }),

/***/ 1240:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.VISITOR_ID_MISMATCH_ERROR = exports.StrategyAbstract = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_ERROR = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
__webpack_require__(3792);
__webpack_require__(2062);
__webpack_require__(3362);
__webpack_require__(3772);
var _index = __webpack_require__(5588);
var _types = __webpack_require__(7099);
var _index2 = __webpack_require__(6701);
var _utils = __webpack_require__(6517);
var _index3 = __webpack_require__(5394);
var _Troubleshooting = __webpack_require__(3142);
var _UsageHit = __webpack_require__(6798);
var _DefaultHitCache = __webpack_require__(2319);
var _DefaultVisitorCache = __webpack_require__(2358);
var _Event = __webpack_require__(8796);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
const LOOKUP_HITS_JSON_ERROR = exports.LOOKUP_HITS_JSON_ERROR = 'JSON DATA must be an array of object';
const LOOKUP_HITS_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = 'JSON DATA must fit the type HitCacheDTO';
const VISITOR_ID_MISMATCH_ERROR = exports.VISITOR_ID_MISMATCH_ERROR = 'Visitor ID mismatch: {0} vs {1}';
class StrategyAbstract {
  get configManager() {
    return this.visitor.configManager;
  }
  get trackingManager() {
    return this.configManager.trackingManager;
  }
  get decisionManager() {
    return this.configManager.decisionManager;
  }
  get config() {
    return this.visitor.config;
  }
  constructor(param) {
    const visitor = param.visitor,
      murmurHash = param.murmurHash;
    this.visitor = visitor;
    this._murmurHash = murmurHash;
  }
  collectEAIEventsAsync(currentPage) {
    return this.visitor.emotionAi.collectEAIEventsAsync(currentPage);
  }
  reportEaiVisitorEvent(event) {
    if (false) {}
  }
  reportEaiPageView(pageView) {
    if (false) {}
  }
  onEAICollectStatusChange(callback) {
    this.visitor.emotionAi.onEAICollectStatusChange(callback);
  }
  cleanup() {
    this.visitor.emotionAi.cleanup();
  }
  updateCampaigns(campaigns) {
    try {
      this.visitor.campaigns = campaigns;
      this.visitor.flagsData = this.decisionManager.getModifications(campaigns);
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
    } catch (error) {
      (0, _utils.logError)(this.config, error.message || error, 'updateCampaigns');
    }
  }
  hasTrackingManager(process) {
    const check = this.trackingManager;
    if (!check) {
      (0, _utils.logError)(this.config, (0, _utils.sprintf)(_index3.TRACKER_MANAGER_MISSING_ERROR), process);
    }
    return !!check;
  }
  setConsent(hasConsented) {
    var _this$visitor$sdkInit;
    const method = 'setConsent';
    this.visitor.hasConsented = hasConsented;
    if (!hasConsented) {
      this.flushVisitor();
    }
    if (!this.hasTrackingManager(method)) {
      return;
    }
    const consentHit = new _Event.Event({
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      label: `${_index3.SDK_INFO.name}:${this.visitor.hasConsented}`,
      action: _index3.FS_CONSENT,
      category: _index.EventCategory.USER_ENGAGEMENT
    });
    consentHit.qaMode = this.config.isQAModeEnabled;
    consentHit.ds = _index3.SDK_APP;
    consentHit.config = this.config;
    this.trackingManager.addHit(consentHit);
    const hitTroubleshooting = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.VISITOR_SEND_HIT,
      logLevel: _index3.LogLevel.INFO,
      traffic: this.visitor.traffic || 0,
      visitorId: this.visitor.visitorId,
      visitorSessionId: this.visitor.instanceId,
      flagshipInstanceId: (_this$visitor$sdkInit = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit.instanceId,
      anonymousId: this.visitor.anonymousId,
      config: this.config,
      hitContent: consentHit.toApiKeys()
    });
    if (this.decisionManager.troubleshooting) {
      this.trackingManager.sendTroubleshootingHit(hitTroubleshooting);
      return;
    }
    this.visitor.consentHitTroubleshooting = hitTroubleshooting;
    (0, _utils.logDebugSprintf)(this.config, _index3.PROCESS_SET_CONSENT, _index3.CONSENT_CHANGED, this.visitor.visitorId, hasConsented);
  }
  checKLookupVisitorDataV1(item) {
    if (!item || !item.data || !item.data.visitorId) {
      return false;
    }
    const campaigns = item.data.campaigns;
    if (!campaigns) {
      return true;
    }
    if (!Array.isArray(campaigns)) {
      return false;
    }
    if ((this.visitor.visitorCacheStatus === _types.VisitorCacheStatus.VISITOR_ID_CACHE || this.visitor.visitorCacheStatus === _types.VisitorCacheStatus.VISITOR_ID_CACHE_WITH_ANONYMOUS_ID_CACHE) && item.data.visitorId !== this.visitor.visitorId) {
      (0, _utils.logInfoSprintf)(this.config, _index3.PROCESS_CACHE, VISITOR_ID_MISMATCH_ERROR, item.data.visitorId, this.visitor.visitorId);
      return false;
    }
    if (this.visitor.visitorCacheStatus === _types.VisitorCacheStatus.ANONYMOUS_ID_CACHE && item.data.visitorId !== this.visitor.anonymousId) {
      (0, _utils.logInfoSprintf)(this.config, _index3.PROCESS_CACHE, VISITOR_ID_MISMATCH_ERROR, item.data.visitorId, this.visitor.anonymousId);
      return false;
    }
    return campaigns.every(x => x.campaignId && x.type && x.variationGroupId && x.variationId);
  }
  checKLookupVisitorData(item) {
    if (item.version === 1) {
      return this.checKLookupVisitorDataV1(item);
    }
    return false;
  }
  tryLookupCache(id) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const visitorCacheInstance = _this.config.visitorCacheImplementation;
      if (!visitorCacheInstance || typeof visitorCacheInstance.lookupVisitor !== 'function') {
        return null;
      }
      return yield visitorCacheInstance.lookupVisitor(id);
    })();
  }
  processValidCache(visitorCache) {
    if (!this.checKLookupVisitorData(visitorCache)) {
      (0, _utils.logErrorSprintf)(this.config, _index3.PROCESS_CACHE, _index3.LOOKUP_VISITOR_JSON_OBJECT_ERROR, _index3.VISITOR_CACHE_VERSION, this.visitor.visitorId);
      return false;
    }
    this.visitor.visitorCache = visitorCache;
    return true;
  }
  lookupVisitor() {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        if (_this2.config.disableCache || _this2.visitor.visitorCache) {
          return;
        }
        _this2.visitor.visitorCacheStatus = _types.VisitorCacheStatus.NONE;
        let visitorCache = yield _this2.tryLookupCache(_this2.visitor.visitorId);
        if (visitorCache) {
          _this2.visitor.visitorCacheStatus = _types.VisitorCacheStatus.VISITOR_ID_CACHE;
        } else if (_this2.visitor.anonymousId) {
          visitorCache = yield _this2.tryLookupCache(_this2.visitor.anonymousId);
          if (visitorCache) {
            _this2.visitor.visitorCacheStatus = _types.VisitorCacheStatus.ANONYMOUS_ID_CACHE;
          }
        }
        (0, _utils.logDebugSprintf)(_this2.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_LOADED, _this2.visitor.visitorId, visitorCache);
        if (!visitorCache || !_this2.processValidCache(visitorCache)) {
          return;
        }
        if (_this2.visitor.visitorCacheStatus === _types.VisitorCacheStatus.VISITOR_ID_CACHE && _this2.visitor.anonymousId) {
          const anonymousVisitorCache = yield _this2.tryLookupCache(_this2.visitor.anonymousId);
          if (anonymousVisitorCache) {
            _this2.visitor.visitorCacheStatus = _types.VisitorCacheStatus.VISITOR_ID_CACHE_WITH_ANONYMOUS_ID_CACHE;
          }
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this2.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_ERROR, _this2.visitor.visitorId, 'lookupVisitor', error.message || error);
      }
    })();
  }
  createVisitorCacheDTO(eAIScore, isEAIDataCollected) {
    var _this$visitor$visitor, _this$visitor$visitor2, _this$visitor$visitor3;
    const assignmentsHistory = {};
    const visitorCacheDTO = {
      version: _index3.VISITOR_CACHE_VERSION,
      data: {
        visitorId: this.visitor.visitorId,
        anonymousId: this.visitor.anonymousId,
        consent: this.visitor.hasConsented,
        context: this.visitor.context,
        eAIScore: ((_this$visitor$visitor = this.visitor.visitorCache) == null || (_this$visitor$visitor = _this$visitor$visitor.data) == null ? void 0 : _this$visitor$visitor.eAIScore) || eAIScore,
        isEAIDataCollected: ((_this$visitor$visitor2 = this.visitor.visitorCache) == null || (_this$visitor$visitor2 = _this$visitor$visitor2.data) == null ? void 0 : _this$visitor$visitor2.isEAIDataCollected) || isEAIDataCollected,
        campaigns: this.visitor.campaigns.map(campaign => {
          assignmentsHistory[campaign.variationGroupId] = campaign.variation.id;
          return {
            campaignId: campaign.id,
            slug: campaign.slug,
            variationGroupId: campaign.variationGroupId,
            variationId: campaign.variation.id,
            isReference: campaign.variation.reference,
            type: campaign.variation.modifications.type,
            activated: false,
            flags: campaign.variation.modifications.value
          };
        })
      }
    };
    visitorCacheDTO.data.assignmentsHistory = _objectSpread(_objectSpread({}, (_this$visitor$visitor3 = this.visitor.visitorCache) == null || (_this$visitor$visitor3 = _this$visitor$visitor3.data) == null ? void 0 : _this$visitor$visitor3.assignmentsHistory), assignmentsHistory);
    return visitorCacheDTO;
  }
  cacheAnonymousVisitor(visitorCacheInstance, visitorCacheDTO) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const visitorCacheStatus = _this3.visitor.visitorCacheStatus;
      if (_this3.visitor.anonymousId && (visitorCacheStatus === _types.VisitorCacheStatus.NONE || visitorCacheStatus === _types.VisitorCacheStatus.VISITOR_ID_CACHE)) {
        const anonymousVisitorCacheDTO = _objectSpread(_objectSpread({}, visitorCacheDTO), {}, {
          data: _objectSpread(_objectSpread({}, visitorCacheDTO.data), {}, {
            visitorId: _this3.visitor.anonymousId,
            anonymousId: null
          })
        });
        yield visitorCacheInstance.cacheVisitor(_this3.visitor.anonymousId, anonymousVisitorCacheDTO);
      }
    })();
  }
  cacheVisitor(eAIScore, isEAIDataCollected) {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const visitorCacheInstance = _this4.config.visitorCacheImplementation;
        if (_this4.config.disableCache || !visitorCacheInstance || typeof visitorCacheInstance.cacheVisitor !== 'function') {
          return;
        }
        const visitorCacheDTO = _this4.createVisitorCacheDTO(eAIScore, isEAIDataCollected);
        yield visitorCacheInstance.cacheVisitor(_this4.visitor.visitorId, visitorCacheDTO);
        yield _this4.cacheAnonymousVisitor(visitorCacheInstance, visitorCacheDTO);
        (0, _utils.logDebugSprintf)(_this4.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_SAVED, _this4.visitor.visitorId, visitorCacheDTO);
        _this4.visitor.visitorCache = visitorCacheDTO;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this4.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_ERROR, _this4.visitor.visitorId, 'cacheVisitor', error.message || error);
      }
    })();
  }
  flushVisitor() {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const visitorCacheInstance = _this5.config.visitorCacheImplementation;
        if (_this5.config.disableCache || !visitorCacheInstance || typeof visitorCacheInstance.flushVisitor !== 'function') {
          return;
        }
        yield visitorCacheInstance.flushVisitor(_this5.visitor.visitorId);
        (0, _utils.logDebugSprintf)(_this5.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_FLUSHED, _this5.visitor.visitorId);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this5.config, _index3.PROCESS_CACHE, _index3.VISITOR_CACHE_ERROR, _this5.visitor.visitorId, 'flushVisitor', error.message || error);
      }
    })();
  }
  addInTrackingManager(hit) {
    return this.trackingManager.addHit(hit);
  }
  sendTroubleshootingHit(hit) {
    var _this6 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this6.trackingManager.sendTroubleshootingHit(hit);
    })();
  }
  sendUsageHit(hit) {
    var _this7 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (_this7.config.disableDeveloperUsageTracking) {
        return;
      }
      const traffic = _this7.visitor.analyticTraffic;
      if (traffic > _index3.ANALYTIC_HIT_ALLOCATION) {
        return;
      }
      return _this7.trackingManager.sendUsageHit(hit);
    })();
  }
  getCurrentDateTime() {
    return new Date();
  }
  getSdkConfigDecisionMode() {
    return this.config.decisionMode === _index2.DecisionMode.DECISION_API ? 'DECISION_API' : this.config.decisionMode;
  }
  sendSdkConfigAnalyticHit() {
    var _this8 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this8$visitor$sdkIni, _this8$visitor$sdkIni2, _this8$visitor$sdkIni3, _this8$config$trackin, _this8$config$trackin2, _this8$config$trackin3;
      if (_this8.config.disableDeveloperUsageTracking) {
        return;
      }
      const hitCacheImplementation = _this8.config.hitCacheImplementation;
      const visitorCacheImplementation = _this8.config.visitorCacheImplementation;
      let sdkConfigUsingCustomHitCache = false;
      let sdkConfigUsingCustomVisitorCache = false;
      if (false) {}
      const analyticData = new _UsageHit.UsageHit({
        label: _types.TroubleshootingLabel.SDK_CONFIG,
        logLevel: _index3.LogLevel.INFO,
        visitorId: (_this8$visitor$sdkIni = _this8.visitor.sdkInitialData) == null ? void 0 : _this8$visitor$sdkIni.instanceId,
        flagshipInstanceId: (_this8$visitor$sdkIni2 = _this8.visitor.sdkInitialData) == null ? void 0 : _this8$visitor$sdkIni2.instanceId,
        config: _this8.config,
        sdkStatus: _this8.visitor.getSdkStatus(),
        lastBucketingTimestamp: _this8.configManager.decisionManager.lastBucketingTimestamp,
        lastInitializationTimestamp: (_this8$visitor$sdkIni3 = _this8.visitor.sdkInitialData) == null ? void 0 : _this8$visitor$sdkIni3.lastInitializationTimestamp,
        sdkConfigMode: _this8.getSdkConfigDecisionMode(),
        sdkConfigLogLevel: _this8.config.logLevel,
        sdkConfigTimeout: _this8.config.timeout,
        sdkConfigPollingInterval: _this8.config.pollingInterval,
        sdkConfigTrackingManagerStrategy: (_this8$config$trackin = _this8.config.trackingManagerConfig) == null ? void 0 : _this8$config$trackin.cacheStrategy,
        sdkConfigTrackingManagerBatchIntervals: (_this8$config$trackin2 = _this8.config.trackingManagerConfig) == null ? void 0 : _this8$config$trackin2.batchIntervals,
        sdkConfigTrackingManagerPoolMaxSize: (_this8$config$trackin3 = _this8.config.trackingManagerConfig) == null ? void 0 : _this8$config$trackin3.poolMaxSize,
        sdkConfigFetchNow: _this8.config.fetchNow,
        sdkConfigReuseVisitorIds: _this8.config.reuseVisitorIds,
        sdkConfigInitialBucketing: _this8.config.initialBucketing,
        sdkConfigDecisionApiUrl: _this8.config.decisionApiUrl,
        sdkConfigHitDeduplicationTime: _this8.config.hitDeduplicationTime,
        sdkConfigUsingOnVisitorExposed: !!_this8.config.onVisitorExposed,
        sdkConfigUsingCustomHitCache: !!sdkConfigUsingCustomHitCache,
        sdkConfigUsingCustomVisitorCache: !!sdkConfigUsingCustomVisitorCache,
        sdkConfigFetchThirdPartyData: _this8.config.fetchThirdPartyData,
        sdkConfigFetchFlagsBufferingTime: _this8.config.fetchFlagsBufferingTime,
        sdkConfigDisableDeveloperUsageTracking: _this8.config.disableDeveloperUsageTracking,
        sdkConfigNextFetchConfig: _this8.config.nextFetchConfig,
        sdkConfigDisableCache: _this8.config.disableCache
      });
      _this8.sendUsageHit(analyticData);
    })();
  }
  sendFetchFlagsTroubleshooting({
    isFromCache,
    campaigns,
    now
  }) {
    var _this9 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this9$decisionManage, _this9$visitor$sdkIni, _this9$visitor$sdkIni2, _this9$visitor$sdkIni3, _this9$visitor$sdkIni4, _this9$config$trackin, _this9$config$trackin2, _this9$config$trackin3;
      const assignmentHistory = {};
      _this9.visitor.flagsData.forEach(item => {
        assignmentHistory[item.variationGroupId] = item.variationId;
      });
      const uniqueId = _this9.visitor.visitorId + ((_this9$decisionManage = _this9.decisionManager.troubleshooting) == null ? void 0 : _this9$decisionManage.endDate.toUTCString());
      const hash = _this9._murmurHash.murmurHash3Int32(uniqueId);
      const traffic = hash % 100;
      _this9.visitor.traffic = traffic;
      const hitCacheImplementation = _this9.config.hitCacheImplementation;
      const visitorCacheImplementation = _this9.config.visitorCacheImplementation;
      let sdkConfigUsingCustomHitCache = false;
      let sdkConfigUsingCustomVisitorCache = false;
      if (false) {}
      const fetchFlagTroubleshooting = new _Troubleshooting.Troubleshooting({
        label: _types.TroubleshootingLabel.VISITOR_FETCH_CAMPAIGNS,
        logLevel: _index3.LogLevel.INFO,
        visitorId: _this9.visitor.visitorId,
        anonymousId: _this9.visitor.anonymousId,
        visitorSessionId: _this9.visitor.instanceId,
        flagshipInstanceId: (_this9$visitor$sdkIni = _this9.visitor.sdkInitialData) == null ? void 0 : _this9$visitor$sdkIni.instanceId,
        traffic,
        config: _this9.config,
        sdkStatus: _this9.visitor.getSdkStatus(),
        visitorContext: _this9.visitor.context,
        visitorCampaigns: campaigns,
        visitorCampaignFromCache: isFromCache ? campaigns : undefined,
        visitorConsent: _this9.visitor.hasConsented,
        visitorIsAuthenticated: !!_this9.visitor.anonymousId,
        visitorFlags: _this9.visitor.flagsData,
        visitorAssignmentHistory: assignmentHistory,
        visitorInitialCampaigns: (_this9$visitor$sdkIni2 = _this9.visitor.sdkInitialData) == null ? void 0 : _this9$visitor$sdkIni2.initialCampaigns,
        visitorInitialFlagsData: (_this9$visitor$sdkIni3 = _this9.visitor.sdkInitialData) == null ? void 0 : _this9$visitor$sdkIni3.initialFlagsData,
        lastBucketingTimestamp: _this9.configManager.decisionManager.lastBucketingTimestamp,
        lastInitializationTimestamp: (_this9$visitor$sdkIni4 = _this9.visitor.sdkInitialData) == null ? void 0 : _this9$visitor$sdkIni4.lastInitializationTimestamp,
        httpResponseTime: Date.now() - now,
        sdkConfigLogLevel: _this9.config.logLevel,
        sdkConfigMode: _this9.getSdkConfigDecisionMode(),
        sdkConfigTimeout: _this9.config.timeout,
        sdkConfigPollingInterval: _this9.config.pollingInterval,
        sdkConfigTrackingManagerStrategy: (_this9$config$trackin = _this9.config.trackingManagerConfig) == null ? void 0 : _this9$config$trackin.cacheStrategy,
        sdkConfigTrackingManagerBatchIntervals: (_this9$config$trackin2 = _this9.config.trackingManagerConfig) == null ? void 0 : _this9$config$trackin2.batchIntervals,
        sdkConfigTrackingManagerPoolMaxSize: (_this9$config$trackin3 = _this9.config.trackingManagerConfig) == null ? void 0 : _this9$config$trackin3.poolMaxSize,
        sdkConfigFetchNow: _this9.config.fetchNow,
        sdkConfigReuseVisitorIds: _this9.config.reuseVisitorIds,
        sdkConfigInitialBucketing: _this9.config.initialBucketing,
        sdkConfigDecisionApiUrl: _this9.config.decisionApiUrl,
        sdkConfigHitDeduplicationTime: _this9.config.hitDeduplicationTime,
        sdkConfigUsingOnVisitorExposed: !!_this9.config.onVisitorExposed,
        sdkConfigUsingCustomHitCache: !!sdkConfigUsingCustomHitCache,
        sdkConfigUsingCustomVisitorCache: !!sdkConfigUsingCustomVisitorCache,
        sdkConfigFetchThirdPartyData: _this9.config.fetchThirdPartyData,
        sdkConfigFetchFlagsBufferingTime: _this9.config.fetchFlagsBufferingTime,
        sdkConfigDisableDeveloperUsageTracking: _this9.config.disableDeveloperUsageTracking,
        sdkConfigNextFetchConfig: _this9.config.nextFetchConfig,
        sdkConfigDisableCache: _this9.config.disableCache
      });
      yield _this9.sendTroubleshootingHit(fetchFlagTroubleshooting);
    })();
  }
  sendConsentHitTroubleshooting() {
    const consentHitTroubleshooting = this.visitor.consentHitTroubleshooting;
    if (!consentHitTroubleshooting) {
      return;
    }
    consentHitTroubleshooting.traffic = this.visitor.traffic;
    this.trackingManager.sendTroubleshootingHit(consentHitTroubleshooting);
    this.visitor.consentHitTroubleshooting = undefined;
  }
  sendSegmentHitTroubleshooting() {
    const segmentHitTroubleshooting = this.visitor.segmentHitTroubleshooting;
    if (!segmentHitTroubleshooting) {
      return;
    }
    segmentHitTroubleshooting.traffic = this.visitor.traffic;
    this.trackingManager.sendTroubleshootingHit(segmentHitTroubleshooting);
    this.visitor.segmentHitTroubleshooting = undefined;
  }
}
exports.StrategyAbstract = StrategyAbstract;

/***/ }),

/***/ 1270:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.MurmurHash = void 0;
/* eslint-disable no-fallthrough */
class MurmurHash {
  murmurHash3Int32(key, seed = 0) {
    let h1, h1b, k1, i;
    const remainder = key.length & 3; // key.length % 4
    const bytes = key.length - remainder;
    h1 = seed;
    const c1 = 0xcc9e2d51;
    const c2 = 0x1b873593;
    i = 0;
    while (i < bytes) {
      k1 = key.charCodeAt(i) & 0xff | (key.charCodeAt(++i) & 0xff) << 8 | (key.charCodeAt(++i) & 0xff) << 16 | (key.charCodeAt(++i) & 0xff) << 24;
      ++i;
      k1 = (k1 & 0xffff) * c1 + (((k1 >>> 16) * c1 & 0xffff) << 16) & 0xffffffff;
      k1 = k1 << 15 | k1 >>> 17;
      k1 = (k1 & 0xffff) * c2 + (((k1 >>> 16) * c2 & 0xffff) << 16) & 0xffffffff;
      h1 ^= k1;
      h1 = h1 << 13 | h1 >>> 19;
      h1b = (h1 & 0xffff) * 5 + (((h1 >>> 16) * 5 & 0xffff) << 16) & 0xffffffff;
      h1 = (h1b & 0xffff) + 0x6b64 + (((h1b >>> 16) + 0xe654 & 0xffff) << 16);
    }
    k1 = 0;
    switch (remainder) {
      case 3:
        k1 ^= (key.charCodeAt(i + 2) & 0xff) << 16;
      case 2:
        k1 ^= (key.charCodeAt(i + 1) & 0xff) << 8;
      case 1:
        k1 ^= key.charCodeAt(i) & 0xff;
        k1 = (k1 & 0xffff) * c1 + (((k1 >>> 16) * c1 & 0xffff) << 16) & 0xffffffff;
        k1 = k1 << 15 | k1 >>> 17;
        k1 = (k1 & 0xffff) * c2 + (((k1 >>> 16) * c2 & 0xffff) << 16) & 0xffffffff;
        h1 ^= k1;
    }
    h1 ^= key.length;
    h1 ^= h1 >>> 16;
    h1 = (h1 & 0xffff) * 0x85ebca6b + (((h1 >>> 16) * 0x85ebca6b & 0xffff) << 16) & 0xffffffff;
    h1 ^= h1 >>> 13;
    h1 = (h1 & 0xffff) * 0xc2b2ae35 + (((h1 >>> 16) * 0xc2b2ae35 & 0xffff) << 16) & 0xffffffff;
    h1 ^= h1 >>> 16;
    return h1 >>> 0;
  }
}
exports.MurmurHash = MurmurHash;

/***/ }),

/***/ 1278:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var DESCRIPTORS = __webpack_require__(3724);
var ownKeys = __webpack_require__(5031);
var toIndexedObject = __webpack_require__(5397);
var getOwnPropertyDescriptorModule = __webpack_require__(7347);
var createProperty = __webpack_require__(4659);

// `Object.getOwnPropertyDescriptors` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
$({ target: 'Object', stat: true, sham: !DESCRIPTORS }, {
  getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
    var O = toIndexedObject(object);
    var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
    var keys = ownKeys(O);
    var result = {};
    var index = 0;
    var key, descriptor;
    while (keys.length > index) {
      descriptor = getOwnPropertyDescriptor(O, key = keys[index++]);
      if (descriptor !== undefined) createProperty(result, key, descriptor);
    }
    return result;
  }
});


/***/ }),

/***/ 1291:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var trunc = __webpack_require__(741);

// `ToIntegerOrInfinity` abstract operation
// https://tc39.es/ecma262/#sec-tointegerorinfinity
module.exports = function (argument) {
  var number = +argument;
  // eslint-disable-next-line no-self-compare -- NaN check
  return number !== number || number === 0 ? 0 : trunc(number);
};


/***/ }),

/***/ 1392:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var uncurryThis = __webpack_require__(7476);
var getOwnPropertyDescriptor = (__webpack_require__(7347).f);
var toLength = __webpack_require__(8014);
var toString = __webpack_require__(655);
var notARegExp = __webpack_require__(511);
var requireObjectCoercible = __webpack_require__(7750);
var correctIsRegExpLogic = __webpack_require__(1436);
var IS_PURE = __webpack_require__(6395);

var stringSlice = uncurryThis(''.slice);
var min = Math.min;

var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic('startsWith');
// https://github.com/zloirock/core-js/pull/702
var MDN_POLYFILL_BUG = !IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function () {
  var descriptor = getOwnPropertyDescriptor(String.prototype, 'startsWith');
  return descriptor && !descriptor.writable;
}();

// `String.prototype.startsWith` method
// https://tc39.es/ecma262/#sec-string.prototype.startswith
$({ target: 'String', proto: true, forced: !MDN_POLYFILL_BUG && !CORRECT_IS_REGEXP_LOGIC }, {
  startsWith: function startsWith(searchString /* , position = 0 */) {
    var that = toString(requireObjectCoercible(this));
    notARegExp(searchString);
    var index = toLength(min(arguments.length > 1 ? arguments[1] : undefined, that.length));
    var search = toString(searchString);
    return stringSlice(that, index, index + search.length) === search;
  }
});


/***/ }),

/***/ 1415:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove this module from `core-js@4` since it's replaced to module below
__webpack_require__(2405);


/***/ }),

/***/ 1436:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var wellKnownSymbol = __webpack_require__(8227);

var MATCH = wellKnownSymbol('match');

module.exports = function (METHOD_NAME) {
  var regexp = /./;
  try {
    '/./'[METHOD_NAME](regexp);
  } catch (error1) {
    try {
      regexp[MATCH] = false;
      return '/./'[METHOD_NAME](regexp);
    } catch (error2) { /* empty */ }
  } return false;
};


/***/ }),

/***/ 1469:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var arraySpeciesConstructor = __webpack_require__(7433);

// `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray, length) {
  return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
};


/***/ }),

/***/ 1481:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var newPromiseCapabilityModule = __webpack_require__(6043);
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(916).CONSTRUCTOR);

// `Promise.reject` method
// https://tc39.es/ecma262/#sec-promise.reject
$({ target: 'Promise', stat: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  reject: function reject(r) {
    var capability = newPromiseCapabilityModule.f(this);
    var capabilityReject = capability.reject;
    capabilityReject(r);
    return capability.promise;
  }
});


/***/ }),

/***/ 1589:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.CacheStrategy = void 0;
/**
 * Define the strategy that will be used for hit caching
 */
let CacheStrategy = exports.CacheStrategy = /*#__PURE__*/function (CacheStrategy) {
  /**
   * When a hit is emitted, it will be first cached in database using IHitCacheImplementation and added into the pool, then after batching and sending, it will also be flushed from database using IHitCacheImplementation.
   *
   * Note: the SDK has a default cache implementation for browser using localStorage
   */
  CacheStrategy[CacheStrategy["CONTINUOUS_CACHING"] = 0] = "CONTINUOUS_CACHING";
  /**
   * When a hit is emitted, it will be added into the pool, then after batching and sending, all database hits will be flushed, then the entire pool will be cached using IHitCacheImplementation for both actions.
   */
  CacheStrategy[CacheStrategy["PERIODIC_CACHING"] = 1] = "PERIODIC_CACHING";
  return CacheStrategy;
}({});

/***/ }),

/***/ 1597:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.FSFetchStatus = void 0;
/**
 * Represents the status of the flags in the Flagship SDK.
 */
let FSFetchStatus = exports.FSFetchStatus = /*#__PURE__*/function (FSFetchStatus) {
  /**
   * The flags have been successfully fetched from the API or re-evaluated in bucketing mode.
   */
  FSFetchStatus["FETCHED"] = "FETCHED";
  /**
   * The flags are currently being fetched from the API or re-evaluated in bucketing mode.
   */
  FSFetchStatus["FETCHING"] = "FETCHING";
  /**
   * The flags need to be re-fetched due to a change in context, or because the flags were loaded from cache or XPC.
   */
  FSFetchStatus["FETCH_REQUIRED"] = "FETCH_REQUIRED";
  /**
   * The SDK is in PANIC mode: All features are disabled except for the one to fetch flags.
   */
  FSFetchStatus["PANIC"] = "PANIC";
  /**
   * The flags have been initialized from initial data provided during visitor creation .
   */
  FSFetchStatus["INITIAL_DATA"] = "INITIAL_DATA";
  return FSFetchStatus;
}({});

/***/ }),

/***/ 1625:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);

module.exports = uncurryThis({}.isPrototypeOf);


/***/ }),

/***/ 1661:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.SdkApi = void 0;
class SdkApi {
  constructor({
    sharedActionTracking
  }) {
    this.sharedActionTracking = sharedActionTracking;
  }
  setVisitor(visitor) {
    this.visitor = visitor;
  }
  getApiV1() {
    return {
      _getActionTrackingNonce: () => {
        var _this$sharedActionTra;
        return (_this$sharedActionTra = this.sharedActionTracking) == null ? void 0 : _this$sharedActionTra.generateNonce();
      },
      _getVisitorId: () => {
        var _this$visitor, _this$visitor2;
        return ((_this$visitor = this.visitor) == null ? void 0 : _this$visitor.isClientSuppliedID) !== true ? (_this$visitor2 = this.visitor) == null ? void 0 : _this$visitor2.visitorId : undefined;
      }
    };
  }
}
exports.SdkApi = SdkApi;

/***/ }),

/***/ 1699:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var uncurryThis = __webpack_require__(9504);
var notARegExp = __webpack_require__(511);
var requireObjectCoercible = __webpack_require__(7750);
var toString = __webpack_require__(655);
var correctIsRegExpLogic = __webpack_require__(1436);

var stringIndexOf = uncurryThis(''.indexOf);

// `String.prototype.includes` method
// https://tc39.es/ecma262/#sec-string.prototype.includes
$({ target: 'String', proto: true, forced: !correctIsRegExpLogic('includes') }, {
  includes: function includes(searchString /* , position = 0 */) {
    return !!~stringIndexOf(
      toString(requireObjectCoercible(this)),
      toString(notARegExp(searchString)),
      arguments.length > 1 ? arguments[1] : undefined
    );
  }
});


/***/ }),

/***/ 1728:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.ERROR_MESSAGE = exports.Batch = exports.BATCH = void 0;
__webpack_require__(2062);
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const BATCH = exports.BATCH = 'BATCH';
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'Please check required fields';
class Batch extends _HitAbstract.HitAbstract {
  get hits() {
    return this._hits;
  }
  set hits(v) {
    this._hits = v;
  }
  constructor(params) {
    super(_objectSpread(_objectSpread({}, params), {}, {
      visitorId: '',
      anonymousId: '',
      type: BATCH
    }));
    this.hits = params.hits;
  }
  isReady() {
    return !!(super.isReady() && this.hits && this.hits.length > 0 && this.hits.every(hit => hit.isReady(false)));
  }
  toApiKeys() {
    var _this$config;
    const apiKeys = {
      [_FlagshipConstant.DS_API_ITEM]: this.ds,
      [_FlagshipConstant.CUSTOMER_ENV_ID_API_ITEM]: `${(_this$config = this.config) == null ? void 0 : _this$config.envId}`,
      [_FlagshipConstant.T_API_ITEM]: this.type,
      [_FlagshipConstant.QT_API_ITEM]: Date.now() - this.createdAt
    };
    apiKeys.h = this.hits.map(hit => {
      const hitKeys = hit.toApiKeys();
      delete hitKeys[_FlagshipConstant.DS_API_ITEM];
      delete hitKeys[_FlagshipConstant.CUSTOMER_ENV_ID_API_ITEM];
      return hitKeys;
    });
    return apiKeys;
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Batch = Batch;

/***/ }),

/***/ 1734:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.FSFetchReasons = void 0;
/**
 * Enum representing the reasons for fetching Flags.
 */
let FSFetchReasons = exports.FSFetchReasons = /*#__PURE__*/function (FSFetchReasons) {
  /**
   * Indicates that a context has been updated or changed.
   */
  FSFetchReasons["UPDATE_CONTEXT"] = "UPDATE_CONTEXT";
  /**
   * Indicates that the XPC method 'authenticate' has been called.
   */
  FSFetchReasons["AUTHENTICATE"] = "AUTHENTICATE";
  /**
   * Indicates that the XPC method 'unauthenticate' has been called.
   */
  FSFetchReasons["UNAUTHENTICATE"] = "UNAUTHENTICATE";
  /**
   * Indicates that fetching flags has failed.
   */
  FSFetchReasons["FLAGS_FETCHING_ERROR"] = "FLAGS_FETCHING_ERROR";
  /**
   * Indicates that flags have been fetched from the cache.
   */
  FSFetchReasons["FLAGS_FETCHED_FROM_CACHE"] = "FLAGS_FETCHED_FROM_CACHE";
  /**
   * Indicates that the visitor has been created.
   */
  FSFetchReasons["FLAGS_NEVER_FETCHED"] = "FLAGS_NEVER_FETCHED";
  /**
   * Indicates that there is no specific reason for fetching flags.
   */
  FSFetchReasons["NONE"] = "NONE";
  return FSFetchReasons;
}({});

/***/ }),

/***/ 1828:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var hasOwn = __webpack_require__(9297);
var toIndexedObject = __webpack_require__(5397);
var indexOf = (__webpack_require__(9617).indexOf);
var hiddenKeys = __webpack_require__(421);

var push = uncurryThis([].push);

module.exports = function (object, names) {
  var O = toIndexedObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) !hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (hasOwn(O, key = names[i++])) {
    ~indexOf(result, key) || push(result, key);
  }
  return result;
};


/***/ }),

/***/ 1844:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(4782);
__webpack_require__(7495);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BucketingManager = void 0;
__webpack_require__(4423);
__webpack_require__(9449);
__webpack_require__(1699);
__webpack_require__(1392);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _FlagshipConstant = __webpack_require__(2932);
var _index = __webpack_require__(5394);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _DecisionManager = __webpack_require__(5232);
var _Segment = __webpack_require__(3545);
var _Troubleshooting = __webpack_require__(3142);
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
class BucketingManager extends _DecisionManager.DecisionManager {
  get _bucketingContent() {
    return this._sdkManager.getBucketingContent();
  }
  constructor({
    httpClient,
    config,
    murmurHash,
    sdkManager,
    trackingManager,
    flagshipInstanceId
  }) {
    super({
      httpClient,
      config,
      trackingManager,
      flagshipInstanceId
    });
    this._murmurHash = murmurHash;
    this._sdkManager = sdkManager;
  }
  sendContext(visitor) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        var _visitor$sdkInitialDa;
        if (Object.keys(visitor.context).length <= 3 || !visitor.hasConsented || !visitor.hasContextBeenUpdated) {
          return;
        }
        visitor.hasContextBeenUpdated = false;
        const SegmentHit = new _Segment.Segment({
          context: visitor.context,
          visitorId: visitor.visitorId,
          anonymousId: visitor.anonymousId
        });
        yield visitor.sendHit(SegmentHit);
        const hitTroubleshooting = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.VISITOR_SEND_HIT,
          logLevel: _index.LogLevel.INFO,
          traffic: visitor.traffic || 0,
          visitorId: visitor.visitorId,
          visitorSessionId: visitor.instanceId,
          flagshipInstanceId: (_visitor$sdkInitialDa = visitor.sdkInitialData) == null ? void 0 : _visitor$sdkInitialDa.instanceId,
          anonymousId: visitor.anonymousId,
          config: _this.config,
          hitContent: SegmentHit.toApiKeys()
        });
        visitor.segmentHitTroubleshooting = hitTroubleshooting;

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logError)(_this.config, error.message || error, 'sendContext');
      }
    })();
  }
  getThirdPartySegment(visitorId) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const url = (0, _utils.sprintf)(_FlagshipConstant.THIRD_PARTY_SEGMENT_URL, _this2.config.envId, visitorId);
      const now = Date.now();
      const contexts = {};
      try {
        const response = yield _this2._httpClient.getAsync(url, {
          nextFetchConfig: _this2.config.nextFetchConfig
        });
        const content = response.body;
        if (Array.isArray(content)) {
          var _iterator = _createForOfIteratorHelper(content),
            _step;
          try {
            for (_iterator.s(); !(_step = _iterator.n()).done;) {
              const item = _step.value;
              contexts[`${item.partner}::${item.segment}`] = item.value;
            }
          } catch (err) {
            _iterator.e(err);
          } finally {
            _iterator.f();
          }
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logError)(_this2.config, (0, _utils.errorFormat)(error.message || error, {
          url,
          nextFetchConfig: _this2.config.nextFetchConfig,
          duration: Date.now() - now
        }), _FlagshipConstant.GET_THIRD_PARTY_SEGMENT);
      }
      return contexts;
    })();
  }
  getCampaignsAsync(visitor) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this3$_bucketingCont;
      if (!_this3._bucketingContent) {
        return null;
      }
      const troubleshooting = (_this3$_bucketingCont = _this3._bucketingContent) == null || (_this3$_bucketingCont = _this3$_bucketingCont.accountSettings) == null ? void 0 : _this3$_bucketingCont.troubleshooting;
      _this3.troubleshooting = undefined;
      if (troubleshooting) {
        _this3.troubleshooting = {
          startDate: new Date(troubleshooting.startDate),
          endDate: new Date(troubleshooting.endDate),
          timezone: troubleshooting.timezone,
          traffic: troubleshooting.traffic
        };
      }
      if (_this3._bucketingContent.panic) {
        _this3.panic = true;
        return [];
      }
      _this3.panic = false;
      if (!_this3._bucketingContent.campaigns) {
        return null;
      }
      if (_this3.config.fetchThirdPartyData) {
        const thirdPartySegments = yield _this3.getThirdPartySegment(visitor.visitorId);
        visitor.updateContext(thirdPartySegments);
      }
      yield _this3.sendContext(visitor);
      const visitorCampaigns = [];
      _this3._bucketingContent.campaigns.forEach(campaign => {
        const currentCampaigns = _this3.getVisitorCampaigns(campaign.variationGroups, campaign.id, campaign.type, visitor);
        if (currentCampaigns) {
          var _campaign$slug;
          currentCampaigns.slug = (_campaign$slug = campaign.slug) != null ? _campaign$slug : null;
          currentCampaigns.name = campaign.name;
          visitorCampaigns.push(currentCampaigns);
        }
      });
      return visitorCampaigns;
    })();
  }
  getVisitorCampaigns(variationGroups, campaignId, campaignType, visitor) {
    var _iterator2 = _createForOfIteratorHelper(variationGroups),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        const variationGroup = _step2.value;
        const check = this.isMatchTargeting(variationGroup, visitor);
        if (check) {
          const variation = this.getVariation(variationGroup, visitor);
          if (!variation) {
            return null;
          }
          return {
            id: campaignId,
            variation,
            variationGroupId: variationGroup.id,
            variationGroupName: variationGroup.name,
            type: campaignType
          };
        }
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    return null;
  }
  getVariation(variationGroup, visitor) {
    const hash = this._murmurHash.murmurHash3Int32(variationGroup.id + visitor.visitorId);
    const hashAllocation = hash % 100;
    let totalAllocation = 0;
    var _iterator3 = _createForOfIteratorHelper(variationGroup.variations),
      _step3;
    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var _visitor$visitorCache;
        const variation = _step3.value;
        const assignmentsHistory = (_visitor$visitorCache = visitor.visitorCache) == null || (_visitor$visitorCache = _visitor$visitorCache.data) == null ? void 0 : _visitor$visitorCache.assignmentsHistory;
        const cacheVariationId = assignmentsHistory ? assignmentsHistory[variationGroup.id] : null;
        if (cacheVariationId) {
          const newVariation = variationGroup.variations.find(x => x.id === cacheVariationId);
          if (!newVariation) {
            continue;
          }
          (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.ALLOCATION, _FlagshipConstant.BUCKETING_VARIATION_CACHE, visitor.visitorId, newVariation.id);
          return {
            id: newVariation.id,
            name: newVariation.name,
            modifications: newVariation.modifications,
            reference: newVariation.reference
          };
        }
        if (variation.allocation === undefined || variation.allocation === 0) {
          continue;
        }
        totalAllocation += variation.allocation;
        if (hashAllocation < totalAllocation) {
          (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.ALLOCATION, _FlagshipConstant.BUCKETING_NEW_ALLOCATION, visitor.visitorId, variation.id, totalAllocation);
          return {
            id: variation.id,
            modifications: variation.modifications,
            reference: variation.reference,
            name: variation.name
          };
        }
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }
    return null;
  }
  isMatchTargeting(variationGroup, visitor) {
    if (!variationGroup || !variationGroup.targeting || !variationGroup.targeting.targetingGroups) {
      return false;
    }
    return variationGroup.targeting.targetingGroups.some(targetingGroup => this.checkAndTargeting(targetingGroup.targetings, visitor));
  }
  isANDListOperator(operator) {
    return ['NOT_EQUALS', 'NOT_CONTAINS'].includes(operator);
  }
  checkAndTargeting(targetings, visitor) {
    let contextValue;
    let check = false;
    var _iterator4 = _createForOfIteratorHelper(targetings),
      _step4;
    try {
      for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
        const _step4$value = _step4.value,
          key = _step4$value.key,
          value = _step4$value.value,
          operator = _step4$value.operator;
        if (operator === 'EXISTS') {
          if (key in visitor.context) {
            check = true;
            continue;
          }
          check = false;
          break;
        }
        if (operator === 'NOT_EXISTS') {
          if (key in visitor.context) {
            check = false;
            break;
          }
          check = true;
          continue;
        }
        if (key === 'fs_all_users') {
          check = true;
          continue;
        }
        if (key === 'fs_users') {
          contextValue = visitor.visitorId;
        } else {
          if (!(key in visitor.context)) {
            check = false;
            break;
          }
          contextValue = visitor.context[key];
        }
        check = this.testOperator(operator, contextValue, value);
        if (!check) {
          break;
        }
      }
    } catch (err) {
      _iterator4.e(err);
    } finally {
      _iterator4.f();
    }
    return check;
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  testListOperatorLoop(operator, contextValue, value, initialCheck) {
    let check = initialCheck;
    var _iterator5 = _createForOfIteratorHelper(value),
      _step5;
    try {
      for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
        const v = _step5.value;
        check = this.testOperator(operator, contextValue, v);
        if (check !== initialCheck) {
          break;
        }
      }
    } catch (err) {
      _iterator5.e(err);
    } finally {
      _iterator5.f();
    }
    return check;
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  testListOperator(operator, contextValue, value) {
    const andOperator = this.isANDListOperator(operator);
    if (andOperator) {
      return this.testListOperatorLoop(operator, contextValue, value, true);
    }
    return this.testListOperatorLoop(operator, contextValue, value, false);
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  testOperator(operator, contextValue, value) {
    let check;
    if (Array.isArray(value)) {
      return this.testListOperator(operator, contextValue, value);
    }
    switch (operator) {
      case 'EQUALS':
        check = contextValue === value;
        break;
      case 'NOT_EQUALS':
        check = contextValue !== value;
        break;
      case 'CONTAINS':
        check = contextValue.toString().includes(value.toString());
        break;
      case 'NOT_CONTAINS':
        check = !contextValue.toString().includes(value.toString());
        break;
      case 'GREATER_THAN':
        check = contextValue > value;
        break;
      case 'LOWER_THAN':
        check = contextValue < value;
        break;
      case 'GREATER_THAN_OR_EQUALS':
        check = contextValue >= value;
        break;
      case 'LOWER_THAN_OR_EQUALS':
        check = contextValue <= value;
        break;
      case 'STARTS_WITH':
        check = contextValue.toString().startsWith(value.toString());
        break;
      case 'ENDS_WITH':
        check = contextValue.toString().endsWith(value.toString());
        break;
      default:
        check = false;
        break;
    }
    return check;
  }
}
exports.BucketingManager = BucketingManager;

/***/ }),

/***/ 1847:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var objectWithoutPropertiesLoose = __webpack_require__(4893);
function _objectWithoutProperties(e, t) {
  if (null == e) return {};
  var o,
    r,
    i = objectWithoutPropertiesLoose(e, t);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
  }
  return i;
}
module.exports = _objectWithoutProperties, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 1921:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(4782);
__webpack_require__(7495);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.NoBatchingContinuousCachingStrategy = void 0;
__webpack_require__(2008);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(2062);
__webpack_require__(6033);
__webpack_require__(5506);
__webpack_require__(3362);
var _slicedToArray2 = _interopRequireDefault(__webpack_require__(5715));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _BatchTriggeredBy = __webpack_require__(8160);
var _index = __webpack_require__(5394);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _BatchingCachingStrategyAbstract = __webpack_require__(3787);
var _ActivateBatch = __webpack_require__(7783);
var _Troubleshooting = __webpack_require__(3142);
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
class NoBatchingContinuousCachingStrategy extends _BatchingCachingStrategyAbstract.BatchingCachingStrategyAbstract {
  constructor(param) {
    super(param);
    this.cacheHitKeys = {};
  }
  activateFlag(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
      hit.key = hitKey;
      yield _this.sendActivate({
        activateHitsPool: [],
        currentActivate: hit,
        batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy.ActivateLength
      });
    })();
  }
  addHit(hit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
      hit.key = hitKey;
      if (hit.type === _index.HitType.EVENT && hit.action === _index.FS_CONSENT && hit.label === `${_index.SDK_INFO.name}:false`) {
        yield _this2.notConsent(hit.visitorId);
      }
      yield _this2.sendHit(hit);
    })();
  }
  addHitInPoolQueue(_hit) {
    return Promise.resolve();
  }
  sendHit(hit) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const headers = {
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const requestBody = hit.toApiKeys();
      const now = Date.now();
      try {
        yield _this3._httpClient.postAsync(_index.HIT_EVENT_URL, {
          headers,
          body: requestBody,
          timeout: _this3.config.timeout,
          nextFetchConfig: _this3.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this3.config, _index.TRACKING_MANAGER, _index.HIT_SENT_SUCCESS, _index.DIRECT_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index.HIT_EVENT_URL,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[_BatchTriggeredBy.BatchTriggeredBy.DirectHit]
        });
        _this3.sendHitsToFsQa([hit]);
        _this3.dispatchHitsToTag([hit]);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        if (hit.type !== _index.HitType.EVENT || hit.action !== _index.FS_CONSENT) {
          _this3.cacheHitKeys[hit.key] = hit.visitorId;
        }
        yield _this3.cacheHit(new Map().set(hit.key, hit));
        (0, _utils.logErrorSprintf)(_this3.config, _index.TRACKING_MANAGER, _index.TRACKING_MANAGER_ERROR, _index.DIRECT_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index.HIT_EVENT_URL,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[_BatchTriggeredBy.BatchTriggeredBy.DirectHit]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_HIT_ROUTE_ERROR,
          logLevel: _index.LogLevel.ERROR,
          visitorId: `${_this3._flagshipInstanceId}`,
          traffic: 0,
          config: _this3.config,
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index.HIT_EVENT_URL,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy.DirectHit
        });
        _this3.sendTroubleshootingHit(monitoringHttpResponse);
      }
    })();
  }
  notConsent(visitorId) {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const cacheHitKeysEntries = Object.entries(_this4.cacheHitKeys).filter(([_, value]) => value === visitorId);
      const keys = [];
      var _iterator = _createForOfIteratorHelper(cacheHitKeysEntries),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          const _step$value = (0, _slicedToArray2.default)(_step.value, 1),
            key = _step$value[0];
          keys.push(key);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      const hitsKeys = Array.from(_this4._hitsPoolQueue).filter(([_, item]) => {
        return ((item == null ? void 0 : item.type) !== _index.HitType.EVENT || (item == null ? void 0 : item.action) !== _index.FS_CONSENT) && (item.visitorId === visitorId || item.anonymousId === visitorId);
      });
      const activateKeys = Array.from(_this4._activatePoolQueue).filter(([_, item]) => {
        return item.visitorId === visitorId || item.anonymousId === visitorId;
      });
      const keysToFlush = [];
      hitsKeys.forEach(([key]) => {
        _this4._hitsPoolQueue.delete(key);
        keysToFlush.push(key);
      });
      activateKeys.forEach(([key]) => {
        _this4._activatePoolQueue.delete(key);
        keysToFlush.push(key);
      });
      const mergedKeys = [...keys, ...keysToFlush];
      if (!mergedKeys.length) {
        return;
      }
      yield _this4.flushHits(mergedKeys);
      _this4.cacheHitKeys = {};
    })();
  }
  sendActivate({
    activateHitsPool,
    currentActivate,
    batchTriggeredBy
  }) {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const headers = {
        [_index.HEADER_X_API_KEY]: _this5.config.apiKey,
        [_index.HEADER_X_SDK_CLIENT]: _index.SDK_INFO.name,
        [_index.HEADER_X_SDK_VERSION]: _index.SDK_INFO.version,
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const activateBatch = new _ActivateBatch.ActivateBatch(Array.from(activateHitsPool.filter(item => Date.now() - item.createdAt < _index.DEFAULT_HIT_CACHE_TIME_MS)), _this5.config);
      if (currentActivate) {
        activateBatch.hits.push(currentActivate);
      }
      const requestBody = activateBatch.toApiKeys();
      const url = _index.BASE_API_URL + _index.URL_ACTIVATE_MODIFICATION;
      const now = Date.now();
      try {
        yield _this5._httpClient.postAsync(url, {
          headers,
          body: requestBody,
          timeout: _this5.config.timeout,
          nextFetchConfig: _this5.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this5.config, _index.TRACKING_MANAGER, _index.HIT_SENT_SUCCESS, _index.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const hitKeysToRemove = activateHitsPool.map(item => item.key);
        activateBatch.hits.forEach(item => {
          _this5.onVisitorExposed(item);
        });
        if (hitKeysToRemove.length) {
          yield _this5.flushHits(hitKeysToRemove);
        }
        _this5.sendHitsToFsQa(activateBatch.hits);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        activateBatch.hits.forEach(item => {
          _this5.cacheHitKeys[item.key] = item.visitorId;
        });
        if (currentActivate) {
          yield _this5.cacheHit(new Map([[currentActivate.key, currentActivate]]));
        }
        (0, _utils.logErrorSprintf)(_this5.config, _index.TRACKING_MANAGER, _index.TRACKING_MANAGER_ERROR, _index.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_ACTIVATE_HIT_ROUTE_ERROR,
          logLevel: _index.LogLevel.ERROR,
          visitorId: `${_this5._flagshipInstanceId}`,
          traffic: 0,
          config: _this5.config,
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseMethod: 'POST',
          httpResponseUrl: url,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now,
          batchTriggeredBy
        });
        _this5.sendTroubleshootingHit(monitoringHttpResponse);
      }
    })();
  }
}
exports.NoBatchingContinuousCachingStrategy = NoBatchingContinuousCachingStrategy;

/***/ }),

/***/ 1955:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var safeGetBuiltIn = __webpack_require__(3389);
var bind = __webpack_require__(6080);
var macrotask = (__webpack_require__(9225).set);
var Queue = __webpack_require__(8265);
var IS_IOS = __webpack_require__(9544);
var IS_IOS_PEBBLE = __webpack_require__(4265);
var IS_WEBOS_WEBKIT = __webpack_require__(7860);
var IS_NODE = __webpack_require__(6193);

var MutationObserver = globalThis.MutationObserver || globalThis.WebKitMutationObserver;
var document = globalThis.document;
var process = globalThis.process;
var Promise = globalThis.Promise;
var microtask = safeGetBuiltIn('queueMicrotask');
var notify, toggle, node, promise, then;

// modern engines have queueMicrotask method
if (!microtask) {
  var queue = new Queue();

  var flush = function () {
    var parent, fn;
    if (IS_NODE && (parent = process.domain)) parent.exit();
    while (fn = queue.get()) try {
      fn();
    } catch (error) {
      if (queue.head) notify();
      throw error;
    }
    if (parent) parent.enter();
  };

  // browsers with MutationObserver, except iOS - https://github.com/zloirock/core-js/issues/339
  // also except WebOS Webkit https://github.com/zloirock/core-js/issues/898
  if (!IS_IOS && !IS_NODE && !IS_WEBOS_WEBKIT && MutationObserver && document) {
    toggle = true;
    node = document.createTextNode('');
    new MutationObserver(flush).observe(node, { characterData: true });
    notify = function () {
      node.data = toggle = !toggle;
    };
  // environments with maybe non-completely correct, but existent Promise
  } else if (!IS_IOS_PEBBLE && Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    promise = Promise.resolve(undefined);
    // workaround of WebKit ~ iOS Safari 10.1 bug
    promise.constructor = Promise;
    then = bind(promise.then, promise);
    notify = function () {
      then(flush);
    };
  // Node.js without promises
  } else if (IS_NODE) {
    notify = function () {
      process.nextTick(flush);
    };
  // for other environments - macrotask based on:
  // - setImmediate
  // - MessageChannel
  // - window.postMessage
  // - onreadystatechange
  // - setTimeout
  } else {
    // `webpack` dev server bug on IE global methods - use bind(fn, global)
    macrotask = bind(macrotask, globalThis);
    notify = function () {
      macrotask(flush);
    };
  }

  microtask = function (fn) {
    if (!queue.head) notify();
    queue.add(fn);
  };
}

module.exports = microtask;


/***/ }),

/***/ 2003:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var IS_PURE = __webpack_require__(6395);
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(916).CONSTRUCTOR);
var NativePromiseConstructor = __webpack_require__(550);
var getBuiltIn = __webpack_require__(7751);
var isCallable = __webpack_require__(4901);
var defineBuiltIn = __webpack_require__(6840);

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;

// `Promise.prototype.catch` method
// https://tc39.es/ecma262/#sec-promise.prototype.catch
$({ target: 'Promise', proto: true, forced: FORCED_PROMISE_CONSTRUCTOR, real: true }, {
  'catch': function (onRejected) {
    return this.then(undefined, onRejected);
  }
});

// makes sure that native promise-based APIs `Promise#catch` properly works with patched `Promise#then`
if (!IS_PURE && isCallable(NativePromiseConstructor)) {
  var method = getBuiltIn('Promise').prototype['catch'];
  if (NativePromisePrototype['catch'] !== method) {
    defineBuiltIn(NativePromisePrototype, 'catch', method, { unsafe: true });
  }
}


/***/ }),

/***/ 2008:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $filter = (__webpack_require__(9213).filter);
var arrayMethodHasSpeciesSupport = __webpack_require__(597);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('filter');

// `Array.prototype.filter` method
// https://tc39.es/ecma262/#sec-array.prototype.filter
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  filter: function filter(callbackfn /* , thisArg */) {
    return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ 2062:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $map = (__webpack_require__(9213).map);
var arrayMethodHasSpeciesSupport = __webpack_require__(597);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('map');

// `Array.prototype.map` method
// https://tc39.es/ecma262/#sec-array.prototype.map
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  map: function map(callbackfn /* , thisArg */) {
    return $map(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ 2106:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var makeBuiltIn = __webpack_require__(283);
var defineProperty = __webpack_require__(4913);

module.exports = function (target, name, descriptor) {
  if (descriptor.get) makeBuiltIn(descriptor.get, name, { getter: true });
  if (descriptor.set) makeBuiltIn(descriptor.set, name, { setter: true });
  return defineProperty.f(target, name, descriptor);
};


/***/ }),

/***/ 2137:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.FSSdkStatus = void 0;
/**
 * Enum representing the status of the Flagship SDK.
 */
let FSSdkStatus = exports.FSSdkStatus = /*#__PURE__*/function (FSSdkStatus) {
  /**
   * It is the default initial status. This status remains until the sdk has been initialized successfully.
   */
  FSSdkStatus[FSSdkStatus["SDK_NOT_INITIALIZED"] = 0] = "SDK_NOT_INITIALIZED";
  /**
   * The SDK is currently initializing.
   */
  FSSdkStatus[FSSdkStatus["SDK_INITIALIZING"] = 1] = "SDK_INITIALIZING";
  /**
   * Flagship SDK is ready but is running in Panic mode: All features are disabled except the one which refresh this status.
   */
  FSSdkStatus[FSSdkStatus["SDK_PANIC"] = 2] = "SDK_PANIC";
  /**
   * The Initialization is done, and Flagship SDK is ready to use.
   */
  FSSdkStatus[FSSdkStatus["SDK_INITIALIZED"] = 3] = "SDK_INITIALIZED";
  return FSSdkStatus;
}({});

/***/ }),

/***/ 2140:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var wellKnownSymbol = __webpack_require__(8227);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};

test[TO_STRING_TAG] = 'z';

module.exports = String(test) === '[object z]';


/***/ }),

/***/ 2177:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Item = exports.ERROR_MESSAGE = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _HitType = __webpack_require__(6147);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'Transaction Id, Item name and item code are required';
class Item extends _HitAbstract.HitAbstract {
  get transactionId() {
    return this._transactionId;
  }

  /**
   * Specify transaction unique identifier.
   */
  set transactionId(v) {
    if (!this.isNotEmptyString(v, 'transactionId')) {
      return;
    }
    this._transactionId = v;
  }
  get productName() {
    return this._productName;
  }

  /**
   * Specify name of the item product.
   */
  set productName(v) {
    if (!this.isNotEmptyString(v, 'productName')) {
      return;
    }
    this._productName = v;
  }
  get productSku() {
    return this._productSku;
  }

  /**
   * Specify the SKU or item code.
   */
  set productSku(v) {
    if (!this.isNotEmptyString(v, 'productSku')) {
      return;
    }
    this._productSku = v;
  }
  get itemPrice() {
    return this._itemPrice;
  }

  /**
   * Specify the price for a single item
   */
  set itemPrice(v) {
    if (!this.isNumeric(v, 'itemPrice')) {
      return;
    }
    this._itemPrice = v;
  }
  get itemQuantity() {
    return this._itemQuantity;
  }

  /**
   * Specify the number of items purchased.
   */
  set itemQuantity(v) {
    if (!this.isInteger(v, 'itemQuantity')) {
      return;
    }
    this._itemQuantity = Math.trunc(v);
  }
  get itemCategory() {
    return this._itemCategory;
  }

  /**
   * Specify the category that the item belongs to
   */
  set itemCategory(v) {
    if (!this.isNotEmptyString(v, 'itemCategory')) {
      return;
    }
    this._itemCategory = v;
  }

  /**
   *Item constructor.
   * @param transactionId : Transaction unique identifier.
   * @param productName : Name of the item product.
   * @param productSku : The SKU or item code.
   */
  constructor(param) {
    super({
      type: _HitType.HitType.ITEM,
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode,
      isActionTrackingHit: param.isActionTrackingHit
    });
    const transactionId = param.transactionId,
      productName = param.productName,
      productSku = param.productSku,
      itemCategory = param.itemCategory,
      itemPrice = param.itemPrice,
      itemQuantity = param.itemQuantity;
    this.transactionId = transactionId;
    this.productName = productName;
    this.productSku = productSku;
    if (itemCategory) {
      this.itemCategory = itemCategory;
    }
    if (itemPrice) {
      this.itemPrice = itemPrice;
    }
    if (itemQuantity) {
      this.itemQuantity = itemQuantity;
    }
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.transactionId && this.productName && this.productSku);
  }
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_FlagshipConstant.TID_API_ITEM] = this.transactionId;
    apiKeys[_FlagshipConstant.IN_API_ITEM] = this.productName;
    apiKeys[_FlagshipConstant.IC_API_ITEM] = this.productSku;
    if (this.itemPrice) {
      apiKeys[_FlagshipConstant.IP_API_ITEM] = this.itemPrice;
    }
    if (this.itemQuantity) {
      apiKeys[_FlagshipConstant.IQ_API_ITEM] = this.itemQuantity;
    }
    if (this.itemCategory) {
      apiKeys[_FlagshipConstant.IV_API_ITEM] = this.itemCategory;
    }
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      transactionId: this.transactionId,
      productName: this.productName,
      productSku: this.productSku,
      itemPrice: this.itemPrice,
      itemQuantity: this.itemQuantity,
      itemCategory: this.itemCategory
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Item = Item;

/***/ }),

/***/ 2195:
/***/ (function(module) {

"use strict";

var check = function (it) {
  return it && it.Math === Math && it;
};

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
module.exports =
  // eslint-disable-next-line es/no-global-this -- safe
  check(typeof globalThis == 'object' && globalThis) ||
  check(typeof window == 'object' && window) ||
  // eslint-disable-next-line no-restricted-globals -- safe
  check(typeof self == 'object' && self) ||
  check(typeof global == 'object' && global) ||
  check(typeof this == 'object' && this) ||
  // eslint-disable-next-line no-new-func -- fallback
  (function () { return this; })() || Function('return this')();


/***/ }),

/***/ 2211:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);

module.exports = !fails(function () {
  function F() { /* empty */ }
  F.prototype.constructor = null;
  // eslint-disable-next-line es/no-object-getprototypeof -- required for testing
  return Object.getPrototypeOf(new F()) !== F.prototype;
});


/***/ }),

/***/ 2285:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.DecisionApiConfig = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _DecisionMode = __webpack_require__(8884);
var _FlagshipConfig = __webpack_require__(7999);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
class DecisionApiConfig extends _FlagshipConfig.FlagshipConfig {
  constructor(param) {
    super(_objectSpread(_objectSpread({}, param), {}, {
      decisionMode: _DecisionMode.DecisionMode.DECISION_API
    }));
  }
}
exports.DecisionApiConfig = DecisionApiConfig;

/***/ }),

/***/ 2293:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var anObject = __webpack_require__(8551);
var aConstructor = __webpack_require__(5548);
var isNullOrUndefined = __webpack_require__(4117);
var wellKnownSymbol = __webpack_require__(8227);

var SPECIES = wellKnownSymbol('species');

// `SpeciesConstructor` abstract operation
// https://tc39.es/ecma262/#sec-speciesconstructor
module.exports = function (O, defaultConstructor) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || isNullOrUndefined(S = anObject(C)[SPECIES]) ? defaultConstructor : aConstructor(S);
};


/***/ }),

/***/ 2319:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.FS_HIT_PREFIX = exports.DefaultHitCache = void 0;
__webpack_require__(3362);
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const FS_HIT_PREFIX = exports.FS_HIT_PREFIX = 'FS_DEFAULT_HIT_CACHE';
class DefaultHitCache {
  cacheHit(hits) {
    const localDatabaseJson = localStorage.getItem(FS_HIT_PREFIX) || '{}';
    const localDatabase = JSON.parse(localDatabaseJson);
    const newLocalDatabase = _objectSpread(_objectSpread({}, localDatabase), hits);
    localStorage.setItem(FS_HIT_PREFIX, JSON.stringify(newLocalDatabase));
    return Promise.resolve();
  }
  lookupHits() {
    const localDatabaseJson = localStorage.getItem(FS_HIT_PREFIX) || '{}';
    const localDatabase = JSON.parse(localDatabaseJson);
    return Promise.resolve(localDatabase);
  }
  flushHits(hitKeys) {
    const localDatabaseJson = localStorage.getItem(FS_HIT_PREFIX) || '{}';
    const localDatabase = JSON.parse(localDatabaseJson);
    hitKeys.forEach(key => {
      delete localDatabase[key];
    });
    localStorage.setItem(FS_HIT_PREFIX, JSON.stringify(localDatabase));
    return Promise.resolve();
  }
  flushAllHits() {
    localStorage.removeItem(FS_HIT_PREFIX);
    return Promise.resolve();
  }
}
exports.DefaultHitCache = DefaultHitCache;

/***/ }),

/***/ 2333:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toIntegerOrInfinity = __webpack_require__(1291);
var toString = __webpack_require__(655);
var requireObjectCoercible = __webpack_require__(7750);

var $RangeError = RangeError;

// `String.prototype.repeat` method implementation
// https://tc39.es/ecma262/#sec-string.prototype.repeat
module.exports = function repeat(count) {
  var str = toString(requireObjectCoercible(this));
  var result = '';
  var n = toIntegerOrInfinity(count);
  if (n < 0 || n === Infinity) throw new $RangeError('Wrong number of repetitions');
  for (;n > 0; (n >>>= 1) && (str += str)) if (n & 1) result += str;
  return result;
};


/***/ }),

/***/ 2357:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var fails = __webpack_require__(9039);
var uncurryThis = __webpack_require__(9504);
var objectGetPrototypeOf = __webpack_require__(2787);
var objectKeys = __webpack_require__(1072);
var toIndexedObject = __webpack_require__(5397);
var $propertyIsEnumerable = (__webpack_require__(8773).f);

var propertyIsEnumerable = uncurryThis($propertyIsEnumerable);
var push = uncurryThis([].push);

// in some IE versions, `propertyIsEnumerable` returns incorrect result on integer keys
// of `null` prototype objects
var IE_BUG = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-create -- safe
  var O = Object.create(null);
  O[2] = 2;
  return !propertyIsEnumerable(O, 2);
});

// `Object.{ entries, values }` methods implementation
var createMethod = function (TO_ENTRIES) {
  return function (it) {
    var O = toIndexedObject(it);
    var keys = objectKeys(O);
    var IE_WORKAROUND = IE_BUG && objectGetPrototypeOf(O) === null;
    var length = keys.length;
    var i = 0;
    var result = [];
    var key;
    while (length > i) {
      key = keys[i++];
      if (!DESCRIPTORS || (IE_WORKAROUND ? key in O : propertyIsEnumerable(O, key))) {
        push(result, TO_ENTRIES ? [key, O[key]] : O[key]);
      }
    }
    return result;
  };
};

module.exports = {
  // `Object.entries` method
  // https://tc39.es/ecma262/#sec-object.entries
  entries: createMethod(true),
  // `Object.values` method
  // https://tc39.es/ecma262/#sec-object.values
  values: createMethod(false)
};


/***/ }),

/***/ 2358:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.VISITOR_PREFIX = exports.DefaultVisitorCache = void 0;
__webpack_require__(3362);
const VISITOR_PREFIX = exports.VISITOR_PREFIX = 'FS_VISITOR_CACHE_';
class DefaultVisitorCache {
  cacheVisitor(visitorId, data) {
    localStorage.setItem(VISITOR_PREFIX + visitorId, JSON.stringify(data));
    return Promise.resolve();
  }
  lookupVisitor(visitorId) {
    const data = localStorage.getItem(VISITOR_PREFIX + visitorId);
    return Promise.resolve(data ? JSON.parse(data) : null);
  }
  flushVisitor(visitorId) {
    localStorage.removeItem(VISITOR_PREFIX + visitorId);
    return Promise.resolve();
  }
}
exports.DefaultVisitorCache = DefaultVisitorCache;

/***/ }),

/***/ 2360:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* global ActiveXObject -- old IE, WSH */
var anObject = __webpack_require__(8551);
var definePropertiesModule = __webpack_require__(6801);
var enumBugKeys = __webpack_require__(8727);
var hiddenKeys = __webpack_require__(421);
var html = __webpack_require__(397);
var documentCreateElement = __webpack_require__(4055);
var sharedKey = __webpack_require__(6119);

var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');

var EmptyConstructor = function () { /* empty */ };

var scriptTag = function (content) {
  return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
};

// Create object with fake `null` prototype: use ActiveX Object with cleared prototype
var NullProtoObjectViaActiveX = function (activeXDocument) {
  activeXDocument.write(scriptTag(''));
  activeXDocument.close();
  var temp = activeXDocument.parentWindow.Object;
  // eslint-disable-next-line no-useless-assignment -- avoid memory leak
  activeXDocument = null;
  return temp;
};

// Create object with fake `null` prototype: use iframe Object with cleared prototype
var NullProtoObjectViaIFrame = function () {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = documentCreateElement('iframe');
  var JS = 'java' + SCRIPT + ':';
  var iframeDocument;
  iframe.style.display = 'none';
  html.appendChild(iframe);
  // https://github.com/zloirock/core-js/issues/475
  iframe.src = String(JS);
  iframeDocument = iframe.contentWindow.document;
  iframeDocument.open();
  iframeDocument.write(scriptTag('document.F=Object'));
  iframeDocument.close();
  return iframeDocument.F;
};

// Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug
var activeXDocument;
var NullProtoObject = function () {
  try {
    activeXDocument = new ActiveXObject('htmlfile');
  } catch (error) { /* ignore */ }
  NullProtoObject = typeof document != 'undefined'
    ? document.domain && activeXDocument
      ? NullProtoObjectViaActiveX(activeXDocument) // old IE
      : NullProtoObjectViaIFrame()
    : NullProtoObjectViaActiveX(activeXDocument); // WSH
  var length = enumBugKeys.length;
  while (length--) delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
  return NullProtoObject();
};

hiddenKeys[IE_PROTO] = true;

// `Object.create` method
// https://tc39.es/ecma262/#sec-object.create
// eslint-disable-next-line es/no-object-create -- safe
module.exports = Object.create || function create(O, Properties) {
  var result;
  if (O !== null) {
    EmptyConstructor[PROTOTYPE] = anObject(O);
    result = new EmptyConstructor();
    EmptyConstructor[PROTOTYPE] = null;
    // add "__proto__" for Object.getPrototypeOf polyfill
    result[IE_PROTO] = O;
  } else result = NullProtoObject();
  return Properties === undefined ? result : definePropertiesModule.f(result, Properties);
};


/***/ }),

/***/ 2405:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var collection = __webpack_require__(6468);
var collectionStrong = __webpack_require__(6938);

// `Set` constructor
// https://tc39.es/ecma262/#sec-set-objects
collection('Set', function (init) {
  return function Set() { return init(this, arguments.length ? arguments[0] : undefined); };
}, collectionStrong);


/***/ }),

/***/ 2413:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.ERROR_MESSAGE = exports.Diagnostic = void 0;
__webpack_require__(3418);
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _FlagshipConstant = __webpack_require__(2932);
var _HitAbstract = __webpack_require__(9079);
var _BatchTriggeredBy = __webpack_require__(8160);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'event category and event action are required';
/**
 * Represents a diagnostic hit.
 */
class Diagnostic extends _HitAbstract.HitAbstract {
  get traffic() {
    return this._traffic;
  }
  set traffic(v) {
    this._traffic = v;
  }
  constructor(param) {
    super({
      type: param.type,
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId
    });
    this.data = _objectSpread(_objectSpread({}, param), {}, {
      version: param.version || '1',
      stackType: param.stackType || 'SDK',
      stackName: param.stackName || _FlagshipConstant.SDK_INFO.name,
      stackVersion: param.stackVersion || _FlagshipConstant.SDK_INFO.version,
      label: param.label,
      logLevel: param.logLevel,
      timestamp: param.timestamp || new Date(Date.now()).toISOString(),
      timeZone: this.getTimezone(),
      ds: _FlagshipConstant.SDK_APP,
      envId: param.envId || param.config.envId
    });
    this._traffic = param.traffic;
    this.config = param.config;
  }

  /**
   * Gets the timezone of the user.
   * @returns The timezone as a string. If the timezone cannot be determined, it returns the offset from UTC in hours.
   */
  getTimezone() {
    var _Intl$DateTimeFormat;
    const timezone = typeof Intl === 'object' ? (_Intl$DateTimeFormat = Intl.DateTimeFormat()) == null || (_Intl$DateTimeFormat = _Intl$DateTimeFormat.resolvedOptions()) == null ? void 0 : _Intl$DateTimeFormat.timeZone : undefined;
    return timezone || `${new Date().getTimezoneOffset() / 60}`;
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any, complexity
  toApiKeys() {
    var _this$data$config;
    const apiKeys = {
      [_FlagshipConstant.VISITOR_ID_API_ITEM]: this.visitorId,
      [_FlagshipConstant.DS_API_ITEM]: this.data.ds,
      [_FlagshipConstant.CUSTOMER_ENV_ID_API_ITEM]: `${(_this$data$config = this.data.config) == null ? void 0 : _this$data$config.envId}`,
      [_FlagshipConstant.T_API_ITEM]: this.type,
      cv: {}
    };
    const customVariable = {
      version: `${this.data.version}`,
      logLevel: `${_index.LogLevel[this.data.logLevel]}`,
      timestamp: `${this.data.timestamp}`,
      timeZone: `${this.data.timeZone}`,
      label: `${this.data.label}`,
      'stack.type': `${this.data.stackType}`,
      'stack.name': `${this.data.stackName}`,
      'stack.version': `${this.data.stackVersion}`
    };
    if (this.data.lastBucketingTimestamp !== undefined) {
      customVariable.lastBucketingTimestamp = `${this.data.lastBucketingTimestamp}`;
    }
    if (this.data.lastInitializationTimestamp !== undefined) {
      customVariable.lastInitializationTimestamp = `${this.data.lastInitializationTimestamp}`;
    }
    if (this.data.flagshipInstanceId !== undefined) {
      customVariable.flagshipInstanceId = `${this.data.flagshipInstanceId}`;
    }
    if (this.data.accountId) {
      customVariable.accountId = `${this.data.accountId}`;
    }
    if (this.data.envId) {
      customVariable.envId = `${this.data.envId}`;
    }
    if (this.data.sdkBucketingFile !== undefined) {
      customVariable.sdkBucketingFile = JSON.stringify(this.data.sdkBucketingFile);
    }
    if (this.data.stackOriginName !== undefined) {
      customVariable['stack.origin.name'] = `${this.data.stackOriginName}`;
    }
    if (this.data.stackOriginVersion !== undefined) {
      customVariable['stack.origin.version'] = `${this.data.stackOriginVersion}`;
    }
    if (this.data.sdkStatus !== undefined) {
      customVariable['sdk.status'] = `${_index.FSSdkStatus[this.data.sdkStatus]}`;
    }
    if (this.data.sdkConfigLogLevel !== undefined) {
      customVariable['sdk.config.logLevel'] = `${_index.LogLevel[this.data.sdkConfigLogLevel]}`;
    }
    if (this.data.sdkConfigMode !== undefined) {
      customVariable['sdk.config.mode'] = `${this.data.sdkConfigMode}`;
    }
    if (this.data.sdkConfigCustomLogManager !== undefined) {
      customVariable['sdk.config.customLogManager'] = `${this.data.sdkConfigCustomLogManager}`;
    }
    if (this.data.sdkConfigCustomCacheManager !== undefined) {
      customVariable['sdk.config.customCacheManager'] = `${this.data.sdkConfigCustomCacheManager}`;
    }
    if (this.data.sdkConfigStatusListener !== undefined) {
      customVariable['sdk.config.custom.StatusListener'] = `${this.data.sdkConfigStatusListener}`;
    }
    if (this.data.sdkConfigTimeout !== undefined) {
      customVariable['sdk.config.timeout'] = `${this.data.sdkConfigTimeout * 1000}`;
    }
    if (this.data.sdkConfigPollingInterval !== undefined) {
      customVariable['sdk.config.pollingTime'] = `${this.data.sdkConfigPollingInterval * 1000}`;
    }
    if (this.data.sdkConfigTrackingManagerStrategy !== undefined) {
      customVariable['sdk.config.trackingManager.strategy'] = `${_index.CacheStrategy[this.data.sdkConfigTrackingManagerStrategy]}`;
    }
    if (this.data.sdkConfigTrackingManagerBatchIntervals !== undefined) {
      customVariable['sdk.config.trackingManager.batchIntervals'] = `${this.data.sdkConfigTrackingManagerBatchIntervals * 1000}`;
    }
    if (this.data.sdkConfigTrackingManagerPoolMaxSize !== undefined) {
      customVariable['sdk.config.trackingManager.poolMaxSize'] = `${this.data.sdkConfigTrackingManagerPoolMaxSize}`;
    }
    if (this.data.sdkConfigFetchNow !== undefined) {
      customVariable['sdk.config.fetchNow'] = `${this.data.sdkConfigFetchNow}`;
    }
    if (this.data.sdkConfigReuseVisitorIds !== undefined) {
      customVariable['sdk.config.reuseVisitorIds'] = `${this.data.sdkConfigReuseVisitorIds}`;
    }
    if (this.data.sdkConfigInitialBucketing !== undefined) {
      customVariable['sdk.config.initialBucketing'] = JSON.stringify(this.data.sdkConfigInitialBucketing);
    }
    if (this.data.sdkConfigDecisionApiUrl !== undefined) {
      customVariable['sdk.config.decisionApiUrl'] = `${this.data.sdkConfigDecisionApiUrl}`;
    }
    if (this.data.sdkConfigHitDeduplicationTime !== undefined) {
      customVariable['sdk.config.hitDeduplicationTime'] = `${this.data.sdkConfigHitDeduplicationTime * 1000}`;
    }
    if (this.data.sdkConfigUsingCustomHitCache !== undefined) {
      customVariable['sdk.config.usingCustomHitCache'] = JSON.stringify(this.data.sdkConfigUsingCustomHitCache);
    }
    if (this.data.sdkConfigUsingCustomVisitorCache !== undefined) {
      customVariable['sdk.config.usingCustomVisitorCache'] = JSON.stringify(this.data.sdkConfigUsingCustomVisitorCache);
    }
    if (this.data.sdkConfigUsingOnVisitorExposed !== undefined) {
      customVariable['sdk.config.usingOnVisitorExposed'] = JSON.stringify(this.data.sdkConfigUsingOnVisitorExposed);
    }
    if (this.data.sdkConfigFetchThirdPartyData !== undefined) {
      customVariable['sdk.config.fetchThirdPartyData'] = JSON.stringify(this.data.sdkConfigFetchThirdPartyData);
    }
    if (this.data.sdkConfigFetchFlagsBufferingTime !== undefined) {
      customVariable['sdk.config.fetchFlagsBufferingTime'] = JSON.stringify(this.data.sdkConfigFetchFlagsBufferingTime * 1000);
    }
    if (this.data.sdkConfigNextFetchConfig !== undefined) {
      customVariable['sdk.config.nextFetchConfig'] = JSON.stringify(this.data.sdkConfigNextFetchConfig);
    }
    if (this.data.sdkConfigDisableDeveloperUsageTracking !== undefined) {
      customVariable['sdk.config.disableDeveloperUsageTracking'] = JSON.stringify(this.data.sdkConfigDisableDeveloperUsageTracking);
    }
    if (this.data.sdkConfigDisableCache !== undefined) {
      customVariable['sdk.config.disableCache'] = JSON.stringify(this.data.sdkConfigDisableCache);
    }
    if (this.data.httpRequestUrl !== undefined) {
      customVariable['http.request.url'] = `${this.data.httpRequestUrl}`;
    }
    if (this.data.httpRequestMethod !== undefined) {
      customVariable['http.request.method'] = `${this.data.httpRequestMethod}`;
    }
    if (this.data.httpRequestHeaders !== undefined) {
      customVariable['http.request.headers'] = JSON.stringify(this.data.httpRequestHeaders);
    }
    if (this.data.httpRequestBody !== undefined) {
      customVariable['http.request.body'] = JSON.stringify(this.data.httpRequestBody);
    }
    if (this.data.httpResponseUrl !== undefined) {
      customVariable['http.response.url'] = `${this.data.httpResponseUrl}`;
    }
    if (this.data.httpResponseMethod !== undefined) {
      customVariable['http.response.method'] = `${this.data.httpResponseMethod}`;
    }
    if (this.data.httpResponseHeaders !== undefined) {
      customVariable['http.response.headers'] = JSON.stringify(this.data.httpResponseHeaders);
    }
    if (this.data.httpResponseCode !== undefined) {
      customVariable['http.response.code'] = `${this.data.httpResponseCode}`;
    }
    if (this.data.httpResponseBody !== undefined) {
      customVariable['http.response.body'] = JSON.stringify(this.data.httpResponseBody);
    }
    if (this.data.httpResponseTime !== undefined) {
      customVariable['http.response.time'] = `${this.data.httpResponseTime}`;
    }
    if (this.visitorId !== undefined) {
      customVariable['visitor.visitorId'] = `${this.visitorId}`;
    }
    if (this.anonymousId !== undefined) {
      customVariable['visitor.anonymousId'] = `${this.anonymousId}`;
    }
    if (this.data.visitorSessionId !== undefined) {
      customVariable['visitor.sessionId'] = `${this.data.visitorSessionId}`;
    }
    if (this.data.visitorStatus !== undefined) {
      customVariable['visitor.status'] = `${this.data.visitorStatus}`;
    }
    if (this.data.visitorInstanceType !== undefined) {
      customVariable['visitor.instanceType'] = `${this.data.visitorInstanceType}`;
    }
    if (this.data.visitorContext !== undefined) {
      for (const key in this.data.visitorContext) {
        const element = this.data.visitorContext[key];
        customVariable[`visitor.context.[${key}]`] = `${element}`;
      }
    }
    if (this.data.visitorConsent !== undefined) {
      customVariable['visitor.consent'] = `${this.data.visitorConsent}`;
    }
    if (this.data.visitorAssignmentHistory !== undefined) {
      for (const key in this.data.visitorAssignmentHistory) {
        const element = this.data.visitorAssignmentHistory[key];
        customVariable[`visitor.assignments.[${key}]`] = element;
      }
    }
    if (this.data.visitorFlags !== undefined) {
      this.data.visitorFlags.forEach((item, flagKey) => {
        for (const itemKey in item) {
          const itemValue = item[itemKey];
          const hasMetadataKey = itemKey === 'value' || itemKey === 'key';
          const value = typeof itemValue === 'string' ? itemValue : JSON.stringify(itemValue);
          customVariable[`visitor.flags.[${flagKey}]${hasMetadataKey ? '' : '.metadata'}.${itemKey}`] = value;
        }
      });
    }
    if (this.data.visitorIsAuthenticated !== undefined) {
      customVariable['visitor.isAuthenticated'] = `${this.data.visitorIsAuthenticated}`;
    }
    if (this.data.visitorInitialCampaigns !== undefined) {
      customVariable['visitor.initialCampaigns'] = JSON.stringify(this.data.visitorInitialCampaigns);
    }
    if (this.data.visitorInitialFlagsData !== undefined) {
      customVariable['visitor.initialFlagsData'] = JSON.stringify(Array.isArray(this.data.visitorInitialFlagsData) ? this.data.visitorInitialFlagsData : Array.from(this.data.visitorInitialFlagsData));
    }
    if (this.data.visitorCampaigns !== undefined) {
      customVariable['visitor.campaigns'] = JSON.stringify(this.data.visitorCampaigns);
    }
    if (this.data.visitorCampaignFromCache !== undefined) {
      customVariable['visitor.campaignFromCache'] = JSON.stringify(this.data.visitorCampaignFromCache);
    }
    if (this.data.contextKey !== undefined) {
      customVariable.contextKey = `${this.data.contextKey}`;
    }
    if (this.data.contextValue !== undefined) {
      customVariable.contextValue = `${this.data.contextValue}`;
    }
    if (this.data.flagKey !== undefined) {
      customVariable['flag.key'] = `${this.data.flagKey}`;
    }
    if (this.data.flagValue !== undefined) {
      customVariable['flag.value'] = `${this.data.flagValue}`;
    }
    if (this.data.flagDefault !== undefined) {
      customVariable['flag.default'] = JSON.stringify(this.data.flagDefault);
    }
    if (this.data.flagMetadataCampaignId !== undefined) {
      customVariable['flag.metadata.campaignId'] = `${this.data.flagMetadataCampaignId}`;
    }
    if (this.data.flagMetadataCampaignName !== undefined) {
      customVariable['flag.metadata.campaignName'] = `${this.data.flagMetadataCampaignName}`;
    }
    if (this.data.flagMetadataVariationGroupId !== undefined) {
      customVariable['flag.metadata.variationGroupId'] = `${this.data.flagMetadataVariationGroupId}`;
    }
    if (this.data.flagMetadataVariationGroupName !== undefined) {
      customVariable['flag.metadata.variationGroupName'] = `${this.data.flagMetadataVariationGroupName}`;
    }
    if (this.data.flagMetadataVariationId !== undefined) {
      customVariable['flag.metadata.variationId'] = `${this.data.flagMetadataVariationId}`;
    }
    if (this.data.flagMetadataVariationName !== undefined) {
      customVariable['flag.metadata.variationName'] = `${this.data.flagMetadataVariationName}`;
    }
    if (this.data.flagMetadataCampaignSlug !== undefined) {
      customVariable['flag.metadata.campaignSlug'] = `${this.data.flagMetadataCampaignSlug}`;
    }
    if (this.data.flagMetadataCampaignType !== undefined) {
      customVariable['flag.metadata.campaignType'] = `${this.data.flagMetadataCampaignType}`;
    }
    if (this.data.flagMetadataCampaignIsReference !== undefined) {
      customVariable['flag.metadata.isReference'] = `${this.data.flagMetadataCampaignIsReference}`;
    }
    if (this.data.hitContent !== undefined) {
      for (const key in this.data.hitContent) {
        const element = this.data.hitContent[key];
        customVariable[`hit.${key}`] = typeof element === 'string' ? element : JSON.stringify(element);
      }
    }
    if (this.data.batchTriggeredBy !== undefined) {
      customVariable.batchTriggeredBy = `${_BatchTriggeredBy.BatchTriggeredBy[this.data.batchTriggeredBy]}`;
    }
    if (this.data.accountSettings !== undefined) {
      for (const key in this.data.accountSettings) {
        const element = this.data.accountSettings[key];
        customVariable[`accountSettings.${key}`] = typeof element === 'string' ? element : JSON.stringify(element);
      }
    }
    if (this.data.eAIScore !== undefined) {
      customVariable['eAIScore.eai.eas'] = this.data.eAIScore.eai.eas;
    }
    if (this.data.isEAIScoreFromLocalCache !== undefined) {
      customVariable.isEAIScoreFromLocalCache = `${this.data.isEAIScoreFromLocalCache}`;
    }
    if (this.data.eAIDataTimestamp !== undefined) {
      customVariable.eAIDataTimestamp = `${this.data.eAIDataTimestamp}`;
    }
    apiKeys.cv = customVariable;
    return apiKeys;
  }
  isReady(checkParent = true) {
    return !!(!checkParent || super.isReady());
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Diagnostic = Diagnostic;

/***/ }),

/***/ 2478:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var toObject = __webpack_require__(8981);

var floor = Math.floor;
var charAt = uncurryThis(''.charAt);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);
// eslint-disable-next-line redos/no-vulnerable -- safe
var SUBSTITUTION_SYMBOLS = /\$([$&'`]|\d{1,2}|<[^>]*>)/g;
var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&'`]|\d{1,2})/g;

// `GetSubstitution` abstract operation
// https://tc39.es/ecma262/#sec-getsubstitution
module.exports = function (matched, str, position, captures, namedCaptures, replacement) {
  var tailPos = position + matched.length;
  var m = captures.length;
  var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;
  if (namedCaptures !== undefined) {
    namedCaptures = toObject(namedCaptures);
    symbols = SUBSTITUTION_SYMBOLS;
  }
  return replace(replacement, symbols, function (match, ch) {
    var capture;
    switch (charAt(ch, 0)) {
      case '$': return '$';
      case '&': return matched;
      case '`': return stringSlice(str, 0, position);
      case "'": return stringSlice(str, tailPos);
      case '<':
        capture = namedCaptures[stringSlice(ch, 1, -1)];
        break;
      default: // \d\d?
        var n = +ch;
        if (n === 0) return match;
        if (n > m) {
          var f = floor(n / 10);
          if (f === 0) return match;
          if (f <= m) return captures[f - 1] === undefined ? charAt(ch, 1) : captures[f - 1] + charAt(ch, 1);
          return match;
        }
        capture = captures[n - 1];
    }
    return capture === undefined ? '' : capture;
  });
};


/***/ }),

/***/ 2480:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove from `core-js@4`
__webpack_require__(5081);


/***/ }),

/***/ 2509:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Page = exports.ERROR_MESSAGE = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _HitType = __webpack_require__(6147);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'documentLocation url is required';
class Page extends _HitAbstract.HitAbstract {
  get documentLocation() {
    return this._documentLocation;
  }
  set documentLocation(v) {
    if (!this.isNotEmptyString(v, 'documentLocation')) {
      return;
    }
    this._documentLocation = v;
  }
  constructor(page) {
    super({
      type: _HitType.HitType.PAGE_VIEW,
      userIp: page.userIp,
      screenResolution: page.screenResolution,
      locale: page.locale,
      sessionNumber: page.sessionNumber,
      visitorId: page.visitorId,
      anonymousId: page.anonymousId,
      qaMode: page.qaMode,
      isActionTrackingHit: page.isActionTrackingHit
    });
    this.documentLocation = page.documentLocation;
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.documentLocation);
  }
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_FlagshipConstant.DL_API_ITEM] = this.documentLocation;
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      documentLocation: this.documentLocation
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Page = Page;

/***/ }),

/***/ 2529:
/***/ ((module) => {

"use strict";

// `CreateIterResultObject` abstract operation
// https://tc39.es/ecma262/#sec-createiterresultobject
module.exports = function (value, done) {
  return { value: value, done: done };
};


/***/ }),

/***/ 2623:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(3772);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.VisitorAbstract = void 0;
__webpack_require__(3792);
__webpack_require__(6033);
__webpack_require__(5506);
__webpack_require__(3362);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _slicedToArray2 = _interopRequireDefault(__webpack_require__(5715));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _DefaultStrategy = __webpack_require__(4811);
var _depsNode = __webpack_require__(4878);
var _NotReadyStrategy = __webpack_require__(5038);
var _PanicStrategy = __webpack_require__(4641);
var _NoConsentStrategy = __webpack_require__(7449);
var _MurmurHash = __webpack_require__(1270);
var _FSFetchStatus = __webpack_require__(1597);
var _FSFetchReasons = __webpack_require__(1734);
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
class VisitorAbstract extends _depsNode.EventEmitter {
  get visitorVariationState() {
    return this._visitorVariationState;
  }
  get isClientSuppliedID() {
    return this._isClientSuppliedID;
  }
  get hasContextBeenUpdated() {
    return this._hasContextBeenUpdated;
  }
  set hasContextBeenUpdated(v) {
    this._hasContextBeenUpdated = v;
  }
  get getCampaignsPromise() {
    return this._getCampaignsPromise;
  }
  set getCampaignsPromise(v) {
    this._getCampaignsPromise = v;
  }
  get onFetchFlagsStatusChanged() {
    return this._onFetchFlagsStatusChanged;
  }
  set onFetchFlagsStatusChanged(v) {
    this._onFetchFlagsStatusChanged = v;
  }
  get flagsStatus() {
    return this._fetchStatus;
  }
  set flagsStatus(v) {
    this._fetchStatus = v;
    if (this.onFetchFlagsStatusChanged) {
      this.onFetchFlagsStatusChanged(v);
    }
  }
  get segmentHitTroubleshooting() {
    return this._segmentHitTroubleshooting;
  }
  set segmentHitTroubleshooting(v) {
    this._segmentHitTroubleshooting = v;
  }
  get consentHitTroubleshooting() {
    return this._consentHitTroubleshooting;
  }
  set consentHitTroubleshooting(v) {
    this._consentHitTroubleshooting = v;
  }
  get sdkInitialData() {
    return this._sdkInitialData;
  }
  getSdkStatus() {
    return VisitorAbstract.SdkStatus;
  }
  get visitorCacheStatus() {
    return this._visitorCacheStatus;
  }
  set visitorCacheStatus(v) {
    this._visitorCacheStatus = v;
  }
  get emotionAi() {
    return this._emotionAi;
  }
  get analyticTraffic() {
    return this._analyticTraffic;
  }
  initBaseProperties(param) {
    const configManager = param.configManager,
      emotionAi = param.emotionAi,
      monitoringData = param.monitoringData,
      visitorProfileCache = param.visitorProfileCache;
    this._murmurHash = param.murmurHash || new _MurmurHash.MurmurHash();
    this._emotionAi = emotionAi;
    this._hasContextBeenUpdated = true;
    this._exposedVariations = {};
    this._sdkInitialData = monitoringData;
    this._instanceId = (0, _utils.uuidV4)();
    this._isCleaningDeDuplicationCache = false;
    this.deDuplicationCache = {};
    this._context = {};
    this._configManager = configManager;
    this.campaigns = [];
    this._visitorProfileCache = visitorProfileCache;
  }

  /**
   * Attempts to retrieve a visitor ID from the ABTasty tag in browser environments
   * @returns The ABTasty visitor ID if available, otherwise undefined
   */
  getVisitorIdFromTag() {
    if (false) { var _window$ABTasty, _window$ABTasty$_isBy, _window$ABTasty2; }
    return undefined;
  }
  hasVisitorProfileClientSuppliedId(visitorProfileCache) {
    return visitorProfileCache.isClientSuppliedId === undefined ? true : visitorProfileCache.isClientSuppliedId;
  }
  initVisitorId(visitorId, isAuthenticated, hasConsented) {
    var _this$_visitorProfile;
    const shouldUseCache = this.config.reuseVisitorIds && hasConsented === true;
    const visitorCache = shouldUseCache ? (_this$_visitorProfile = this._visitorProfileCache) == null ? void 0 : _this$_visitorProfile.loadVisitorProfile() : null;
    if (visitorCache) {
      (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.PROCESS_NEW_VISITOR, _FlagshipConstant.VISITOR_PROFILE_LOADED, visitorCache);
    }
    this._isClientSuppliedID = false;
    if (visitorId) {
      this.visitorId = visitorId;
      this._isClientSuppliedID = true;
    } else if (!isAuthenticated && visitorCache != null && visitorCache.anonymousId) {
      this.visitorId = visitorCache.anonymousId;
      this._isClientSuppliedID = this.hasVisitorProfileClientSuppliedId(visitorCache);
    } else if (visitorCache != null && visitorCache.visitorId) {
      this.visitorId = visitorCache.visitorId;
      this._isClientSuppliedID = this.hasVisitorProfileClientSuppliedId(visitorCache);
    } else {
      this.visitorId = this.getVisitorIdFromTag() || this.generateVisitorId();
    }
    this._anonymousId = null;
    if (isAuthenticated) {
      this._anonymousId = (visitorCache == null ? void 0 : visitorCache.anonymousId) || (0, _utils.uuidV4)();
    }
  }
  constructor(param) {
    const visitorId = param.visitorId,
      context = param.context,
      isAuthenticated = param.isAuthenticated,
      hasConsented = param.hasConsented,
      initialFlagsData = param.initialFlagsData,
      initialCampaigns = param.initialCampaigns,
      onFetchFlagsStatusChanged = param.onFlagsStatusChanged;
    super();
    (0, _defineProperty2.default)(this, "lastFetchFlagsTimestamp", 0);
    this._visitorVariationState = param.visitorVariationState || {};
    this.initBaseProperties(param);
    this.initVisitorId(visitorId, isAuthenticated, hasConsented);
    this.initAnalyticTraffic();
    this.setConsent(hasConsented || false);
    this.updateContext(context);
    this.loadPredefinedContext();
    (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.PROCESS_NEW_VISITOR, _FlagshipConstant.PREDEFINED_CONTEXT_LOADED, {
      fs_client: _index.SDK_INFO.name,
      fs_version: _index.SDK_INFO.version,
      fs_users: this.visitorId
    });
    this.updateCache();
    const strategy = this.getStrategy();
    this.setInitialFlags(initialFlagsData, strategy);
    this.setInitializeCampaigns(initialCampaigns, !!initialFlagsData, strategy);
    this.onFetchFlagsStatusChanged = onFetchFlagsStatusChanged;
    this.updateFlagsStatus();
    this._emotionAi.init(this);
    (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.PROCESS_NEW_VISITOR, _FlagshipConstant.VISITOR_CREATED, this.visitorId, this.context, !!isAuthenticated, !!this.hasConsented);
  }
  updateFlagsStatus() {
    if (this._flags.size > 0) {
      this.flagsStatus = {
        status: _FSFetchStatus.FSFetchStatus.INITIAL_DATA,
        reason: _FSFetchReasons.FSFetchReasons.NONE
      };
      return;
    }
    this.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.FLAGS_NEVER_FETCHED
    };
  }
  updateCache() {
    var _this$_visitorProfile2;
    const visitorProfile = this.hasConsented ? {
      visitorId: this.visitorId,
      anonymousId: this.anonymousId,
      isClientSuppliedId: this.isClientSuppliedID
    } : undefined;
    (_this$_visitorProfile2 = this._visitorProfileCache) == null || _this$_visitorProfile2.saveVisitorProfile(visitorProfile);
  }
  get traffic() {
    return this._traffic;
  }
  set traffic(v) {
    this._traffic = v;
  }
  get instanceId() {
    return this._instanceId;
  }
  getCurrentDateTime() {
    return new Date();
  }
  initAnalyticTraffic() {
    const uniqueId = this.visitorId + this.getCurrentDateTime().toDateString();
    const hash = this._murmurHash.murmurHash3Int32(uniqueId);
    this._analyticTraffic = hash % 1000;
  }
  generateVisitorId() {
    const visitorId = (0, _utils.uuidV4)();
    (0, _utils.logDebugSprintf)(this.config, _FlagshipConstant.PROCESS_NEW_VISITOR, _FlagshipConstant.VISITOR_ID_GENERATED, visitorId);
    return visitorId;
  }
  clearDeDuplicationCache(deDuplicationTime) {
    if (this._isCleaningDeDuplicationCache) {
      return;
    }
    this._isCleaningDeDuplicationCache = true;
    const entries = Object.entries(this.deDuplicationCache);
    for (var _i = 0, _entries = entries; _i < _entries.length; _i++) {
      const _entries$_i = (0, _slicedToArray2.default)(_entries[_i], 2),
        key = _entries$_i[0],
        value = _entries$_i[1];
      if (Date.now() - value > deDuplicationTime * 1000) {
        delete this.deDuplicationCache[key];
      }
    }
    this._isCleaningDeDuplicationCache = false;
  }
  setInitialFlags(flags, strategy) {
    this._flags = new Map();
    if (!Array.isArray(flags)) {
      return;
    }
    flags.forEach(item => {
      var _hexToValue;
      this._flags.set(item.key, {
        key: item.key,
        campaignId: item.campaignId,
        campaignName: item.campaignName,
        variationGroupId: item.variationGroupId,
        variationGroupName: item.variationGroupName,
        variationId: item.variationId,
        variationName: item.variationName,
        isReference: item.isReference,
        value: (_hexToValue = (0, _utils.hexToValue)(item.hex, this.config)) == null ? void 0 : _hexToValue.v,
        slug: item.slug,
        campaignType: item.campaignType
      });
    });
    strategy == null || strategy.sendVisitorAllocatedVariations();
  }
  setInitializeCampaigns(campaigns, hasInitialFlags, strategy) {
    if (campaigns && Array.isArray(campaigns) && !hasInitialFlags) {
      strategy == null || strategy.updateCampaigns(campaigns);
      strategy == null || strategy.sendVisitorAllocatedVariations();
    }
  }
  loadPredefinedContext() {
    this.context.fs_client = _index.SDK_INFO.name;
    this.context.fs_version = _index.SDK_INFO.version;
    this.context.fs_users = this.visitorId;
  }
  get visitorId() {
    return this._visitorId;
  }
  set visitorId(v) {
    if (!v || typeof v !== 'string') {
      (0, _utils.logError)(this.config, _index.VISITOR_ID_ERROR, 'VISITOR ID');
      return;
    }
    this._visitorId = v;
    this.loadPredefinedContext();
    this.visitorCache = undefined;
  }
  get hasConsented() {
    return this._hasConsented;
  }
  set hasConsented(v) {
    this._hasConsented = v;
  }
  setConsent(hasConsented) {
    this.hasConsented = hasConsented;
    this.getStrategy().setConsent(hasConsented);
  }
  get context() {
    return this._context;
  }
  set context(v) {
    this._context = {};
    this.updateContext(v);
  }
  get flagsData() {
    return this._flags;
  }
  set flagsData(v) {
    this._flags = v;
  }
  get configManager() {
    return this._configManager;
  }
  get config() {
    return this.configManager.config;
  }
  get campaigns() {
    return this._campaigns;
  }
  set campaigns(v) {
    this._campaigns = v;
  }
  get anonymousId() {
    return this._anonymousId;
  }
  set anonymousId(v) {
    this._anonymousId = v;
  }
  getStrategy() {
    let strategy;
    const params = {
      visitor: this,
      murmurHash: new _MurmurHash.MurmurHash()
    };
    const status = this.getSdkStatus();
    if (status === undefined || status === _index.FSSdkStatus.SDK_NOT_INITIALIZED) {
      strategy = new _NotReadyStrategy.NotReadyStrategy(params);
    } else if (status === _index.FSSdkStatus.SDK_PANIC) {
      strategy = new _PanicStrategy.PanicStrategy(params);
    } else if (!this.hasConsented) {
      strategy = new _NoConsentStrategy.NoConsentStrategy(params);
    } else {
      strategy = new _DefaultStrategy.DefaultStrategy(params);
    }
    return strategy;
  }
  sendExposedVariation(flag) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (false) {}
    })();
  }
  collectEAIEventsAsync(currentPage) {
    return this.getStrategy().collectEAIEventsAsync(currentPage);
  }
  sendEaiVisitorEvent(event) {
    this.getStrategy().reportEaiVisitorEvent(event);
  }
  sendEaiPageView(pageView) {
    this.getStrategy().reportEaiPageView(pageView);
  }
  onEAICollectStatusChange(callback) {
    this.getStrategy().onEAICollectStatusChange(callback);
  }
  cleanup() {
    this.getStrategy().cleanup();
  }
  getCachedEAIScore() {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this2$visitorCache;
      if (!_this2.visitorCache) {
        yield _this2.getStrategy().lookupVisitor();
      }
      return (_this2$visitorCache = _this2.visitorCache) == null || (_this2$visitorCache = _this2$visitorCache.data) == null ? void 0 : _this2$visitorCache.eAIScore;
    })();
  }
  isEAIDataCollected() {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this3$visitorCache;
      if (!_this3.visitorCache) {
        yield _this3.getStrategy().lookupVisitor();
      }
      return ((_this3$visitorCache = _this3.visitorCache) == null || (_this3$visitorCache = _this3$visitorCache.data) == null ? void 0 : _this3$visitorCache.isEAIDataCollected) || false;
    })();
  }
  setCachedEAIScore(eAIScore) {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this4.getStrategy().cacheVisitor(eAIScore);
    })();
  }
  setIsEAIDataCollected(isEAIDataCollected) {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this5.getStrategy().cacheVisitor(undefined, isEAIDataCollected);
    })();
  }
  sendTroubleshooting(hit) {
    return this.getStrategy().sendTroubleshootingHit(hit);
  }
  sendUsageHit(hit) {
    return this.getStrategy().sendUsageHit(hit);
  }
  addInTrackingManager(hit) {
    return this.getStrategy().addInTrackingManager(hit);
  }
}
exports.VisitorAbstract = VisitorAbstract;

/***/ }),

/***/ 2652:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var bind = __webpack_require__(6080);
var call = __webpack_require__(9565);
var anObject = __webpack_require__(8551);
var tryToString = __webpack_require__(6823);
var isArrayIteratorMethod = __webpack_require__(4209);
var lengthOfArrayLike = __webpack_require__(6198);
var isPrototypeOf = __webpack_require__(1625);
var getIterator = __webpack_require__(81);
var getIteratorMethod = __webpack_require__(851);
var iteratorClose = __webpack_require__(9539);

var $TypeError = TypeError;

var Result = function (stopped, result) {
  this.stopped = stopped;
  this.result = result;
};

var ResultPrototype = Result.prototype;

module.exports = function (iterable, unboundFunction, options) {
  var that = options && options.that;
  var AS_ENTRIES = !!(options && options.AS_ENTRIES);
  var IS_RECORD = !!(options && options.IS_RECORD);
  var IS_ITERATOR = !!(options && options.IS_ITERATOR);
  var INTERRUPTED = !!(options && options.INTERRUPTED);
  var fn = bind(unboundFunction, that);
  var iterator, iterFn, index, length, result, next, step;

  var stop = function (condition) {
    if (iterator) iteratorClose(iterator, 'normal', condition);
    return new Result(true, condition);
  };

  var callFn = function (value) {
    if (AS_ENTRIES) {
      anObject(value);
      return INTERRUPTED ? fn(value[0], value[1], stop) : fn(value[0], value[1]);
    } return INTERRUPTED ? fn(value, stop) : fn(value);
  };

  if (IS_RECORD) {
    iterator = iterable.iterator;
  } else if (IS_ITERATOR) {
    iterator = iterable;
  } else {
    iterFn = getIteratorMethod(iterable);
    if (!iterFn) throw new $TypeError(tryToString(iterable) + ' is not iterable');
    // optimisation for array iterators
    if (isArrayIteratorMethod(iterFn)) {
      for (index = 0, length = lengthOfArrayLike(iterable); length > index; index++) {
        result = callFn(iterable[index]);
        if (result && isPrototypeOf(ResultPrototype, result)) return result;
      } return new Result(false);
    }
    iterator = getIterator(iterable, iterFn);
  }

  next = IS_RECORD ? iterable.next : iterator.next;
  while (!(step = call(next, iterator)).done) {
    try {
      result = callFn(step.value);
    } catch (error) {
      iteratorClose(iterator, 'throw', error);
    }
    if (typeof result == 'object' && result && isPrototypeOf(ResultPrototype, result)) return result;
  } return new Result(false);
};


/***/ }),

/***/ 2744:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-isextensible, es/no-object-preventextensions -- required for testing
  return Object.isExtensible(Object.preventExtensions({}));
});


/***/ }),

/***/ 2777:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var isObject = __webpack_require__(34);
var isSymbol = __webpack_require__(757);
var getMethod = __webpack_require__(5966);
var ordinaryToPrimitive = __webpack_require__(4270);
var wellKnownSymbol = __webpack_require__(8227);

var $TypeError = TypeError;
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

// `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive
module.exports = function (input, pref) {
  if (!isObject(input) || isSymbol(input)) return input;
  var exoticToPrim = getMethod(input, TO_PRIMITIVE);
  var result;
  if (exoticToPrim) {
    if (pref === undefined) pref = 'default';
    result = call(exoticToPrim, input, pref);
    if (!isObject(result) || isSymbol(result)) return result;
    throw new $TypeError("Can't convert object to primitive value");
  }
  if (pref === undefined) pref = 'number';
  return ordinaryToPrimitive(input, pref);
};


/***/ }),

/***/ 2787:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var hasOwn = __webpack_require__(9297);
var isCallable = __webpack_require__(4901);
var toObject = __webpack_require__(8981);
var sharedKey = __webpack_require__(6119);
var CORRECT_PROTOTYPE_GETTER = __webpack_require__(2211);

var IE_PROTO = sharedKey('IE_PROTO');
var $Object = Object;
var ObjectPrototype = $Object.prototype;

// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
// eslint-disable-next-line es/no-object-getprototypeof -- safe
module.exports = CORRECT_PROTOTYPE_GETTER ? $Object.getPrototypeOf : function (O) {
  var object = toObject(O);
  if (hasOwn(object, IE_PROTO)) return object[IE_PROTO];
  var constructor = object.constructor;
  if (isCallable(constructor) && object instanceof constructor) {
    return constructor.prototype;
  } return object instanceof $Object ? ObjectPrototype : null;
};


/***/ }),

/***/ 2796:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var isCallable = __webpack_require__(4901);

var replacement = /#|\.prototype\./;

var isForced = function (feature, detection) {
  var value = data[normalize(feature)];
  return value === POLYFILL ? true
    : value === NATIVE ? false
    : isCallable(detection) ? fails(detection)
    : !!detection;
};

var normalize = isForced.normalize = function (string) {
  return String(string).replace(replacement, '.').toLowerCase();
};

var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';

module.exports = isForced;


/***/ }),

/***/ 2812:
/***/ ((module) => {

"use strict";

var $TypeError = TypeError;

module.exports = function (passed, required) {
  if (passed < required) throw new $TypeError('Not enough arguments');
  return passed;
};


/***/ }),

/***/ 2816:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Transaction = exports.ERROR_MESSAGE = exports.CURRENCY_ERROR = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _HitType = __webpack_require__(6147);
var _utils = __webpack_require__(6517);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; } /* eslint-disable @typescript-eslint/no-explicit-any */
const CURRENCY_ERROR = exports.CURRENCY_ERROR = '{0} must be a string and have exactly 3 letters';
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'Transaction Id and Transaction affiliation are required';
class Transaction extends _HitAbstract.HitAbstract {
  get transactionId() {
    return this._transactionId;
  }
  set transactionId(v) {
    if (!this.isNotEmptyString(v, 'transactionId')) {
      return;
    }
    this._transactionId = v;
  }
  get affiliation() {
    return this._affiliation;
  }
  set affiliation(v) {
    if (!this.isNotEmptyString(v, 'affiliation')) {
      return;
    }
    this._affiliation = v;
  }
  get taxes() {
    return this._taxes;
  }
  set taxes(v) {
    if (!this.isNumeric(v, 'taxes')) {
      return;
    }
    this._taxes = v;
  }
  get currency() {
    return this._currency;
  }
  set currency(v) {
    if (!v || typeof v !== 'string' || v.length !== 3) {
      (0, _utils.logError)(this.config, (0, _utils.sprintf)(CURRENCY_ERROR, 'currency'), 'currency');
      return;
    }
    this._currency = v;
  }
  get couponCode() {
    return this._couponCode;
  }
  set couponCode(v) {
    if (!this.isNotEmptyString(v, 'couponCode')) {
      return;
    }
    this._couponCode = v;
  }
  get itemCount() {
    return this._itemCount;
  }
  set itemCount(v) {
    if (!this.isInteger(v, 'itemCount')) {
      return;
    }
    this._itemCount = Math.trunc(v);
  }
  get shippingMethod() {
    return this._shippingMethod;
  }
  set shippingMethod(v) {
    if (!this.isNotEmptyString(v, 'shippingMethod')) {
      return;
    }
    this._shippingMethod = v;
  }
  get paymentMethod() {
    return this._paymentMethod;
  }
  set paymentMethod(v) {
    if (!this.isNotEmptyString(v, 'paymentMethod')) {
      return;
    }
    this._paymentMethod = v;
  }
  get totalRevenue() {
    return this._totalRevenue;
  }
  set totalRevenue(v) {
    if (!this.isNumeric(v, 'totalRevenue')) {
      return;
    }
    this._totalRevenue = v;
  }
  get shippingCosts() {
    return this._shippingCosts;
  }
  set shippingCosts(v) {
    if (!this.isNumeric(v, 'shippingCosts')) {
      return;
    }
    this._shippingCosts = v;
  }
  constructor(param) {
    super({
      type: _HitType.HitType.TRANSACTION,
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode,
      isActionTrackingHit: param.isActionTrackingHit
    });
    const transactionId = param.transactionId,
      affiliation = param.affiliation,
      taxes = param.taxes,
      currency = param.currency,
      couponCode = param.couponCode,
      itemCount = param.itemCount,
      shippingMethod = param.shippingMethod,
      paymentMethod = param.paymentMethod,
      totalRevenue = param.totalRevenue,
      shippingCosts = param.shippingCosts;
    this.transactionId = transactionId;
    this.affiliation = affiliation;
    if (taxes) {
      this.taxes = taxes;
    }
    if (currency) {
      this.currency = currency;
    }
    if (couponCode) {
      this.couponCode = couponCode;
    }
    if (itemCount) {
      this.itemCount = itemCount;
    }
    if (shippingMethod) {
      this.shippingMethod = shippingMethod;
    }
    if (paymentMethod) {
      this.paymentMethod = paymentMethod;
    }
    if (totalRevenue) {
      this.totalRevenue = totalRevenue;
    }
    if (shippingCosts) {
      this.shippingCosts = shippingCosts;
    }
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.transactionId && this.affiliation);
  }
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_FlagshipConstant.TID_API_ITEM] = this.transactionId;
    apiKeys[_FlagshipConstant.TA_API_ITEM] = this.affiliation;
    if (this.taxes) {
      apiKeys[_FlagshipConstant.TT_API_ITEM] = this.taxes;
    }
    if (this.currency) {
      apiKeys[_FlagshipConstant.TC_API_ITEM] = this.currency;
    }
    if (this.couponCode) {
      apiKeys[_FlagshipConstant.TCC_API_ITEM] = this.couponCode;
    }
    if (this.itemCount) {
      apiKeys[_FlagshipConstant.ICN_API_ITEM] = this.itemCount;
    }
    if (this.shippingMethod) {
      apiKeys[_FlagshipConstant.SM_API_ITEM] = this.shippingMethod;
    }
    if (this.paymentMethod) {
      apiKeys[_FlagshipConstant.PM_API_ITEM] = this.paymentMethod;
    }
    if (this.totalRevenue) {
      apiKeys[_FlagshipConstant.TR_API_ITEM] = this.totalRevenue;
    }
    if (this.shippingCosts) {
      apiKeys[_FlagshipConstant.TS_API_ITEM] = this.shippingCosts;
    }
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      transactionId: this.transactionId,
      affiliation: this.affiliation,
      taxes: this.taxes,
      currency: this.currency,
      couponCode: this.couponCode,
      itemCount: this.itemCount,
      shippingMethod: this.shippingMethod,
      paymentMethod: this.paymentMethod,
      totalRevenue: this.totalRevenue,
      shippingCosts: this.shippingCosts
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Transaction = Transaction;

/***/ }),

/***/ 2839:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);

var navigator = globalThis.navigator;
var userAgent = navigator && navigator.userAgent;

module.exports = userAgent ? String(userAgent) : '';


/***/ }),

/***/ 2932:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.FS_QA_ASSISTANT_STAGING = exports.FS_QA_ASSISTANT_SCRIPT_TAG_ID = exports.FS_QA_ASSISTANT_LOCAL = exports.FS_QA_ASSISTANT = exports.FS_IS_QA_MODE_ENABLED = exports.FS_FORCED_VARIATIONS = exports.FS_CONSENT = exports.FLAG_VISITOR_EXPOSED = exports.FLAG_VALUE = exports.FLAG_METADATA = exports.FLAGSHIP_VISITOR_NOT_AUTHENTICATE = exports.FLAGSHIP_SDK = exports.FETCH_FLAG_BUFFERING_DEFAULT_TIME = exports.FETCH_FLAGS_STARTED = exports.FETCH_FLAGS_PANIC_MODE = exports.FETCH_FLAGS_MISSING = exports.FETCH_FLAGS_FROM_CAMPAIGNS = exports.FETCH_FLAGS_BUFFERING_MESSAGE = exports.FETCH_EAI_SCORE_SUCCESS = exports.FETCH_EAI_SCORE_ERROR = exports.FETCH_EAI_SCORE = exports.FETCH_CAMPAIGNS_SUCCESS = exports.FETCH_CAMPAIGNS_FROM_CACHE = exports.EXPOSE_ALL_KEYS = exports.EVENT_VALUE_API_ITEM = exports.EVENT_SUFFIX = exports.EVENT_LABEL_API_ITEM = exports.EVENT_CATEGORY_API_ITEM = exports.EVENT_ACTION_API_ITEM = exports.ERROR = exports.EMOTION_AI_UC_URL = exports.EMOTION_AI_EVENT_URL = exports.EMIT_STATUS = exports.EMIT_READY = exports.EAI_SCORE_CONTEXT_KEY = exports.DS_API_ITEM = exports.DL_API_ITEM = exports.DIRECT_HIT = exports.DEFAULT_SERVER_TIME_INTERVAL = exports.DEFAULT_SERVER_POOL_MAX_SIZE = exports.DEFAULT_POLLING_INTERVAL = exports.DEFAULT_HIT_CACHE_TIME_MS = exports.DEFAULT_DEDUPLICATION_TIME = exports.DEFAULT_BROWSER_TIME_INTERVAL = exports.DEFAULT_BROWSER_POOL_MAX_SIZE = exports.DECISION_MANAGER_MISSING_ERROR = exports.CUSTOMER_UID = exports.CUSTOMER_ENV_ID_API_ITEM = exports.CUSTOMER_ENV_ID_API_ACTIVATE = exports.CURL_LIBRARY_IS_NOT_LOADED = exports.CONTEXT_VALUE_ERROR = exports.CONTEXT_PARAM_ERROR = exports.CONTEXT_OBJET_PARAM_UPDATE = exports.CONTEXT_NULL_ERROR = exports.CONTEXT_KEY_VALUE_UPDATE = exports.CONTEXT_KEY_ERROR = exports.CONSENT_NOT_SPECIFY_WARNING = exports.CONSENT_CHANGED = exports.CLIENT_CACHE_KEY = exports.CLICK_PATH_DELAY_MS = exports.CLEAR_CONTEXT = exports.CDN_ACCOUNT_SETTINGS_URL = exports.CAMPAIGN_ID = exports.BUCKETING_VARIATION_CACHE = exports.BUCKETING_POOLING_STOPPED = exports.BUCKETING_POOLING_STARTED = exports.BUCKETING_NEW_ALLOCATION = exports.BUCKETING_API_URL = exports.BUCKETING_API_CONTEXT_URL = exports.BATCH_SENT_SUCCESS = exports.BATCH_MAX_SIZE = exports.BATCH_LOOP_STOPPED = exports.BATCH_LOOP_STARTED = exports.BATCH_HIT = exports.BATCH = exports.BASE_API_URL = exports.AUTHENTICATE = exports.ANONYMOUS_ID = exports.ANALYTIC_HIT_ALLOCATION = exports.ANALYTICS_HIT_SENT_SUCCESS = exports.ANALYTICS_HIT_ADDED_IN_QUEUE = exports.ALL_HITS_FLUSHED = exports.ALLOCATION = exports.ADD_USAGE_HIT = exports.ADD_TROUBLESHOOTING_HIT = exports.ADD_HIT = exports.ADD_ACTIVATE = exports.ACTIVATE_SENT_SUCCESS = exports.ACTIVATE_MODIFICATION_KEY_ERROR = exports.ACTIVATE_MODIFICATION_ERROR = exports.ACTIVATE_HIT = exports.ACTIVATE_BATCH_LENGTH = exports.ACTIVATE_ADDED_IN_QUEUE = exports.ACTION_TRACKING_SENDING_HIT_ERROR = exports.ACTION_TRACKING_SENDING_HIT = exports.ACTION_TRACKING_INVALID_NONCE = exports.ACTION_TRACKING_INVALID_HIT = exports.ACTION_TRACKING_HIT_RECEIVED = exports.ACTION_TRACKING_DISPATCHED = exports.ACTION_TRACKING = void 0;
exports.SEND_ACTIVATE = exports.SDK_VERSION = exports.SDK_STATUS_CHANGED = exports.SDK_STARTED_INFO = exports.SDK_INFO = exports.SDK_APP = exports.SCROLL_END_DELAY_MS = exports.SCREEN_RESOLUTION_API_ITEM = exports.SCORING_INTERVAL = exports.SAVE_VISITOR_INSTANCE = exports.REQUEST_TIME_OUT = exports.QT_API_ITEM = exports.QA_MODE_API_ITEM = exports.QA_ASSISTANT_STAGING_URL = exports.QA_ASSISTANT_PROD_URL = exports.QA_ASSISTANT_LOCAL_URL = exports.PROCESS_UPDATE_CONTEXT = exports.PROCESS_SYNCHRONIZED_MODIFICATION = exports.PROCESS_SET_CONSENT = exports.PROCESS_SEND_HIT = exports.PROCESS_SEND_ACTIVATE = exports.PROCESS_SDK_STATUS = exports.PROCESS_NEW_VISITOR = exports.PROCESS_MODIFICATIONS_FOR_CAMPAIGN = exports.PROCESS_LOOKUP_HIT = exports.PROCESS_INITIALIZATION = exports.PROCESS_GET_MODIFICATION_INFO = exports.PROCESS_GET_MODIFICATION = exports.PROCESS_GET_CAMPAIGNS = exports.PROCESS_GET_ALL_MODIFICATION = exports.PROCESS_FLUSH_HIT = exports.PROCESS_FETCHING_FLAGS = exports.PROCESS_CLEAR_CONTEXT = exports.PROCESS_CACHE_HIT = exports.PROCESS_CACHE = exports.PROCESS_BUCKETING = exports.PROCESS_ACTIVE_MODIFICATION = exports.PROCESS = exports.PREDEFINED_CONTEXT_TYPE_ERROR = exports.PREDEFINED_CONTEXT_LOADED = exports.POLLING_EVENT_FAILED = exports.POLLING_EVENT_300 = exports.POLLING_EVENT_200 = exports.PM_API_ITEM = exports.PANIC_MODE_ERROR = exports.PANIC_MODE_DOC_URL = exports.NO_FLAG_METADATA = exports.NO_BATCHING_WITH_CONTINUOUS_CACHING_STRATEGY = exports.NEW_VISITOR_NOT_READY = exports.METHOD_DEACTIVATED_SEND_CONSENT_ERROR = exports.METHOD_DEACTIVATED_ERROR = exports.METHOD_DEACTIVATED_CONSENT_ERROR = exports.METHOD_DEACTIVATED_BUCKETING_ERROR = exports.METADATA_SDK_NOT_READY = exports.METADATA_PANIC_MODE = exports.MAX_SCORING_POLLING_TIME = exports.MAX_LAST_COLLECTING_TIME_MS = exports.MAX_COLLECTING_TIME_MS = exports.MAX_CLICK_PATH_LENGTH = exports.MAX_ACTIVATE_HIT_PER_BATCH = exports.LOOKUP_VISITOR_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = exports.JS_DOC_URL = exports.IV_API_ITEM = exports.IQ_API_ITEM = exports.IP_API_ITEM = exports.IN_API_ITEM = exports.INITIALIZATION_STARTING = exports.INITIALIZATION_PARAM_ERROR = exports.IC_API_ITEM = exports.ICN_API_ITEM = exports.HIT_SENT_SUCCESS = exports.HIT_EVENT_URL = exports.HIT_DATA_FLUSHED = exports.HIT_CONSENT_URL = exports.HIT_CACHE_VERSION = exports.HIT_CACHE_SAVED = exports.HIT_CACHE_LOADED = exports.HIT_CACHE_ERROR = exports.HIT_API_URL = exports.HIT_ADDED_IN_QUEUE = exports.HEADER_X_SDK_VERSION = exports.HEADER_X_SDK_CLIENT = exports.HEADER_X_ENV_ID = exports.HEADER_X_API_KEY = exports.HEADER_CONTENT_TYPE = exports.HEADER_APPLICATION_JSON = exports.GET_THIRD_PARTY_SEGMENT = exports.GET_MODIFICATION_MISSING_ERROR = exports.GET_MODIFICATION_KEY_ERROR = exports.GET_MODIFICATION_ERROR = exports.GET_MODIFICATION_CAST_ERROR = exports.GET_METADATA_CAST_ERROR = exports.GET_FLAG_VALUE = exports.GET_FLAG_NOT_FOUND = exports.GET_FLAG_MISSING_ERROR = exports.GET_FLAG_ERROR = exports.GET_FLAG_CAST_ERROR = exports.GET_FLAG = exports.FS_QA_URL = void 0;
exports.VISITOR_UNAUTHENTICATE = exports.VISITOR_SYNC_FLAGS_MESSAGE = exports.VISITOR_PROFILE_LOADED = exports.VISITOR_ID_GENERATED = exports.VISITOR_ID_FROM_AB_TASTY_TAG = exports.VISITOR_ID_ERROR = exports.VISITOR_ID_API_ITEM = exports.VISITOR_EXPOSED_VALUE_NOT_CALLED = exports.VISITOR_EAI_SCORE_KEY = exports.VISITOR_CREATED = exports.VISITOR_CONSENT = exports.VISITOR_CACHE_VERSION = exports.VISITOR_CACHE_SAVED = exports.VISITOR_CACHE_LOADED = exports.VISITOR_CACHE_FLUSHED = exports.VISITOR_CACHE_ERROR = exports.VISITOR_AUTHENTICATE_VISITOR_ID_ERROR = exports.VISITOR_AUTHENTICATE = exports.VISITOR_ALREADY_AUTHENTICATE = exports.VARIATION_ID_API_ITEM = exports.VARIATION_GROUP_ID_API_ITEM_ACTIVATE = exports.VARIATION_GROUP_ID_API_ITEM = exports.USER_LANGUAGE = exports.USER_IP_API_ITEM = exports.USER_EXPOSED_FLAG_ERROR = exports.USER_EXPOSED_CAST_ERROR = exports.USAGE_HIT_URL = exports.URL_CAMPAIGNS = exports.URL_ACTIVATE_MODIFICATION = exports.UNAUTHENTICATE = exports.T_API_ITEM = exports.TYPE_INTEGER_ERROR = exports.TYPE_ERROR = exports.TT_API_ITEM = exports.TS_API_ITEM = exports.TR_API_ITEM = exports.TRUSTED_QA_ORIGINS = exports.TROUBLESHOOTING_SENT_SUCCESS = exports.TROUBLESHOOTING_HIT_URL = exports.TROUBLESHOOTING_HIT_ADDED_IN_QUEUE = exports.TRACKING_MANAGER_ERROR = exports.TRACKING_MANAGER = exports.TRACKER_MANAGER_MISSING_ERROR = exports.TID_API_ITEM = exports.THIRD_PARTY_SEGMENT_URL = exports.TC_API_ITEM = exports.TCC_API_ITEM = exports.TA_API_ITEM = exports.TAG_QA_URL = exports.TAG_QA_ASSISTANT_STAGING = exports.TAG_QA_ASSISTANT_LOCAL = exports.TAG_QA_ASSISTANT = exports.S_API_ITEM = exports.SM_API_ITEM = exports.SL_ITEM = exports.SESSION_NUMBER = exports.SEND_USAGE_HIT = exports.SEND_TROUBLESHOOTING = exports.SEND_SEGMENT_HIT = exports.SEND_HIT = exports.SEND_EAI_EVENT_SUCCESS = exports.SEND_EAI_EVENT_ERROR = exports.SEND_EAI_EVENT = exports.SEND_CONTEXT_EVENT = exports.SEND_BATCH = void 0;
var _sdkVersion = __webpack_require__(4116);
/**
 * SDK language
 */

const SDK_INFO = exports.SDK_INFO = {
  name: 'TypeScript',
  version: _sdkVersion.version
};

/**
 * Default request timeout in second
 */
const REQUEST_TIME_OUT = exports.REQUEST_TIME_OUT = 2;
const FETCH_FLAG_BUFFERING_DEFAULT_TIME = exports.FETCH_FLAG_BUFFERING_DEFAULT_TIME = 2;
const DEFAULT_DEDUPLICATION_TIME = exports.DEFAULT_DEDUPLICATION_TIME = 2.5;
const DEFAULT_POLLING_INTERVAL = exports.DEFAULT_POLLING_INTERVAL = 5;
const DEFAULT_SERVER_TIME_INTERVAL = exports.DEFAULT_SERVER_TIME_INTERVAL = 10;
const DEFAULT_SERVER_POOL_MAX_SIZE = exports.DEFAULT_SERVER_POOL_MAX_SIZE = 100;
const DEFAULT_BROWSER_TIME_INTERVAL = exports.DEFAULT_BROWSER_TIME_INTERVAL = 5;
const DEFAULT_BROWSER_POOL_MAX_SIZE = exports.DEFAULT_BROWSER_POOL_MAX_SIZE = 10;
const BATCH_MAX_SIZE = exports.BATCH_MAX_SIZE = 2500000;
const ANALYTIC_HIT_ALLOCATION = exports.ANALYTIC_HIT_ALLOCATION = 1;
const MAX_COLLECTING_TIME_MS = exports.MAX_COLLECTING_TIME_MS = 30000;
const MAX_LAST_COLLECTING_TIME_MS = exports.MAX_LAST_COLLECTING_TIME_MS = 120000;
const MAX_CLICK_PATH_LENGTH = exports.MAX_CLICK_PATH_LENGTH = 1900;
const MAX_SCORING_POLLING_TIME = exports.MAX_SCORING_POLLING_TIME = 120000;
const SCROLL_END_DELAY_MS = exports.SCROLL_END_DELAY_MS = 200;
const CLICK_PATH_DELAY_MS = exports.CLICK_PATH_DELAY_MS = 500;
const SCORING_INTERVAL = exports.SCORING_INTERVAL = 5000;

/**
 * Decision api base url
 */
const BASE_API_URL = exports.BASE_API_URL = 'https://decision.flagship.io/v2/';
const HIT_API_URL = exports.HIT_API_URL = 'https://ariane.abtasty.com';
const HIT_EVENT_URL = exports.HIT_EVENT_URL = 'https://events.flagship.io';
const TROUBLESHOOTING_HIT_URL = exports.TROUBLESHOOTING_HIT_URL = 'https://events.flagship.io/troubleshooting';
const USAGE_HIT_URL = exports.USAGE_HIT_URL = 'https://events.flagship.io/analytics';
const BUCKETING_API_URL = exports.BUCKETING_API_URL = 'https://cdn.flagship.io/{0}/bucketing.json';
const BUCKETING_API_CONTEXT_URL = exports.BUCKETING_API_CONTEXT_URL = 'https://decision.flagship.io/v2/{0}/events';
const THIRD_PARTY_SEGMENT_URL = exports.THIRD_PARTY_SEGMENT_URL = 'https://api-data-connector.flagship.io/accounts/{0}/segments/{1}';
const HIT_CONSENT_URL = exports.HIT_CONSENT_URL = 'https://ariane.abtasty.com';
const URL_CAMPAIGNS = exports.URL_CAMPAIGNS = '/campaigns';
const URL_ACTIVATE_MODIFICATION = exports.URL_ACTIVATE_MODIFICATION = 'activate';
const QA_ASSISTANT_PROD_URL = exports.QA_ASSISTANT_PROD_URL = 'https://qa-assistant.flagship.io/bundle.js';
const QA_ASSISTANT_STAGING_URL = exports.QA_ASSISTANT_STAGING_URL = 'https://staging-qa-assistant.flagship.io/bundle.js';
const QA_ASSISTANT_LOCAL_URL = exports.QA_ASSISTANT_LOCAL_URL = 'https://local-qa-assistant.flagship.io:5000/bundle.js';
const FS_QA_ASSISTANT = exports.FS_QA_ASSISTANT = 'fs_qa_assistant';
const FS_QA_ASSISTANT_STAGING = exports.FS_QA_ASSISTANT_STAGING = 'fs_qa_assistant_staging';
const FS_QA_ASSISTANT_LOCAL = exports.FS_QA_ASSISTANT_LOCAL = 'fs_qa_assistant_local';
const TAG_QA_ASSISTANT = exports.TAG_QA_ASSISTANT = 'abtasty_qa_assistant';
const TAG_QA_ASSISTANT_STAGING = exports.TAG_QA_ASSISTANT_STAGING = 'abtasty_qa_assistant_staging';
const TAG_QA_ASSISTANT_LOCAL = exports.TAG_QA_ASSISTANT_LOCAL = 'abtasty_qa_assistant_local';
const TAG_QA_URL = exports.TAG_QA_URL = 'qa-assistant.abtasty.com';
const FS_QA_URL = exports.FS_QA_URL = 'qa-assistant.flagship.io';
const EXPOSE_ALL_KEYS = exports.EXPOSE_ALL_KEYS = 'exposeAllKeys';
const SEND_CONTEXT_EVENT = exports.SEND_CONTEXT_EVENT = 'sendContextEvent';
const FS_CONSENT = exports.FS_CONSENT = 'fs_consent';
const FS_IS_QA_MODE_ENABLED = exports.FS_IS_QA_MODE_ENABLED = 'FS_IS_QA_MODE_ENABLED';
const FS_FORCED_VARIATIONS = exports.FS_FORCED_VARIATIONS = 'FS_FORCED_VARIATIONS';
const FS_QA_ASSISTANT_SCRIPT_TAG_ID = exports.FS_QA_ASSISTANT_SCRIPT_TAG_ID = 'FS_QA_ASSISTANT_SCRIPT_TAG_ID';
const EMOTION_AI_UC_URL = exports.EMOTION_AI_UC_URL = 'https://uc-info.flagship.io/v1/segments/clients/{0}/visitors/{1}';
const CDN_ACCOUNT_SETTINGS_URL = exports.CDN_ACCOUNT_SETTINGS_URL = 'https://cdn.flagship.io/{0}/accountSettings.json';
const VISITOR_EAI_SCORE_KEY = exports.VISITOR_EAI_SCORE_KEY = '{0}_EAIScore';
const EMOTION_AI_EVENT_URL = exports.EMOTION_AI_EVENT_URL = 'https://events.flagship.io/emotionsai';
const SEND_EAI_EVENT = exports.SEND_EAI_EVENT = 'sendEAIEvent';
const SEND_EAI_EVENT_ERROR = exports.SEND_EAI_EVENT_ERROR = 'Failed to send Emotion AI event: {0}';
const SEND_EAI_EVENT_SUCCESS = exports.SEND_EAI_EVENT_SUCCESS = 'Emotion AI event sent: {0}';
const FETCH_EAI_SCORE = exports.FETCH_EAI_SCORE = 'fetchEAIScore';
const FETCH_EAI_SCORE_ERROR = exports.FETCH_EAI_SCORE_ERROR = 'visitor {0}, Failed to fetch EAIScore: {1}';
const FETCH_EAI_SCORE_SUCCESS = exports.FETCH_EAI_SCORE_SUCCESS = 'visitor {0}, EAIScore fetched: {1}';
const EAI_SCORE_CONTEXT_KEY = exports.EAI_SCORE_CONTEXT_KEY = 'eai::eas';
const CLIENT_CACHE_KEY = exports.CLIENT_CACHE_KEY = 'FS_CLIENT_VISITOR';

/**
 * SDK version
 */
const SDK_VERSION = exports.SDK_VERSION = _sdkVersion.version;
const VISITOR_CACHE_VERSION = exports.VISITOR_CACHE_VERSION = 1;
const HIT_CACHE_VERSION = exports.HIT_CACHE_VERSION = 1;
const DEFAULT_HIT_CACHE_TIME_MS = exports.DEFAULT_HIT_CACHE_TIME_MS = 14400000;
const MAX_ACTIVATE_HIT_PER_BATCH = exports.MAX_ACTIVATE_HIT_PER_BATCH = 100;

/**
 * Message Info
 */

const SDK_STARTED_INFO = exports.SDK_STARTED_INFO = 'Flagship SDK (version: {0}) {1} in **{2}** mode';
const FLAGSHIP_SDK = exports.FLAGSHIP_SDK = 'Flagship SDK';
const EMIT_READY = exports.EMIT_READY = 'ready';
const EMIT_STATUS = exports.EMIT_STATUS = 'status';
const NO_BATCHING_WITH_CONTINUOUS_CACHING_STRATEGY = exports.NO_BATCHING_WITH_CONTINUOUS_CACHING_STRATEGY = 3;
const JS_DOC_URL = exports.JS_DOC_URL = 'https://docs.developers.flagship.io/docs/js-v3';
const PANIC_MODE_DOC_URL = exports.PANIC_MODE_DOC_URL = 'https://docs.developers.flagship.io/docs/glossary#panic-mode';

/**
 * Message Error
 */
const INITIALIZATION_PARAM_ERROR = exports.INITIALIZATION_PARAM_ERROR = `Params 'envId' and 'apiKey' must not be null or empty.
  Learn more: ${JS_DOC_URL}#initialization`;
const ERROR = exports.ERROR = 'error';
const CONTEXT_NULL_ERROR = exports.CONTEXT_NULL_ERROR = 'Context must not to be null';
const CONTEXT_PARAM_ERROR = exports.CONTEXT_PARAM_ERROR = "params {0} must be a non null String, and 'value' must be one of the following types , Number, Boolean";
const GET_MODIFICATION_CAST_ERROR = exports.GET_MODIFICATION_CAST_ERROR = 'Modification for key {0} has a different type. Default value is returned.';
const GET_MODIFICATION_MISSING_ERROR = exports.GET_MODIFICATION_MISSING_ERROR = 'No modification for key {0}. Default value is returned.';
const GET_MODIFICATION_KEY_ERROR = exports.GET_MODIFICATION_KEY_ERROR = 'Key {0} must not be null. Default value is returned.';
const ACTIVATE_MODIFICATION_KEY_ERROR = exports.ACTIVATE_MODIFICATION_KEY_ERROR = 'Key {0} must not be null, no activate will be sent.';
const GET_MODIFICATION_ERROR = exports.GET_MODIFICATION_ERROR = 'No modification for key {0}.';
const GET_FLAG_ERROR = exports.GET_FLAG_ERROR = 'No flag for key {0}.';
const ACTIVATE_MODIFICATION_ERROR = exports.ACTIVATE_MODIFICATION_ERROR = 'No modification for key {0}, no activate will be sent.';
const DECISION_MANAGER_MISSING_ERROR = exports.DECISION_MANAGER_MISSING_ERROR = 'decisionManager must not be null.';
const TRACKER_MANAGER_MISSING_ERROR = exports.TRACKER_MANAGER_MISSING_ERROR = 'trackerManager must not be null.';
const CURL_LIBRARY_IS_NOT_LOADED = exports.CURL_LIBRARY_IS_NOT_LOADED = 'curl library is not loaded';
const TYPE_ERROR = exports.TYPE_ERROR = '{0} must be a {1}';
const TYPE_INTEGER_ERROR = exports.TYPE_INTEGER_ERROR = 'value of {0} is not an {1}, it will be truncated to {1}';
const VISITOR_ID_ERROR = exports.VISITOR_ID_ERROR = 'visitorId must not be null or empty';
const PANIC_MODE_ERROR = exports.PANIC_MODE_ERROR = '{0} deactivated while panic mode is on.';
const METHOD_DEACTIVATED_CONSENT_ERROR = exports.METHOD_DEACTIVATED_CONSENT_ERROR = 'Method {0} is deactivated for visitor {1} : visitor did not consent.';
const METHOD_DEACTIVATED_ERROR = exports.METHOD_DEACTIVATED_ERROR = 'Visitor {0}, method {1} is deactivated while SDK status is: {2}.';
const METHOD_DEACTIVATED_SEND_CONSENT_ERROR = exports.METHOD_DEACTIVATED_SEND_CONSENT_ERROR = 'Send consent hit is deactivated while SDK status is: {1}.';
const TROUBLESHOOTING_HIT_ADDED_IN_QUEUE = exports.TROUBLESHOOTING_HIT_ADDED_IN_QUEUE = 'The TROUBLESHOOTING HIT has been added to the pool queue : {0}';
const ANALYTICS_HIT_ADDED_IN_QUEUE = exports.ANALYTICS_HIT_ADDED_IN_QUEUE = 'The USAGE HIT has been added to the pool queue : {0}';
const ACTIVATE_ADDED_IN_QUEUE = exports.ACTIVATE_ADDED_IN_QUEUE = 'The ACTIVATE has been added to the pool queue : {0}';
const HIT_ADDED_IN_QUEUE = exports.HIT_ADDED_IN_QUEUE = 'The HIT has been added into the pool queue : {0}';
const ADD_HIT = exports.ADD_HIT = 'ADD HIT';
const ADD_TROUBLESHOOTING_HIT = exports.ADD_TROUBLESHOOTING_HIT = 'ADD TROUBLESHOOTING HIT';
const ADD_USAGE_HIT = exports.ADD_USAGE_HIT = 'ADD USAGE HIT';
const ADD_ACTIVATE = exports.ADD_ACTIVATE = 'ADD ACTIVATE';
const BATCH_SENT_SUCCESS = exports.BATCH_SENT_SUCCESS = 'Batch hit has been sent : {0}';
const TROUBLESHOOTING_SENT_SUCCESS = exports.TROUBLESHOOTING_SENT_SUCCESS = 'Troubleshooting hit has been sent : {0}';
const ANALYTICS_HIT_SENT_SUCCESS = exports.ANALYTICS_HIT_SENT_SUCCESS = 'Usage hit has been sent : {0}';
const ACTIVATE_SENT_SUCCESS = exports.ACTIVATE_SENT_SUCCESS = 'Activate hit has been sent : {0}';
const SEND_BATCH = exports.SEND_BATCH = 'SEND BATCH';
const SEND_TROUBLESHOOTING = exports.SEND_TROUBLESHOOTING = 'SEND TROUBLESHOOTING';
const SEND_USAGE_HIT = exports.SEND_USAGE_HIT = 'SEND USAGE HIT';
const SEND_ACTIVATE = exports.SEND_ACTIVATE = 'SEND ACTIVATE';
const SEND_SEGMENT_HIT = exports.SEND_SEGMENT_HIT = 'SEND SEGMENT HIT';
const SEND_HIT = exports.SEND_HIT = 'SEND HIT';
const EVENT_SUFFIX = exports.EVENT_SUFFIX = 'events';
const FETCH_FLAGS_BUFFERING_MESSAGE = exports.FETCH_FLAGS_BUFFERING_MESSAGE = 'Visitor {0}, fetchFlags has been ignored and will continue to be ignored for the next {1}ms, this delay can be changed with `fetchFlagsBufferingTime` option in the SDK config';
const VISITOR_SYNC_FLAGS_MESSAGE = exports.VISITOR_SYNC_FLAGS_MESSAGE = 'without calling `fetchFlags` method afterwards. So, the value of the flag `{1}` might be outdated';
const NEW_VISITOR_NOT_READY = exports.NEW_VISITOR_NOT_READY = `You can't create a new visitor without first calling the "Flagship.start" method.
Learn more: ${JS_DOC_URL}#initialization
`;
const LOOKUP_HITS_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = 'JSON DATA must fit the type HitCacheDTO';
const ACTIVATE_BATCH_LENGTH = exports.ACTIVATE_BATCH_LENGTH = 5;

// Process
const PROCESS = exports.PROCESS = 'process';
const PROCESS_INITIALIZATION = exports.PROCESS_INITIALIZATION = 'INITIALIZATION';
const PROCESS_UPDATE_CONTEXT = exports.PROCESS_UPDATE_CONTEXT = 'UPDATE CONTEXT';
const PROCESS_GET_MODIFICATION = exports.PROCESS_GET_MODIFICATION = 'GET MODIFICATION';
const PROCESS_GET_MODIFICATION_INFO = exports.PROCESS_GET_MODIFICATION_INFO = 'GET MODIFICATION INFO';
const PROCESS_NEW_VISITOR = exports.PROCESS_NEW_VISITOR = 'NEW VISITOR';
const PROCESS_ACTIVE_MODIFICATION = exports.PROCESS_ACTIVE_MODIFICATION = 'ACTIVE MODIFICATION';
const PROCESS_SYNCHRONIZED_MODIFICATION = exports.PROCESS_SYNCHRONIZED_MODIFICATION = 'SYNCHRONIZED MODIFICATION';
const PROCESS_SEND_HIT = exports.PROCESS_SEND_HIT = 'ADD HIT';
const PROCESS_SEND_ACTIVATE = exports.PROCESS_SEND_ACTIVATE = 'SEND ACTIVATE';
const PROCESS_GET_CAMPAIGNS = exports.PROCESS_GET_CAMPAIGNS = 'GET CAMPAIGNS';
const PROCESS_GET_ALL_MODIFICATION = exports.PROCESS_GET_ALL_MODIFICATION = 'GET ALL MODIFICATIONS';
const PROCESS_MODIFICATIONS_FOR_CAMPAIGN = exports.PROCESS_MODIFICATIONS_FOR_CAMPAIGN = 'GET MODIFICATION FOR CAMPAIGN';
const PROCESS_CACHE_HIT = exports.PROCESS_CACHE_HIT = 'CACHE HIT';
const PROCESS_FLUSH_HIT = exports.PROCESS_FLUSH_HIT = 'FLUSH HIT';
const PROCESS_LOOKUP_HIT = exports.PROCESS_LOOKUP_HIT = 'LOOKUP HIT';

// Api items

const BATCH = exports.BATCH = 'batch';
const CUSTOMER_ENV_ID_API_ITEM = exports.CUSTOMER_ENV_ID_API_ITEM = 'cid';
const CUSTOMER_ENV_ID_API_ACTIVATE = exports.CUSTOMER_ENV_ID_API_ACTIVATE = 'cid';
const CUSTOMER_UID = exports.CUSTOMER_UID = 'cuid';
const ANONYMOUS_ID = exports.ANONYMOUS_ID = 'aid';
const VISITOR_ID_API_ITEM = exports.VISITOR_ID_API_ITEM = 'vid';
const VARIATION_GROUP_ID_API_ITEM = exports.VARIATION_GROUP_ID_API_ITEM = 'caid';
const VARIATION_GROUP_ID_API_ITEM_ACTIVATE = exports.VARIATION_GROUP_ID_API_ITEM_ACTIVATE = 'caid';
const VISITOR_CONSENT = exports.VISITOR_CONSENT = 'vc';
const CAMPAIGN_ID = exports.CAMPAIGN_ID = 'caid';
const VARIATION_ID_API_ITEM = exports.VARIATION_ID_API_ITEM = 'vaid';
const DS_API_ITEM = exports.DS_API_ITEM = 'ds';
const T_API_ITEM = exports.T_API_ITEM = 't';
const QT_API_ITEM = exports.QT_API_ITEM = 'qt';
const QA_MODE_API_ITEM = exports.QA_MODE_API_ITEM = 'qa';
const DL_API_ITEM = exports.DL_API_ITEM = 'dl';
const SL_ITEM = exports.SL_ITEM = 'sl';
const SDK_APP = exports.SDK_APP = 'APP';
const TID_API_ITEM = exports.TID_API_ITEM = 'tid';
const TA_API_ITEM = exports.TA_API_ITEM = 'ta';
const TT_API_ITEM = exports.TT_API_ITEM = 'tt';
const TC_API_ITEM = exports.TC_API_ITEM = 'tc';
const TCC_API_ITEM = exports.TCC_API_ITEM = 'tcc';
const ICN_API_ITEM = exports.ICN_API_ITEM = 'icn';
const SM_API_ITEM = exports.SM_API_ITEM = 'sm';
const PM_API_ITEM = exports.PM_API_ITEM = 'pm';
const TR_API_ITEM = exports.TR_API_ITEM = 'tr';
const TS_API_ITEM = exports.TS_API_ITEM = 'ts';
const IN_API_ITEM = exports.IN_API_ITEM = 'in';
const IC_API_ITEM = exports.IC_API_ITEM = 'ic';
const IP_API_ITEM = exports.IP_API_ITEM = 'ip';
const IQ_API_ITEM = exports.IQ_API_ITEM = 'iq';
const IV_API_ITEM = exports.IV_API_ITEM = 'iv';
const S_API_ITEM = exports.S_API_ITEM = 's';
const EVENT_CATEGORY_API_ITEM = exports.EVENT_CATEGORY_API_ITEM = 'ec';
const EVENT_ACTION_API_ITEM = exports.EVENT_ACTION_API_ITEM = 'ea';
const EVENT_LABEL_API_ITEM = exports.EVENT_LABEL_API_ITEM = 'el';
const EVENT_VALUE_API_ITEM = exports.EVENT_VALUE_API_ITEM = 'ev';
const USER_IP_API_ITEM = exports.USER_IP_API_ITEM = 'uip';
const SCREEN_RESOLUTION_API_ITEM = exports.SCREEN_RESOLUTION_API_ITEM = 'sr';
const USER_LANGUAGE = exports.USER_LANGUAGE = 'ul';
const SESSION_NUMBER = exports.SESSION_NUMBER = 'sn';
const HEADER_X_API_KEY = exports.HEADER_X_API_KEY = 'x-api-key';
const HEADER_X_ENV_ID = exports.HEADER_X_ENV_ID = 'x-env-id';
const HEADER_CONTENT_TYPE = exports.HEADER_CONTENT_TYPE = 'Content-Type';
const HEADER_X_SDK_CLIENT = exports.HEADER_X_SDK_CLIENT = 'x-sdk-client';
const HEADER_X_SDK_VERSION = exports.HEADER_X_SDK_VERSION = 'x-sdk-version';
const HEADER_APPLICATION_JSON = exports.HEADER_APPLICATION_JSON = 'application/json';

// Log

const INITIALIZATION_STARTING = exports.INITIALIZATION_STARTING = 'Flagship SDK version {0} is starting in {1} mode with config {2}';
const BUCKETING_POOLING_STARTED = exports.BUCKETING_POOLING_STARTED = 'Bucketing polling process has been started';
const BUCKETING_POOLING_STOPPED = exports.BUCKETING_POOLING_STOPPED = 'Bucketing polling process has been stopped';
const PROCESS_BUCKETING = exports.PROCESS_BUCKETING = 'BUCKETING';
const POLLING_EVENT_200 = exports.POLLING_EVENT_200 = 'Polling event with code status 200 : {0}';
const POLLING_EVENT_300 = exports.POLLING_EVENT_300 = 'Polling event with code status 304';
const POLLING_EVENT_FAILED = exports.POLLING_EVENT_FAILED = 'Polling event failed with error';
const PROCESS_SDK_STATUS = exports.PROCESS_SDK_STATUS = 'SDK STATUS';
const SDK_STATUS_CHANGED = exports.SDK_STATUS_CHANGED = 'SDK status has changed:  {0}';
const SAVE_VISITOR_INSTANCE = exports.SAVE_VISITOR_INSTANCE = 'Visitor {0} has been saved in SDK instance';
const VISITOR_CREATED = exports.VISITOR_CREATED = 'Visitor {0} has been created with context {1}, isAuthenticated:{2} and hasConsented {3}';
const VISITOR_PROFILE_LOADED = exports.VISITOR_PROFILE_LOADED = 'Visitor profile has been loaded {0}';
const VISITOR_ID_FROM_AB_TASTY_TAG = exports.VISITOR_ID_FROM_AB_TASTY_TAG = "The visitor ID '{0}' has been retrieved from ABTasty tag";
const VISITOR_ID_GENERATED = exports.VISITOR_ID_GENERATED = 'Visitor identifier is empty. A UUID {0} has been generated.';
const PREDEFINED_CONTEXT_LOADED = exports.PREDEFINED_CONTEXT_LOADED = 'Predefined Context have been loaded {0}';
const CONTEXT_KEY_ERROR = exports.CONTEXT_KEY_ERROR = `Visitor {0}, the key '{1}' must be a non null String.
Learn more: ${JS_DOC_URL}#updating-the-visitor-context`;
const CONTEXT_VALUE_ERROR = exports.CONTEXT_VALUE_ERROR = `Visitor {0}, 'value' for key '{1}[], must be one of the following types : String, Number, Boolean
Learn more: ${JS_DOC_URL}#updating-the-visitor-context`;
const PREDEFINED_CONTEXT_TYPE_ERROR = exports.PREDEFINED_CONTEXT_TYPE_ERROR = `visitor {0}, Predefined Context {0} must be of type {1}
Learn more: ${JS_DOC_URL}#predefined-user-context-keys-`;
const CONTEXT_KEY_VALUE_UPDATE = exports.CONTEXT_KEY_VALUE_UPDATE = 'visitor `{0}`, context have been updated: key {1}, value {2}, Context {3}';
const CONTEXT_OBJET_PARAM_UPDATE = exports.CONTEXT_OBJET_PARAM_UPDATE = 'visitor `{0}`, context have been updated: key/value {1}, Context {2}';
const CLEAR_CONTEXT = exports.CLEAR_CONTEXT = 'visitor `{0}`, context has been cleared cleared `{1}`';
const PROCESS_CLEAR_CONTEXT = exports.PROCESS_CLEAR_CONTEXT = 'CLEAR_CONTEXT';
const CONSENT_CHANGED = exports.CONSENT_CHANGED = 'Visitor `{0}` consent has been changed : {1}';
const PROCESS_SET_CONSENT = exports.PROCESS_SET_CONSENT = 'SET_CONSENT';
const FETCH_CAMPAIGNS_SUCCESS = exports.FETCH_CAMPAIGNS_SUCCESS = 'Visitor {0}, anonymousId {1} with context {2} has just fetched campaigns {3} in {4} ms';
const FETCH_CAMPAIGNS_FROM_CACHE = exports.FETCH_CAMPAIGNS_FROM_CACHE = 'Visitor {0}, anonymousId {1} with context {2} has just fetched campaigns from cache {3} in {4} ms';
const FETCH_FLAGS_FROM_CAMPAIGNS = exports.FETCH_FLAGS_FROM_CAMPAIGNS = 'Visitor {0}, anonymousId {1} with context {2} has just fetched flags {3} from Campaigns';
const FETCH_FLAGS_STARTED = exports.FETCH_FLAGS_STARTED = 'visitor `{0}` fetchFlags process is started';
const FETCH_FLAGS_PANIC_MODE = exports.FETCH_FLAGS_PANIC_MODE = 'Panic mode is enabled : all feature are disabled except fetchFlags.';
const PROCESS_FETCHING_FLAGS = exports.PROCESS_FETCHING_FLAGS = 'FETCH_FLAGS';
const GET_FLAG_MISSING_ERROR = exports.GET_FLAG_MISSING_ERROR = 'For the visitor "{0}", no flags were found with the key "{1}". Therefore, the default value "{2}" has been returned.';
const GET_FLAG_NOT_FOUND = exports.GET_FLAG_NOT_FOUND = 'For the visitor "{0}", no flags were found with the key "{1}". Therefore, an empty flag has been returned.';
const FETCH_FLAGS_MISSING = exports.FETCH_FLAGS_MISSING = 'Visitor {0} has {1} without calling fetchFlags method, ';
const FLAG_VALUE = exports.FLAG_VALUE = 'FLAG_VALUE';
const GET_FLAG = exports.GET_FLAG = 'GET_FLAG';
const GET_FLAG_CAST_ERROR = exports.GET_FLAG_CAST_ERROR = 'For the visitor "{0}", the flag with key "{1}" has a different type compared to the default value. Therefore, the default value "{2}" has been returned.';
const GET_FLAG_VALUE = exports.GET_FLAG_VALUE = 'Visitor {0}, Flag for key {1} returns value {2}';
const USER_EXPOSED_FLAG_ERROR = exports.USER_EXPOSED_FLAG_ERROR = 'For the visitor "{0}", no flags were found with the key "{1}". As a result, user exposure will not be sent.';
const VISITOR_EXPOSED_VALUE_NOT_CALLED = exports.VISITOR_EXPOSED_VALUE_NOT_CALLED = 'For the visitor `{0}`,assuming the getValue() method has not previously been invoked for the flag `{0}`. Therefore, the exposure is canceled';
const FLAG_VISITOR_EXPOSED = exports.FLAG_VISITOR_EXPOSED = 'FLAG_VISITOR_EXPOSED';
const USER_EXPOSED_CAST_ERROR = exports.USER_EXPOSED_CAST_ERROR = 'For the visitor `{0}, the flag with the key `{0}` has a different type compared to the default value. Therefore, the exposure is interrupted';
const GET_METADATA_CAST_ERROR = exports.GET_METADATA_CAST_ERROR = 'Visitor {0}, Flag for key {1} has a different type with default value: Empty metadata object is returned {2}';
const FLAG_METADATA = exports.FLAG_METADATA = 'FLAG_METADATA';
const NO_FLAG_METADATA = exports.NO_FLAG_METADATA = 'Visitor {0}, No Flags found for key {1}:  Empty metadata object is returned';
const METADATA_SDK_NOT_READY = exports.METADATA_SDK_NOT_READY = `Visitor {0}, Flag for key {1} Method Flag.metadata is deactivated while SDK status is NOT_READY: Empty metadata object is returned {2}
Learn more: ${JS_DOC_URL}#getting-flags-campaigns-metadata`;
const METADATA_PANIC_MODE = exports.METADATA_PANIC_MODE = `Visitor {0}, Flag for key {1} Method Flag.metadata is deactivated while SDK status is PANIC: Empty metadata object is returned {2}
Learn more: ${PANIC_MODE_DOC_URL}`;
const AUTHENTICATE = exports.AUTHENTICATE = 'AUTHENTICATE';
const VISITOR_AUTHENTICATE = exports.VISITOR_AUTHENTICATE = 'The visitor is authenticated with new visitor ID {0} anonymous ID {1}';
const VISITOR_ALREADY_AUTHENTICATE = exports.VISITOR_ALREADY_AUTHENTICATE = 'The visitor is already authenticated with visitor ID {0}';
const METHOD_DEACTIVATED_BUCKETING_ERROR = exports.METHOD_DEACTIVATED_BUCKETING_ERROR = 'Visitor {0}, Method {1} is deactivated on Bucketing mode';
const VISITOR_AUTHENTICATE_VISITOR_ID_ERROR = exports.VISITOR_AUTHENTICATE_VISITOR_ID_ERROR = `Visitor {0}, visitorId must not be null or empty
Learn more: ${JS_DOC_URL}#authenticate`;
const VISITOR_UNAUTHENTICATE = exports.VISITOR_UNAUTHENTICATE = 'The visitor is unauthenticated with visitor ID {0}';
const UNAUTHENTICATE = exports.UNAUTHENTICATE = 'UNAUTHENTICATE';
const FLAGSHIP_VISITOR_NOT_AUTHENTICATE = exports.FLAGSHIP_VISITOR_NOT_AUTHENTICATE = 'Visitor {0} is not authenticated yet';
const ALLOCATION = exports.ALLOCATION = 'ALLOCATION';
const BUCKETING_VARIATION_CACHE = exports.BUCKETING_VARIATION_CACHE = 'Visitor {0}, Variation {1} selected from cache.';
const BUCKETING_NEW_ALLOCATION = exports.BUCKETING_NEW_ALLOCATION = 'Visitor {0}, Variation {1} selected with allocation {2}.';
const LOOKUP_VISITOR_JSON_OBJECT_ERROR = exports.LOOKUP_VISITOR_JSON_OBJECT_ERROR = `lookupVisitor method has loaded a bad format version ({0}) for visitor {1}.
Learn more: ${JS_DOC_URL}#managing-visitor-cache`;
const PROCESS_CACHE = exports.PROCESS_CACHE = 'CACHE';
const VISITOR_CACHE_ERROR = exports.VISITOR_CACHE_ERROR = 'visitor {0}. {1} threw an exception {2}';
const HIT_CACHE_ERROR = exports.HIT_CACHE_ERROR = '{0} threw an exception {1}';
const VISITOR_CACHE_LOADED = exports.VISITOR_CACHE_LOADED = 'Visitor {0}, visitor cache has been loaded from database: {1}';
const VISITOR_CACHE_SAVED = exports.VISITOR_CACHE_SAVED = 'Visitor {0}, visitor cache has been saved into database : {0}';
const VISITOR_CACHE_FLUSHED = exports.VISITOR_CACHE_FLUSHED = 'Visitor {0}, visitor cache has been flushed from database.';
const HIT_CACHE_LOADED = exports.HIT_CACHE_LOADED = 'Hits cache has been loaded from database: {0}';
const HIT_CACHE_SAVED = exports.HIT_CACHE_SAVED = 'Hit cache has been saved into database : {0}';
const HIT_DATA_FLUSHED = exports.HIT_DATA_FLUSHED = 'The following hit keys have been flushed from database : {0}';
const ALL_HITS_FLUSHED = exports.ALL_HITS_FLUSHED = 'All hits cache has been flushed from database';
const BATCH_LOOP_STARTED = exports.BATCH_LOOP_STARTED = 'The Batch Loop has been started with a time interval of {0} ms';
const TRACKING_MANAGER = exports.TRACKING_MANAGER = 'TRACKING_MANAGER';
const BATCH_LOOP_STOPPED = exports.BATCH_LOOP_STOPPED = 'The Batch Loop has been stopped';
const TRACKING_MANAGER_ERROR = exports.TRACKING_MANAGER_ERROR = '{0} Unexpected Error occurred {1}';
const HIT_SENT_SUCCESS = exports.HIT_SENT_SUCCESS = '{0} has been sent : {1}';
const ACTIVATE_HIT = exports.ACTIVATE_HIT = 'ACTIVATE HIT';
const BATCH_HIT = exports.BATCH_HIT = 'BATCH HIT';
const DIRECT_HIT = exports.DIRECT_HIT = 'HIT';
const GET_THIRD_PARTY_SEGMENT = exports.GET_THIRD_PARTY_SEGMENT = 'GET_THIRD_PARTY_SEGMENT';
const CONSENT_NOT_SPECIFY_WARNING = exports.CONSENT_NOT_SPECIFY_WARNING = 'Consent has not been specified. By default, consent is set to false, which may result in some features being deactivated.';
const ACTION_TRACKING = exports.ACTION_TRACKING = 'Action Tracking';
const ACTION_TRACKING_HIT_RECEIVED = exports.ACTION_TRACKING_HIT_RECEIVED = 'Hit received: {0}';
const ACTION_TRACKING_INVALID_HIT = exports.ACTION_TRACKING_INVALID_HIT = 'Invalid hit data: {0}';
const ACTION_TRACKING_INVALID_NONCE = exports.ACTION_TRACKING_INVALID_NONCE = 'Invalid nonce: {0}';
const ACTION_TRACKING_SENDING_HIT = exports.ACTION_TRACKING_SENDING_HIT = 'Sending hit: {0}';
const ACTION_TRACKING_SENDING_HIT_ERROR = exports.ACTION_TRACKING_SENDING_HIT_ERROR = 'Failed to send hit: {0}';
const ACTION_TRACKING_DISPATCHED = exports.ACTION_TRACKING_DISPATCHED = 'action tracking hits have been dispatched {0}';
const TRUSTED_QA_ORIGINS = exports.TRUSTED_QA_ORIGINS = ['https://local-qa-assistant.abtasty.com:5000', 'https://staging-qa-assistant.abtasty.com', 'https://qa-assistant.abtasty.com', 'https://qa-assistant.flagship.io', 'https://local-qa-assistant.flagship.io:5000', 'https://staging-qa-assistant.flagship.io'];

/***/ }),

/***/ 2967:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* eslint-disable no-proto -- safe */
var uncurryThisAccessor = __webpack_require__(6706);
var isObject = __webpack_require__(34);
var requireObjectCoercible = __webpack_require__(7750);
var aPossiblePrototype = __webpack_require__(3506);

// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
// Works with __proto__ only. Old v8 can't work with null proto objects.
// eslint-disable-next-line es/no-object-setprototypeof -- safe
module.exports = Object.setPrototypeOf || ('__proto__' in {} ? function () {
  var CORRECT_SETTER = false;
  var test = {};
  var setter;
  try {
    setter = uncurryThisAccessor(Object.prototype, '__proto__', 'set');
    setter(test, []);
    CORRECT_SETTER = test instanceof Array;
  } catch (error) { /* empty */ }
  return function setPrototypeOf(O, proto) {
    requireObjectCoercible(O);
    aPossiblePrototype(proto);
    if (!isObject(O)) return O;
    if (CORRECT_SETTER) setter(O, proto);
    else O.__proto__ = proto;
    return O;
  };
}() : undefined);


/***/ }),

/***/ 2987:
/***/ ((module) => {

function _arrayWithHoles(r) {
  if (Array.isArray(r)) return r;
}
module.exports = _arrayWithHoles, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 3063:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// https://github.com/zloirock/core-js/issues/280
var userAgent = __webpack_require__(2839);

module.exports = /Version\/10(?:\.\d+){1,2}(?: [\w./]+)?(?: Mobile\/\w+)? Safari\//.test(userAgent);


/***/ }),

/***/ 3084:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BatchingContinuousCachingStrategy = void 0;
__webpack_require__(2008);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(2062);
__webpack_require__(4782);
__webpack_require__(6033);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _FlagshipConstant = __webpack_require__(2932);
var _BatchTriggeredBy = __webpack_require__(8160);
var _index = __webpack_require__(5394);
var _ActivateBatch = __webpack_require__(7783);
var _utils = __webpack_require__(6517);
var _BatchingCachingStrategyAbstract = __webpack_require__(3787);
var _types = __webpack_require__(7099);
var _Troubleshooting = __webpack_require__(3142);
class BatchingContinuousCachingStrategy extends _BatchingCachingStrategyAbstract.BatchingCachingStrategyAbstract {
  addHitInPoolQueue(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this._hitsPoolQueue.set(hit.key, hit);
      yield _this.cacheHit(new Map([[hit.key, hit]]));
    })();
  }
  sendActivateHitBatch(activateBatch, batchTriggeredBy, currentActivate) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const headers = {
        [_index.HEADER_X_API_KEY]: _this2.config.apiKey,
        [_index.HEADER_X_SDK_CLIENT]: _index.SDK_INFO.name,
        [_index.HEADER_X_SDK_VERSION]: _index.SDK_INFO.version,
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      if (currentActivate) {
        activateBatch.hits.push(currentActivate);
      }
      const requestBody = activateBatch.toApiKeys();
      const url = _index.BASE_API_URL + _index.URL_ACTIVATE_MODIFICATION;
      const now = Date.now();
      try {
        yield _this2._httpClient.postAsync(url, {
          headers,
          body: requestBody,
          timeout: _this2.config.timeout,
          nextFetchConfig: _this2.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this2.config, _FlagshipConstant.TRACKING_MANAGER, _FlagshipConstant.HIT_SENT_SUCCESS, _FlagshipConstant.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const hitKeysToRemove = activateBatch.hits.filter(item => item.key !== (currentActivate == null ? void 0 : currentActivate.key)).map(item => item.key);
        activateBatch.hits.forEach(item => {
          _this2.onVisitorExposed(item);
        });
        if (hitKeysToRemove.length) {
          yield _this2.flushHits(hitKeysToRemove);
        }
        _this2.sendHitsToFsQa(activateBatch.hits);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        activateBatch.hits.forEach(item => {
          _this2._activatePoolQueue.set(item.key, item);
        });
        if (currentActivate) {
          yield _this2.cacheHit(new Map([[currentActivate.key, currentActivate]]));
        }
        (0, _utils.logErrorSprintf)(_this2.config, _FlagshipConstant.TRACKING_MANAGER, _FlagshipConstant.TRACKING_MANAGER_ERROR, _FlagshipConstant.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_ACTIVATE_HIT_ROUTE_ERROR,
          logLevel: _index.LogLevel.ERROR,
          visitorId: `${_this2._flagshipInstanceId}`,
          traffic: 0,
          config: _this2.config,
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now,
          batchTriggeredBy
        });
        _this2.sendTroubleshootingHit(monitoringHttpResponse);
      }
    })();
  }
  sendActivate({
    activateHitsPool,
    currentActivate,
    batchTriggeredBy
  }) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const filteredItems = Array.from(activateHitsPool.filter(item => Date.now() - item.createdAt < _FlagshipConstant.DEFAULT_HIT_CACHE_TIME_MS));
      if (!filteredItems.length && currentActivate) {
        const batch = new _ActivateBatch.ActivateBatch([], _this3.config);
        yield _this3.sendActivateHitBatch(batch, batchTriggeredBy, currentActivate);
        return;
      }
      for (let i = 0; i < filteredItems.length; i += _FlagshipConstant.MAX_ACTIVATE_HIT_PER_BATCH) {
        const batch = new _ActivateBatch.ActivateBatch(filteredItems.slice(i, i + _FlagshipConstant.MAX_ACTIVATE_HIT_PER_BATCH), _this3.config);
        _this3.sendActivateHitBatch(batch, batchTriggeredBy, i === 0 ? currentActivate : undefined);
      }
    })();
  }
}
exports.BatchingContinuousCachingStrategy = BatchingContinuousCachingStrategy;

/***/ }),

/***/ 3138:
/***/ ((module) => {

"use strict";

module.exports = function (a, b) {
  try {
    // eslint-disable-next-line no-console -- safe
    arguments.length === 1 ? console.error(a) : console.error(a, b);
  } catch (error) { /* empty */ }
};


/***/ }),

/***/ 3142:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Troubleshooting = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _Diagnostic = __webpack_require__(2413);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
class Troubleshooting extends _Diagnostic.Diagnostic {
  constructor(param) {
    super(_objectSpread(_objectSpread({}, param), {}, {
      type: 'TROUBLESHOOTING'
    }));
  }
}
exports.Troubleshooting = Troubleshooting;

/***/ }),

/***/ 3167:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isCallable = __webpack_require__(4901);
var isObject = __webpack_require__(34);
var setPrototypeOf = __webpack_require__(2967);

// makes subclassing work correct for wrapped built-ins
module.exports = function ($this, dummy, Wrapper) {
  var NewTarget, NewTargetPrototype;
  if (
    // it can work only with native `setPrototypeOf`
    setPrototypeOf &&
    // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
    isCallable(NewTarget = dummy.constructor) &&
    NewTarget !== Wrapper &&
    isObject(NewTargetPrototype = NewTarget.prototype) &&
    NewTargetPrototype !== Wrapper.prototype
  ) setPrototypeOf($this, NewTargetPrototype);
  return $this;
};


/***/ }),

/***/ 3233:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.ApiSdkManager = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _types = __webpack_require__(7099);
var _FlagshipConstant = __webpack_require__(2932);
var _utils = __webpack_require__(6517);
var _LogLevel = __webpack_require__(4288);
var _Troubleshooting = __webpack_require__(3142);
class ApiSdkManager {
  constructor({
    httpClient,
    sdkConfig,
    trackingManager,
    flagshipInstanceId
  }) {
    this._httpClient = httpClient;
    this._config = sdkConfig;
    this._trackingManager = trackingManager;
    this._flagshipInstanceId = flagshipInstanceId;
  }
  resetSdk() {
    this._EAIConfig = undefined;
  }
  getBucketingContent() {
    return undefined;
  }
  sendTroubleshooting(accountSettings, url, response, now) {
    const troubleshooting = new _Troubleshooting.Troubleshooting({
      flagshipInstanceId: this._flagshipInstanceId,
      label: _types.TroubleshootingLabel.ACCOUNT_SETTINGS,
      logLevel: _LogLevel.LogLevel.DEBUG,
      visitorId: this._flagshipInstanceId,
      config: this._config,
      accountSettings,
      traffic: 0,
      httpRequestMethod: 'POST',
      httpRequestUrl: url,
      httpResponseHeaders: response == null ? void 0 : response.headers,
      httpResponseCode: response == null ? void 0 : response.status,
      httpResponseTime: Date.now() - now
    });
    this._trackingManager.initTroubleshootingHit = troubleshooting;
  }
  sendErrorTroubleshooting(url, error, now) {
    const troubleshootingHit = new _Troubleshooting.Troubleshooting({
      visitorId: this._flagshipInstanceId,
      flagshipInstanceId: this._flagshipInstanceId,
      label: _types.TroubleshootingLabel.ACCOUNT_SETTINGS_ERROR,
      traffic: 0,
      logLevel: _LogLevel.LogLevel.ERROR,
      config: this._config,
      httpRequestMethod: 'POST',
      httpRequestUrl: url,
      httpResponseBody: error == null ? void 0 : error.message,
      httpResponseHeaders: error == null ? void 0 : error.headers,
      httpResponseCode: error == null ? void 0 : error.status,
      httpResponseTime: Date.now() - now
    });
    this._trackingManager.initTroubleshootingHit = troubleshootingHit;
  }
  initSdk() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const now = Date.now();
      const url = (0, _utils.sprintf)(_FlagshipConstant.CDN_ACCOUNT_SETTINGS_URL, _this._config.envId);
      try {
        const response = yield _this._httpClient.getAsync(url);
        _this._EAIConfig = response.body.accountSettings;
        _this.sendTroubleshooting(response.body.accountSettings, url, response, now);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this._config, 'Error while fetching EAI config: {0}', (error == null ? void 0 : error.message) || error);
        _this.sendErrorTroubleshooting(url, error, now);
      }
    })();
  }
  getEAIConfig() {
    return this._EAIConfig;
  }
}
exports.ApiSdkManager = ApiSdkManager;

/***/ }),

/***/ 3263:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BucketingSdkManager = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _types = __webpack_require__(7099);
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _Troubleshooting = __webpack_require__(3142);
class BucketingSdkManager {
  constructor({
    httpClient,
    sdkConfig,
    trackingManager,
    flagshipInstanceId
  }) {
    this._httpClient = httpClient;
    this._config = sdkConfig;
    this._trackingManager = trackingManager;
    this._bucketingContent = sdkConfig.initialBucketing;
    this._flagshipInstanceId = flagshipInstanceId;
    this._lastModified = '';
  }
  resetSdk() {
    if (this._intervalID) {
      clearInterval(this._intervalID);
    }
    this._isPooling = false;
    this._intervalID = undefined;
    this._lastModified = '';
    this._bucketingContent = undefined;
    this._EAIConfig = undefined;
    (0, _utils.logInfo)(this._config, _index.BUCKETING_POOLING_STOPPED, _index.PROCESS_BUCKETING);
  }
  getBucketingContent() {
    return this._bucketingContent;
  }
  initSdk() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const timeout = _this._config.pollingInterval * 1000;
      yield _this.fetchBucketingFile();
      if (timeout === 0) {
        return;
      }
      (0, _utils.logInfo)(_this._config, _index.BUCKETING_POOLING_STARTED, _index.PROCESS_BUCKETING);
      _this._intervalID = setInterval(() => {
        _this.fetchBucketingFile();
      }, timeout);
    })();
  }
  getEAIConfig() {
    return this._EAIConfig;
  }
  sendTroubleshooting(headers, url, response, now) {
    const troubleshootingHit = new _Troubleshooting.Troubleshooting({
      visitorId: this._flagshipInstanceId,
      flagshipInstanceId: this._flagshipInstanceId,
      label: _types.TroubleshootingLabel.SDK_BUCKETING_FILE,
      traffic: 0,
      logLevel: _index.LogLevel.INFO,
      config: this._config,
      httpRequestHeaders: headers,
      httpRequestMethod: 'POST',
      httpRequestUrl: url,
      httpResponseBody: response == null ? void 0 : response.body,
      httpResponseHeaders: response == null ? void 0 : response.headers,
      httpResponseCode: response == null ? void 0 : response.status,
      httpResponseTime: Date.now() - now
    });
    this._trackingManager.initTroubleshootingHit = troubleshootingHit;
  }
  sendErrorTroubleshooting(headers, url, error, now) {
    const troubleshootingHit = new _Troubleshooting.Troubleshooting({
      visitorId: this._flagshipInstanceId,
      flagshipInstanceId: this._flagshipInstanceId,
      label: _types.TroubleshootingLabel.SDK_BUCKETING_FILE_ERROR,
      traffic: 0,
      logLevel: _index.LogLevel.INFO,
      config: this._config,
      httpRequestHeaders: headers,
      httpRequestMethod: 'POST',
      httpRequestUrl: url,
      httpResponseBody: error == null ? void 0 : error.message,
      httpResponseHeaders: error == null ? void 0 : error.headers,
      httpResponseCode: error == null ? void 0 : error.statusCode,
      httpResponseTime: Date.now() - now
    });
    this._trackingManager.initTroubleshootingHit = troubleshootingHit;
  }
  handlePollingResponse(params) {
    var _response$headers;
    const response = params.response;
    if (response.status === 200) {
      var _this$_bucketingConte, _this$_bucketingConte2;
      (0, _utils.logDebugSprintf)(this._config, _index.PROCESS_BUCKETING, _index.POLLING_EVENT_200, response.body);
      this._bucketingContent = response.body;
      this._lastBucketingTimestamp = new Date().toISOString();
      this._EAIConfig = {
        eaiCollectEnabled: !!((_this$_bucketingConte = this._bucketingContent) != null && (_this$_bucketingConte = _this$_bucketingConte.accountSettings) != null && _this$_bucketingConte.eaiActivationEnabled),
        eaiActivationEnabled: !!((_this$_bucketingConte2 = this._bucketingContent) != null && (_this$_bucketingConte2 = _this$_bucketingConte2.accountSettings) != null && _this$_bucketingConte2.eaiCollectEnabled)
      };
      this.sendTroubleshooting(params.headers, params.url, response, params.now);
    } else if (response.status === 304) {
      (0, _utils.logDebug)(this._config, _index.POLLING_EVENT_300, _index.PROCESS_BUCKETING);
    }
    const lastModified = response == null || (_response$headers = response.headers) == null ? void 0 : _response$headers['last-modified'];
    if (lastModified) {
      if (this._lastModified !== lastModified && this._config.onBucketingUpdated) {
        this._config.onBucketingUpdated(new Date(lastModified));
      }
      this._lastModified = lastModified;
    }
    this._isPooling = false;
  }
  fetchBucketingFile() {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (_this2._isPooling) {
        return;
      }
      _this2._isPooling = true;
      const url = (0, _utils.sprintf)(_index.BUCKETING_API_URL, _this2._config.envId);
      const headers = {
        [_index.HEADER_X_SDK_CLIENT]: _index.SDK_INFO.name,
        [_index.HEADER_X_SDK_VERSION]: _index.SDK_INFO.version,
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const now = Date.now();
      try {
        if (_this2._lastModified) {
          headers['if-modified-since'] = _this2._lastModified;
        }
        const response = yield _this2._httpClient.getAsync(url, {
          headers,
          timeout: _this2._config.timeout,
          nextFetchConfig: _this2._config.nextFetchConfig
        });
        _this2.handlePollingResponse({
          response,
          headers,
          url,
          now
        });

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        _this2._isPooling = false;
        (0, _utils.logError)(_this2._config, (0, _utils.errorFormat)(_index.POLLING_EVENT_FAILED, {
          url,
          headers,
          error,
          nextFetchConfig: _this2._config.nextFetchConfig,
          method: 'GET',
          duration: Date.now() - now
        }), _index.PROCESS_BUCKETING);
        _this2.sendErrorTroubleshooting(headers, url, error, now);
      }
    })();
  }
}
exports.BucketingSdkManager = BucketingSdkManager;

/***/ }),

/***/ 3361:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.Visitor = void 0;
var _depsNode = __webpack_require__(4878);
var _index = __webpack_require__(5394);
/**
 * The `Visitor` class represents a unique user within your application. It aids in
 * managing the visitor's data and fetching the corresponding flags for the visitor
 * from the [Flagship platform](https://app.flagship.io/login) .
 */
class Visitor extends _depsNode.EventEmitter {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any

  constructor(visitorDelegate) {
    super();
    this.visitorDelegate = visitorDelegate;
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    this._onReady = err => {
      this.emit(_index.EMIT_READY, err);
    };
    this.visitorDelegate.on(_index.EMIT_READY, this._onReady);
    const instance = this;
    instance.sendEaiVisitorEvent = event => {
      this.visitorDelegate.sendEaiVisitorEvent(event);
    };
    instance.sendEaiPageView = pageView => {
      this.visitorDelegate.sendEaiPageView(pageView);
    };
    instance.onEAICollectStatusChange = callback => {
      this.visitorDelegate.onEAICollectStatusChange(callback);
    };
  }

  /**
   * @inheritdoc
   */
  get visitorId() {
    return this.visitorDelegate.visitorId;
  }

  /**
   * @inheritdoc
   */
  set visitorId(v) {
    this.visitorDelegate.visitorId = v;
  }

  /**
   * @inheritdoc
   */
  get anonymousId() {
    return this.visitorDelegate.anonymousId;
  }

  /**
   * @inheritdoc
   */
  get config() {
    return this.visitorDelegate.config;
  }

  /**
   * @inheritdoc
   */
  get context() {
    return this.visitorDelegate.context;
  }

  /**
   * @inheritdoc
   */
  get flagsStatus() {
    return this.visitorDelegate.flagsStatus;
  }

  /**
   * @inheritdoc
   */
  get hasConsented() {
    return this.visitorDelegate.hasConsented;
  }

  /**
   * @inheritdoc
   */
  setConsent(hasConsented) {
    this.visitorDelegate.setConsent(hasConsented);
  }

  /**
   * @inheritdoc
   */

  updateContext(context, value) {
    this.visitorDelegate.updateContext(context, value);
  }

  /**
   * @inheritdoc
   */
  clearContext() {
    this.visitorDelegate.clearContext();
  }

  /**
   * @inheritdoc
   */
  getFlag(key) {
    return this.visitorDelegate.getFlag(key);
  }

  /**
   * @inheritdoc
   */
  getFlags() {
    return this.visitorDelegate.getFlags();
  }

  /**
   * @inheritdoc
   */
  fetchFlags() {
    return this.visitorDelegate.fetchFlags();
  }

  /**
   * @inheritdoc
   */

  sendHit(hit) {
    return this.visitorDelegate.sendHit(hit);
  }

  /**
   * @inheritdoc
   */

  sendHits(hits) {
    return this.visitorDelegate.sendHits(hits);
  }

  /**
   * @inheritdoc
   */
  authenticate(visitorId) {
    this.visitorDelegate.authenticate(visitorId);
  }

  /**
   * @inheritdoc
   */
  unauthenticate() {
    this.visitorDelegate.unauthenticate();
  }

  /**
   * @inheritdoc
   */
  collectEAIEventsAsync(...args) {
    let currentPage;
    if (args.length > 0) {
      currentPage = args[0];
    }
    return this.visitorDelegate.collectEAIEventsAsync(currentPage);
  }

  /**
   * @inheritdoc
   */

  cleanup() {
    this.visitorDelegate.cleanup();
    this.visitorDelegate.off(_index.EMIT_READY, this._onReady);
  }
}
exports.Visitor = Visitor;

/***/ }),

/***/ 3362:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove this module from `core-js@4` since it's split to modules listed below
__webpack_require__(436);
__webpack_require__(6499);
__webpack_require__(2003);
__webpack_require__(7743);
__webpack_require__(1481);
__webpack_require__(280);


/***/ }),

/***/ 3389:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var DESCRIPTORS = __webpack_require__(3724);

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Avoid NodeJS experimental warning
module.exports = function (name) {
  if (!DESCRIPTORS) return globalThis[name];
  var descriptor = getOwnPropertyDescriptor(globalThis, name);
  return descriptor && descriptor.value;
};


/***/ }),

/***/ 3392:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);

var id = 0;
var postfix = Math.random();
var toString = uncurryThis(1.0.toString);

module.exports = function (key) {
  return 'Symbol(' + (key === undefined ? '' : key) + ')_' + toString(++id + postfix, 36);
};


/***/ }),

/***/ 3418:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var from = __webpack_require__(7916);
var checkCorrectnessOfIteration = __webpack_require__(4428);

var INCORRECT_ITERATION = !checkCorrectnessOfIteration(function (iterable) {
  // eslint-disable-next-line es/no-array-from -- required for testing
  Array.from(iterable);
});

// `Array.from` method
// https://tc39.es/ecma262/#sec-array.from
$({ target: 'Array', stat: true, forced: INCORRECT_ITERATION }, {
  from: from
});


/***/ }),

/***/ 3438:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var anObject = __webpack_require__(8551);
var isObject = __webpack_require__(34);
var newPromiseCapability = __webpack_require__(6043);

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};


/***/ }),

/***/ 3451:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var uncurryThis = __webpack_require__(9504);
var hiddenKeys = __webpack_require__(421);
var isObject = __webpack_require__(34);
var hasOwn = __webpack_require__(9297);
var defineProperty = (__webpack_require__(4913).f);
var getOwnPropertyNamesModule = __webpack_require__(8480);
var getOwnPropertyNamesExternalModule = __webpack_require__(298);
var isExtensible = __webpack_require__(4124);
var uid = __webpack_require__(3392);
var FREEZING = __webpack_require__(2744);

var REQUIRED = false;
var METADATA = uid('meta');
var id = 0;

var setMetadata = function (it) {
  defineProperty(it, METADATA, { value: {
    objectID: 'O' + id++, // object ID
    weakData: {}          // weak collections IDs
  } });
};

var fastKey = function (it, create) {
  // return a primitive with prefix
  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
  if (!hasOwn(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return 'F';
    // not necessary to add metadata
    if (!create) return 'E';
    // add missing metadata
    setMetadata(it);
  // return object ID
  } return it[METADATA].objectID;
};

var getWeakData = function (it, create) {
  if (!hasOwn(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return true;
    // not necessary to add metadata
    if (!create) return false;
    // add missing metadata
    setMetadata(it);
  // return the store of weak collections IDs
  } return it[METADATA].weakData;
};

// add metadata on freeze-family methods calling
var onFreeze = function (it) {
  if (FREEZING && REQUIRED && isExtensible(it) && !hasOwn(it, METADATA)) setMetadata(it);
  return it;
};

var enable = function () {
  meta.enable = function () { /* empty */ };
  REQUIRED = true;
  var getOwnPropertyNames = getOwnPropertyNamesModule.f;
  var splice = uncurryThis([].splice);
  var test = {};
  test[METADATA] = 1;

  // prevent exposing of metadata key
  if (getOwnPropertyNames(test).length) {
    getOwnPropertyNamesModule.f = function (it) {
      var result = getOwnPropertyNames(it);
      for (var i = 0, length = result.length; i < length; i++) {
        if (result[i] === METADATA) {
          splice(result, i, 1);
          break;
        }
      } return result;
    };

    $({ target: 'Object', stat: true, forced: true }, {
      getOwnPropertyNames: getOwnPropertyNamesExternalModule.f
    });
  }
};

var meta = module.exports = {
  enable: enable,
  fastKey: fastKey,
  getWeakData: getWeakData,
  onFreeze: onFreeze
};

hiddenKeys[METADATA] = true;


/***/ }),

/***/ 3506:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isPossiblePrototype = __webpack_require__(3925);

var $String = String;
var $TypeError = TypeError;

module.exports = function (argument) {
  if (isPossiblePrototype(argument)) return argument;
  throw new $TypeError("Can't set " + $String(argument) + ' as a prototype');
};


/***/ }),

/***/ 3517:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var fails = __webpack_require__(9039);
var isCallable = __webpack_require__(4901);
var classof = __webpack_require__(6955);
var getBuiltIn = __webpack_require__(7751);
var inspectSource = __webpack_require__(3706);

var noop = function () { /* empty */ };
var construct = getBuiltIn('Reflect', 'construct');
var constructorRegExp = /^\s*(?:class|function)\b/;
var exec = uncurryThis(constructorRegExp.exec);
var INCORRECT_TO_STRING = !constructorRegExp.test(noop);

var isConstructorModern = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  try {
    construct(noop, [], argument);
    return true;
  } catch (error) {
    return false;
  }
};

var isConstructorLegacy = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  switch (classof(argument)) {
    case 'AsyncFunction':
    case 'GeneratorFunction':
    case 'AsyncGeneratorFunction': return false;
  }
  try {
    // we can't check .prototype since constructors produced by .bind haven't it
    // `Function#toString` throws on some built-it function in some legacy engines
    // (for example, `DOMQuad` and similar in FF41-)
    return INCORRECT_TO_STRING || !!exec(constructorRegExp, inspectSource(argument));
  } catch (error) {
    return true;
  }
};

isConstructorLegacy.sham = true;

// `IsConstructor` abstract operation
// https://tc39.es/ecma262/#sec-isconstructor
module.exports = !construct || fails(function () {
  var called;
  return isConstructorModern(isConstructorModern.call)
    || !isConstructorModern(Object)
    || !isConstructorModern(function () { called = true; })
    || called;
}) ? isConstructorLegacy : isConstructorModern;


/***/ }),

/***/ 3545:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Segment = exports.ERROR_MESSAGE = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'data property is required';
class Segment extends _HitAbstract.HitAbstract {
  get context() {
    return this._context;
  }
  set context(v) {
    this._context = v;
  }
  constructor(param) {
    super({
      type: 'SEGMENT',
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode,
      isActionTrackingHit: param.isActionTrackingHit
    });
    this.context = param.context;
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.context);
  }
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_index.S_API_ITEM] = this.context;
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      context: this.context
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Segment = Segment;

/***/ }),

/***/ 3609:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
Object.defineProperty(exports, "LocalAbortController", ({
  enumerable: true,
  get: function get() {
    return _nodeAbortController.AbortController;
  }
}));
var _nodeAbortController = __webpack_require__(4985);

/***/ }),

/***/ 3635:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var globalThis = __webpack_require__(2195);

// babel-minify and Closure Compiler transpiles RegExp('.', 's') -> /./s and it causes SyntaxError
var $RegExp = globalThis.RegExp;

module.exports = fails(function () {
  var re = $RegExp('.', 's');
  return !(re.dotAll && re.test('\n') && re.flags === 's');
});


/***/ }),

/***/ 3693:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var toPropertyKey = __webpack_require__(7736);
function _defineProperty(e, r, t) {
  return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : e[r] = t, e;
}
module.exports = _defineProperty, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 3706:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var isCallable = __webpack_require__(4901);
var store = __webpack_require__(7629);

var functionToString = uncurryThis(Function.toString);

// this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
if (!isCallable(store.inspectSource)) {
  store.inspectSource = function (it) {
    return functionToString(it);
  };
}

module.exports = store.inspectSource;


/***/ }),

/***/ 3717:
/***/ ((__unused_webpack_module, exports) => {

"use strict";

// eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;


/***/ }),

/***/ 3724:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);

// Detect IE8's incomplete defineProperty implementation
module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty({}, 1, { get: function () { return 7; } })[1] !== 7;
});


/***/ }),

/***/ 3738:
/***/ ((module) => {

function _typeof(o) {
  "@babel/helpers - typeof";

  return module.exports = _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
    return typeof o;
  } : function (o) {
    return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
  }, module.exports.__esModule = true, module.exports["default"] = module.exports, _typeof(o);
}
module.exports = _typeof, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 3772:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove this module from `core-js@4` since it's replaced to module below
__webpack_require__(5746);


/***/ }),

/***/ 3787:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(4782);
__webpack_require__(1278);
__webpack_require__(7495);
__webpack_require__(3772);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BatchingCachingStrategyAbstract = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _slicedToArray2 = _interopRequireDefault(__webpack_require__(5715));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
__webpack_require__(2008);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(2062);
__webpack_require__(6033);
__webpack_require__(3362);
var _index = __webpack_require__(6701);
var _BatchTriggeredBy = __webpack_require__(8160);
var _index2 = __webpack_require__(5394);
var _Activate = __webpack_require__(9995);
var _Troubleshooting = __webpack_require__(3142);
var _index3 = __webpack_require__(5588);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _Batch = __webpack_require__(1728);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
class BatchingCachingStrategyAbstract {
  get flagshipInstanceId() {
    return this._flagshipInstanceId;
  }
  get troubleshootingData() {
    return this._troubleshootingData;
  }
  set troubleshootingData(v) {
    this._troubleshootingData = v;
  }
  get config() {
    return this._config;
  }
  get initTroubleshootingHit() {
    return this._initTroubleshootingHit;
  }
  set initTroubleshootingHit(v) {
    this._initTroubleshootingHit = v;
  }
  constructor(param) {
    const config = param.config,
      hitsPoolQueue = param.hitsPoolQueue,
      httpClient = param.httpClient,
      activatePoolQueue = param.activatePoolQueue,
      troubleshootingQueue = param.troubleshootingQueue,
      flagshipInstanceId = param.flagshipInstanceId,
      analyticHitQueue = param.analyticHitQueue,
      initTroubleshootingHi = param.initTroubleshootingHit,
      sharedActionTracking = param.sharedActionTracking;
    this._HitsToFsQa = [];
    this._hasInitTroubleshootingHitSent = false;
    this._config = config;
    this._hitsPoolQueue = hitsPoolQueue;
    this._httpClient = httpClient;
    this._activatePoolQueue = activatePoolQueue;
    this._troubleshootingQueue = troubleshootingQueue;
    this._flagshipInstanceId = flagshipInstanceId;
    this._usageHitQueue = analyticHitQueue;
    this._isUsageHitQueueSending = false;
    this._isTroubleshootingQueueSending = false;
    this._initTroubleshootingHit = initTroubleshootingHi;
    this._sharedActionTracking = sharedActionTracking;
  }
  dispatchHitsToTag(hits) {
    if (false) { var _this$_sharedActionTr, _iterator, _step; }
  }
  sendHitsToFsQa(hits) {
    if (false) {}
  }
  addHit(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this$config$tracking;
      const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
      hit.key = hitKey;
      yield _this.addHitInPoolQueue(hit);
      if (hit.type === _index2.HitType.EVENT && hit.action === _index2.FS_CONSENT && hit.label === `${_index2.SDK_INFO.name}:false`) {
        yield _this.notConsent(hit.visitorId);
      }
      (0, _utils.logDebugSprintf)(_this.config, _index2.TRACKING_MANAGER, _index2.HIT_ADDED_IN_QUEUE, hit.toApiKeys());
      if ((_this$config$tracking = _this.config.trackingManagerConfig) != null && _this$config$tracking.poolMaxSize && _this._hitsPoolQueue.size >= _this.config.trackingManagerConfig.poolMaxSize && _this.config.decisionMode !== _index.DecisionMode.BUCKETING_EDGE) {
        _this.sendBatch();
      }
    })();
  }
  activateFlag(paramHit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const hit = new _Activate.Activate(paramHit);
      hit.config = _this2.config;
      const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
      hit.key = hitKey;
      if (_this2.config.decisionMode === _index.DecisionMode.BUCKETING_EDGE || _this2.config.batchActivateHits) {
        yield _this2.activateFlagEdgeMode(hit);
        (0, _utils.logDebug)(_this2.config, (0, _utils.sprintf)(_index2.ACTIVATE_ADDED_IN_QUEUE, JSON.stringify(hit.toApiKeys())), _index2.ADD_ACTIVATE);
        return;
      }
      let activateHitsPool = [];
      if (_this2._activatePoolQueue.size) {
        activateHitsPool = Array.from(_this2._activatePoolQueue.values());
      }
      _this2._activatePoolQueue.clear();
      yield _this2.sendActivate({
        activateHitsPool,
        currentActivate: hit,
        batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy.ActivateLength
      });
    })();
  }
  activateFlagEdgeMode(hit) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this3._activatePoolQueue.set(hit.key, hit);
      yield _this3.cacheHit(new Map([[hit.key, hit]]));
    })();
  }
  onVisitorExposed(activate) {
    const onVisitorExposed = this.config.onVisitorExposed;
    if (typeof onVisitorExposed !== 'function') {
      return;
    }
    const fromFlag = {
      key: activate.flagKey,
      value: activate.flagValue,
      defaultValue: activate.flagDefaultValue,
      metadata: activate.flagMetadata
    };
    const exposedVisitor = {
      id: activate.visitorId,
      anonymousId: activate.anonymousId,
      context: activate.visitorContext
    };
    onVisitorExposed({
      exposedVisitor,
      fromFlag
    });
  }
  sendBatch(batchTriggeredBy = _BatchTriggeredBy.BatchTriggeredBy.BatchLength) {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (_this4._activatePoolQueue.size) {
        const activateHits = Array.from(_this4._activatePoolQueue.values());
        _this4._activatePoolQueue.clear();
        yield _this4.sendActivate({
          activateHitsPool: activateHits,
          batchTriggeredBy
        });
      }
      const batch = new _Batch.Batch({
        hits: [],
        ds: _index2.SDK_APP
      });
      batch.config = _this4.config;
      const hitKeysToRemove = [];
      var _iterator2 = _createForOfIteratorHelper(_this4._hitsPoolQueue),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          const _step2$value = (0, _slicedToArray2.default)(_step2.value, 2),
            key = _step2$value[0],
            item = _step2$value[1];
          if (Date.now() - item.createdAt >= _index2.DEFAULT_HIT_CACHE_TIME_MS) {
            hitKeysToRemove.push(key);
            continue;
          }
          const batchSize = JSON.stringify(batch).length;
          if (batchSize > _index2.BATCH_MAX_SIZE) {
            break;
          }
          batch.hits.push(item);
          hitKeysToRemove.push(key);
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      hitKeysToRemove.forEach(key => {
        _this4._hitsPoolQueue.delete(key);
      });
      if (!batch.hits.length) {
        return;
      }
      const headers = {
        [_index2.HEADER_CONTENT_TYPE]: _index2.HEADER_APPLICATION_JSON
      };
      const requestBody = batch.toApiKeys();
      const now = Date.now();
      try {
        yield _this4._httpClient.postAsync(_index2.HIT_EVENT_URL, {
          headers,
          body: requestBody,
          timeout: _this4.config.timeout,
          nextFetchConfig: _this4.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this4.config, _index2.TRACKING_MANAGER, _index2.HIT_SENT_SUCCESS, _index2.BATCH_HIT, {
          url: _index2.HIT_EVENT_URL,
          body: requestBody,
          headers,
          nextFetchConfig: _this4.config.nextFetchConfig,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        yield _this4.flushHits(hitKeysToRemove);
        _this4.sendHitsToFsQa(batch.hits);
        _this4.dispatchHitsToTag(batch.hits);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        batch.hits.forEach(hit => {
          _this4._hitsPoolQueue.set(hit.key, hit);
        });
        (0, _utils.logErrorSprintf)(_this4.config, _index2.TRACKING_MANAGER, _index2.TRACKING_MANAGER_ERROR, _index2.BATCH_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index2.HIT_EVENT_URL,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_BATCH_HIT_ROUTE_RESPONSE_ERROR,
          logLevel: _index2.LogLevel.ERROR,
          visitorId: `${_this4._flagshipInstanceId}`,
          flagshipInstanceId: _this4._flagshipInstanceId,
          traffic: 0,
          config: _this4.config,
          httpRequestBody: batch.hits,
          httpRequestHeaders: headers,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseMethod: 'POST',
          httpResponseUrl: _index2.HIT_EVENT_URL,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now,
          batchTriggeredBy
        });
        _this4.sendTroubleshootingHit(monitoringHttpResponse);
      }
    })();
  }
  notConsent(visitorId) {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const hitKeys = Array.from(_this5._hitsPoolQueue).filter(([, item]) => {
        return ((item == null ? void 0 : item.type) !== _index2.HitType.EVENT || (item == null ? void 0 : item.action) !== _index2.FS_CONSENT) && (item.visitorId === visitorId || item.anonymousId === visitorId);
      });
      const activateKeys = Array.from(_this5._activatePoolQueue).filter(([, item]) => {
        return item.visitorId === visitorId || item.anonymousId === visitorId;
      });
      const keysToFlush = [];
      hitKeys.forEach(([key]) => {
        _this5._hitsPoolQueue.delete(key);
        keysToFlush.push(key);
      });
      activateKeys.forEach(([key]) => {
        _this5._activatePoolQueue.delete(key);
        keysToFlush.push(key);
      });
      if (!keysToFlush.length) {
        return;
      }
      yield _this5.flushHits(keysToFlush);
    })();
  }
  cacheHit(hits) {
    var _this6 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const hitCacheImplementation = _this6.config.hitCacheImplementation;
        if (_this6.config.disableCache || !hitCacheImplementation || typeof hitCacheImplementation.cacheHit !== 'function') {
          return;
        }
        const data = {};
        hits.forEach((item, key) => {
          const hitData = {
            version: _index2.HIT_CACHE_VERSION,
            data: {
              visitorId: item.visitorId,
              anonymousId: item.anonymousId,
              type: item.type,
              content: item.toObject(),
              time: Date.now()
            }
          };
          data[key] = hitData;
        });
        yield hitCacheImplementation.cacheHit(data);
        (0, _utils.logDebugSprintf)(_this6.config, _index2.PROCESS_CACHE_HIT, _index2.HIT_CACHE_SAVED, data);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this6.config, _index2.PROCESS_CACHE, _index2.HIT_CACHE_ERROR, 'cacheHit', error.message || error);
      }
    })();
  }
  flushHits(hitKeys) {
    var _this7 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const hitCacheImplementation = _this7.config.hitCacheImplementation;
        if (_this7.config.disableCache || !hitCacheImplementation || typeof hitCacheImplementation.flushHits !== 'function') {
          return;
        }
        yield hitCacheImplementation.flushHits(hitKeys);
        (0, _utils.logDebugSprintf)(_this7.config, _index2.PROCESS_CACHE, _index2.HIT_DATA_FLUSHED, hitKeys);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this7.config, _index2.PROCESS_CACHE, _index2.HIT_CACHE_ERROR, 'flushHits', error.message || error);
      }
    })();
  }
  flushAllHits() {
    var _this8 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const hitCacheImplementation = _this8.config.hitCacheImplementation;
        if (_this8.config.disableCache || !hitCacheImplementation || typeof hitCacheImplementation.flushAllHits !== 'function') {
          return;
        }
        yield hitCacheImplementation.flushAllHits();
        (0, _utils.logDebug)(_this8.config, _index2.ALL_HITS_FLUSHED, _index2.PROCESS_FLUSH_HIT);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this8.config, _index2.PROCESS_CACHE, _index2.HIT_CACHE_ERROR, 'flushAllHits', error.message || error);
      }
    })();
  }

  // #region Troubleshooting

  isTroubleshootingActivated() {
    if (!this.troubleshootingData) {
      return false;
    }
    const now = new Date();
    const isStarted = now >= this.troubleshootingData.startDate;
    if (!isStarted) {
      return false;
    }
    const isFinished = now > this.troubleshootingData.endDate;
    if (isFinished) {
      this._hasInitTroubleshootingHitSent = false;
      return false;
    }
    return true;
  }
  addTroubleshootingHit(hit) {
    var _this9 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!hit.key) {
        const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
        hit.key = hitKey;
      }
      _this9._troubleshootingQueue.set(hit.key, hit);
      (0, _utils.logDebug)(_this9.config, (0, _utils.sprintf)(_index2.TROUBLESHOOTING_HIT_ADDED_IN_QUEUE, JSON.stringify(hit.toApiKeys())), _index2.ADD_TROUBLESHOOTING_HIT);
    })();
  }
  sendTroubleshootingHit(hit) {
    var _this10 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!_this10.isTroubleshootingActivated() || hit.traffic === undefined || _this10.troubleshootingData.traffic < hit.traffic) {
        return;
      }
      const requestBody = hit.toApiKeys();
      const now = Date.now();
      try {
        yield _this10._httpClient.postAsync(_index2.TROUBLESHOOTING_HIT_URL, {
          body: requestBody
        });
        (0, _utils.logDebug)(_this10.config, (0, _utils.sprintf)(_index2.TROUBLESHOOTING_SENT_SUCCESS, JSON.stringify(_objectSpread(_objectSpread({}, requestBody), {}, {
          duration: Date.now() - now
        }))), _index2.SEND_TROUBLESHOOTING);
        if (hit.key) {
          _this10._troubleshootingQueue.delete(hit.key);
          yield _this10.flushHits([hit.key]);
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        if (_this10.isTroubleshootingActivated()) {
          yield _this10.addTroubleshootingHit(hit);
        }
        (0, _utils.logError)(_this10.config, (0, _utils.errorFormat)(error.message || error, {
          url: _index2.TROUBLESHOOTING_HIT_URL,
          headers: {},
          body: requestBody,
          duration: Date.now() - now
        }), _index2.SEND_BATCH);
      }
    })();
  }
  sendInitTroubleshootingHit() {
    var _this11 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!_this11.isTroubleshootingActivated() || !_this11._initTroubleshootingHit || _this11._hasInitTroubleshootingHitSent) {
        return;
      }
      yield _this11.sendTroubleshootingHit(_this11._initTroubleshootingHit);
      _this11._hasInitTroubleshootingHitSent = true;
    })();
  }
  sendTroubleshootingQueue() {
    var _this12 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this12.sendInitTroubleshootingHit();
      if (!_this12.isTroubleshootingActivated() || _this12._isTroubleshootingQueueSending || _this12._troubleshootingQueue.size === 0) {
        return;
      }
      _this12._isTroubleshootingQueueSending = true;

      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      for (var _i = 0, _Array$from = Array.from(_this12._troubleshootingQueue); _i < _Array$from.length; _i++) {
        const _Array$from$_i = (0, _slicedToArray2.default)(_Array$from[_i], 2),
          _ = _Array$from$_i[0],
          item = _Array$from$_i[1];
        yield _this12.sendTroubleshootingHit(item);
      }
      _this12._isTroubleshootingQueueSending = false;
    })();
  }

  // #endregion

  // #region Analytic hit
  addUsageHit(hit) {
    var _this13 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!hit.key) {
        const hitKey = `${hit.visitorId}:${(0, _utils.uuidV4)()}`;
        hit.key = hitKey;
      }
      _this13._usageHitQueue.set(hit.key, hit);
      (0, _utils.logDebug)(_this13.config, (0, _utils.sprintf)(_index2.ANALYTICS_HIT_ADDED_IN_QUEUE, JSON.stringify(hit.toApiKeys())), _index2.ADD_USAGE_HIT);
    })();
  }
  sendUsageHit(hit) {
    var _this14 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const requestBody = hit.toApiKeys();
      const now = Date.now();
      try {
        yield _this14._httpClient.postAsync(_index2.USAGE_HIT_URL, {
          body: requestBody
        });
        (0, _utils.logDebug)(_this14.config, (0, _utils.sprintf)(_index2.ANALYTICS_HIT_SENT_SUCCESS, JSON.stringify(_objectSpread(_objectSpread({}, requestBody), {}, {
          duration: Date.now() - now
        }))), _index2.SEND_USAGE_HIT);
        if (hit.key) {
          _this14._usageHitQueue.delete(hit.key);
          yield _this14.flushHits([hit.key]);
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        yield _this14.addUsageHit(hit);
        (0, _utils.logError)(_this14.config, (0, _utils.errorFormat)(error.message || error, {
          url: _index2.USAGE_HIT_URL,
          headers: {},
          body: requestBody,
          duration: Date.now() - now
        }), _index2.SEND_BATCH);
      }
    })();
  }
  sendUsageHitQueue() {
    var _this15 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (_this15._isUsageHitQueueSending || _this15._usageHitQueue.size === 0) {
        return;
      }
      _this15._isUsageHitQueueSending = true;
      for (var _i2 = 0, _Array$from2 = Array.from(_this15._usageHitQueue); _i2 < _Array$from2.length; _i2++) {
        const _Array$from2$_i = (0, _slicedToArray2.default)(_Array$from2[_i2], 2),
          item = _Array$from2$_i[1];
        yield _this15.sendUsageHit(item);
      }
      _this15._isUsageHitQueueSending = false;
    })();
  }

  // #endregion
}
exports.BatchingCachingStrategyAbstract = BatchingCachingStrategyAbstract;

/***/ }),

/***/ 3792:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toIndexedObject = __webpack_require__(5397);
var addToUnscopables = __webpack_require__(6469);
var Iterators = __webpack_require__(6269);
var InternalStateModule = __webpack_require__(1181);
var defineProperty = (__webpack_require__(4913).f);
var defineIterator = __webpack_require__(1088);
var createIterResultObject = __webpack_require__(2529);
var IS_PURE = __webpack_require__(6395);
var DESCRIPTORS = __webpack_require__(3724);

var ARRAY_ITERATOR = 'Array Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR);

// `Array.prototype.entries` method
// https://tc39.es/ecma262/#sec-array.prototype.entries
// `Array.prototype.keys` method
// https://tc39.es/ecma262/#sec-array.prototype.keys
// `Array.prototype.values` method
// https://tc39.es/ecma262/#sec-array.prototype.values
// `Array.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-array.prototype-@@iterator
// `CreateArrayIterator` internal method
// https://tc39.es/ecma262/#sec-createarrayiterator
module.exports = defineIterator(Array, 'Array', function (iterated, kind) {
  setInternalState(this, {
    type: ARRAY_ITERATOR,
    target: toIndexedObject(iterated), // target
    index: 0,                          // next index
    kind: kind                         // kind
  });
// `%ArrayIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%arrayiteratorprototype%.next
}, function () {
  var state = getInternalState(this);
  var target = state.target;
  var index = state.index++;
  if (!target || index >= target.length) {
    state.target = null;
    return createIterResultObject(undefined, true);
  }
  switch (state.kind) {
    case 'keys': return createIterResultObject(index, false);
    case 'values': return createIterResultObject(target[index], false);
  } return createIterResultObject([index, target[index]], false);
}, 'values');

// argumentsList[@@iterator] is %ArrayProto_values%
// https://tc39.es/ecma262/#sec-createunmappedargumentsobject
// https://tc39.es/ecma262/#sec-createmappedargumentsobject
var values = Iterators.Arguments = Iterators.Array;

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');

// V8 ~ Chrome 45- bug
if (!IS_PURE && DESCRIPTORS && values.name !== 'values') try {
  defineProperty(values, 'name', { value: 'values' });
} catch (error) { /* empty */ }


/***/ }),

/***/ 3925:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isObject = __webpack_require__(34);

module.exports = function (argument) {
  return isObject(argument) || argument === null;
};


/***/ }),

/***/ 3994:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var IteratorPrototype = (__webpack_require__(7657).IteratorPrototype);
var create = __webpack_require__(2360);
var createPropertyDescriptor = __webpack_require__(6980);
var setToStringTag = __webpack_require__(687);
var Iterators = __webpack_require__(6269);

var returnThis = function () { return this; };

module.exports = function (IteratorConstructor, NAME, next, ENUMERABLE_NEXT) {
  var TO_STRING_TAG = NAME + ' Iterator';
  IteratorConstructor.prototype = create(IteratorPrototype, { next: createPropertyDescriptor(+!ENUMERABLE_NEXT, next) });
  setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
  Iterators[TO_STRING_TAG] = returnThis;
  return IteratorConstructor;
};


/***/ }),

/***/ 4006:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var defineBuiltIns = __webpack_require__(6279);
var getWeakData = (__webpack_require__(3451).getWeakData);
var anInstance = __webpack_require__(679);
var anObject = __webpack_require__(8551);
var isNullOrUndefined = __webpack_require__(4117);
var isObject = __webpack_require__(34);
var iterate = __webpack_require__(2652);
var ArrayIterationModule = __webpack_require__(9213);
var hasOwn = __webpack_require__(9297);
var InternalStateModule = __webpack_require__(1181);

var setInternalState = InternalStateModule.set;
var internalStateGetterFor = InternalStateModule.getterFor;
var find = ArrayIterationModule.find;
var findIndex = ArrayIterationModule.findIndex;
var splice = uncurryThis([].splice);
var id = 0;

// fallback for uncaught frozen keys
var uncaughtFrozenStore = function (state) {
  return state.frozen || (state.frozen = new UncaughtFrozenStore());
};

var UncaughtFrozenStore = function () {
  this.entries = [];
};

var findUncaughtFrozen = function (store, key) {
  return find(store.entries, function (it) {
    return it[0] === key;
  });
};

UncaughtFrozenStore.prototype = {
  get: function (key) {
    var entry = findUncaughtFrozen(this, key);
    if (entry) return entry[1];
  },
  has: function (key) {
    return !!findUncaughtFrozen(this, key);
  },
  set: function (key, value) {
    var entry = findUncaughtFrozen(this, key);
    if (entry) entry[1] = value;
    else this.entries.push([key, value]);
  },
  'delete': function (key) {
    var index = findIndex(this.entries, function (it) {
      return it[0] === key;
    });
    if (~index) splice(this.entries, index, 1);
    return !!~index;
  }
};

module.exports = {
  getConstructor: function (wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
    var Constructor = wrapper(function (that, iterable) {
      anInstance(that, Prototype);
      setInternalState(that, {
        type: CONSTRUCTOR_NAME,
        id: id++,
        frozen: null
      });
      if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], { that: that, AS_ENTRIES: IS_MAP });
    });

    var Prototype = Constructor.prototype;

    var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);

    var define = function (that, key, value) {
      var state = getInternalState(that);
      var data = getWeakData(anObject(key), true);
      if (data === true) uncaughtFrozenStore(state).set(key, value);
      else data[state.id] = value;
      return that;
    };

    defineBuiltIns(Prototype, {
      // `{ WeakMap, WeakSet }.prototype.delete(key)` methods
      // https://tc39.es/ecma262/#sec-weakmap.prototype.delete
      // https://tc39.es/ecma262/#sec-weakset.prototype.delete
      'delete': function (key) {
        var state = getInternalState(this);
        if (!isObject(key)) return false;
        var data = getWeakData(key);
        if (data === true) return uncaughtFrozenStore(state)['delete'](key);
        return data && hasOwn(data, state.id) && delete data[state.id];
      },
      // `{ WeakMap, WeakSet }.prototype.has(key)` methods
      // https://tc39.es/ecma262/#sec-weakmap.prototype.has
      // https://tc39.es/ecma262/#sec-weakset.prototype.has
      has: function has(key) {
        var state = getInternalState(this);
        if (!isObject(key)) return false;
        var data = getWeakData(key);
        if (data === true) return uncaughtFrozenStore(state).has(key);
        return data && hasOwn(data, state.id);
      }
    });

    defineBuiltIns(Prototype, IS_MAP ? {
      // `WeakMap.prototype.get(key)` method
      // https://tc39.es/ecma262/#sec-weakmap.prototype.get
      get: function get(key) {
        var state = getInternalState(this);
        if (isObject(key)) {
          var data = getWeakData(key);
          if (data === true) return uncaughtFrozenStore(state).get(key);
          if (data) return data[state.id];
        }
      },
      // `WeakMap.prototype.set(key, value)` method
      // https://tc39.es/ecma262/#sec-weakmap.prototype.set
      set: function set(key, value) {
        return define(this, key, value);
      }
    } : {
      // `WeakSet.prototype.add(value)` method
      // https://tc39.es/ecma262/#sec-weakset.prototype.add
      add: function add(value) {
        return define(this, value, true);
      }
    });

    return Constructor;
  }
};


/***/ }),

/***/ 4055:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var isObject = __webpack_require__(34);

var document = globalThis.document;
// typeof document.createElement is 'object' in old IE
var EXISTS = isObject(document) && isObject(document.createElement);

module.exports = function (it) {
  return EXISTS ? document.createElement(it) : {};
};


/***/ }),

/***/ 4116:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.version = void 0;
// Generated by genversion.
const version = exports.version = '5.1.6';

/***/ }),

/***/ 4117:
/***/ ((module) => {

"use strict";

// we can't use just `it == null` since of `document.all` special case
// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot-aec
module.exports = function (it) {
  return it === null || it === undefined;
};


/***/ }),

/***/ 4124:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var isObject = __webpack_require__(34);
var classof = __webpack_require__(4576);
var ARRAY_BUFFER_NON_EXTENSIBLE = __webpack_require__(5652);

// eslint-disable-next-line es/no-object-isextensible -- safe
var $isExtensible = Object.isExtensible;
var FAILS_ON_PRIMITIVES = fails(function () { $isExtensible(1); });

// `Object.isExtensible` method
// https://tc39.es/ecma262/#sec-object.isextensible
module.exports = (FAILS_ON_PRIMITIVES || ARRAY_BUFFER_NON_EXTENSIBLE) ? function isExtensible(it) {
  if (!isObject(it)) return false;
  if (ARRAY_BUFFER_NON_EXTENSIBLE && classof(it) === 'ArrayBuffer') return false;
  return $isExtensible ? $isExtensible(it) : true;
} : $isExtensible;


/***/ }),

/***/ 4209:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var wellKnownSymbol = __webpack_require__(8227);
var Iterators = __webpack_require__(6269);

var ITERATOR = wellKnownSymbol('iterator');
var ArrayPrototype = Array.prototype;

// check on default Array iterator
module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
};


/***/ }),

/***/ 4215:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* global Bun, Deno -- detection */
var globalThis = __webpack_require__(2195);
var userAgent = __webpack_require__(2839);
var classof = __webpack_require__(4576);

var userAgentStartsWith = function (string) {
  return userAgent.slice(0, string.length) === string;
};

module.exports = (function () {
  if (userAgentStartsWith('Bun/')) return 'BUN';
  if (userAgentStartsWith('Cloudflare-Workers')) return 'CLOUDFLARE';
  if (userAgentStartsWith('Deno/')) return 'DENO';
  if (userAgentStartsWith('Node.js/')) return 'NODE';
  if (globalThis.Bun && typeof Bun.version == 'string') return 'BUN';
  if (globalThis.Deno && typeof Deno.version == 'object') return 'DENO';
  if (classof(globalThis.process) === 'process') return 'NODE';
  if (globalThis.window && globalThis.document) return 'BROWSER';
  return 'REST';
})();


/***/ }),

/***/ 4226:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Screen = exports.ERROR_MESSAGE = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FlagshipConstant = __webpack_require__(2932);
var _HitType = __webpack_require__(6147);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'Screen name is required';
class Screen extends _HitAbstract.HitAbstract {
  get documentLocation() {
    return this._documentLocation;
  }
  set documentLocation(v) {
    if (!this.isNotEmptyString(v, 'documentLocation')) {
      return;
    }
    this._documentLocation = v;
  }
  constructor(param) {
    super({
      type: _HitType.HitType.SCREEN_VIEW,
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode,
      isActionTrackingHit: param.isActionTrackingHit
    });
    this.documentLocation = param.documentLocation;
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.documentLocation);
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_FlagshipConstant.DL_API_ITEM] = this.documentLocation;
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      documentLocation: this.documentLocation
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Screen = Screen;

/***/ }),

/***/ 4229:
/***/ ((module) => {

"use strict";
module.exports = require("node-fetch");

/***/ }),

/***/ 4265:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var userAgent = __webpack_require__(2839);

module.exports = /ipad|iphone|ipod/i.test(userAgent) && typeof Pebble != 'undefined';


/***/ }),

/***/ 4270:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var isCallable = __webpack_require__(4901);
var isObject = __webpack_require__(34);

var $TypeError = TypeError;

// `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive
module.exports = function (input, pref) {
  var fn, val;
  if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
  if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  throw new $TypeError("Can't convert object to primitive value");
};


/***/ }),

/***/ 4288:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.LogLevel = void 0;
let LogLevel = exports.LogLevel = /*#__PURE__*/function (LogLevel) {
  /**
     * NONE = 0: Logging will be disabled.
     */
  LogLevel[LogLevel["NONE"] = 0] = "NONE";
  /**
     * EMERGENCY = 1: Only emergencies will be logged.
     */
  LogLevel[LogLevel["EMERGENCY"] = 1] = "EMERGENCY";
  /**
     * ALERT = 2: Only alerts and above will be logged.
     */
  LogLevel[LogLevel["ALERT"] = 2] = "ALERT";
  /**
     * CRITICAL = 3: Only critical and above will be logged.
     */
  LogLevel[LogLevel["CRITICAL"] = 3] = "CRITICAL";
  /**
     * ERROR = 4: Only errors and above will be logged.
     */
  LogLevel[LogLevel["ERROR"] = 4] = "ERROR";
  /**
     * WARNING = 5: Only warnings and above will be logged.
     */
  LogLevel[LogLevel["WARNING"] = 5] = "WARNING";
  /**
     * NOTICE = 6: Only notices and above will be logged.
     */
  LogLevel[LogLevel["NOTICE"] = 6] = "NOTICE";
  /**
     * INFO = 7: Only info logs and above will be logged.
     */
  LogLevel[LogLevel["INFO"] = 7] = "INFO";
  /**
     * DEBUG = 8: Only debug logs and above will be logged.
     */
  LogLevel[LogLevel["DEBUG"] = 8] = "DEBUG";
  /**
     * ALL = 9: All logs will be logged.
     */
  LogLevel[LogLevel["ALL"] = 9] = "ALL";
  return LogLevel;
}({});

/***/ }),

/***/ 4353:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.EdgeManager = void 0;
var _BucketingManager = __webpack_require__(1844);
class EdgeManager extends _BucketingManager.BucketingManager {
  getCampaignsAsync(visitor) {
    return super.getCampaignsAsync(visitor);
  }
}
exports.EdgeManager = EdgeManager;

/***/ }),

/***/ 4376:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var classof = __webpack_require__(4576);

// `IsArray` abstract operation
// https://tc39.es/ecma262/#sec-isarray
// eslint-disable-next-line es/no-array-isarray -- safe
module.exports = Array.isArray || function isArray(argument) {
  return classof(argument) === 'Array';
};


/***/ }),

/***/ 4423:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $includes = (__webpack_require__(9617).includes);
var fails = __webpack_require__(9039);
var addToUnscopables = __webpack_require__(6469);

// FF99+ bug
var BROKEN_ON_SPARSE = fails(function () {
  // eslint-disable-next-line es/no-array-prototype-includes -- detection
  return !Array(1).includes();
});

// `Array.prototype.includes` method
// https://tc39.es/ecma262/#sec-array.prototype.includes
$({ target: 'Array', proto: true, forced: BROKEN_ON_SPARSE }, {
  includes: function includes(el /* , fromIndex = 0 */) {
    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
  }
});

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('includes');


/***/ }),

/***/ 4428:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var wellKnownSymbol = __webpack_require__(8227);

var ITERATOR = wellKnownSymbol('iterator');
var SAFE_CLOSING = false;

try {
  var called = 0;
  var iteratorWithReturn = {
    next: function () {
      return { done: !!called++ };
    },
    'return': function () {
      SAFE_CLOSING = true;
    }
  };
  iteratorWithReturn[ITERATOR] = function () {
    return this;
  };
  // eslint-disable-next-line es/no-array-from, no-throw-literal -- required for testing
  Array.from(iteratorWithReturn, function () { throw 2; });
} catch (error) { /* empty */ }

module.exports = function (exec, SKIP_CLOSING) {
  try {
    if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
  } catch (error) { return false; } // workaround of old WebKit + `eval` bug
  var ITERATION_SUPPORT = false;
  try {
    var object = {};
    object[ITERATOR] = function () {
      return {
        next: function () {
          return { done: ITERATION_SUPPORT = true };
        }
      };
    };
    exec(object);
  } catch (error) { /* empty */ }
  return ITERATION_SUPPORT;
};


/***/ }),

/***/ 4495:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* eslint-disable es/no-symbol -- required for testing */
var V8_VERSION = __webpack_require__(9519);
var fails = __webpack_require__(9039);
var globalThis = __webpack_require__(2195);

var $String = globalThis.String;

// eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing
module.exports = !!Object.getOwnPropertySymbols && !fails(function () {
  var symbol = Symbol('symbol detection');
  // Chrome 38 Symbol has incorrect toString conversion
  // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
  // nb: Do not call `String` directly to avoid this being optimized out to `symbol+''` which will,
  // of course, fail.
  return !$String(symbol) || !(Object(symbol) instanceof Symbol) ||
    // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});


/***/ }),

/***/ 4576:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);

var toString = uncurryThis({}.toString);
var stringSlice = uncurryThis(''.slice);

module.exports = function (it) {
  return stringSlice(toString(it), 8, -1);
};


/***/ }),

/***/ 4641:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.PanicStrategy = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _DefaultStrategy = __webpack_require__(4811);
var _FSFlagMetadata = __webpack_require__(8889);
class PanicStrategy extends _DefaultStrategy.DefaultStrategy {
  setConsent(hasConsented) {
    this.visitor.hasConsented = hasConsented;
  }
  updateContext() {
    this.log('updateContext');
  }
  clearContext() {
    this.log('clearContext');
  }
  lookupHits() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  lookupVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cacheVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cacheHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  fetchCampaignsFromCache() {
    return [];
  }
  sendVisitorAllocatedVariations() {
    //
  }
  sendHit(_hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this.log('sendHit');
    })();
  }
  sendHits(_hits) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this2.log('sendHits');
    })();
  }
  getFlagValue(param) {
    this.log('Flag.value');
    return param.defaultValue;
  }
  visitorExposed() {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this3.log(_index.FLAG_VISITOR_EXPOSED);
    })();
  }
  getFlagMetadata(param) {
    const emptyMetaData = _FSFlagMetadata.FSFlagMetadata.Empty();
    (0, _utils.logInfoSprintf)(this.config, _index.FLAG_METADATA, _index.METADATA_PANIC_MODE, this.visitor.visitorId, param.key, emptyMetaData);
    return emptyMetaData;
  }
  sendTroubleshootingHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  sendSdkConfigAnalyticHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  sendActivate(_flagDto, _defaultValue) {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  collectEAIEventsAsync() {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this4.log('collectEAIData');
    })();
  }
  reportEaiPageView() {
    //
  }
  reportEaiVisitorEvent() {
    //
  }
  onEAICollectStatusChange() {
    //
  }
  addInTrackingManager() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  log(methodName) {
    (0, _utils.logInfoSprintf)(this.config, methodName, _index.METHOD_DEACTIVATED_ERROR, this.visitor.visitorId, methodName, _index.FSSdkStatus[_index.FSSdkStatus.SDK_PANIC]);
  }
}
exports.PanicStrategy = PanicStrategy;

/***/ }),

/***/ 4659:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var definePropertyModule = __webpack_require__(4913);
var createPropertyDescriptor = __webpack_require__(6980);

module.exports = function (object, key, value) {
  if (DESCRIPTORS) definePropertyModule.f(object, key, createPropertyDescriptor(0, value));
  else object[key] = value;
};


/***/ }),

/***/ 4707:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.FSFlag = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _FSFetchStatus = __webpack_require__(1597);
var _FSFlagStatus = __webpack_require__(5819);
var _FSFlagMetadata = __webpack_require__(8889);
var _forceVariation = __webpack_require__(5769);
class FSFlag {
  constructor(param) {
    (0, _defineProperty2.default)(this, "hasGetValueBeenCalled", false);
    const key = param.key,
      visitor = param.visitor;
    this._key = key;
    this._visitor = visitor;
  }
  exists() {
    var _this$_visitor;
    if (!this._visitor) {
      return false;
    }
    const flagDTO = (_this$_visitor = this._visitor) == null ? void 0 : _this$_visitor.flagsData.get(this._key);
    const forcedFlagDTO = (0, _forceVariation.forceVariation)({
      flagDTO,
      config: this._visitor.config,
      visitorVariationState: this._visitor.visitorVariationState
    });
    const flag = forcedFlagDTO || flagDTO;
    return !!(flag != null && flag.campaignId && flag != null && flag.variationId && flag != null && flag.variationGroupId);
  }
  get metadata() {
    if (!this._visitor) {
      return _FSFlagMetadata.FSFlagMetadata.Empty();
    }
    const flagDTO = this._visitor.flagsData.get(this._key);
    const forcedFlagDTO = (0, _forceVariation.forceVariation)({
      flagDTO,
      config: this._visitor.config,
      visitorVariationState: this._visitor.visitorVariationState
    });
    return this._visitor.getFlagMetadata({
      key: this._key,
      flag: forcedFlagDTO || flagDTO
    });
  }
  visitorExposed() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!_this._visitor) {
        return;
      }
      const flagDTO = _this._visitor.flagsData.get(_this._key);
      const forcedFlagDTO = (0, _forceVariation.forceVariation)({
        flagDTO,
        config: _this._visitor.config,
        visitorVariationState: _this._visitor.visitorVariationState
      });
      return _this._visitor.visitorExposed({
        key: _this._key,
        flag: forcedFlagDTO || flagDTO,
        defaultValue: _this._defaultValue,
        hasGetValueBeenCalled: _this.hasGetValueBeenCalled
      });
    })();
  }
  getValue(defaultValue, visitorExposed = true) {
    this._defaultValue = defaultValue;
    this.hasGetValueBeenCalled = true;
    if (!this._visitor) {
      return defaultValue;
    }
    const flagDTO = this._visitor.flagsData.get(this._key);
    const forcedFlagDTO = (0, _forceVariation.forceVariation)({
      flagDTO,
      config: this._visitor.config,
      visitorVariationState: this._visitor.visitorVariationState
    });
    const flag = forcedFlagDTO || flagDTO;
    this._visitor.sendExposedVariation(flag);
    return this._visitor.getFlagValue({
      key: this._key,
      defaultValue,
      flag,
      visitorExposed
    });
  }
  get status() {
    var _this$_visitor2, _this$_visitor3, _this$_visitor4;
    if (((_this$_visitor2 = this._visitor) == null || (_this$_visitor2 = _this$_visitor2.flagsStatus) == null ? void 0 : _this$_visitor2.status) === _FSFetchStatus.FSFetchStatus.PANIC) {
      return _FSFlagStatus.FSFlagStatus.PANIC;
    }
    if (!this.exists()) {
      return _FSFlagStatus.FSFlagStatus.NOT_FOUND;
    }
    if (((_this$_visitor3 = this._visitor) == null || (_this$_visitor3 = _this$_visitor3.flagsStatus) == null ? void 0 : _this$_visitor3.status) === _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED || ((_this$_visitor4 = this._visitor) == null || (_this$_visitor4 = _this$_visitor4.flagsStatus) == null ? void 0 : _this$_visitor4.status) === _FSFetchStatus.FSFetchStatus.FETCHING) {
      return _FSFlagStatus.FSFlagStatus.FETCH_REQUIRED;
    }
    return _FSFlagStatus.FSFlagStatus.FETCHED;
  }
}
exports.FSFlag = FSFlag;

/***/ }),

/***/ 4782:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var isArray = __webpack_require__(4376);
var isConstructor = __webpack_require__(3517);
var isObject = __webpack_require__(34);
var toAbsoluteIndex = __webpack_require__(5610);
var lengthOfArrayLike = __webpack_require__(6198);
var toIndexedObject = __webpack_require__(5397);
var createProperty = __webpack_require__(4659);
var wellKnownSymbol = __webpack_require__(8227);
var arrayMethodHasSpeciesSupport = __webpack_require__(597);
var nativeSlice = __webpack_require__(7680);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('slice');

var SPECIES = wellKnownSymbol('species');
var $Array = Array;
var max = Math.max;

// `Array.prototype.slice` method
// https://tc39.es/ecma262/#sec-array.prototype.slice
// fallback for not array-like ES3 strings and DOM objects
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  slice: function slice(start, end) {
    var O = toIndexedObject(this);
    var length = lengthOfArrayLike(O);
    var k = toAbsoluteIndex(start, length);
    var fin = toAbsoluteIndex(end === undefined ? length : end, length);
    // inline `ArraySpeciesCreate` for usage native `Array#slice` where it's possible
    var Constructor, result, n;
    if (isArray(O)) {
      Constructor = O.constructor;
      // cross-realm fallback
      if (isConstructor(Constructor) && (Constructor === $Array || isArray(Constructor.prototype))) {
        Constructor = undefined;
      } else if (isObject(Constructor)) {
        Constructor = Constructor[SPECIES];
        if (Constructor === null) Constructor = undefined;
      }
      if (Constructor === $Array || Constructor === undefined) {
        return nativeSlice(O, k, fin);
      }
    }
    result = new (Constructor === undefined ? $Array : Constructor)(max(fin - k, 0));
    for (n = 0; k < fin; k++, n++) if (k in O) createProperty(result, n, O[k]);
    result.length = n;
    return result;
  }
});


/***/ }),

/***/ 4811:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(2008);
__webpack_require__(3418);
__webpack_require__(4782);
__webpack_require__(1278);
__webpack_require__(7495);
__webpack_require__(3772);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.TYPE_HIT_REQUIRED_ERROR = exports.HIT_NULL_ERROR = exports.DefaultStrategy = void 0;
__webpack_require__(3792);
__webpack_require__(2062);
__webpack_require__(6033);
__webpack_require__(3362);
__webpack_require__(9479);
var _objectWithoutProperties2 = _interopRequireDefault(__webpack_require__(1847));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _StrategyAbstract = __webpack_require__(1240);
var _FlagshipContext = __webpack_require__(7877);
var _FSFlagMetadata = __webpack_require__(8889);
var _FSFetchStatus = __webpack_require__(1597);
var _FSFetchReasons = __webpack_require__(1734);
var _Transaction = __webpack_require__(2816);
var _Item = __webpack_require__(2177);
var _Page = __webpack_require__(2509);
var _Screen = __webpack_require__(4226);
var _Event = __webpack_require__(8796);
var _Troubleshooting = __webpack_require__(3142);
const _excluded = ["createdAt"];
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const TYPE_HIT_REQUIRED_ERROR = exports.TYPE_HIT_REQUIRED_ERROR = 'property type is required and must ';
const HIT_NULL_ERROR = exports.HIT_NULL_ERROR = 'Hit must not be null';
class DefaultStrategy extends _StrategyAbstract.StrategyAbstract {
  checkPredefinedContext(key, value) {
    const type = _FlagshipContext.FLAGSHIP_CONTEXT[key];
    if (!type) {
      return null;
    }
    let check = false;
    if (type === 'string') {
      check = typeof value === 'string';
    } else if (type === 'number') {
      check = typeof value === 'number';
    }
    if (!check) {
      (0, _utils.logErrorSprintf)(this.config, _index.PROCESS_UPDATE_CONTEXT, _index.PREDEFINED_CONTEXT_TYPE_ERROR, this.visitor.visitorId, key, type);
    }
    return check;
  }
  updateContextKeyValue(key, value) {
    const valueType = typeof value;
    if (typeof key !== 'string' || key === '') {
      (0, _utils.logErrorSprintf)(this.config, _index.PROCESS_UPDATE_CONTEXT, _index.CONTEXT_KEY_ERROR, this.visitor.visitorId, key);
      return;
    }
    if (valueType !== 'string' && valueType !== 'number' && valueType !== 'boolean') {
      (0, _utils.logErrorSprintf)(this.config, _index.PROCESS_UPDATE_CONTEXT, _index.CONTEXT_VALUE_ERROR, this.visitor.visitorId, key);
      return;
    }
    if (key === _FlagshipContext.FLAGSHIP_CLIENT || key === _FlagshipContext.FLAGSHIP_VERSION || key === _FlagshipContext.FLAGSHIP_VISITOR) {
      return;
    }
    const predefinedContext = this.checkPredefinedContext(key, value);
    if (typeof predefinedContext === 'boolean' && !predefinedContext) {
      return;
    }
    this.visitor.context[key] = value;
  }
  checkAndUpdateContext(oldContext, newContext, value) {
    if ((0, _utils.deepEqual)(oldContext, newContext)) {
      return;
    }
    this.visitor.hasContextBeenUpdated = true;
    this.visitor.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.UPDATE_CONTEXT
    };
    (0, _utils.logDebugSprintf)(this.config, _index.PROCESS_UPDATE_CONTEXT, _index.CONTEXT_KEY_VALUE_UPDATE, this.visitor.visitorId, newContext, value, this.visitor.context);
  }
  updateContext(context, value) {
    const oldContext = _objectSpread({}, this.visitor.context);
    if (typeof context === 'string') {
      this.updateContextKeyValue(context, value);
      const newContext = this.visitor.context;
      this.checkAndUpdateContext(oldContext, newContext, value);
      return;
    }
    if (!context) {
      (0, _utils.logError)(this.visitor.config, _index.CONTEXT_NULL_ERROR, _index.PROCESS_UPDATE_CONTEXT);
      return;
    }
    for (const key in context) {
      const value = context[key];
      this.updateContextKeyValue(key, value);
    }
    const newContext = this.visitor.context;
    this.checkAndUpdateContext(oldContext, newContext, context);
  }
  clearContext() {
    const oldContext = _objectSpread({}, this.visitor.context);
    this.visitor.context = {};
    this.visitor.loadPredefinedContext();
    const newContext = this.visitor.context;
    if ((0, _utils.deepEqual)(oldContext, newContext)) {
      return;
    }
    this.visitor.hasContextBeenUpdated = true;
    this.visitor.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.UPDATE_CONTEXT
    };
    (0, _utils.logDebugSprintf)(this.config, _index.PROCESS_CLEAR_CONTEXT, _index.CLEAR_CONTEXT, this.visitor.visitorId, this.visitor.context);
  }
  isDeDuplicated(key, deDuplicationTime) {
    if (deDuplicationTime === 0) {
      return false;
    }
    const deDuplicationCache = this.visitor.deDuplicationCache[key];
    if (deDuplicationCache && Date.now() - deDuplicationCache <= deDuplicationTime * 1000) {
      return true;
    }
    this.visitor.deDuplicationCache[key] = Date.now();
    this.visitor.clearDeDuplicationCache(deDuplicationTime);
    return false;
  }
  sendActivate(flagDto, defaultValue) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this$visitor$sdkInit;
      const activateHit = {
        variationGroupId: flagDto.variationGroupId,
        variationId: flagDto.variationId,
        visitorId: _this.visitor.visitorId,
        anonymousId: _this.visitor.anonymousId,
        flagKey: flagDto.key,
        flagValue: flagDto.value,
        flagDefaultValue: defaultValue,
        visitorContext: _this.visitor.context,
        flagMetadata: {
          campaignName: flagDto.campaignName,
          campaignId: flagDto.campaignId,
          campaignType: flagDto.campaignType,
          variationGroupId: flagDto.variationGroupId,
          variationGroupName: flagDto.variationGroupName,
          variationId: flagDto.variationId,
          variationName: flagDto.variationName,
          slug: flagDto.slug,
          isReference: flagDto.isReference
        },
        qaMode: _this.config.isQAModeEnabled
      };
      if (_this.isDeDuplicated(JSON.stringify(activateHit), _this.config.hitDeduplicationTime)) {
        const logData = {
          visitorId: _this.visitor.visitorId,
          anonymousId: _this.visitor.anonymousId,
          flag: flagDto,
          delay: 0
        };
        (0, _utils.logDebug)(_this.config, (0, _utils.sprintf)('Activate {0} is deduplicated', JSON.stringify(logData)), _index.PROCESS_SEND_HIT);
        return;
      }
      yield _this.trackingManager.activateFlag(activateHit);
      const activateTroubleshooting = new _Troubleshooting.Troubleshooting({
        label: _types.TroubleshootingLabel.VISITOR_SEND_ACTIVATE,
        logLevel: _index.LogLevel.INFO,
        traffic: _this.visitor.traffic,
        visitorId: activateHit.visitorId,
        flagshipInstanceId: (_this$visitor$sdkInit = _this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit.instanceId,
        visitorSessionId: _this.visitor.instanceId,
        anonymousId: activateHit.anonymousId,
        config: _this.config,
        hitContent: activateHit
      });
      _this.sendTroubleshootingHit(activateTroubleshooting);
    })();
  }
  sendHit(hit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!_this2.hasTrackingManager(_index.PROCESS_SEND_HIT)) {
        return;
      }
      yield _this2.prepareAndSendHit(hit);
    })();
  }
  sendHits(hits) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (!_this3.hasTrackingManager(_index.PROCESS_SEND_HIT)) {
        return;
      }
      var _iterator = _createForOfIteratorHelper(hits),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          const hit = _step.value;
          yield _this3.prepareAndSendHit(hit);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    })();
  }
  getHit(hit) {
    return (0, _asyncToGenerator2.default)(function* () {
      let newHit = null;
      switch (hit.type.toUpperCase()) {
        case _index.HitType.EVENT:
          {
            newHit = new _Event.Event(hit);
            break;
          }
        case _index.HitType.ITEM:
          {
            newHit = new _Item.Item(hit);
            break;
          }
        case _index.HitType.PAGE_VIEW:
          {
            newHit = new _Page.Page(hit);
            break;
          }
        case _index.HitType.SCREEN_VIEW:
          {
            newHit = new _Screen.Screen(hit);
            break;
          }
        case _index.HitType.TRANSACTION:
          {
            newHit = new _Transaction.Transaction(hit);
            break;
          }
      }
      return newHit;
    })();
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  isHitAbstract(hit) {
    return hit && typeof hit.toApiKeys === 'function' && typeof hit.isReady === 'function' && typeof hit.getErrorMessage === 'function' && typeof hit.toObject === 'function';
  }
  prepareAndSendHit(hit, functionName = _index.PROCESS_SEND_HIT) {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      let hitInstance;
      if (!(hit != null && hit.type)) {
        (0, _utils.logError)(_this4.config, HIT_NULL_ERROR, functionName);
        return;
      }
      if (_this4.isHitAbstract(hit)) {
        hitInstance = hit;
      } else {
        const hitFromInt = yield _this4.getHit(hit);
        if (!hitFromInt) {
          (0, _utils.logError)(_this4.config, TYPE_HIT_REQUIRED_ERROR, functionName);
          return;
        }
        hitInstance = hitFromInt;
      }
      hitInstance.visitorId = _this4.visitor.visitorId;
      hitInstance.ds = _index.SDK_APP;
      hitInstance.config = _this4.config;
      hitInstance.anonymousId = _this4.visitor.anonymousId;
      hitInstance.qaMode = _this4.config.isQAModeEnabled;

      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      const _hitInstance$toObject = hitInstance.toObject(),
        createdAt = _hitInstance$toObject.createdAt,
        hitInstanceItem = (0, _objectWithoutProperties2.default)(_hitInstance$toObject, _excluded);
      if (_this4.isDeDuplicated(JSON.stringify(hitInstanceItem), _this4.config.hitDeduplicationTime)) {
        return;
      }
      if (!hitInstance.isReady()) {
        (0, _utils.logError)(_this4.config, hitInstance.getErrorMessage(), functionName);
        return;
      }
      try {
        var _this4$visitor$sdkIni;
        yield _this4.trackingManager.addHit(hitInstance);
        if (hitInstance.type === 'SEGMENT') {
          return;
        }
        const sendHitTroubleshooting = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.VISITOR_SEND_HIT,
          logLevel: _index.LogLevel.INFO,
          traffic: _this4.visitor.traffic,
          visitorId: hitInstance.visitorId,
          flagshipInstanceId: (_this4$visitor$sdkIni = _this4.visitor.sdkInitialData) == null ? void 0 : _this4$visitor$sdkIni.instanceId,
          visitorSessionId: _this4.visitor.instanceId,
          anonymousId: hitInstance.anonymousId,
          config: _this4.config,
          hitContent: hitInstance.toApiKeys()
        });
        _this4.sendTroubleshootingHit(sendHitTroubleshooting);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logError)(_this4.config, error.message || error, functionName);
      }
    })();
  }
  authenticate(visitorId) {
    var _this$visitor$sdkInit2;
    if (!visitorId) {
      (0, _utils.logErrorSprintf)(this.config, _index.AUTHENTICATE, _index.VISITOR_AUTHENTICATE_VISITOR_ID_ERROR, this.visitor.visitorId);
      return;
    }
    if (this.visitor.anonymousId) {
      (0, _utils.logWarningSprintf)(this.config, _index.AUTHENTICATE, _index.VISITOR_ALREADY_AUTHENTICATE, this.visitor.visitorId, this.visitor.anonymousId);
      return;
    }
    this.visitor.anonymousId = this.visitor.visitorId;
    this.visitor.visitorId = visitorId;
    const monitoring = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.VISITOR_AUTHENTICATE,
      logLevel: _index.LogLevel.INFO,
      flagshipInstanceId: (_this$visitor$sdkInit2 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit2.instanceId,
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      visitorContext: this.visitor.context,
      traffic: this.visitor.traffic,
      config: this.config
    });
    this.sendTroubleshootingHit(monitoring);
    this.visitor.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.AUTHENTICATE
    };
    (0, _utils.logDebugSprintf)(this.config, _index.AUTHENTICATE, _index.VISITOR_AUTHENTICATE, this.visitor.visitorId, this.visitor.anonymousId);
  }
  unauthenticate() {
    var _this$visitor$sdkInit3;
    if (!this.visitor.anonymousId) {
      (0, _utils.logErrorSprintf)(this.config, _index.UNAUTHENTICATE, _index.FLAGSHIP_VISITOR_NOT_AUTHENTICATE, this.visitor.visitorId);
      return;
    }
    this.visitor.visitorId = this.visitor.anonymousId;
    this.visitor.anonymousId = null;
    const monitoring = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.VISITOR_UNAUTHENTICATE,
      logLevel: _index.LogLevel.INFO,
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      flagshipInstanceId: (_this$visitor$sdkInit3 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit3.instanceId,
      visitorContext: this.visitor.context,
      traffic: this.visitor.traffic,
      config: this.config
    });
    this.sendTroubleshootingHit(monitoring);
    this.visitor.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.UNAUTHENTICATE
    };
    (0, _utils.logDebugSprintf)(this.config, _index.UNAUTHENTICATE, _index.VISITOR_UNAUTHENTICATE, this.visitor.visitorId);
  }
  handleFetchFlagsError(error, now, campaigns) {
    var _this$visitor$sdkInit4, _this$visitor$sdkInit5, _this$visitor$sdkInit6, _this$visitor$sdkInit7, _this$config$tracking, _this$config$tracking2, _this$config$tracking3;
    this.visitor.emit(_index.EMIT_READY, error);
    const message = error instanceof Error ? error.message : error;
    const errorMessage = (0, _utils.errorFormat)(message, {
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      context: this.visitor.context,
      statusReason: this.visitor.flagsStatus.reason,
      duration: Date.now() - now
    });
    (0, _utils.logError)(this.config, errorMessage, _index.PROCESS_FETCHING_FLAGS);
    this.visitor.flagsStatus = {
      status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
      reason: _FSFetchReasons.FSFetchReasons.FLAGS_FETCHING_ERROR
    };
    const troubleshootingHit = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.VISITOR_FETCH_CAMPAIGNS_ERROR,
      logLevel: _index.LogLevel.INFO,
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      visitorSessionId: this.visitor.instanceId,
      flagshipInstanceId: (_this$visitor$sdkInit4 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit4.instanceId,
      traffic: this.visitor.traffic,
      config: this.config,
      visitorContext: this.visitor.context,
      sdkStatus: this.visitor.getSdkStatus(),
      visitorCampaigns: campaigns,
      visitorConsent: this.visitor.hasConsented,
      visitorIsAuthenticated: !!this.visitor.anonymousId,
      visitorFlags: this.visitor.flagsData,
      visitorInitialCampaigns: (_this$visitor$sdkInit5 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit5.initialCampaigns,
      visitorInitialFlagsData: (_this$visitor$sdkInit6 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit6.initialFlagsData,
      lastBucketingTimestamp: this.configManager.decisionManager.lastBucketingTimestamp,
      lastInitializationTimestamp: (_this$visitor$sdkInit7 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit7.lastInitializationTimestamp,
      httpResponseTime: Date.now() - now,
      sdkConfigMode: this.getSdkConfigDecisionMode(),
      sdkConfigTimeout: this.config.timeout,
      sdkConfigPollingInterval: this.config.pollingInterval,
      sdkConfigTrackingManagerStrategy: (_this$config$tracking = this.config.trackingManagerConfig) == null ? void 0 : _this$config$tracking.cacheStrategy,
      sdkConfigTrackingManagerBatchIntervals: (_this$config$tracking2 = this.config.trackingManagerConfig) == null ? void 0 : _this$config$tracking2.batchIntervals,
      sdkConfigTrackingManagerPoolMaxSize: (_this$config$tracking3 = this.config.trackingManagerConfig) == null ? void 0 : _this$config$tracking3.poolMaxSize,
      sdkConfigFetchNow: this.config.fetchNow,
      sdkConfigReuseVisitorIds: this.config.reuseVisitorIds,
      sdkConfigInitialBucketing: this.config.initialBucketing,
      sdkConfigDecisionApiUrl: this.config.decisionApiUrl,
      sdkConfigHitDeduplicationTime: this.config.hitDeduplicationTime
    });
    this.trackingManager.addTroubleshootingHit(troubleshootingHit);
  }
  getCampaigns(now) {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      let campaigns = null;
      const functionName = _index.PROCESS_FETCHING_FLAGS;
      try {
        const time = Date.now() - _this5.visitor.lastFetchFlagsTimestamp;
        const fetchStatus = _this5.visitor.flagsStatus.status;
        if (fetchStatus === _FSFetchStatus.FSFetchStatus.FETCHING && _this5.visitor.getCampaignsPromise) {
          campaigns = yield _this5.visitor.getCampaignsPromise;
          return {
            campaigns,
            isFetching: true
          };
        }
        const fetchFlagBufferingTime = _this5.config.fetchFlagsBufferingTime * 1000;
        if (fetchStatus === _FSFetchStatus.FSFetchStatus.FETCHED && time < fetchFlagBufferingTime) {
          (0, _utils.logInfoSprintf)(_this5.config, functionName, _index.FETCH_FLAGS_BUFFERING_MESSAGE, _this5.visitor.visitorId, fetchFlagBufferingTime - time);
          return {
            campaigns,
            isBuffered: true
          };
        }
        (0, _utils.logDebugSprintf)(_this5.config, functionName, _index.FETCH_FLAGS_STARTED, _this5.visitor.visitorId);
        _this5.visitor.flagsStatus = {
          status: _FSFetchStatus.FSFetchStatus.FETCHING,
          reason: _FSFetchReasons.FSFetchReasons.NONE
        };
        yield _this5.lookupVisitor();
        yield _this5.visitor.emotionAi.fetchEAIScore();
        _this5.visitor.getCampaignsPromise = _this5.decisionManager.getCampaignsAsync(_this5.visitor);
        campaigns = yield _this5.visitor.getCampaignsPromise;
        _this5.visitor.lastFetchFlagsTimestamp = Date.now();
        if (_this5.decisionManager.isPanic()) {
          _this5.visitor.flagsStatus = {
            status: _FSFetchStatus.FSFetchStatus.PANIC,
            reason: _FSFetchReasons.FSFetchReasons.NONE
          };
        }
        _this5.configManager.trackingManager.troubleshootingData = _this5.decisionManager.troubleshooting;
        (0, _utils.logDebugSprintf)(_this5.config, functionName, _index.FETCH_CAMPAIGNS_SUCCESS, _this5.visitor.visitorId, _this5.visitor.anonymousId, _this5.visitor.context, campaigns, Date.now() - now);
        return {
          campaigns
        };
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logError)(_this5.config, error.message, _index.PROCESS_FETCHING_FLAGS);
        _this5.visitor.flagsStatus = {
          status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
          reason: _FSFetchReasons.FSFetchReasons.FLAGS_FETCHING_ERROR
        };
        return {
          error: error,
          campaigns
        };
      }
    })();
  }
  fetchCampaignsFromCache(visitor) {
    var _visitor$visitorCache;
    if (!Array.isArray(visitor == null || (_visitor$visitorCache = visitor.visitorCache) == null ? void 0 : _visitor$visitorCache.data.campaigns)) {
      return null;
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return visitor.visitorCache.data.campaigns.map(campaign => {
      return {
        id: campaign.campaignId,
        variationGroupId: campaign.variationGroupId,
        slug: campaign.slug,
        variation: {
          id: campaign.variationId,
          reference: !!campaign.isReference,
          modifications: {
            type: campaign.type,
            value: campaign.flags
          }
        }
      };
    });
  }
  handleNoCampaigns(now) {
    const campaigns = this.fetchCampaignsFromCache(this.visitor);
    if (campaigns) {
      this.visitor.flagsStatus = {
        status: _FSFetchStatus.FSFetchStatus.FETCH_REQUIRED,
        reason: _FSFetchReasons.FSFetchReasons.FLAGS_FETCHED_FROM_CACHE
      };
      (0, _utils.logDebugSprintf)(this.config, _index.PROCESS_FETCHING_FLAGS, _index.FETCH_CAMPAIGNS_FROM_CACHE, this.visitor.visitorId, this.visitor.anonymousId, this.visitor.context, campaigns, Date.now() - now);
    }
    return campaigns;
  }
  sendVisitorAllocatedVariations() {
    if (false) {}
  }
  extractFlags(campaigns) {
    const flags = new Map();
    campaigns.forEach(campaign => {
      const object = campaign.variation.modifications.value;
      for (const key in object) {
        const value = object[key];
        flags.set(key, {
          key,
          campaignId: campaign.id,
          campaignName: campaign.name || '',
          variationGroupId: campaign.variationGroupId,
          variationGroupName: campaign.variationGroupName || '',
          variationId: campaign.variation.id,
          variationName: campaign.variation.name || '',
          isReference: !!campaign.variation.reference,
          campaignType: campaign.type,
          slug: campaign.slug,
          value
        });
      }
    });
    return flags;
  }
  fetchFlags() {
    var _this6 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const now = Date.now();
      let campaigns = null;
      const _yield$_this6$getCamp = yield _this6.getCampaigns(now),
        fetchedCampaigns = _yield$_this6$getCamp.campaigns,
        fetchCampaignError = _yield$_this6$getCamp.error,
        isFetching = _yield$_this6$getCamp.isFetching,
        isBuffered = _yield$_this6$getCamp.isBuffered;
      if (isFetching || isBuffered) {
        return;
      }
      campaigns = fetchedCampaigns;
      try {
        if (!campaigns) {
          campaigns = _this6.handleNoCampaigns(now);
        }
        campaigns = campaigns || [];
        _this6.visitor.campaigns = campaigns;
        _this6.visitor.flagsData = _this6.extractFlags(_this6.visitor.campaigns);
        _this6.cacheVisitor();
        _this6.visitor.emit(_index.EMIT_READY, fetchCampaignError);
        if (_this6.visitor.flagsStatus.status === _FSFetchStatus.FSFetchStatus.FETCHING) {
          _this6.visitor.flagsStatus = {
            status: _FSFetchStatus.FSFetchStatus.FETCHED,
            reason: _FSFetchReasons.FSFetchReasons.NONE
          };
        }
        _this6.sendVisitorAllocatedVariations();
        (0, _utils.logDebugSprintf)(_this6.config, _index.PROCESS_FETCHING_FLAGS, _index.FETCH_FLAGS_FROM_CAMPAIGNS, _this6.visitor.visitorId, _this6.visitor.anonymousId, _this6.visitor.context, _this6.visitor.flagsData);
        if (_this6.decisionManager.troubleshooting) {
          _this6.sendFetchFlagsTroubleshooting({
            campaigns,
            now,
            isFromCache: _this6.visitor.flagsStatus.reason === _FSFetchReasons.FSFetchReasons.FLAGS_FETCHED_FROM_CACHE
          });
          _this6.sendConsentHitTroubleshooting();
          _this6.sendSegmentHitTroubleshooting();
        }
        _this6.sendSdkConfigAnalyticHit();
      } catch (error) {
        _this6.handleFetchFlagsError(error, now, campaigns);
      }
    })();
  }
  visitorExposed(param) {
    var _this7 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const key = param.key,
        flag = param.flag,
        defaultValue = param.defaultValue,
        hasGetValueBeenCalled = param.hasGetValueBeenCalled;
      if (!flag) {
        (0, _utils.logWarningSprintf)(_this7.visitor.config, _index.FLAG_VISITOR_EXPOSED, _index.USER_EXPOSED_FLAG_ERROR, _this7.visitor.visitorId, key);
        _this7.sendFlagTroubleshooting(_types.TroubleshootingLabel.VISITOR_EXPOSED_FLAG_NOT_FOUND, key, defaultValue);
        return;
      }
      if (!hasGetValueBeenCalled) {
        (0, _utils.logWarningSprintf)(_this7.visitor.config, _index.FLAG_VISITOR_EXPOSED, _index.VISITOR_EXPOSED_VALUE_NOT_CALLED, _this7.visitor.visitorId, key);
        _this7.sendFlagTroubleshooting(_types.TroubleshootingLabel.FLAG_VALUE_NOT_CALLED, key, defaultValue, true);
        return;
      }
      if (defaultValue !== null && defaultValue !== undefined && flag.value !== null && !(0, _utils.hasSameType)(flag.value, defaultValue)) {
        (0, _utils.logWarningSprintf)(_this7.visitor.config, _index.FLAG_VISITOR_EXPOSED, _index.USER_EXPOSED_CAST_ERROR, _this7.visitor.visitorId, key);
        _this7.sendFlagTroubleshooting(_types.TroubleshootingLabel.VISITOR_EXPOSED_TYPE_WARNING, key, defaultValue);
        return;
      }
      yield _this7.sendActivate(flag, defaultValue);
    })();
  }
  sendFlagTroubleshooting(label, key, defaultValue, visitorExposed) {
    var _this$visitor$sdkInit8;
    const troubleshooting = new _Troubleshooting.Troubleshooting({
      label,
      logLevel: _index.LogLevel.WARNING,
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      visitorSessionId: this.visitor.instanceId,
      flagshipInstanceId: (_this$visitor$sdkInit8 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit8.instanceId,
      traffic: this.visitor.traffic,
      config: this.config,
      visitorContext: this.visitor.context,
      flagKey: key,
      flagDefault: defaultValue,
      visitorExposed
    });
    this.sendTroubleshootingHit(troubleshooting);
  }
  getFlagValue(param) {
    const key = param.key,
      defaultValue = param.defaultValue,
      flag = param.flag,
      visitorExposed = param.visitorExposed;
    if (!flag) {
      (0, _utils.logWarningSprintf)(this.config, _index.FLAG_VALUE, _index.GET_FLAG_MISSING_ERROR, this.visitor.visitorId, key, defaultValue);
      this.sendFlagTroubleshooting(_types.TroubleshootingLabel.GET_FLAG_VALUE_FLAG_NOT_FOUND, key, defaultValue, visitorExposed);
      return defaultValue;
    }
    if (visitorExposed) {
      this.sendActivate(flag, defaultValue);
    }
    if (flag.value === null) {
      return defaultValue;
    }
    if (defaultValue !== null && defaultValue !== undefined && !(0, _utils.hasSameType)(flag.value, defaultValue)) {
      (0, _utils.logWarningSprintf)(this.config, _index.FLAG_VALUE, _index.GET_FLAG_CAST_ERROR, this.visitor.visitorId, key, defaultValue);
      this.sendFlagTroubleshooting(_types.TroubleshootingLabel.GET_FLAG_VALUE_TYPE_WARNING, key, defaultValue, visitorExposed);
      return defaultValue;
    }
    (0, _utils.logDebugSprintf)(this.config, _index.FLAG_VALUE, _index.GET_FLAG_VALUE, this.visitor.visitorId, key, flag.value);
    return flag.value;
  }
  SendFlagMetadataTroubleshooting(key) {
    var _this$visitor$sdkInit9;
    (0, _utils.logWarningSprintf)(this.config, _index.FLAG_METADATA, _index.NO_FLAG_METADATA, this.visitor.visitorId, key);
    const monitoring = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.GET_FLAG_METADATA_TYPE_WARNING,
      logLevel: _index.LogLevel.WARNING,
      visitorId: this.visitor.visitorId,
      anonymousId: this.visitor.anonymousId,
      visitorSessionId: this.visitor.instanceId,
      flagshipInstanceId: (_this$visitor$sdkInit9 = this.visitor.sdkInitialData) == null ? void 0 : _this$visitor$sdkInit9.instanceId,
      traffic: this.visitor.traffic,
      config: this.config,
      visitorContext: this.visitor.context,
      flagKey: key
    });
    this.sendTroubleshootingHit(monitoring);
  }
  getFlagMetadata(param) {
    const key = param.key,
      flag = param.flag;
    if (!flag) {
      (0, _utils.logWarningSprintf)(this.config, _index.FLAG_METADATA, _index.NO_FLAG_METADATA, this.visitor.visitorId, key);
      this.SendFlagMetadataTroubleshooting(key);
      return _FSFlagMetadata.FSFlagMetadata.Empty();
    }
    const metadata = new _FSFlagMetadata.FSFlagMetadata({
      campaignId: flag.campaignId,
      campaignName: flag.campaignName,
      variationGroupId: flag.variationGroupId,
      variationGroupName: flag.variationGroupName,
      variationId: flag.variationId,
      variationName: flag.variationName,
      isReference: !!flag.isReference,
      campaignType: flag.campaignType,
      slug: flag.slug
    });
    return metadata;
  }
}
exports.DefaultStrategy = DefaultStrategy;

/***/ }),

/***/ 4834:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(9463);
__webpack_require__(4782);
__webpack_require__(7495);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.BatchingPeriodicCachingStrategy = void 0;
__webpack_require__(2008);
__webpack_require__(3418);
__webpack_require__(3792);
__webpack_require__(6033);
var _slicedToArray2 = _interopRequireDefault(__webpack_require__(5715));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _BatchTriggeredBy = __webpack_require__(8160);
var _index = __webpack_require__(5394);
var _ActivateBatch = __webpack_require__(7783);
var _Batch = __webpack_require__(1728);
var _Troubleshooting = __webpack_require__(3142);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _BatchingCachingStrategyAbstract = __webpack_require__(3787);
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
class BatchingPeriodicCachingStrategy extends _BatchingCachingStrategyAbstract.BatchingCachingStrategyAbstract {
  addHitInPoolQueue(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this._hitsPoolQueue.set(hit.key, hit);
    })();
  }
  sendActivate({
    activateHitsPool,
    currentActivate,
    batchTriggeredBy
  }) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const headers = {
        [_index.HEADER_X_API_KEY]: _this2.config.apiKey,
        [_index.HEADER_X_SDK_CLIENT]: _index.SDK_INFO.name,
        [_index.HEADER_X_SDK_VERSION]: _index.SDK_INFO.version,
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const activateBatch = new _ActivateBatch.ActivateBatch(Array.from(activateHitsPool.filter(item => Date.now() - item.createdAt < _index.DEFAULT_HIT_CACHE_TIME_MS)), _this2.config);
      if (currentActivate) {
        activateBatch.hits.push(currentActivate);
      }
      const requestBody = activateBatch.toApiKeys();
      const url = _index.BASE_API_URL + _index.URL_ACTIVATE_MODIFICATION;
      const now = Date.now();
      try {
        yield _this2._httpClient.postAsync(url, {
          headers,
          body: requestBody,
          timeout: _this2.config.timeout,
          nextFetchConfig: _this2.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this2.config, _index.TRACKING_MANAGER, _index.HIT_SENT_SUCCESS, _index.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        activateBatch.hits.forEach(item => {
          _this2.onVisitorExposed(item);
        });
        _this2.sendHitsToFsQa(activateBatch.hits);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        activateBatch.hits.forEach(item => {
          _this2._activatePoolQueue.set(item.key, item);
        });
        (0, _utils.logErrorSprintf)(_this2.config, _index.TRACKING_MANAGER, _index.TRACKING_MANAGER_ERROR, _index.ACTIVATE_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_ACTIVATE_HIT_ROUTE_ERROR,
          logLevel: _index.LogLevel.ERROR,
          visitorId: `${_this2._flagshipInstanceId}`,
          traffic: 0,
          config: _this2.config,
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: url,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now,
          batchTriggeredBy
        });
        _this2.sendTroubleshootingHit(monitoringHttpResponse);
      }
    })();
  }
  sendBatch(batchTriggeredBy = _BatchTriggeredBy.BatchTriggeredBy.BatchLength) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      let hasActivateHit = false;
      if (_this3._activatePoolQueue.size) {
        const activateHits = Array.from(_this3._activatePoolQueue.values());
        _this3._activatePoolQueue.clear();
        yield _this3.sendActivate({
          activateHitsPool: activateHits,
          batchTriggeredBy
        });
        hasActivateHit = true;
      }
      const headers = {
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const batch = new _Batch.Batch({
        hits: []
      });
      batch.config = _this3.config;
      let batchSize = 0;
      const hitKeysToRemove = [];
      var _iterator = _createForOfIteratorHelper(_this3._hitsPoolQueue),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          const _step$value = (0, _slicedToArray2.default)(_step.value, 2),
            key = _step$value[0],
            item = _step$value[1];
          if (Date.now() - item.createdAt >= _index.DEFAULT_HIT_CACHE_TIME_MS) {
            hitKeysToRemove.push(key);
            continue;
          }
          batchSize = JSON.stringify(batch).length;
          if (batchSize > _index.BATCH_MAX_SIZE) {
            break;
          }
          batch.hits.push(item);
          hitKeysToRemove.push(key);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      hitKeysToRemove.forEach(key => {
        _this3._hitsPoolQueue.delete(key);
      });
      if (!batch.hits.length) {
        if (hasActivateHit) {
          yield _this3.cacheHit(_this3._activatePoolQueue);
        }
        return;
      }
      const requestBody = batch.toApiKeys();
      const now = Date.now();
      try {
        yield _this3._httpClient.postAsync(_index.HIT_EVENT_URL, {
          headers,
          body: requestBody,
          timeout: _this3.config.timeout,
          nextFetchConfig: _this3.config.nextFetchConfig
        });
        (0, _utils.logDebugSprintf)(_this3.config, _index.TRACKING_MANAGER, _index.HIT_SENT_SUCCESS, _index.BATCH_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index.HIT_EVENT_URL,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        _this3.sendHitsToFsQa(batch.hits);
        _this3.dispatchHitsToTag(batch.hits);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        batch.hits.forEach(hit => {
          _this3._hitsPoolQueue.set(hit.key, hit);
        });
        (0, _utils.logErrorSprintf)(_this3.config, _index.TRACKING_MANAGER, _index.TRACKING_MANAGER_ERROR, _index.BATCH_HIT, {
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpRequestMethod: 'POST',
          httpRequestUrl: _index.HIT_EVENT_URL,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          duration: Date.now() - now,
          batchTriggeredBy: _BatchTriggeredBy.BatchTriggeredBy[batchTriggeredBy]
        });
        const monitoringHttpResponse = new _Troubleshooting.Troubleshooting({
          label: _types.TroubleshootingLabel.SEND_BATCH_HIT_ROUTE_RESPONSE_ERROR,
          logLevel: _index.LogLevel.ERROR,
          visitorId: `${_this3._flagshipInstanceId}`,
          traffic: 0,
          config: _this3.config,
          httpRequestBody: requestBody,
          httpRequestHeaders: headers,
          httpResponseBody: error == null ? void 0 : error.message,
          httpResponseHeaders: error == null ? void 0 : error.headers,
          httpResponseMethod: 'POST',
          httpResponseUrl: _index.HIT_EVENT_URL,
          httpResponseCode: error == null ? void 0 : error.statusCode,
          httpResponseTime: Date.now() - now
        });
        _this3.addTroubleshootingHit(monitoringHttpResponse);
      }
      const mergedQueue = new Map([..._this3._hitsPoolQueue, ..._this3._activatePoolQueue]);
      yield _this3.flushAllHits();
      yield _this3.cacheHit(mergedQueue);
    })();
  }
}
exports.BatchingPeriodicCachingStrategy = BatchingPeriodicCachingStrategy;

/***/ }),

/***/ 4864:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var globalThis = __webpack_require__(2195);
var uncurryThis = __webpack_require__(9504);
var isForced = __webpack_require__(2796);
var inheritIfRequired = __webpack_require__(3167);
var createNonEnumerableProperty = __webpack_require__(6699);
var create = __webpack_require__(2360);
var getOwnPropertyNames = (__webpack_require__(8480).f);
var isPrototypeOf = __webpack_require__(1625);
var isRegExp = __webpack_require__(788);
var toString = __webpack_require__(655);
var getRegExpFlags = __webpack_require__(1034);
var stickyHelpers = __webpack_require__(8429);
var proxyAccessor = __webpack_require__(1056);
var defineBuiltIn = __webpack_require__(6840);
var fails = __webpack_require__(9039);
var hasOwn = __webpack_require__(9297);
var enforceInternalState = (__webpack_require__(1181).enforce);
var setSpecies = __webpack_require__(7633);
var wellKnownSymbol = __webpack_require__(8227);
var UNSUPPORTED_DOT_ALL = __webpack_require__(3635);
var UNSUPPORTED_NCG = __webpack_require__(8814);

var MATCH = wellKnownSymbol('match');
var NativeRegExp = globalThis.RegExp;
var RegExpPrototype = NativeRegExp.prototype;
var SyntaxError = globalThis.SyntaxError;
var exec = uncurryThis(RegExpPrototype.exec);
var charAt = uncurryThis(''.charAt);
var replace = uncurryThis(''.replace);
var stringIndexOf = uncurryThis(''.indexOf);
var stringSlice = uncurryThis(''.slice);
// TODO: Use only proper RegExpIdentifierName
var IS_NCG = /^\?<[^\s\d!#%&*+<=>@^][^\s!#%&*+<=>@^]*>/;
var re1 = /a/g;
var re2 = /a/g;

// "new" should create a new object, old webkit bug
var CORRECT_NEW = new NativeRegExp(re1) !== re1;

var MISSED_STICKY = stickyHelpers.MISSED_STICKY;
var UNSUPPORTED_Y = stickyHelpers.UNSUPPORTED_Y;

var BASE_FORCED = DESCRIPTORS &&
  (!CORRECT_NEW || MISSED_STICKY || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG || fails(function () {
    re2[MATCH] = false;
    // RegExp constructor can alter flags and IsRegExp works correct with @@match
    // eslint-disable-next-line sonarjs/inconsistent-function-call -- required for testing
    return NativeRegExp(re1) !== re1 || NativeRegExp(re2) === re2 || String(NativeRegExp(re1, 'i')) !== '/a/i';
  }));

var handleDotAll = function (string) {
  var length = string.length;
  var index = 0;
  var result = '';
  var brackets = false;
  var chr;
  for (; index <= length; index++) {
    chr = charAt(string, index);
    if (chr === '\\') {
      result += chr + charAt(string, ++index);
      continue;
    }
    if (!brackets && chr === '.') {
      result += '[\\s\\S]';
    } else {
      if (chr === '[') {
        brackets = true;
      } else if (chr === ']') {
        brackets = false;
      } result += chr;
    }
  } return result;
};

var handleNCG = function (string) {
  var length = string.length;
  var index = 0;
  var result = '';
  var named = [];
  var names = create(null);
  var brackets = false;
  var ncg = false;
  var groupid = 0;
  var groupname = '';
  var chr;
  for (; index <= length; index++) {
    chr = charAt(string, index);
    if (chr === '\\') {
      chr += charAt(string, ++index);
    } else if (chr === ']') {
      brackets = false;
    } else if (!brackets) switch (true) {
      case chr === '[':
        brackets = true;
        break;
      case chr === '(':
        result += chr;
        // ignore non-capturing groups
        if (stringSlice(string, index + 1, index + 3) === '?:') {
          continue;
        }
        if (exec(IS_NCG, stringSlice(string, index + 1))) {
          index += 2;
          ncg = true;
        }
        groupid++;
        continue;
      case chr === '>' && ncg:
        if (groupname === '' || hasOwn(names, groupname)) {
          throw new SyntaxError('Invalid capture group name');
        }
        names[groupname] = true;
        named[named.length] = [groupname, groupid];
        ncg = false;
        groupname = '';
        continue;
    }
    if (ncg) groupname += chr;
    else result += chr;
  } return [result, named];
};

// `RegExp` constructor
// https://tc39.es/ecma262/#sec-regexp-constructor
if (isForced('RegExp', BASE_FORCED)) {
  var RegExpWrapper = function RegExp(pattern, flags) {
    var thisIsRegExp = isPrototypeOf(RegExpPrototype, this);
    var patternIsRegExp = isRegExp(pattern);
    var flagsAreUndefined = flags === undefined;
    var groups = [];
    var rawPattern = pattern;
    var rawFlags, dotAll, sticky, handled, result, state;

    if (!thisIsRegExp && patternIsRegExp && flagsAreUndefined && pattern.constructor === RegExpWrapper) {
      return pattern;
    }

    if (patternIsRegExp || isPrototypeOf(RegExpPrototype, pattern)) {
      pattern = pattern.source;
      if (flagsAreUndefined) flags = getRegExpFlags(rawPattern);
    }

    pattern = pattern === undefined ? '' : toString(pattern);
    flags = flags === undefined ? '' : toString(flags);
    rawPattern = pattern;

    if (UNSUPPORTED_DOT_ALL && 'dotAll' in re1) {
      dotAll = !!flags && stringIndexOf(flags, 's') > -1;
      if (dotAll) flags = replace(flags, /s/g, '');
    }

    rawFlags = flags;

    if (MISSED_STICKY && 'sticky' in re1) {
      sticky = !!flags && stringIndexOf(flags, 'y') > -1;
      if (sticky && UNSUPPORTED_Y) flags = replace(flags, /y/g, '');
    }

    if (UNSUPPORTED_NCG) {
      handled = handleNCG(pattern);
      pattern = handled[0];
      groups = handled[1];
    }

    result = inheritIfRequired(NativeRegExp(pattern, flags), thisIsRegExp ? this : RegExpPrototype, RegExpWrapper);

    if (dotAll || sticky || groups.length) {
      state = enforceInternalState(result);
      if (dotAll) {
        state.dotAll = true;
        state.raw = RegExpWrapper(handleDotAll(pattern), rawFlags);
      }
      if (sticky) state.sticky = true;
      if (groups.length) state.groups = groups;
    }

    if (pattern !== rawPattern) try {
      // fails in old engines, but we have no alternatives for unsupported regex syntax
      createNonEnumerableProperty(result, 'source', rawPattern === '' ? '(?:)' : rawPattern);
    } catch (error) { /* empty */ }

    return result;
  };

  for (var keys = getOwnPropertyNames(NativeRegExp), index = 0; keys.length > index;) {
    proxyAccessor(RegExpWrapper, NativeRegExp, keys[index++]);
  }

  RegExpPrototype.constructor = RegExpWrapper;
  RegExpWrapper.prototype = RegExpPrototype;
  defineBuiltIn(globalThis, 'RegExp', RegExpWrapper, { constructor: true });
}

// https://tc39.es/ecma262/#sec-get-regexp-@@species
setSpecies('RegExp');


/***/ }),

/***/ 4878:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
Object.defineProperty(exports, "EventEmitter", ({
  enumerable: true,
  get: function get() {
    return _events.EventEmitter;
  }
}));
Object.defineProperty(exports, "LocalAbortController", ({
  enumerable: true,
  get: function get() {
    return _LocalAbortController.LocalAbortController;
  }
}));
exports.myFetch = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _nodeFetch = _interopRequireDefault(__webpack_require__(4229));
var _events = __webpack_require__(6261);
var _LocalAbortController = __webpack_require__(3609);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function getHttpAgent() {
  const globalOption = {};
  if (typeof window === 'undefined') {
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const _require = __webpack_require__(8611),
      HttpAgent = _require.Agent;

    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const _require2 = __webpack_require__(5692),
      HttpAgents = _require2.Agent;
    globalOption.agent = parsedURL => {
      return parsedURL.protocol === 'http:' ? new HttpAgent({
        keepAlive: true
      }) : new HttpAgents({
        keepAlive: true
      });
    };
  }
  return globalOption;
}
const myFetch = exports.myFetch = /*#__PURE__*/function () {
  var _ref = (0, _asyncToGenerator2.default)(function* (input, init) {
    const globalOption = getHttpAgent();
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return (0, _nodeFetch.default)(input, _objectSpread(_objectSpread({}, globalOption), init));
  });
  return function myFetch(_x, _x2) {
    return _ref.apply(this, arguments);
  };
}();

/***/ }),

/***/ 4893:
/***/ ((module) => {

function _objectWithoutPropertiesLoose(r, e) {
  if (null == r) return {};
  var t = {};
  for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
    if (-1 !== e.indexOf(n)) continue;
    t[n] = r[n];
  }
  return t;
}
module.exports = _objectWithoutPropertiesLoose, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 4901:
/***/ ((module) => {

"use strict";

// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot
var documentAll = typeof document == 'object' && document.all;

// `IsCallable` abstract operation
// https://tc39.es/ecma262/#sec-iscallable
// eslint-disable-next-line unicorn/no-typeof-undefined -- required for testing
module.exports = typeof documentAll == 'undefined' && documentAll !== undefined ? function (argument) {
  return typeof argument == 'function' || argument === documentAll;
} : function (argument) {
  return typeof argument == 'function';
};


/***/ }),

/***/ 4913:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var IE8_DOM_DEFINE = __webpack_require__(5917);
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(8686);
var anObject = __webpack_require__(8551);
var toPropertyKey = __webpack_require__(6969);

var $TypeError = TypeError;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var $defineProperty = Object.defineProperty;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var ENUMERABLE = 'enumerable';
var CONFIGURABLE = 'configurable';
var WRITABLE = 'writable';

// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
exports.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (typeof O === 'function' && P === 'prototype' && 'value' in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
    var current = $getOwnPropertyDescriptor(O, P);
    if (current && current[WRITABLE]) {
      O[P] = Attributes.value;
      Attributes = {
        configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
        enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
        writable: false
      };
    }
  } return $defineProperty(O, P, Attributes);
} : $defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return $defineProperty(O, P, Attributes);
  } catch (error) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw new $TypeError('Accessors not supported');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),

/***/ 4985:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

const { EventEmitter } = __webpack_require__(6261);

class AbortSignal {
  constructor() {
    this.eventEmitter = new EventEmitter();
    this.onabort = null;
    this.aborted = false;
    this.reason = undefined;
  }
  toString() {
    return "[object AbortSignal]";
  }
  get [Symbol.toStringTag]() {
    return "AbortSignal";
  }
  removeEventListener(name, handler) {
    this.eventEmitter.removeListener(name, handler);
  }
  addEventListener(name, handler) {
    this.eventEmitter.on(name, handler);
  }
  dispatchEvent(type) {
    const event = { type, target: this };
    const handlerName = `on${type}`;

    if (typeof this[handlerName] === "function") this[handlerName](event);

    this.eventEmitter.emit(type, event);
  }
  throwIfAborted() {
    if (this.aborted) {
      throw this.reason;
    }
  }
  static abort(reason) {
    const controller = new AbortController();
    controller.abort();
    return controller.signal;
  }
  static timeout(time) {
    const controller = new AbortController();
    setTimeout(() => controller.abort(new Error("TimeoutError")), time);
    return controller.signal;
  }
}
class AbortController {
  constructor() {
    this.signal = new AbortSignal();
  }
  abort(reason) {
    if (this.signal.aborted) return;

    this.signal.aborted = true;

    if (reason) this.signal.reason = reason;
    else this.signal.reason = new Error("AbortError");

    this.signal.dispatchEvent("abort");
  }
  toString() {
    return "[object AbortController]";
  }
  get [Symbol.toStringTag]() {
    return "AbortController";
  }
}

module.exports = { AbortController, AbortSignal };


/***/ }),

/***/ 4994:
/***/ ((module) => {

function _interopRequireDefault(e) {
  return e && e.__esModule ? e : {
    "default": e
  };
}
module.exports = _interopRequireDefault, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 5031:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var getBuiltIn = __webpack_require__(7751);
var uncurryThis = __webpack_require__(9504);
var getOwnPropertyNamesModule = __webpack_require__(8480);
var getOwnPropertySymbolsModule = __webpack_require__(3717);
var anObject = __webpack_require__(8551);

var concat = uncurryThis([].concat);

// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
  var keys = getOwnPropertyNamesModule.f(anObject(it));
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
};


/***/ }),

/***/ 5038:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.NotReadyStrategy = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _DefaultStrategy = __webpack_require__(4811);
var _FSFlagMetadata = __webpack_require__(8889);
class NotReadyStrategy extends _DefaultStrategy.DefaultStrategy {
  lookupHits() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  lookupVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cacheVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  collectEAIEventsAsync() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this.log('collectEAIData');
    })();
  }
  onEAICollectStatusChange() {
    //
  }
  reportEaiPageView() {
    //
  }
  reportEaiVisitorEvent() {
    //
  }
  sendVisitorAllocatedVariations() {
    //
  }
  sendHit(_hit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this2.log('sendHit');
    })();
  }
  sendHits(_hits) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this3.log('sendHits');
    })();
  }
  fetchFlags() {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this4.log('fetchFlags');
    })();
  }
  getFlagValue(param) {
    this.log('Flag.value');
    return param.defaultValue;
  }
  visitorExposed() {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this5.log(_index.FLAG_VISITOR_EXPOSED);
    })();
  }
  getFlagMetadata(param) {
    const emptyMetaData = _FSFlagMetadata.FSFlagMetadata.Empty();
    (0, _utils.logErrorSprintf)(this.config, _index.FLAG_METADATA, _index.METADATA_SDK_NOT_READY, this.visitor.visitorId, param.key, emptyMetaData);
    return emptyMetaData;
  }
  sendActivate(_flagDto, _defaultValue) {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  sendTroubleshootingHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  sendSdkConfigAnalyticHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  addInTrackingManager() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  log(methodName) {
    (0, _utils.logErrorSprintf)(this.config, methodName, _index.METHOD_DEACTIVATED_ERROR, this.visitor.visitorId, methodName, _index.FSSdkStatus[_index.FSSdkStatus.SDK_NOT_INITIALIZED]);
  }
}
exports.NotReadyStrategy = NotReadyStrategy;

/***/ }),

/***/ 5081:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var globalThis = __webpack_require__(2195);

// `globalThis` object
// https://tc39.es/ecma262/#sec-globalthis
$({ global: true, forced: globalThis.globalThis !== globalThis }, {
  globalThis: globalThis
});


/***/ }),

/***/ 5232:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.DecisionManager = void 0;
__webpack_require__(3792);
__webpack_require__(6033);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _types = __webpack_require__(7099);
var _utils = __webpack_require__(6517);
var _Troubleshooting = __webpack_require__(3142);
class DecisionManager {
  get trackingManager() {
    return this._trackingManager;
  }
  set trackingManager(v) {
    this._trackingManager = v;
  }
  get flagshipInstanceId() {
    return this._flagshipInstanceId;
  }
  set flagshipInstanceId(v) {
    this._flagshipInstanceId = v;
  }
  get lastBucketingTimestamp() {
    return this._lastBucketingTimestamp;
  }
  get troubleshooting() {
    return this._troubleshooting;
  }
  set troubleshooting(v) {
    this._troubleshooting = v;
  }
  get config() {
    return this._config;
  }
  set panic(v) {
    this.updateFlagshipStatus(v ? _index.FSSdkStatus.SDK_PANIC : _index.FSSdkStatus.SDK_INITIALIZED);
    this._panic = v;
    if (v) {
      (0, _utils.logDebug)(this.config, _index.FETCH_FLAGS_PANIC_MODE, _index.PROCESS_FETCHING_FLAGS);
    }
  }
  statusChangedCallback(v) {
    this._statusChangedCallback = v;
  }
  constructor({
    httpClient,
    config,
    trackingManager,
    flagshipInstanceId
  }) {
    (0, _defineProperty2.default)(this, "_panic", false);
    this._config = config;
    this._httpClient = httpClient;
    this._trackingManager = trackingManager;
    this._flagshipInstanceId = flagshipInstanceId || '';
  }
  updateFlagshipStatus(v) {
    if (typeof this._statusChangedCallback === 'function' && this._statusChangedCallback) {
      this._statusChangedCallback(v);
    }
  }
  getModifications(campaigns) {
    const modifications = new Map();
    campaigns.forEach(campaign => {
      const object = campaign.variation.modifications.value;
      for (const key in object) {
        const value = object[key];
        modifications.set(key, {
          key,
          campaignId: campaign.id,
          campaignName: campaign.name || '',
          variationGroupId: campaign.variationGroupId,
          variationGroupName: campaign.variationGroupName || '',
          variationId: campaign.variation.id,
          variationName: campaign.variation.name || '',
          isReference: !!campaign.variation.reference,
          campaignType: campaign.type,
          slug: campaign.slug,
          value
        });
      }
    });
    return modifications;
  }
  isPanic() {
    return this._panic;
  }
  handleTroubleshootingError(params) {
    var _this$trackingManager;
    const error = params.error,
      visitor = params.visitor,
      requestBody = params.requestBody,
      headers = params.headers,
      url = params.url,
      now = params.now;
    const troubleshooting = new _Troubleshooting.Troubleshooting({
      label: _types.TroubleshootingLabel.GET_CAMPAIGNS_ROUTE_RESPONSE_ERROR,
      logLevel: _index.LogLevel.ERROR,
      visitorId: visitor.visitorId,
      flagshipInstanceId: this.flagshipInstanceId,
      anonymousId: visitor.anonymousId,
      visitorSessionId: visitor.instanceId,
      traffic: 100,
      config: this.config,
      visitorContext: visitor.context,
      httpRequestBody: requestBody,
      httpRequestHeaders: headers,
      httpRequestMethod: 'POST',
      httpRequestUrl: url,
      httpResponseBody: error == null ? void 0 : error.message,
      httpResponseHeaders: error == null ? void 0 : error.headers,
      httpResponseCode: error == null ? void 0 : error.statusCode,
      httpResponseTime: Date.now() - now
    });
    (_this$trackingManager = this.trackingManager) == null || _this$trackingManager.addTroubleshootingHit(troubleshooting);
    const errorMessage = (0, _utils.errorFormat)(error.message || error, {
      url,
      headers,
      body: requestBody,
      duration: Date.now() - now
    });
    throw new Error(errorMessage);
  }
  getDecisionApiCampaignsAsync(visitor) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const headers = {
        [_index.HEADER_X_API_KEY]: `${_this.config.apiKey}`,
        [_index.HEADER_X_SDK_CLIENT]: _index.SDK_INFO.name,
        [_index.HEADER_X_SDK_VERSION]: _index.SDK_INFO.version,
        [_index.HEADER_CONTENT_TYPE]: _index.HEADER_APPLICATION_JSON
      };
      const requestBody = {
        visitorId: visitor.visitorId,
        anonymousId: visitor.anonymousId,
        trigger_hit: false,
        context: visitor.context,
        visitor_consent: visitor.hasConsented
      };
      const url = `${_this.config.decisionApiUrl || _index.BASE_API_URL}${_this.config.envId}${_index.URL_CAMPAIGNS}?${_index.EXPOSE_ALL_KEYS}=true&extras[]=accountSettings`;
      const now = Date.now();
      try {
        var _response$body, _response$body2, _response$body3;
        const response = yield _this._httpClient.postAsync(url, {
          headers,
          timeout: _this.config.timeout,
          body: requestBody,
          nextFetchConfig: _this.config.nextFetchConfig
        });
        _this.panic = !!(response != null && (_response$body = response.body) != null && _response$body.panic);
        let campaigns = null;
        if (response != null && (_response$body2 = response.body) != null && _response$body2.campaigns) {
          campaigns = response.body.campaigns;
        }
        const troubleshooting = response == null || (_response$body3 = response.body) == null || (_response$body3 = _response$body3.extras) == null || (_response$body3 = _response$body3.accountSettings) == null ? void 0 : _response$body3.troubleshooting;
        if (troubleshooting) {
          _this.troubleshooting = {
            startDate: new Date(troubleshooting.startDate),
            endDate: new Date(troubleshooting.endDate),
            timezone: troubleshooting.timezone,
            traffic: troubleshooting.traffic
          };
        }
        return campaigns;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        _this.handleTroubleshootingError({
          error,
          visitor,
          requestBody,
          headers,
          url,
          now
        });
      }
    })();
  }
}
exports.DecisionManager = DecisionManager;

/***/ }),

/***/ 5343:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.FlagshipLogManager = void 0;
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
/* eslint-disable no-console */

class FlagshipLogManager {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any

  // eslint-disable-next-line @typescript-eslint/no-explicit-any

  // eslint-disable-next-line @typescript-eslint/no-explicit-any

  constructor() {
    var _console$error, _console$warn, _console$debug;
    this.consoleError = (_console$error = console.error) != null ? _console$error : console.log;
    this.consoleWarn = (_console$warn = console.warn) != null ? _console$warn : console.log;
    this.consoleDebug = (_console$debug = console.debug) != null ? _console$debug : console.log;
  }
  emergency(message, tag) {
    this.consoleError((0, _utils.formatLogOutput)(_index.LogLevel.EMERGENCY, message, tag));
  }
  alert(message, tag) {
    this.consoleError((0, _utils.formatLogOutput)(_index.LogLevel.ALERT, message, tag));
  }
  critical(message, tag) {
    this.consoleError((0, _utils.formatLogOutput)(_index.LogLevel.CRITICAL, message, tag));
  }
  error(message, tag) {
    this.consoleError((0, _utils.formatLogOutput)(_index.LogLevel.ERROR, message, tag));
  }
  warning(message, tag) {
    this.consoleWarn((0, _utils.formatLogOutput)(_index.LogLevel.WARNING, message, tag));
  }
  notice(message, tag) {
    this.log(_index.LogLevel.NOTICE, message, tag);
  }
  info(message, tag) {
    console.info((0, _utils.formatLogOutput)(_index.LogLevel.INFO, message, tag));
  }
  debug(message, tag) {
    this.consoleDebug((0, _utils.formatLogOutput)(_index.LogLevel.DEBUG, message, tag));
  }
  log(level, message, tag) {
    console.log((0, _utils.formatLogOutput)(level, message, tag));
  }
}
exports.FlagshipLogManager = FlagshipLogManager;

/***/ }),

/***/ 5394:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
var _LogLevel = __webpack_require__(4288);
Object.keys(_LogLevel).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _LogLevel[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _LogLevel[key];
    }
  });
});
var _FlagshipConstant = __webpack_require__(2932);
Object.keys(_FlagshipConstant).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FlagshipConstant[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FlagshipConstant[key];
    }
  });
});
var _FSSdkStatus = __webpack_require__(2137);
Object.keys(_FSSdkStatus).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSSdkStatus[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSSdkStatus[key];
    }
  });
});
var _HitType = __webpack_require__(6147);
Object.keys(_HitType).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _HitType[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _HitType[key];
    }
  });
});
var _CacheStrategy = __webpack_require__(1589);
Object.keys(_CacheStrategy).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _CacheStrategy[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _CacheStrategy[key];
    }
  });
});
var _FSFetchReasons = __webpack_require__(1734);
Object.keys(_FSFetchReasons).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSFetchReasons[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSFetchReasons[key];
    }
  });
});
var _FSFetchStatus = __webpack_require__(1597);
Object.keys(_FSFetchStatus).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSFetchStatus[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSFetchStatus[key];
    }
  });
});
var _FSFlagStatus = __webpack_require__(5819);
Object.keys(_FSFlagStatus).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSFlagStatus[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSFlagStatus[key];
    }
  });
});

/***/ }),

/***/ 5397:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// toObject with fallback for non-array-like ES3 strings
var IndexedObject = __webpack_require__(7055);
var requireObjectCoercible = __webpack_require__(7750);

module.exports = function (it) {
  return IndexedObject(requireObjectCoercible(it));
};


/***/ }),

/***/ 5440:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var apply = __webpack_require__(8745);
var call = __webpack_require__(9565);
var uncurryThis = __webpack_require__(9504);
var fixRegExpWellKnownSymbolLogic = __webpack_require__(9228);
var fails = __webpack_require__(9039);
var anObject = __webpack_require__(8551);
var isCallable = __webpack_require__(4901);
var isNullOrUndefined = __webpack_require__(4117);
var toIntegerOrInfinity = __webpack_require__(1291);
var toLength = __webpack_require__(8014);
var toString = __webpack_require__(655);
var requireObjectCoercible = __webpack_require__(7750);
var advanceStringIndex = __webpack_require__(7829);
var getMethod = __webpack_require__(5966);
var getSubstitution = __webpack_require__(2478);
var regExpExec = __webpack_require__(6682);
var wellKnownSymbol = __webpack_require__(8227);

var REPLACE = wellKnownSymbol('replace');
var max = Math.max;
var min = Math.min;
var concat = uncurryThis([].concat);
var push = uncurryThis([].push);
var stringIndexOf = uncurryThis(''.indexOf);
var stringSlice = uncurryThis(''.slice);

var maybeToString = function (it) {
  return it === undefined ? it : String(it);
};

// IE <= 11 replaces $0 with the whole match, as if it was $&
// https://stackoverflow.com/questions/6024666/getting-ie-to-replace-a-regex-with-the-literal-string-0
var REPLACE_KEEPS_$0 = (function () {
  // eslint-disable-next-line regexp/prefer-escape-replacement-dollar-char -- required for testing
  return 'a'.replace(/./, '$0') === '$0';
})();

// Safari <= 13.0.3(?) substitutes nth capture where n>m with an empty string
var REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE = (function () {
  if (/./[REPLACE]) {
    return /./[REPLACE]('a', '$0') === '';
  }
  return false;
})();

var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function () {
  var re = /./;
  re.exec = function () {
    var result = [];
    result.groups = { a: '7' };
    return result;
  };
  // eslint-disable-next-line regexp/no-useless-dollar-replacements -- false positive
  return ''.replace(re, '$<a>') !== '7';
});

// @@replace logic
fixRegExpWellKnownSymbolLogic('replace', function (_, nativeReplace, maybeCallNative) {
  var UNSAFE_SUBSTITUTE = REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE ? '$' : '$0';

  return [
    // `String.prototype.replace` method
    // https://tc39.es/ecma262/#sec-string.prototype.replace
    function replace(searchValue, replaceValue) {
      var O = requireObjectCoercible(this);
      var replacer = isNullOrUndefined(searchValue) ? undefined : getMethod(searchValue, REPLACE);
      return replacer
        ? call(replacer, searchValue, O, replaceValue)
        : call(nativeReplace, toString(O), searchValue, replaceValue);
    },
    // `RegExp.prototype[@@replace]` method
    // https://tc39.es/ecma262/#sec-regexp.prototype-@@replace
    function (string, replaceValue) {
      var rx = anObject(this);
      var S = toString(string);

      if (
        typeof replaceValue == 'string' &&
        stringIndexOf(replaceValue, UNSAFE_SUBSTITUTE) === -1 &&
        stringIndexOf(replaceValue, '$<') === -1
      ) {
        var res = maybeCallNative(nativeReplace, rx, S, replaceValue);
        if (res.done) return res.value;
      }

      var functionalReplace = isCallable(replaceValue);
      if (!functionalReplace) replaceValue = toString(replaceValue);

      var global = rx.global;
      var fullUnicode;
      if (global) {
        fullUnicode = rx.unicode;
        rx.lastIndex = 0;
      }

      var results = [];
      var result;
      while (true) {
        result = regExpExec(rx, S);
        if (result === null) break;

        push(results, result);
        if (!global) break;

        var matchStr = toString(result[0]);
        if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
      }

      var accumulatedResult = '';
      var nextSourcePosition = 0;
      for (var i = 0; i < results.length; i++) {
        result = results[i];

        var matched = toString(result[0]);
        var position = max(min(toIntegerOrInfinity(result.index), S.length), 0);
        var captures = [];
        var replacement;
        // NOTE: This is equivalent to
        //   captures = result.slice(1).map(maybeToString)
        // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
        // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
        // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.
        for (var j = 1; j < result.length; j++) push(captures, maybeToString(result[j]));
        var namedCaptures = result.groups;
        if (functionalReplace) {
          var replacerArgs = concat([matched], captures, position, S);
          if (namedCaptures !== undefined) push(replacerArgs, namedCaptures);
          replacement = toString(apply(replaceValue, undefined, replacerArgs));
        } else {
          replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
        }
        if (position >= nextSourcePosition) {
          accumulatedResult += stringSlice(S, nextSourcePosition, position) + replacement;
          nextSourcePosition = position + matched.length;
        }
      }

      return accumulatedResult + stringSlice(S, nextSourcePosition);
    }
  ];
}, !REPLACE_SUPPORTS_NAMED_GROUPS || !REPLACE_KEEPS_$0 || REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE);


/***/ }),

/***/ 5506:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $entries = (__webpack_require__(2357).entries);

// `Object.entries` method
// https://tc39.es/ecma262/#sec-object.entries
$({ target: 'Object', stat: true }, {
  entries: function entries(O) {
    return $entries(O);
  }
});


/***/ }),

/***/ 5548:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isConstructor = __webpack_require__(3517);
var tryToString = __webpack_require__(6823);

var $TypeError = TypeError;

// `Assert: IsConstructor(argument) is true`
module.exports = function (argument) {
  if (isConstructor(argument)) return argument;
  throw new $TypeError(tryToString(argument) + ' is not a constructor');
};


/***/ }),

/***/ 5578:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
var _exportNames = {
  Flagship: true,
  DecisionApiConfig: true,
  DecisionMode: true,
  EventCategory: true,
  FSSdkStatus: true,
  LogLevel: true,
  HitType: true,
  CacheStrategy: true,
  FSFetchReasons: true,
  FSFetchStatus: true,
  FSFlagStatus: true,
  Visitor: true
};
Object.defineProperty(exports, "CacheStrategy", ({
  enumerable: true,
  get: function get() {
    return _index3.CacheStrategy;
  }
}));
Object.defineProperty(exports, "DecisionApiConfig", ({
  enumerable: true,
  get: function get() {
    return _index.DecisionApiConfig;
  }
}));
Object.defineProperty(exports, "DecisionMode", ({
  enumerable: true,
  get: function get() {
    return _index.DecisionMode;
  }
}));
Object.defineProperty(exports, "EventCategory", ({
  enumerable: true,
  get: function get() {
    return _index2.EventCategory;
  }
}));
Object.defineProperty(exports, "FSFetchReasons", ({
  enumerable: true,
  get: function get() {
    return _index3.FSFetchReasons;
  }
}));
Object.defineProperty(exports, "FSFetchStatus", ({
  enumerable: true,
  get: function get() {
    return _index3.FSFetchStatus;
  }
}));
Object.defineProperty(exports, "FSFlagStatus", ({
  enumerable: true,
  get: function get() {
    return _index3.FSFlagStatus;
  }
}));
Object.defineProperty(exports, "FSSdkStatus", ({
  enumerable: true,
  get: function get() {
    return _index3.FSSdkStatus;
  }
}));
Object.defineProperty(exports, "Flagship", ({
  enumerable: true,
  get: function get() {
    return _Flagship.Flagship;
  }
}));
Object.defineProperty(exports, "HitType", ({
  enumerable: true,
  get: function get() {
    return _index3.HitType;
  }
}));
Object.defineProperty(exports, "LogLevel", ({
  enumerable: true,
  get: function get() {
    return _index3.LogLevel;
  }
}));
Object.defineProperty(exports, "Visitor", ({
  enumerable: true,
  get: function get() {
    return _index4.Visitor;
  }
}));
exports["default"] = void 0;
var _Flagship = __webpack_require__(8204);
var _index = __webpack_require__(6701);
var _index2 = __webpack_require__(5588);
var _index3 = __webpack_require__(5394);
var _FlagshipContext = __webpack_require__(7877);
Object.keys(_FlagshipContext).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _FlagshipContext[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FlagshipContext[key];
    }
  });
});
var _types = __webpack_require__(7099);
Object.keys(_types).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _types[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _types[key];
    }
  });
});
var _index4 = __webpack_require__(5791);
var _index5 = __webpack_require__(8045);
Object.keys(_index5).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _index5[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _index5[key];
    }
  });
});
var _default = exports["default"] = _Flagship.Flagship;

/***/ }),

/***/ 5588:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
Object.defineProperty(exports, "EventCategory", ({
  enumerable: true,
  get: function get() {
    return _EventCategory.EventCategory;
  }
}));
var _EventCategory = __webpack_require__(568);

/***/ }),

/***/ 5610:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toIntegerOrInfinity = __webpack_require__(1291);

var max = Math.max;
var min = Math.min;

// Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
module.exports = function (index, length) {
  var integer = toIntegerOrInfinity(index);
  return integer < 0 ? max(integer + length, 0) : min(integer, length);
};


/***/ }),

/***/ 5652:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// FF26- bug: ArrayBuffers are non-extensible, but Object.isExtensible does not report it
var fails = __webpack_require__(9039);

module.exports = fails(function () {
  if (typeof ArrayBuffer == 'function') {
    var buffer = new ArrayBuffer(8);
    // eslint-disable-next-line es/no-object-isextensible, es/no-object-defineproperty -- safe
    if (Object.isExtensible(buffer)) Object.defineProperty(buffer, 'a', { value: 8 });
  }
});


/***/ }),

/***/ 5678:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.EdgeSdkManager = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
class EdgeSdkManager {
  constructor({
    httpClient,
    sdkConfig,
    trackingManager,
    flagshipInstanceId
  }) {
    this._httpClient = httpClient;
    this._config = sdkConfig;
    this._trackingManager = trackingManager;
    this._bucketingContent = sdkConfig.initialBucketing;
    this._flagshipInstanceId = flagshipInstanceId;
  }
  resetSdk() {
    //
  }
  getBucketingContent() {
    return this._bucketingContent;
  }
  initSdk() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  getEAIConfig() {
    return undefined;
  }
}
exports.EdgeSdkManager = EdgeSdkManager;

/***/ }),

/***/ 5692:
/***/ ((module) => {

"use strict";
module.exports = require("https");

/***/ }),

/***/ 5701:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $padEnd = (__webpack_require__(533).end);
var WEBKIT_BUG = __webpack_require__(3063);

// `String.prototype.padEnd` method
// https://tc39.es/ecma262/#sec-string.prototype.padend
$({ target: 'String', proto: true, forced: WEBKIT_BUG }, {
  padEnd: function padEnd(maxLength /* , fillString = ' ' */) {
    return $padEnd(this, maxLength, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ 5715:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var arrayWithHoles = __webpack_require__(2987);
var iterableToArrayLimit = __webpack_require__(1156);
var unsupportedIterableToArray = __webpack_require__(7122);
var nonIterableRest = __webpack_require__(7752);
function _slicedToArray(r, e) {
  return arrayWithHoles(r) || iterableToArrayLimit(r, e) || unsupportedIterableToArray(r, e) || nonIterableRest();
}
module.exports = _slicedToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 5745:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var store = __webpack_require__(7629);

module.exports = function (key, value) {
  return store[key] || (store[key] = value || {});
};


/***/ }),

/***/ 5746:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var FREEZING = __webpack_require__(2744);
var globalThis = __webpack_require__(2195);
var uncurryThis = __webpack_require__(9504);
var defineBuiltIns = __webpack_require__(6279);
var InternalMetadataModule = __webpack_require__(3451);
var collection = __webpack_require__(6468);
var collectionWeak = __webpack_require__(4006);
var isObject = __webpack_require__(34);
var enforceInternalState = (__webpack_require__(1181).enforce);
var fails = __webpack_require__(9039);
var NATIVE_WEAK_MAP = __webpack_require__(8622);

var $Object = Object;
// eslint-disable-next-line es/no-array-isarray -- safe
var isArray = Array.isArray;
// eslint-disable-next-line es/no-object-isextensible -- safe
var isExtensible = $Object.isExtensible;
// eslint-disable-next-line es/no-object-isfrozen -- safe
var isFrozen = $Object.isFrozen;
// eslint-disable-next-line es/no-object-issealed -- safe
var isSealed = $Object.isSealed;
// eslint-disable-next-line es/no-object-freeze -- safe
var freeze = $Object.freeze;
// eslint-disable-next-line es/no-object-seal -- safe
var seal = $Object.seal;

var IS_IE11 = !globalThis.ActiveXObject && 'ActiveXObject' in globalThis;
var InternalWeakMap;

var wrapper = function (init) {
  return function WeakMap() {
    return init(this, arguments.length ? arguments[0] : undefined);
  };
};

// `WeakMap` constructor
// https://tc39.es/ecma262/#sec-weakmap-constructor
var $WeakMap = collection('WeakMap', wrapper, collectionWeak);
var WeakMapPrototype = $WeakMap.prototype;
var nativeSet = uncurryThis(WeakMapPrototype.set);

// Chakra Edge bug: adding frozen arrays to WeakMap unfreeze them
var hasMSEdgeFreezingBug = function () {
  return FREEZING && fails(function () {
    var frozenArray = freeze([]);
    nativeSet(new $WeakMap(), frozenArray, 1);
    return !isFrozen(frozenArray);
  });
};

// IE11 WeakMap frozen keys fix
// We can't use feature detection because it crash some old IE builds
// https://github.com/zloirock/core-js/issues/485
if (NATIVE_WEAK_MAP) if (IS_IE11) {
  InternalWeakMap = collectionWeak.getConstructor(wrapper, 'WeakMap', true);
  InternalMetadataModule.enable();
  var nativeDelete = uncurryThis(WeakMapPrototype['delete']);
  var nativeHas = uncurryThis(WeakMapPrototype.has);
  var nativeGet = uncurryThis(WeakMapPrototype.get);
  defineBuiltIns(WeakMapPrototype, {
    'delete': function (key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeDelete(this, key) || state.frozen['delete'](key);
      } return nativeDelete(this, key);
    },
    has: function has(key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeHas(this, key) || state.frozen.has(key);
      } return nativeHas(this, key);
    },
    get: function get(key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeHas(this, key) ? nativeGet(this, key) : state.frozen.get(key);
      } return nativeGet(this, key);
    },
    set: function set(key, value) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        nativeHas(this, key) ? nativeSet(this, key, value) : state.frozen.set(key, value);
      } else nativeSet(this, key, value);
      return this;
    }
  });
// Chakra Edge frozen keys fix
} else if (hasMSEdgeFreezingBug()) {
  defineBuiltIns(WeakMapPrototype, {
    set: function set(key, value) {
      var arrayIntegrityLevel;
      if (isArray(key)) {
        if (isFrozen(key)) arrayIntegrityLevel = freeze;
        else if (isSealed(key)) arrayIntegrityLevel = seal;
      }
      nativeSet(this, key, value);
      if (arrayIntegrityLevel) arrayIntegrityLevel(key);
      return this;
    }
  });
}


/***/ }),

/***/ 5769:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.forceVariation = forceVariation;
var _utils = __webpack_require__(6517);
function forceVariation({
  flagDTO,
  config,
  visitorVariationState
}) {
  if (false) {}
  return undefined;
}

/***/ }),

/***/ 5791:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
Object.defineProperty(exports, "DefaultStrategy", ({
  enumerable: true,
  get: function get() {
    return _DefaultStrategy.DefaultStrategy;
  }
}));
Object.defineProperty(exports, "NoConsentStrategy", ({
  enumerable: true,
  get: function get() {
    return _NoConsentStrategy.NoConsentStrategy;
  }
}));
Object.defineProperty(exports, "NotReadyStrategy", ({
  enumerable: true,
  get: function get() {
    return _NotReadyStrategy.NotReadyStrategy;
  }
}));
Object.defineProperty(exports, "PanicStrategy", ({
  enumerable: true,
  get: function get() {
    return _PanicStrategy.PanicStrategy;
  }
}));
Object.defineProperty(exports, "Visitor", ({
  enumerable: true,
  get: function get() {
    return _Visitor.Visitor;
  }
}));
Object.defineProperty(exports, "VisitorDelegate", ({
  enumerable: true,
  get: function get() {
    return _VisitorDelegate.VisitorDelegate;
  }
}));
var _NoConsentStrategy = __webpack_require__(7449);
var _NotReadyStrategy = __webpack_require__(5038);
var _DefaultStrategy = __webpack_require__(4811);
var _PanicStrategy = __webpack_require__(4641);
var _Visitor = __webpack_require__(3361);
var _VisitorDelegate = __webpack_require__(9102);

/***/ }),

/***/ 5819:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.FSFlagStatus = void 0;
/**
 * Represents the status of the flags in the Flagship SDK.
 */
let FSFlagStatus = exports.FSFlagStatus = /*#__PURE__*/function (FSFlagStatus) {
  /**
   * The flags have been successfully fetched from the API or re-evaluated in bucketing mode.
   */
  FSFlagStatus["FETCHED"] = "FETCHED";
  /**
   * The flags need to be re-fetched due to a change in context, or because the flags were loaded from cache or XPC.
   */
  FSFlagStatus["FETCH_REQUIRED"] = "FETCH_REQUIRED";
  /**
   * The flag was not found or do not exist.
   */
  FSFlagStatus["NOT_FOUND"] = "NOT_FOUND";
  /**
   * The SDK is in PANIC mode: All features are disabled except for the one to fetch flags.
   */
  FSFlagStatus["PANIC"] = "PANIC";
  return FSFlagStatus;
}({});

/***/ }),

/***/ 5821:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.HttpClient = void 0;
__webpack_require__(4423);
__webpack_require__(1699);
__webpack_require__(2480);
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _HttpError = __webpack_require__(8858);
var _index = __webpack_require__(5394);
var _depsNode = __webpack_require__(4878);
class HttpClient {
  getResponse(response) {
    return (0, _asyncToGenerator2.default)(function* () {
      const applicationType = response.headers.get('Content-Type');
      const checkJson = applicationType == null ? void 0 : applicationType.includes('application/json');
      let body;
      const headers = {};
      response.headers.forEach((value, key) => {
        headers[key] = value;
      });
      if (checkJson && response.ok && response.status !== 204) {
        body = yield response.json();
      }
      if (response.status >= 400) {
        const bodyString = yield response.text();
        throw new _HttpError.HttpError(response.status, bodyString || response.statusText, headers);
      }
      return {
        status: response.status,
        body,
        headers
      };
    })();
  }
  getAbortController() {
    const AbortCtrl = typeof globalThis.AbortController !== 'undefined' ? globalThis.AbortController : _depsNode.LocalAbortController;
    return new AbortCtrl();
  }
  getAsync(url, options) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const c = _this.getAbortController();
      const id = setTimeout(() => c.abort(), (options != null && options.timeout ? options.timeout : _index.REQUEST_TIME_OUT) * 1000);
      try {
        const response = yield (0, _depsNode.myFetch)(url, {
          method: 'GET',
          headers: options == null ? void 0 : options.headers,
          signal: c.signal,
          keepalive: true,
          next: options == null ? void 0 : options.nextFetchConfig
        });
        return _this.getResponse(response);
      } finally {
        clearTimeout(id);
      }
    })();
  }
  postAsync(url, options) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const c = _this2.getAbortController();
      const id = setTimeout(() => c.abort(), options.timeout ? options.timeout * 1000 : _index.REQUEST_TIME_OUT * 1000);
      try {
        const response = yield (0, _depsNode.myFetch)(url, {
          method: 'POST',
          headers: options.headers,
          body: JSON.stringify(options.body),
          signal: c.signal,
          keepalive: true,
          next: options == null ? void 0 : options.nextFetchConfig
        });
        return _this2.getResponse(response);
      } finally {
        clearTimeout(id);
      }
    })();
  }
}
exports.HttpClient = HttpClient;

/***/ }),

/***/ 5825:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.TrackingManager = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _BatchTriggeredBy = __webpack_require__(8160);
var _TrackingManagerAbstract = __webpack_require__(7855);
class TrackingManager extends _TrackingManagerAbstract.TrackingManagerAbstract {
  activateFlag(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this.strategy.activateFlag(hit);
    })();
  }
  addHit(hit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this2.strategy.addHit(hit);
    })();
  }
  sendBatch() {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this3.strategy.sendBatch(_BatchTriggeredBy.BatchTriggeredBy.Flush);
    })();
  }
}
exports.TrackingManager = TrackingManager;

/***/ }),

/***/ 5917:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var fails = __webpack_require__(9039);
var createElement = __webpack_require__(4055);

// Thanks to IE8 for its funny defineProperty
module.exports = !DESCRIPTORS && !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(createElement('div'), 'a', {
    get: function () { return 7; }
  }).a !== 7;
});


/***/ }),

/***/ 5966:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var aCallable = __webpack_require__(9306);
var isNullOrUndefined = __webpack_require__(4117);

// `GetMethod` abstract operation
// https://tc39.es/ecma262/#sec-getmethod
module.exports = function (V, P) {
  var func = V[P];
  return isNullOrUndefined(func) ? undefined : aCallable(func);
};


/***/ }),

/***/ 6033:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove this module from `core-js@4` since it's replaced to module below
__webpack_require__(8523);


/***/ }),

/***/ 6034:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $values = (__webpack_require__(2357).values);

// `Object.values` method
// https://tc39.es/ecma262/#sec-object.values
$({ target: 'Object', stat: true }, {
  values: function values(O) {
    return $values(O);
  }
});


/***/ }),

/***/ 6043:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var aCallable = __webpack_require__(9306);

var $TypeError = TypeError;

var PromiseCapability = function (C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw new $TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aCallable(resolve);
  this.reject = aCallable(reject);
};

// `NewPromiseCapability` abstract operation
// https://tc39.es/ecma262/#sec-newpromisecapability
module.exports.f = function (C) {
  return new PromiseCapability(C);
};


/***/ }),

/***/ 6080:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(7476);
var aCallable = __webpack_require__(9306);
var NATIVE_BIND = __webpack_require__(616);

var bind = uncurryThis(uncurryThis.bind);

// optional / simple context binding
module.exports = function (fn, that) {
  aCallable(fn);
  return that === undefined ? fn : NATIVE_BIND ? bind(fn, that) : function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),

/***/ 6119:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var shared = __webpack_require__(5745);
var uid = __webpack_require__(3392);

var keys = shared('keys');

module.exports = function (key) {
  return keys[key] || (keys[key] = uid(key));
};


/***/ }),

/***/ 6147:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.HitType = void 0;
let HitType = exports.HitType = /*#__PURE__*/function (HitType) {
  /**
   * User has seen a URL
   *
   */
  HitType["PAGE_VIEW"] = "PAGEVIEW";
  /**
   * User has seen a URL
   *
   */
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  HitType["PAGE"] = "PAGEVIEW";
  /**
   * User has seen a screen.
   *
   */
  HitType["SCREEN_VIEW"] = "SCREENVIEW";
  /**
   * User has seen a screen.
   *
   */
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  HitType["SCREEN"] = "SCREENVIEW";
  /**
   * User has made a transaction.
   *
   */
  HitType["TRANSACTION"] = "TRANSACTION";
  /**
   * Item bought in a transaction.
   *
   */
  HitType["ITEM"] = "ITEM";
  /**
   * User has made a specific action.
   *
   */
  HitType["EVENT"] = "EVENT";
  return HitType;
}({});

/***/ }),

/***/ 6193:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var ENVIRONMENT = __webpack_require__(4215);

module.exports = ENVIRONMENT === 'NODE';


/***/ }),

/***/ 6198:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toLength = __webpack_require__(8014);

// `LengthOfArrayLike` abstract operation
// https://tc39.es/ecma262/#sec-lengthofarraylike
module.exports = function (obj) {
  return toLength(obj.length);
};


/***/ }),

/***/ 6261:
/***/ ((module) => {

"use strict";
module.exports = require("events");

/***/ }),

/***/ 6269:
/***/ ((module) => {

"use strict";

module.exports = {};


/***/ }),

/***/ 6279:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var defineBuiltIn = __webpack_require__(6840);

module.exports = function (target, src, options) {
  for (var key in src) defineBuiltIn(target, key, src[key], options);
  return target;
};


/***/ }),

/***/ 6295:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;

/***/ }),

/***/ 6319:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var anObject = __webpack_require__(8551);
var iteratorClose = __webpack_require__(9539);

// call something on iterator step with safe closing on error
module.exports = function (iterator, fn, value, ENTRIES) {
  try {
    return ENTRIES ? fn(anObject(value)[0], value[1]) : fn(value);
  } catch (error) {
    iteratorClose(iterator, 'throw', error);
  }
};


/***/ }),

/***/ 6395:
/***/ ((module) => {

"use strict";

module.exports = false;


/***/ }),

/***/ 6421:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.SharedActionSource = exports.ImportHitType = void 0;
let SharedActionSource = exports.SharedActionSource = /*#__PURE__*/function (SharedActionSource) {
  SharedActionSource["ABT_TAG_TRACK_ACTION"] = "ABT_TAG_TRACK_ACTION";
  SharedActionSource["ABT_WEB_SDK_TRACK_ACTION"] = "ABT_WEB_SDK_TRACK_ACTION";
  return SharedActionSource;
}({});
let ImportHitType = exports.ImportHitType = /*#__PURE__*/function (ImportHitType) {
  ImportHitType["Event"] = "Event";
  ImportHitType["Item"] = "Item";
  ImportHitType["Page"] = "Page";
  ImportHitType["Screen"] = "Screen";
  ImportHitType["Transaction"] = "Transaction";
  ImportHitType["Segment"] = "Segment";
  ImportHitType["Activate"] = "Activate";
  ImportHitType["Troubleshooting"] = "Troubleshooting";
  ImportHitType["UsageHit"] = "UsageHit";
  ImportHitType["ActivateBatch"] = "ActivateBatch";
  ImportHitType["Batch"] = "Batch";
  ImportHitType["HitAbstract"] = "HitAbstract";
  ImportHitType["Diagnostic"] = "Diagnostic";
  return ImportHitType;
}({});

/***/ }),

/***/ 6468:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var globalThis = __webpack_require__(2195);
var uncurryThis = __webpack_require__(9504);
var isForced = __webpack_require__(2796);
var defineBuiltIn = __webpack_require__(6840);
var InternalMetadataModule = __webpack_require__(3451);
var iterate = __webpack_require__(2652);
var anInstance = __webpack_require__(679);
var isCallable = __webpack_require__(4901);
var isNullOrUndefined = __webpack_require__(4117);
var isObject = __webpack_require__(34);
var fails = __webpack_require__(9039);
var checkCorrectnessOfIteration = __webpack_require__(4428);
var setToStringTag = __webpack_require__(687);
var inheritIfRequired = __webpack_require__(3167);

module.exports = function (CONSTRUCTOR_NAME, wrapper, common) {
  var IS_MAP = CONSTRUCTOR_NAME.indexOf('Map') !== -1;
  var IS_WEAK = CONSTRUCTOR_NAME.indexOf('Weak') !== -1;
  var ADDER = IS_MAP ? 'set' : 'add';
  var NativeConstructor = globalThis[CONSTRUCTOR_NAME];
  var NativePrototype = NativeConstructor && NativeConstructor.prototype;
  var Constructor = NativeConstructor;
  var exported = {};

  var fixMethod = function (KEY) {
    var uncurriedNativeMethod = uncurryThis(NativePrototype[KEY]);
    defineBuiltIn(NativePrototype, KEY,
      KEY === 'add' ? function add(value) {
        uncurriedNativeMethod(this, value === 0 ? 0 : value);
        return this;
      } : KEY === 'delete' ? function (key) {
        return IS_WEAK && !isObject(key) ? false : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : KEY === 'get' ? function get(key) {
        return IS_WEAK && !isObject(key) ? undefined : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : KEY === 'has' ? function has(key) {
        return IS_WEAK && !isObject(key) ? false : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : function set(key, value) {
        uncurriedNativeMethod(this, key === 0 ? 0 : key, value);
        return this;
      }
    );
  };

  var REPLACE = isForced(
    CONSTRUCTOR_NAME,
    !isCallable(NativeConstructor) || !(IS_WEAK || NativePrototype.forEach && !fails(function () {
      new NativeConstructor().entries().next();
    }))
  );

  if (REPLACE) {
    // create collection constructor
    Constructor = common.getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER);
    InternalMetadataModule.enable();
  } else if (isForced(CONSTRUCTOR_NAME, true)) {
    var instance = new Constructor();
    // early implementations not supports chaining
    var HASNT_CHAINING = instance[ADDER](IS_WEAK ? {} : -0, 1) !== instance;
    // V8 ~ Chromium 40- weak-collections throws on primitives, but should return false
    var THROWS_ON_PRIMITIVES = fails(function () { instance.has(1); });
    // most early implementations doesn't supports iterables, most modern - not close it correctly
    // eslint-disable-next-line no-new -- required for testing
    var ACCEPT_ITERABLES = checkCorrectnessOfIteration(function (iterable) { new NativeConstructor(iterable); });
    // for early implementations -0 and +0 not the same
    var BUGGY_ZERO = !IS_WEAK && fails(function () {
      // V8 ~ Chromium 42- fails only with 5+ elements
      var $instance = new NativeConstructor();
      var index = 5;
      while (index--) $instance[ADDER](index, index);
      return !$instance.has(-0);
    });

    if (!ACCEPT_ITERABLES) {
      Constructor = wrapper(function (dummy, iterable) {
        anInstance(dummy, NativePrototype);
        var that = inheritIfRequired(new NativeConstructor(), dummy, Constructor);
        if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], { that: that, AS_ENTRIES: IS_MAP });
        return that;
      });
      Constructor.prototype = NativePrototype;
      NativePrototype.constructor = Constructor;
    }

    if (THROWS_ON_PRIMITIVES || BUGGY_ZERO) {
      fixMethod('delete');
      fixMethod('has');
      IS_MAP && fixMethod('get');
    }

    if (BUGGY_ZERO || HASNT_CHAINING) fixMethod(ADDER);

    // weak collections should not contains .clear method
    if (IS_WEAK && NativePrototype.clear) delete NativePrototype.clear;
  }

  exported[CONSTRUCTOR_NAME] = Constructor;
  $({ global: true, constructor: true, forced: Constructor !== NativeConstructor }, exported);

  setToStringTag(Constructor, CONSTRUCTOR_NAME);

  if (!IS_WEAK) common.setStrong(Constructor, CONSTRUCTOR_NAME, IS_MAP);

  return Constructor;
};


/***/ }),

/***/ 6469:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var wellKnownSymbol = __webpack_require__(8227);
var create = __webpack_require__(2360);
var defineProperty = (__webpack_require__(4913).f);

var UNSCOPABLES = wellKnownSymbol('unscopables');
var ArrayPrototype = Array.prototype;

// Array.prototype[@@unscopables]
// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
if (ArrayPrototype[UNSCOPABLES] === undefined) {
  defineProperty(ArrayPrototype, UNSCOPABLES, {
    configurable: true,
    value: create(null)
  });
}

// add a key to Array.prototype[@@unscopables]
module.exports = function (key) {
  ArrayPrototype[UNSCOPABLES][key] = true;
};


/***/ }),

/***/ 6499:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var call = __webpack_require__(9565);
var aCallable = __webpack_require__(9306);
var newPromiseCapabilityModule = __webpack_require__(6043);
var perform = __webpack_require__(1103);
var iterate = __webpack_require__(2652);
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(537);

// `Promise.all` method
// https://tc39.es/ecma262/#sec-promise.all
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  all: function all(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var resolve = capability.resolve;
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      var values = [];
      var counter = 0;
      var remaining = 1;
      iterate(iterable, function (promise) {
        var index = counter++;
        var alreadyCalled = false;
        remaining++;
        call($promiseResolve, C, promise).then(function (value) {
          if (alreadyCalled) return;
          alreadyCalled = true;
          values[index] = value;
          --remaining || resolve(values);
        }, reject);
      });
      --remaining || resolve(values);
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),

/***/ 6517:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.deepEqual = deepEqual;
exports.errorFormat = errorFormat;
exports.formatLogOutput = formatLogOutput;
exports.hasSameType = hasSameType;
exports.hexToValue = hexToValue;
exports.isBrowser = isBrowser;
exports.logDebug = logDebug;
exports.logDebugSprintf = logDebugSprintf;
exports.logError = logError;
exports.logErrorSprintf = logErrorSprintf;
exports.logInfo = logInfo;
exports.logInfoSprintf = logInfoSprintf;
exports.logWarning = logWarning;
exports.logWarningSprintf = logWarningSprintf;
exports.onDomReady = onDomReady;
exports.sprintf = sprintf;
exports.uuidV4 = uuidV4;
exports.valueToHex = valueToHex;
exports.visitorFlagSyncStatusMessage = visitorFlagSyncStatusMessage;
__webpack_require__(3418);
__webpack_require__(4423);
__webpack_require__(3792);
__webpack_require__(4782);
__webpack_require__(6033);
__webpack_require__(4864);
__webpack_require__(7495);
__webpack_require__(1699);
__webpack_require__(5701);
__webpack_require__(8156);
__webpack_require__(5440);
var _FSFetchReasons = __webpack_require__(1734);
var _index = __webpack_require__(5394);
/**
 * Return a formatted string
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
function sprintf(format, ...value) {
  let formatted = format;
  for (let i = 0; i < value.length; i++) {
    const item = value[i];
    const element = typeof item === 'string' ? item : JSON.stringify(item instanceof Map ? Array.from(item.values()) : item);
    formatted = formatted.replace(new RegExp(`\\{${i}\\}`, 'g'), element);
  }
  return formatted;
}
function logErrorSprintf(config, tag, message, ...arg) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.ERROR) {
    return;
  }
  const customMessage = sprintf(message, ...arg);
  logError(config, customMessage, tag);
}
function logError(config, message, tag) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.ERROR) {
    return;
  }
  if (typeof config.onLog === 'function') {
    config.onLog(_index.LogLevel.ERROR, tag, message);
  }
  if (config.logManager && typeof config.logManager.error === 'function') {
    config.logManager.error(message, tag);
  }
}
function logWarningSprintf(config, tag, message, ...arg) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.WARNING) {
    return;
  }
  const customMessage = sprintf(message, ...arg);
  logWarning(config, customMessage, tag);
}
function logWarning(config, message, tag) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.WARNING) {
    return;
  }
  if (typeof config.onLog === 'function') {
    config.onLog(_index.LogLevel.WARNING, tag, message);
  }
  if (config.logManager && typeof config.logManager.warning === 'function') {
    config.logManager.warning(message, tag);
  }
}
function logInfoSprintf(config, tag, message, ...arg) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.INFO) {
    return;
  }
  const customMessage = sprintf(message, ...arg);
  logInfo(config, customMessage, tag);
}
function logInfo(config, message, tag) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.INFO) {
    return;
  }
  if (typeof config.onLog === 'function') {
    config.onLog(_index.LogLevel.INFO, tag, message);
  }
  if (config.logManager && typeof config.logManager.info === 'function') {
    config.logManager.info(message, tag);
  }
}
function logDebugSprintf(config, tag, message, ...arg) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.DEBUG) {
    return;
  }
  const customMessage = sprintf(message, ...arg);
  logDebug(config, customMessage, tag);
}
function logDebug(config, message, tag) {
  if (!config || !config.logLevel || config.logLevel < _index.LogLevel.DEBUG) {
    return;
  }
  if (typeof config.onLog === 'function') {
    config.onLog(_index.LogLevel.DEBUG, tag, message);
  }
  if (config.logManager && typeof config.logManager.debug === 'function') {
    config.logManager.debug(message, tag);
  }
}
function isBrowser() {
  return typeof window !== 'undefined' && typeof window.document !== 'undefined';
}
function hasSameType(flagValue, defaultValue) {
  if (typeof flagValue !== typeof defaultValue) {
    return false;
  }
  if (typeof flagValue === 'object' && typeof defaultValue === 'object' && Array.isArray(flagValue) !== Array.isArray(defaultValue)) {
    return false;
  }
  return true;
}
function uuidV4() {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (char) {
    const rand = Math.random() * 16 | 0;
    const value = char === 'x' ? rand : rand & 0x3 | 0x8;
    return value.toString(16);
  });
}
function errorFormat(message, errorData) {
  return JSON.stringify({
    message,
    data: errorData
  });
}
function visitorFlagSyncStatusMessage(reason) {
  let message = '';
  switch (reason) {
    case _FSFetchReasons.FSFetchReasons.FLAGS_NEVER_FETCHED:
      message = `Visitor \`{0}\` has been created ${_index.VISITOR_SYNC_FLAGS_MESSAGE}`;
      break;
    case _FSFetchReasons.FSFetchReasons.UPDATE_CONTEXT:
      message = `Visitor context for visitor \`{0}\` has been updated ${_index.VISITOR_SYNC_FLAGS_MESSAGE}`;
      break;
    case _FSFetchReasons.FSFetchReasons.AUTHENTICATE:
      message = `Visitor \`{0}\` has been authenticated ${_index.VISITOR_SYNC_FLAGS_MESSAGE}`;
      break;
    case _FSFetchReasons.FSFetchReasons.UNAUTHENTICATE:
      message = `Visitor \`{0}\` has been unauthenticated ${_index.VISITOR_SYNC_FLAGS_MESSAGE}`;
      break;
    case _FSFetchReasons.FSFetchReasons.FLAGS_FETCHING_ERROR:
      message = 'There was an error while fetching flags for visitor `{0}`. So the value of the flag `{1}` may be outdated"';
      break;
    case _FSFetchReasons.FSFetchReasons.FLAGS_FETCHED_FROM_CACHE:
      message = 'Flags for visitor `{0}` have been fetched from cache';
      break;
    default:
      break;
  }
  return message;
}
function valueToHex(value) {
  const jsonString = JSON.stringify(value);
  const hex = Array.from(jsonString, char => char.charCodeAt(0).toString(16)).join('');
  return hex;
}
function hexToValue(hex, config) {
  if (typeof hex !== 'string') {
    logErrorSprintf(config, 'hexToValue', 'Invalid hex string: {0}', hex);
    return null;
  }
  let jsonString = '';
  for (let i = 0; i < hex.length; i += 2) {
    const hexChar = hex.slice(i, i + 2);
    const charCode = parseInt(hexChar, 16);
    if (isNaN(charCode)) {
      logErrorSprintf(config, 'hexToValue', 'Invalid hex character: {0}', hexChar);
      return null;
    }
    jsonString += String.fromCharCode(charCode);
  }
  try {
    const value = JSON.parse(jsonString);
    return value;
  } catch (error) {
    logErrorSprintf(config, 'hexToValue', 'Error while parsing JSON: {0}', error);
    return null;
  }
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
function deepEqual(obj1, obj2) {
  if (obj1 === obj2) return true;
  if (typeof obj1 !== 'object' || typeof obj2 !== 'object' || obj1 == null || obj2 == null) {
    return false;
  }
  const keys1 = Object.keys(obj1);
  const keys2 = Object.keys(obj2);
  if (keys1.length !== keys2.length) return false;
  for (var _i = 0, _keys = keys1; _i < _keys.length; _i++) {
    const key = _keys[_i];
    if (!keys2.includes(key) || !deepEqual(obj1[key], obj2[key])) {
      return false;
    }
  }
  return true;
}
function onDomReady(callback) {
  if (false) {}
  return false;
}
function formatLogOutput(level, message, tag) {
  const now = new Date();
  const formatTwoDigits = value => {
    return value.toString().padStart(2, '0');
  };
  const formatMilliseconds = value => {
    return value.toString().padStart(3, '0');
  };
  const colorCodes = {
    [_index.LogLevel.EMERGENCY]: '\x1b[1;37;41m',
    [_index.LogLevel.ALERT]: '\x1b[1;37;45m',
    [_index.LogLevel.CRITICAL]: '\x1b[1;37;41m',
    [_index.LogLevel.ERROR]: '\x1b[1;37;41m',
    [_index.LogLevel.WARNING]: '\x1b[33;1m',
    [_index.LogLevel.NOTICE]: '\x1b[36;1m',
    [_index.LogLevel.INFO]: '\x1b[32;1m',
    [_index.LogLevel.DEBUG]: '\x1b[90;1m',
    [_index.LogLevel.NONE]: '',
    [_index.LogLevel.ALL]: '\x1b[90;1m'
  };
  const resetColor = '\x1b[0m';
  const colorCode = colorCodes[level] || '';
  const year = now.getFullYear();
  const month = formatTwoDigits(now.getMonth() + 1);
  const day = formatTwoDigits(now.getDate());
  const hours = formatTwoDigits(now.getHours());
  const minutes = formatTwoDigits(now.getMinutes());
  const seconds = formatTwoDigits(now.getSeconds());
  const milliseconds = formatMilliseconds(now.getMilliseconds());
  const timestamp = `${year}-${month}-${day} ${hours}:${minutes}:${seconds}.${milliseconds}`;
  const levelName = _index.LogLevel[level].padEnd(2);
  return `${colorCode}[${timestamp}] [${_index.FLAGSHIP_SDK}] [${levelName}] [${tag}] ${message}${resetColor}`;
}

/***/ }),

/***/ 6518:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var getOwnPropertyDescriptor = (__webpack_require__(7347).f);
var createNonEnumerableProperty = __webpack_require__(6699);
var defineBuiltIn = __webpack_require__(6840);
var defineGlobalProperty = __webpack_require__(9433);
var copyConstructorProperties = __webpack_require__(7740);
var isForced = __webpack_require__(2796);

/*
  options.target         - name of the target object
  options.global         - target is the global object
  options.stat           - export as static methods of target
  options.proto          - export as prototype methods of target
  options.real           - real prototype method for the `pure` version
  options.forced         - export even if the native feature is available
  options.bind           - bind methods to the target, required for the `pure` version
  options.wrap           - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe         - use the simple assignment of property instead of delete + defineProperty
  options.sham           - add a flag to not completely full polyfills
  options.enumerable     - export as enumerable property
  options.dontCallGetSet - prevent calling a getter on target
  options.name           - the .name of the function if it does not match the key
*/
module.exports = function (options, source) {
  var TARGET = options.target;
  var GLOBAL = options.global;
  var STATIC = options.stat;
  var FORCED, target, key, targetProperty, sourceProperty, descriptor;
  if (GLOBAL) {
    target = globalThis;
  } else if (STATIC) {
    target = globalThis[TARGET] || defineGlobalProperty(TARGET, {});
  } else {
    target = globalThis[TARGET] && globalThis[TARGET].prototype;
  }
  if (target) for (key in source) {
    sourceProperty = source[key];
    if (options.dontCallGetSet) {
      descriptor = getOwnPropertyDescriptor(target, key);
      targetProperty = descriptor && descriptor.value;
    } else targetProperty = target[key];
    FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
    // contained in target
    if (!FORCED && targetProperty !== undefined) {
      if (typeof sourceProperty == typeof targetProperty) continue;
      copyConstructorProperties(sourceProperty, targetProperty);
    }
    // add a flag to not completely full polyfills
    if (options.sham || (targetProperty && targetProperty.sham)) {
      createNonEnumerableProperty(sourceProperty, 'sham', true);
    }
    defineBuiltIn(target, key, sourceProperty, options);
  }
};


/***/ }),

/***/ 6682:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var anObject = __webpack_require__(8551);
var isCallable = __webpack_require__(4901);
var classof = __webpack_require__(4576);
var regexpExec = __webpack_require__(7323);

var $TypeError = TypeError;

// `RegExpExec` abstract operation
// https://tc39.es/ecma262/#sec-regexpexec
module.exports = function (R, S) {
  var exec = R.exec;
  if (isCallable(exec)) {
    var result = call(exec, R, S);
    if (result !== null) anObject(result);
    return result;
  }
  if (classof(R) === 'RegExp') return call(regexpExec, R, S);
  throw new $TypeError('RegExp#exec called on incompatible receiver');
};


/***/ }),

/***/ 6699:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var definePropertyModule = __webpack_require__(4913);
var createPropertyDescriptor = __webpack_require__(6980);

module.exports = DESCRIPTORS ? function (object, key, value) {
  return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),

/***/ 6701:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
var _exportNames = {
  BucketingConfig: true,
  FlagshipConfig: true,
  DecisionMode: true
};
Object.defineProperty(exports, "BucketingConfig", ({
  enumerable: true,
  get: function get() {
    return _BucketingConfig.BucketingConfig;
  }
}));
Object.defineProperty(exports, "DecisionMode", ({
  enumerable: true,
  get: function get() {
    return _DecisionMode.DecisionMode;
  }
}));
Object.defineProperty(exports, "FlagshipConfig", ({
  enumerable: true,
  get: function get() {
    return _FlagshipConfig.FlagshipConfig;
  }
}));
var _ConfigManager = __webpack_require__(8388);
Object.keys(_ConfigManager).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _ConfigManager[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _ConfigManager[key];
    }
  });
});
var _DecisionApiConfig = __webpack_require__(2285);
Object.keys(_DecisionApiConfig).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _DecisionApiConfig[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _DecisionApiConfig[key];
    }
  });
});
var _BucketingConfig = __webpack_require__(509);
var _FlagshipConfig = __webpack_require__(7999);
var _DecisionMode = __webpack_require__(8884);

/***/ }),

/***/ 6706:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var aCallable = __webpack_require__(9306);

module.exports = function (object, key, method) {
  try {
    // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
    return uncurryThis(aCallable(Object.getOwnPropertyDescriptor(object, key)[method]));
  } catch (error) { /* empty */ }
};


/***/ }),

/***/ 6798:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.UsageHit = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _Diagnostic = __webpack_require__(2413);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
class UsageHit extends _Diagnostic.Diagnostic {
  constructor(param) {
    super(_objectSpread(_objectSpread({}, param), {}, {
      type: 'USAGE'
    }));
  }
}
exports.UsageHit = UsageHit;

/***/ }),

/***/ 6801:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(8686);
var definePropertyModule = __webpack_require__(4913);
var anObject = __webpack_require__(8551);
var toIndexedObject = __webpack_require__(5397);
var objectKeys = __webpack_require__(1072);

// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es/no-object-defineproperties -- safe
exports.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
  anObject(O);
  var props = toIndexedObject(Properties);
  var keys = objectKeys(Properties);
  var length = keys.length;
  var index = 0;
  var key;
  while (length > index) definePropertyModule.f(O, key = keys[index++], props[key]);
  return O;
};


/***/ }),

/***/ 6823:
/***/ ((module) => {

"use strict";

var $String = String;

module.exports = function (argument) {
  try {
    return $String(argument);
  } catch (error) {
    return 'Object';
  }
};


/***/ }),

/***/ 6840:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isCallable = __webpack_require__(4901);
var definePropertyModule = __webpack_require__(4913);
var makeBuiltIn = __webpack_require__(283);
var defineGlobalProperty = __webpack_require__(9433);

module.exports = function (O, key, value, options) {
  if (!options) options = {};
  var simple = options.enumerable;
  var name = options.name !== undefined ? options.name : key;
  if (isCallable(value)) makeBuiltIn(value, name, options);
  if (options.global) {
    if (simple) O[key] = value;
    else defineGlobalProperty(key, value);
  } else {
    try {
      if (!options.unsafe) delete O[key];
      else if (O[key]) simple = true;
    } catch (error) { /* empty */ }
    if (simple) O[key] = value;
    else definePropertyModule.f(O, key, {
      value: value,
      enumerable: false,
      configurable: !options.nonConfigurable,
      writable: !options.nonWritable
    });
  } return O;
};


/***/ }),

/***/ 6938:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var create = __webpack_require__(2360);
var defineBuiltInAccessor = __webpack_require__(2106);
var defineBuiltIns = __webpack_require__(6279);
var bind = __webpack_require__(6080);
var anInstance = __webpack_require__(679);
var isNullOrUndefined = __webpack_require__(4117);
var iterate = __webpack_require__(2652);
var defineIterator = __webpack_require__(1088);
var createIterResultObject = __webpack_require__(2529);
var setSpecies = __webpack_require__(7633);
var DESCRIPTORS = __webpack_require__(3724);
var fastKey = (__webpack_require__(3451).fastKey);
var InternalStateModule = __webpack_require__(1181);

var setInternalState = InternalStateModule.set;
var internalStateGetterFor = InternalStateModule.getterFor;

module.exports = {
  getConstructor: function (wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
    var Constructor = wrapper(function (that, iterable) {
      anInstance(that, Prototype);
      setInternalState(that, {
        type: CONSTRUCTOR_NAME,
        index: create(null),
        first: null,
        last: null,
        size: 0
      });
      if (!DESCRIPTORS) that.size = 0;
      if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], { that: that, AS_ENTRIES: IS_MAP });
    });

    var Prototype = Constructor.prototype;

    var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);

    var define = function (that, key, value) {
      var state = getInternalState(that);
      var entry = getEntry(that, key);
      var previous, index;
      // change existing entry
      if (entry) {
        entry.value = value;
      // create new entry
      } else {
        state.last = entry = {
          index: index = fastKey(key, true),
          key: key,
          value: value,
          previous: previous = state.last,
          next: null,
          removed: false
        };
        if (!state.first) state.first = entry;
        if (previous) previous.next = entry;
        if (DESCRIPTORS) state.size++;
        else that.size++;
        // add to index
        if (index !== 'F') state.index[index] = entry;
      } return that;
    };

    var getEntry = function (that, key) {
      var state = getInternalState(that);
      // fast case
      var index = fastKey(key);
      var entry;
      if (index !== 'F') return state.index[index];
      // frozen object case
      for (entry = state.first; entry; entry = entry.next) {
        if (entry.key === key) return entry;
      }
    };

    defineBuiltIns(Prototype, {
      // `{ Map, Set }.prototype.clear()` methods
      // https://tc39.es/ecma262/#sec-map.prototype.clear
      // https://tc39.es/ecma262/#sec-set.prototype.clear
      clear: function clear() {
        var that = this;
        var state = getInternalState(that);
        var entry = state.first;
        while (entry) {
          entry.removed = true;
          if (entry.previous) entry.previous = entry.previous.next = null;
          entry = entry.next;
        }
        state.first = state.last = null;
        state.index = create(null);
        if (DESCRIPTORS) state.size = 0;
        else that.size = 0;
      },
      // `{ Map, Set }.prototype.delete(key)` methods
      // https://tc39.es/ecma262/#sec-map.prototype.delete
      // https://tc39.es/ecma262/#sec-set.prototype.delete
      'delete': function (key) {
        var that = this;
        var state = getInternalState(that);
        var entry = getEntry(that, key);
        if (entry) {
          var next = entry.next;
          var prev = entry.previous;
          delete state.index[entry.index];
          entry.removed = true;
          if (prev) prev.next = next;
          if (next) next.previous = prev;
          if (state.first === entry) state.first = next;
          if (state.last === entry) state.last = prev;
          if (DESCRIPTORS) state.size--;
          else that.size--;
        } return !!entry;
      },
      // `{ Map, Set }.prototype.forEach(callbackfn, thisArg = undefined)` methods
      // https://tc39.es/ecma262/#sec-map.prototype.foreach
      // https://tc39.es/ecma262/#sec-set.prototype.foreach
      forEach: function forEach(callbackfn /* , that = undefined */) {
        var state = getInternalState(this);
        var boundFunction = bind(callbackfn, arguments.length > 1 ? arguments[1] : undefined);
        var entry;
        while (entry = entry ? entry.next : state.first) {
          boundFunction(entry.value, entry.key, this);
          // revert to the last existing entry
          while (entry && entry.removed) entry = entry.previous;
        }
      },
      // `{ Map, Set}.prototype.has(key)` methods
      // https://tc39.es/ecma262/#sec-map.prototype.has
      // https://tc39.es/ecma262/#sec-set.prototype.has
      has: function has(key) {
        return !!getEntry(this, key);
      }
    });

    defineBuiltIns(Prototype, IS_MAP ? {
      // `Map.prototype.get(key)` method
      // https://tc39.es/ecma262/#sec-map.prototype.get
      get: function get(key) {
        var entry = getEntry(this, key);
        return entry && entry.value;
      },
      // `Map.prototype.set(key, value)` method
      // https://tc39.es/ecma262/#sec-map.prototype.set
      set: function set(key, value) {
        return define(this, key === 0 ? 0 : key, value);
      }
    } : {
      // `Set.prototype.add(value)` method
      // https://tc39.es/ecma262/#sec-set.prototype.add
      add: function add(value) {
        return define(this, value = value === 0 ? 0 : value, value);
      }
    });
    if (DESCRIPTORS) defineBuiltInAccessor(Prototype, 'size', {
      configurable: true,
      get: function () {
        return getInternalState(this).size;
      }
    });
    return Constructor;
  },
  setStrong: function (Constructor, CONSTRUCTOR_NAME, IS_MAP) {
    var ITERATOR_NAME = CONSTRUCTOR_NAME + ' Iterator';
    var getInternalCollectionState = internalStateGetterFor(CONSTRUCTOR_NAME);
    var getInternalIteratorState = internalStateGetterFor(ITERATOR_NAME);
    // `{ Map, Set }.prototype.{ keys, values, entries, @@iterator }()` methods
    // https://tc39.es/ecma262/#sec-map.prototype.entries
    // https://tc39.es/ecma262/#sec-map.prototype.keys
    // https://tc39.es/ecma262/#sec-map.prototype.values
    // https://tc39.es/ecma262/#sec-map.prototype-@@iterator
    // https://tc39.es/ecma262/#sec-set.prototype.entries
    // https://tc39.es/ecma262/#sec-set.prototype.keys
    // https://tc39.es/ecma262/#sec-set.prototype.values
    // https://tc39.es/ecma262/#sec-set.prototype-@@iterator
    defineIterator(Constructor, CONSTRUCTOR_NAME, function (iterated, kind) {
      setInternalState(this, {
        type: ITERATOR_NAME,
        target: iterated,
        state: getInternalCollectionState(iterated),
        kind: kind,
        last: null
      });
    }, function () {
      var state = getInternalIteratorState(this);
      var kind = state.kind;
      var entry = state.last;
      // revert to the last existing entry
      while (entry && entry.removed) entry = entry.previous;
      // get next entry
      if (!state.target || !(state.last = entry = entry ? entry.next : state.state.first)) {
        // or finish the iteration
        state.target = null;
        return createIterResultObject(undefined, true);
      }
      // return step by kind
      if (kind === 'keys') return createIterResultObject(entry.key, false);
      if (kind === 'values') return createIterResultObject(entry.value, false);
      return createIterResultObject([entry.key, entry.value], false);
    }, IS_MAP ? 'entries' : 'values', !IS_MAP, true);

    // `{ Map, Set }.prototype[@@species]` accessors
    // https://tc39.es/ecma262/#sec-get-map-@@species
    // https://tc39.es/ecma262/#sec-get-set-@@species
    setSpecies(CONSTRUCTOR_NAME);
  }
};


/***/ }),

/***/ 6955:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var TO_STRING_TAG_SUPPORT = __webpack_require__(2140);
var isCallable = __webpack_require__(4901);
var classofRaw = __webpack_require__(4576);
var wellKnownSymbol = __webpack_require__(8227);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var $Object = Object;

// ES3 wrong here
var CORRECT_ARGUMENTS = classofRaw(function () { return arguments; }()) === 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (error) { /* empty */ }
};

// getting tag from ES6+ `Object.prototype.toString`
module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function (it) {
  var O, tag, result;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (tag = tryGet(O = $Object(it), TO_STRING_TAG)) == 'string' ? tag
    // builtinTag case
    : CORRECT_ARGUMENTS ? classofRaw(O)
    // ES3 arguments fallback
    : (result = classofRaw(O)) === 'Object' && isCallable(O.callee) ? 'Arguments' : result;
};


/***/ }),

/***/ 6969:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toPrimitive = __webpack_require__(2777);
var isSymbol = __webpack_require__(757);

// `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey
module.exports = function (argument) {
  var key = toPrimitive(argument, 'string');
  return isSymbol(key) ? key : key + '';
};


/***/ }),

/***/ 6980:
/***/ ((module) => {

"use strict";

module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),

/***/ 7040:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* eslint-disable es/no-symbol -- required for testing */
var NATIVE_SYMBOL = __webpack_require__(4495);

module.exports = NATIVE_SYMBOL &&
  !Symbol.sham &&
  typeof Symbol.iterator == 'symbol';


/***/ }),

/***/ 7055:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var fails = __webpack_require__(9039);
var classof = __webpack_require__(4576);

var $Object = Object;
var split = uncurryThis(''.split);

// fallback for non-array-like ES3 and non-enumerable old V8 strings
module.exports = fails(function () {
  // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
  // eslint-disable-next-line no-prototype-builtins -- safe
  return !$Object('z').propertyIsEnumerable(0);
}) ? function (it) {
  return classof(it) === 'String' ? split(it, '') : $Object(it);
} : $Object;


/***/ }),

/***/ 7099:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.VisitorCacheStatus = exports.TroubleshootingLabel = exports.ABTastyWebSDKPostMessageType = void 0;
/**
 * Represents the status of visitor fetch for flag data.
 */
/**
 * Represents a new visitor.
 */
let TroubleshootingLabel = exports.TroubleshootingLabel = /*#__PURE__*/function (TroubleshootingLabel) {
  TroubleshootingLabel["VISITOR_SEND_HIT"] = "VISITOR_SEND_HIT";
  TroubleshootingLabel["VISITOR_FETCH_CAMPAIGNS_ERROR"] = "VISITOR_FETCH_CAMPAIGNS_ERROR";
  TroubleshootingLabel["VISITOR_FETCH_CAMPAIGNS"] = "VISITOR_FETCH_CAMPAIGNS";
  TroubleshootingLabel["VISITOR_AUTHENTICATE"] = "VISITOR_AUTHENTICATE";
  TroubleshootingLabel["VISITOR_UNAUTHENTICATE"] = "VISITOR_UNAUTHENTICATE";
  TroubleshootingLabel["VISITOR_EXPOSED_FLAG_NOT_FOUND"] = "VISITOR_EXPOSED_FLAG_NOT_FOUND";
  TroubleshootingLabel["FLAG_VALUE_NOT_CALLED"] = "FLAG_VALUE_NOT_CALLED";
  TroubleshootingLabel["GET_FLAG_VALUE_FLAG_NOT_FOUND"] = "GET_FLAG_VALUE_FLAG_NOT_FOUND";
  TroubleshootingLabel["GET_FLAG_METADATA_TYPE_WARNING"] = "GET_FLAG_METADATA_TYPE_WARNING";
  TroubleshootingLabel["GET_FLAG_VALUE_TYPE_WARNING"] = "GET_FLAG_VALUE_TYPE_WARNING";
  TroubleshootingLabel["VISITOR_EXPOSED_TYPE_WARNING"] = "VISITOR_EXPOSED_TYPE_WARNING";
  TroubleshootingLabel["VISITOR_SEND_ACTIVATE"] = "VISITOR_SEND_ACTIVATE";
  TroubleshootingLabel["GET_CAMPAIGNS_ROUTE_RESPONSE_ERROR"] = "GET_CAMPAIGNS_ROUTE_RESPONSE_ERROR";
  TroubleshootingLabel["GET_CAMPAIGNS_ROUTE_RESPONSE"] = "GET_CAMPAIGNS_ROUTE_RESPONSE";
  TroubleshootingLabel["SDK_BUCKETING_FILE"] = "SDK_BUCKETING_FILE";
  TroubleshootingLabel["SEND_ACTIVATE_HIT_ROUTE_ERROR"] = "SEND_ACTIVATE_HIT_ROUTE_ERROR";
  TroubleshootingLabel["SEND_BATCH_HIT_ROUTE_RESPONSE_ERROR"] = "SEND_BATCH_HIT_ROUTE_RESPONSE_ERROR";
  TroubleshootingLabel["SEND_HIT_ROUTE_ERROR"] = "SEND_HIT_ROUTE_ERROR";
  TroubleshootingLabel["SDK_BUCKETING_FILE_ERROR"] = "SDK_BUCKETING_FILE_ERROR";
  TroubleshootingLabel["SDK_CONFIG"] = "SDK_CONFIG";
  TroubleshootingLabel["ACCOUNT_SETTINGS"] = "ACCOUNT_SETTINGS";
  TroubleshootingLabel["ACCOUNT_SETTINGS_ERROR"] = "ACCOUNT_SETTINGS_ERROR";
  TroubleshootingLabel["EMOTION_AI_SCORE"] = "EMOTION_EAI_SCORE";
  TroubleshootingLabel["EMOTION_AI_SCORE_FROM_LOCAL_CACHE"] = "EMOTION_AI_SCORE_FROM_LOCAL_CACHE";
  TroubleshootingLabel["EMOTION_AI_SCORE_ERROR"] = "EMOTION_EAI_SCORE_ERROR";
  TroubleshootingLabel["EMOTION_AI_VISITOR_EVENT"] = "EMOTION_AI_VISITOR_EVENT";
  TroubleshootingLabel["EMOTION_AI_VISITOR_EVENT_ERROR"] = "EMOTION_AI_VISITOR_EVENT_ERROR";
  TroubleshootingLabel["EMOTION_AI_PAGE_VIEW"] = "EMOTION_AI_PAGE_VIEW";
  TroubleshootingLabel["EMOTION_AI_PAGE_VIEW_ERROR"] = "EMOTION_AI_PAGE_VIEW_ERROR";
  TroubleshootingLabel["EMOTION_AI_START_COLLECTING"] = "EMOTION_AI_START_COLLECTING";
  TroubleshootingLabel["EMOTION_AI_STOP_COLLECTING"] = "EMOTION_AI_STOP_COLLECTING";
  TroubleshootingLabel["EMOTION_AI_START_SCORING"] = "EMOTION_AI_START_SCORING";
  TroubleshootingLabel["EMOTION_AI_SCORING_FAILED"] = "EMOTION_AI_SCORING_FAILED";
  TroubleshootingLabel["EMOTION_AI_SCORING_SUCCESS"] = "EMOTION_AI_SCORING_SUCCESS";
  return TroubleshootingLabel;
}({});
let VisitorCacheStatus = exports.VisitorCacheStatus = /*#__PURE__*/function (VisitorCacheStatus) {
  VisitorCacheStatus["NONE"] = "NONE";
  VisitorCacheStatus["ANONYMOUS_ID_CACHE"] = "ANONYMOUS_ID_CACHE";
  VisitorCacheStatus["VISITOR_ID_CACHE"] = "VISITOR_ID_CACHE";
  VisitorCacheStatus["VISITOR_ID_CACHE_WITH_ANONYMOUS_ID_CACHE"] = "VISITOR_ID_CACHE_WITH_ANONYMOUS_ID_CACHE";
  return VisitorCacheStatus;
}({});
let ABTastyWebSDKPostMessageType = exports.ABTastyWebSDKPostMessageType = /*#__PURE__*/function (ABTastyWebSDKPostMessageType) {
  ABTastyWebSDKPostMessageType["AB_TASTY_WEB_SDK_INITIALIZED"] = "AB_TASTY_WEB_SDK_INITIALIZED";
  return ABTastyWebSDKPostMessageType;
}({});

/***/ }),

/***/ 7122:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var arrayLikeToArray = __webpack_require__(79);
function _unsupportedIterableToArray(r, a) {
  if (r) {
    if ("string" == typeof r) return arrayLikeToArray(r, a);
    var t = {}.toString.call(r).slice(8, -1);
    return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? arrayLikeToArray(r, a) : void 0;
  }
}
module.exports = _unsupportedIterableToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 7323:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

/* eslint-disable regexp/no-empty-capturing-group, regexp/no-empty-group, regexp/no-lazy-ends -- testing */
/* eslint-disable regexp/no-useless-quantifier -- testing */
var call = __webpack_require__(9565);
var uncurryThis = __webpack_require__(9504);
var toString = __webpack_require__(655);
var regexpFlags = __webpack_require__(7979);
var stickyHelpers = __webpack_require__(8429);
var shared = __webpack_require__(5745);
var create = __webpack_require__(2360);
var getInternalState = (__webpack_require__(1181).get);
var UNSUPPORTED_DOT_ALL = __webpack_require__(3635);
var UNSUPPORTED_NCG = __webpack_require__(8814);

var nativeReplace = shared('native-string-replace', String.prototype.replace);
var nativeExec = RegExp.prototype.exec;
var patchedExec = nativeExec;
var charAt = uncurryThis(''.charAt);
var indexOf = uncurryThis(''.indexOf);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);

var UPDATES_LAST_INDEX_WRONG = (function () {
  var re1 = /a/;
  var re2 = /b*/g;
  call(nativeExec, re1, 'a');
  call(nativeExec, re2, 'a');
  return re1.lastIndex !== 0 || re2.lastIndex !== 0;
})();

var UNSUPPORTED_Y = stickyHelpers.BROKEN_CARET;

// nonparticipating capturing group, copied from es5-shim's String#split patch.
var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;

var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG;

if (PATCH) {
  patchedExec = function exec(string) {
    var re = this;
    var state = getInternalState(re);
    var str = toString(string);
    var raw = state.raw;
    var result, reCopy, lastIndex, match, i, object, group;

    if (raw) {
      raw.lastIndex = re.lastIndex;
      result = call(patchedExec, raw, str);
      re.lastIndex = raw.lastIndex;
      return result;
    }

    var groups = state.groups;
    var sticky = UNSUPPORTED_Y && re.sticky;
    var flags = call(regexpFlags, re);
    var source = re.source;
    var charsAdded = 0;
    var strCopy = str;

    if (sticky) {
      flags = replace(flags, 'y', '');
      if (indexOf(flags, 'g') === -1) {
        flags += 'g';
      }

      strCopy = stringSlice(str, re.lastIndex);
      // Support anchored sticky behavior.
      if (re.lastIndex > 0 && (!re.multiline || re.multiline && charAt(str, re.lastIndex - 1) !== '\n')) {
        source = '(?: ' + source + ')';
        strCopy = ' ' + strCopy;
        charsAdded++;
      }
      // ^(? + rx + ) is needed, in combination with some str slicing, to
      // simulate the 'y' flag.
      reCopy = new RegExp('^(?:' + source + ')', flags);
    }

    if (NPCG_INCLUDED) {
      reCopy = new RegExp('^' + source + '$(?!\\s)', flags);
    }
    if (UPDATES_LAST_INDEX_WRONG) lastIndex = re.lastIndex;

    match = call(nativeExec, sticky ? reCopy : re, strCopy);

    if (sticky) {
      if (match) {
        match.input = stringSlice(match.input, charsAdded);
        match[0] = stringSlice(match[0], charsAdded);
        match.index = re.lastIndex;
        re.lastIndex += match[0].length;
      } else re.lastIndex = 0;
    } else if (UPDATES_LAST_INDEX_WRONG && match) {
      re.lastIndex = re.global ? match.index + match[0].length : lastIndex;
    }
    if (NPCG_INCLUDED && match && match.length > 1) {
      // Fix browsers whose `exec` methods don't consistently return `undefined`
      // for NPCG, like IE8. NOTE: This doesn't work for /(.?)?/
      call(nativeReplace, match[0], reCopy, function () {
        for (i = 1; i < arguments.length - 2; i++) {
          if (arguments[i] === undefined) match[i] = undefined;
        }
      });
    }

    if (match && groups) {
      match.groups = object = create(null);
      for (i = 0; i < groups.length; i++) {
        group = groups[i];
        object[group[0]] = match[group[1]];
      }
    }

    return match;
  };
}

module.exports = patchedExec;


/***/ }),

/***/ 7347:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var call = __webpack_require__(9565);
var propertyIsEnumerableModule = __webpack_require__(8773);
var createPropertyDescriptor = __webpack_require__(6980);
var toIndexedObject = __webpack_require__(5397);
var toPropertyKey = __webpack_require__(6969);
var hasOwn = __webpack_require__(9297);
var IE8_DOM_DEFINE = __webpack_require__(5917);

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
  O = toIndexedObject(O);
  P = toPropertyKey(P);
  if (IE8_DOM_DEFINE) try {
    return $getOwnPropertyDescriptor(O, P);
  } catch (error) { /* empty */ }
  if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
};


/***/ }),

/***/ 7406:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.EdgeConfig = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _DecisionMode = __webpack_require__(8884);
var _FlagshipConfig = __webpack_require__(7999);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
class EdgeConfig extends _FlagshipConfig.FlagshipConfig {
  constructor(param) {
    super(_objectSpread(_objectSpread({}, param), {}, {
      decisionMode: _DecisionMode.DecisionMode.BUCKETING_EDGE
    }));
  }
}
exports.EdgeConfig = EdgeConfig;

/***/ }),

/***/ 7433:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isArray = __webpack_require__(4376);
var isConstructor = __webpack_require__(3517);
var isObject = __webpack_require__(34);
var wellKnownSymbol = __webpack_require__(8227);

var SPECIES = wellKnownSymbol('species');
var $Array = Array;

// a part of `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray) {
  var C;
  if (isArray(originalArray)) {
    C = originalArray.constructor;
    // cross-realm fallback
    if (isConstructor(C) && (C === $Array || isArray(C.prototype))) C = undefined;
    else if (isObject(C)) {
      C = C[SPECIES];
      if (C === null) C = undefined;
    }
  } return C === undefined ? $Array : C;
};


/***/ }),

/***/ 7449:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.NoConsentStrategy = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _DefaultStrategy = __webpack_require__(4811);
class NoConsentStrategy extends _DefaultStrategy.DefaultStrategy {
  lookupHits() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  lookupVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cacheVisitor() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cacheHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  collectEAIEventsAsync() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this.log('collectEAIData');
    })();
  }
  reportEaiPageView() {
    //
  }
  reportEaiVisitorEvent() {
    //
  }
  onEAICollectStatusChange() {
    //
  }
  fetchCampaignsFromCache() {
    return [];
  }
  sendHit(_hit) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this2.log('sendHit');
    })();
  }
  sendHits(_hits) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this3.log('sendHits');
    })();
  }
  sendActivate(_flagDto, _defaultValue) {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  visitorExposed() {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      _this4.log(_index.FLAG_VISITOR_EXPOSED);
    })();
  }
  sendTroubleshootingHit() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  addInTrackingManager() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  log(methodName) {
    (0, _utils.logInfo)(this.config, (0, _utils.sprintf)(_index.METHOD_DEACTIVATED_CONSENT_ERROR, methodName, this.visitor.visitorId), methodName);
  }
}
exports.NoConsentStrategy = NoConsentStrategy;

/***/ }),

/***/ 7476:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var classofRaw = __webpack_require__(4576);
var uncurryThis = __webpack_require__(9504);

module.exports = function (fn) {
  // Nashorn bug:
  //   https://github.com/zloirock/core-js/issues/1128
  //   https://github.com/zloirock/core-js/issues/1130
  if (classofRaw(fn) === 'Function') return uncurryThis(fn);
};


/***/ }),

/***/ 7495:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var exec = __webpack_require__(7323);

// `RegExp.prototype.exec` method
// https://tc39.es/ecma262/#sec-regexp.prototype.exec
$({ target: 'RegExp', proto: true, forced: /./.exec !== exec }, {
  exec: exec
});


/***/ }),

/***/ 7629:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var IS_PURE = __webpack_require__(6395);
var globalThis = __webpack_require__(2195);
var defineGlobalProperty = __webpack_require__(9433);

var SHARED = '__core-js_shared__';
var store = module.exports = globalThis[SHARED] || defineGlobalProperty(SHARED, {});

(store.versions || (store.versions = [])).push({
  version: '3.41.0',
  mode: IS_PURE ? 'pure' : 'global',
  copyright: '© 2014-2025 Denis Pushkarev (zloirock.ru)',
  license: 'https://github.com/zloirock/core-js/blob/v3.41.0/LICENSE',
  source: 'https://github.com/zloirock/core-js'
});


/***/ }),

/***/ 7633:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var getBuiltIn = __webpack_require__(7751);
var defineBuiltInAccessor = __webpack_require__(2106);
var wellKnownSymbol = __webpack_require__(8227);
var DESCRIPTORS = __webpack_require__(3724);

var SPECIES = wellKnownSymbol('species');

module.exports = function (CONSTRUCTOR_NAME) {
  var Constructor = getBuiltIn(CONSTRUCTOR_NAME);

  if (DESCRIPTORS && Constructor && !Constructor[SPECIES]) {
    defineBuiltInAccessor(Constructor, SPECIES, {
      configurable: true,
      get: function () { return this; }
    });
  }
};


/***/ }),

/***/ 7657:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var isCallable = __webpack_require__(4901);
var isObject = __webpack_require__(34);
var create = __webpack_require__(2360);
var getPrototypeOf = __webpack_require__(2787);
var defineBuiltIn = __webpack_require__(6840);
var wellKnownSymbol = __webpack_require__(8227);
var IS_PURE = __webpack_require__(6395);

var ITERATOR = wellKnownSymbol('iterator');
var BUGGY_SAFARI_ITERATORS = false;

// `%IteratorPrototype%` object
// https://tc39.es/ecma262/#sec-%iteratorprototype%-object
var IteratorPrototype, PrototypeOfArrayIteratorPrototype, arrayIterator;

/* eslint-disable es/no-array-prototype-keys -- safe */
if ([].keys) {
  arrayIterator = [].keys();
  // Safari 8 has buggy iterators w/o `next`
  if (!('next' in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;
  else {
    PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
    if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
  }
}

var NEW_ITERATOR_PROTOTYPE = !isObject(IteratorPrototype) || fails(function () {
  var test = {};
  // FF44- legacy iterators case
  return IteratorPrototype[ITERATOR].call(test) !== test;
});

if (NEW_ITERATOR_PROTOTYPE) IteratorPrototype = {};
else if (IS_PURE) IteratorPrototype = create(IteratorPrototype);

// `%IteratorPrototype%[@@iterator]()` method
// https://tc39.es/ecma262/#sec-%iteratorprototype%-@@iterator
if (!isCallable(IteratorPrototype[ITERATOR])) {
  defineBuiltIn(IteratorPrototype, ITERATOR, function () {
    return this;
  });
}

module.exports = {
  IteratorPrototype: IteratorPrototype,
  BUGGY_SAFARI_ITERATORS: BUGGY_SAFARI_ITERATORS
};


/***/ }),

/***/ 7680:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);

module.exports = uncurryThis([].slice);


/***/ }),

/***/ 7736:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var _typeof = (__webpack_require__(3738)["default"]);
var toPrimitive = __webpack_require__(9045);
function toPropertyKey(t) {
  var i = toPrimitive(t, "string");
  return "symbol" == _typeof(i) ? i : i + "";
}
module.exports = toPropertyKey, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 7740:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var hasOwn = __webpack_require__(9297);
var ownKeys = __webpack_require__(5031);
var getOwnPropertyDescriptorModule = __webpack_require__(7347);
var definePropertyModule = __webpack_require__(4913);

module.exports = function (target, source, exceptions) {
  var keys = ownKeys(source);
  var defineProperty = definePropertyModule.f;
  var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    if (!hasOwn(target, key) && !(exceptions && hasOwn(exceptions, key))) {
      defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
  }
};


/***/ }),

/***/ 7743:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var call = __webpack_require__(9565);
var aCallable = __webpack_require__(9306);
var newPromiseCapabilityModule = __webpack_require__(6043);
var perform = __webpack_require__(1103);
var iterate = __webpack_require__(2652);
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(537);

// `Promise.race` method
// https://tc39.es/ecma262/#sec-promise.race
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  race: function race(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      iterate(iterable, function (promise) {
        call($promiseResolve, C, promise).then(capability.resolve, reject);
      });
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),

/***/ 7750:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isNullOrUndefined = __webpack_require__(4117);

var $TypeError = TypeError;

// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function (it) {
  if (isNullOrUndefined(it)) throw new $TypeError("Can't call method on " + it);
  return it;
};


/***/ }),

/***/ 7751:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var isCallable = __webpack_require__(4901);

var aFunction = function (argument) {
  return isCallable(argument) ? argument : undefined;
};

module.exports = function (namespace, method) {
  return arguments.length < 2 ? aFunction(globalThis[namespace]) : globalThis[namespace] && globalThis[namespace][method];
};


/***/ }),

/***/ 7752:
/***/ ((module) => {

function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
module.exports = _nonIterableRest, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 7783:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.ActivateBatch = void 0;
__webpack_require__(2062);
var _index = __webpack_require__(5394);
class ActivateBatch {
  get hits() {
    return this._hits;
  }
  get config() {
    return this._config;
  }
  constructor(hits, config) {
    this._config = config;
    this._hits = hits;
  }
  toApiKeys() {
    var _this$config;
    return {
      [_index.CUSTOMER_ENV_ID_API_ITEM]: `${(_this$config = this.config) == null ? void 0 : _this$config.envId}`,
      [_index.BATCH]: this.hits.map(x => {
        const apiKeys = x.toApiKeys();
        delete apiKeys[_index.CUSTOMER_ENV_ID_API_ITEM];
        return apiKeys;
      })
    };
  }
}
exports.ActivateBatch = ActivateBatch;

/***/ }),

/***/ 7829:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var charAt = (__webpack_require__(8183).charAt);

// `AdvanceStringIndex` abstract operation
// https://tc39.es/ecma262/#sec-advancestringindex
module.exports = function (S, index, unicode) {
  return index + (unicode ? charAt(S, index).length : 1);
};


/***/ }),

/***/ 7855:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.TrackingManagerAbstract = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_ERROR = void 0;
__webpack_require__(3792);
__webpack_require__(6033);
__webpack_require__(5506);
var _slicedToArray2 = _interopRequireDefault(__webpack_require__(5715));
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _CacheStrategy = __webpack_require__(1589);
var _Segment = __webpack_require__(3545);
var _Event = __webpack_require__(8796);
var _Screen = __webpack_require__(4226);
var _utils = __webpack_require__(6517);
var _BatchingContinuousCachingStrategy = __webpack_require__(3084);
var _BatchingPeriodicCachingStrategy = __webpack_require__(4834);
var _NoBatchingContinuousCachingStrategy = __webpack_require__(1921);
var _Activate = __webpack_require__(9995);
var _BatchTriggeredBy = __webpack_require__(8160);
var _Item = __webpack_require__(2177);
var _Page = __webpack_require__(2509);
var _Transaction = __webpack_require__(2816);
const LOOKUP_HITS_JSON_ERROR = exports.LOOKUP_HITS_JSON_ERROR = 'JSON DATA must be an array of object';
const LOOKUP_HITS_JSON_OBJECT_ERROR = exports.LOOKUP_HITS_JSON_OBJECT_ERROR = 'JSON DATA must fit the type HitCacheDTO';
class TrackingManagerAbstract {
  get initTroubleshootingHit() {
    return this._initTroubleshootingHit;
  }
  set initTroubleshootingHit(v) {
    this._initTroubleshootingHit = v;
    this.strategy.initTroubleshootingHit = v;
  }
  get flagshipInstanceId() {
    return this._flagshipInstanceId;
  }
  get troubleshootingData() {
    return this.strategy.troubleshootingData;
  }
  set troubleshootingData(v) {
    this.strategy.troubleshootingData = v;
  }
  constructor(httpClient, config, flagshipInstanceId, sharedActionTracking) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (0, _defineProperty2.default)(this, "_isPooling", false);
    this._flagshipInstanceId = flagshipInstanceId;
    this._hitsPoolQueue = new Map();
    this._activatePoolQueue = new Map();
    this._troubleshootingQueue = new Map();
    this._analyticHitQueue = new Map();
    this._httpClient = httpClient;
    this._config = config;
    this._sharedActionTracking = sharedActionTracking;
    this.strategy = this.initStrategy();
    this.lookupHits();
  }
  initStrategy() {
    var _this$config$tracking;
    let strategy;
    const argument = {
      config: this.config,
      httpClient: this.httpClient,
      hitsPoolQueue: this._hitsPoolQueue,
      activatePoolQueue: this._activatePoolQueue,
      troubleshootingQueue: this._troubleshootingQueue,
      analyticHitQueue: this._analyticHitQueue,
      flagshipInstanceId: this.flagshipInstanceId,
      initTroubleshootingHit: this.initTroubleshootingHit,
      sharedActionTracking: this._sharedActionTracking
    };
    switch ((_this$config$tracking = this.config.trackingManagerConfig) == null ? void 0 : _this$config$tracking.cacheStrategy) {
      case _CacheStrategy.CacheStrategy.PERIODIC_CACHING:
        strategy = new _BatchingPeriodicCachingStrategy.BatchingPeriodicCachingStrategy(argument);
        break;
      case _CacheStrategy.CacheStrategy.CONTINUOUS_CACHING:
        strategy = new _BatchingContinuousCachingStrategy.BatchingContinuousCachingStrategy(argument);
        break;
      default:
        strategy = new _NoBatchingContinuousCachingStrategy.NoBatchingContinuousCachingStrategy(argument);
        break;
    }
    return strategy;
  }
  get httpClient() {
    return this._httpClient;
  }
  get config() {
    return this._config;
  }
  sendTroubleshootingHit(hit) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this.strategy.sendTroubleshootingHit(hit);
    })();
  }
  startBatchingLoop() {
    var _this$config$tracking2;
    const batchIntervals = (_this$config$tracking2 = this.config.trackingManagerConfig) == null ? void 0 : _this$config$tracking2.batchIntervals;
    if (batchIntervals === 0) {
      return;
    }
    const timeInterval = batchIntervals * 1000;
    (0, _utils.logInfoSprintf)(this.config, _index.TRACKING_MANAGER, _index.BATCH_LOOP_STARTED, timeInterval);
    this._intervalID = setInterval(() => {
      this.batchingLoop();
    }, timeInterval);
  }
  stopBatchingLoop() {
    clearInterval(this._intervalID);
    this._isPooling = false;
    (0, _utils.logInfo)(this.config, _index.BATCH_LOOP_STOPPED, _index.TRACKING_MANAGER);
  }
  batchingLoop() {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      if (_this2._isPooling) {
        return;
      }
      _this2._isPooling = true;
      yield _this2.strategy.sendBatch(_BatchTriggeredBy.BatchTriggeredBy.Timer);
      yield _this2.strategy.sendTroubleshootingQueue();
      yield _this2.strategy.sendUsageHitQueue();
      _this2._isPooling = false;
    })();
  }
  checkLookupHitData(item) {
    var _item$data, _item$data2;
    if ((item == null ? void 0 : item.version) === 1 && item != null && (_item$data = item.data) != null && _item$data.type && item != null && (_item$data2 = item.data) != null && _item$data2.content) {
      return true;
    }
    (0, _utils.logError)(this.config, LOOKUP_HITS_JSON_OBJECT_ERROR, _index.PROCESS_LOOKUP_HIT);
    return false;
  }
  extractHitData(key, item) {
    return (0, _asyncToGenerator2.default)(function* () {
      let hit;
      switch (item.data.type) {
        case _index.HitType.EVENT:
          {
            hit = new _Event.Event(item.data.content);
            break;
          }
        case _index.HitType.ITEM:
          {
            hit = new _Item.Item(item.data.content);
            break;
          }
        case _index.HitType.PAGE:
          {
            hit = new _Page.Page(item.data.content);
            break;
          }
        case _index.HitType.SCREEN:
          {
            hit = new _Screen.Screen(item.data.content);
            break;
          }
        case 'SEGMENT':
          {
            hit = new _Segment.Segment(item.data.content);
            break;
          }
        case _index.HitType.TRANSACTION:
          {
            hit = new _Transaction.Transaction(item.data.content);
            break;
          }
        default:
      }
      return hit;
    })();
  }
  processCachedHits(hitsCache) {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const checkHitTime = time => Date.now() - time <= _index.DEFAULT_HIT_CACHE_TIME_MS;
      const wrongHitKeys = [];
      const entries = Object.entries(hitsCache);
      for (var _i = 0, _entries = entries; _i < _entries.length; _i++) {
        const _entries$_i = (0, _slicedToArray2.default)(_entries[_i], 2),
          key = _entries$_i[0],
          item = _entries$_i[1];
        if (!_this3.checkLookupHitData(item) || !checkHitTime(item.data.time)) {
          wrongHitKeys.push(key);
          continue;
        }
        if (item.data.type === 'ACTIVATE') {
          const hit = new _Activate.Activate(item.data.content);
          hit.key = key;
          hit.createdAt = item.data.content.createdAt;
          hit.config = _this3.config;
          _this3._activatePoolQueue.set(key, hit);
          continue;
        }
        const hit = yield _this3.extractHitData(key, item);
        if (!hit) {
          wrongHitKeys.push(key);
          continue;
        }
        hit.key = key;
        hit.createdAt = item.data.content.createdAt;
        hit.config = _this3.config;
        _this3._hitsPoolQueue.set(key, hit);
      }
      if (wrongHitKeys.length) {
        yield _this3.strategy.flushHits(wrongHitKeys);
      }
    })();
  }
  lookupHits() {
    var _this4 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      try {
        const hitCacheImplementation = _this4.config.hitCacheImplementation;
        if (_this4.config.disableCache || !hitCacheImplementation || typeof hitCacheImplementation.lookupHits !== 'function') {
          return;
        }
        const hitsCache = yield hitCacheImplementation.lookupHits();
        (0, _utils.logDebugSprintf)(_this4.config, _index.PROCESS_CACHE, _index.HIT_CACHE_LOADED, hitsCache);
        if (!hitsCache || !Object.keys(hitsCache).length) {
          return;
        }
        yield _this4.processCachedHits(hitsCache);

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (error) {
        (0, _utils.logErrorSprintf)(_this4.config, _index.PROCESS_CACHE, _index.HIT_CACHE_ERROR, 'lookupHits', error.message || error);
      }
    })();
  }
  sendUsageHit(hit) {
    var _this5 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this5.strategy.sendUsageHit(hit);
    })();
  }
  addTroubleshootingHit(hit) {
    var _this6 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this6.strategy.addTroubleshootingHit(hit);
    })();
  }
}
exports.TrackingManagerAbstract = TrackingManagerAbstract;

/***/ }),

/***/ 7860:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var userAgent = __webpack_require__(2839);

module.exports = /web0s(?!.*chrome)/i.test(userAgent);


/***/ }),

/***/ 7877:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.OS_VERSION_NAME = exports.OS_VERSION_CODE = exports.OS_NAME = exports.LOCATION_REGION = exports.LOCATION_LONG = exports.LOCATION_LAT = exports.LOCATION_COUNTRY = exports.LOCATION_CITY = exports.IP = exports.INTERNET_CONNECTION = exports.INTERFACE_NAME = exports.FLAGSHIP_VISITOR = exports.FLAGSHIP_VERSION = exports.FLAGSHIP_CONTEXT = exports.FLAGSHIP_CLIENT = exports.DEVICE_TYPE = exports.DEVICE_MODEL = exports.DEVICE_LOCALE = exports.CARRIER_NAME = exports.APP_VERSION_NAME = exports.APP_VERSION_CODE = void 0;
/**
     * Current device locale
     */
const DEVICE_LOCALE = exports.DEVICE_LOCALE = 'sdk_deviceLanguage';
/**
  * Current device type  tablet, pc, server, iot, other

  */
const DEVICE_TYPE = exports.DEVICE_TYPE = 'sdk_deviceType';
/**
  * Current device model
  */
const DEVICE_MODEL = exports.DEVICE_MODEL = 'sdk_deviceModel';
/**
  * Current visitor city

  */
const LOCATION_CITY = exports.LOCATION_CITY = 'sdk_city';
/**
  * Current visitor region

  */
const LOCATION_REGION = exports.LOCATION_REGION = 'sdk_region';

/**
  * Current visitor country

  */
const LOCATION_COUNTRY = exports.LOCATION_COUNTRY = 'sdk_country';

/**
  * Current visitor latitude

  */
const LOCATION_LAT = exports.LOCATION_LAT = 'sdk_lat';

/**
  * Current visitor longitude

  */
const LOCATION_LONG = exports.LOCATION_LONG = 'sdk_long';

/**
  * Device public ip

  */
const IP = exports.IP = 'sdk_ip';

/**
  * OS name

  */
const OS_NAME = exports.OS_NAME = 'sdk_osName';

/**
  * OS version name

  */
const OS_VERSION_NAME = exports.OS_VERSION_NAME = 'sdk_osVersionName';

/**
  * OS version code

  */
const OS_VERSION_CODE = exports.OS_VERSION_CODE = 'sdk_osVersionCode';

/**
  * Carrier operator
  */
const CARRIER_NAME = exports.CARRIER_NAME = 'sdk_carrierName';

/**
  * Internet connection type : 4G, 5G, Fiber

  */
const INTERNET_CONNECTION = exports.INTERNET_CONNECTION = 'sdk_internetConnection';

/**
  * Customer app version name

  */
const APP_VERSION_NAME = exports.APP_VERSION_NAME = 'sdk_versionName';

/**
  * Customer app version code

  */
const APP_VERSION_CODE = exports.APP_VERSION_CODE = 'sdk_versionCode';

/**
  * Current customer app interface name

  */
const INTERFACE_NAME = exports.INTERFACE_NAME = 'sdk_interfaceName';

/**
  * Flagship SDK client name

  */
const FLAGSHIP_CLIENT = exports.FLAGSHIP_CLIENT = 'fs_client';

/**
  * Flagship SDK version name

  */
const FLAGSHIP_VERSION = exports.FLAGSHIP_VERSION = 'fs_version';

/**
  * Current visitor id

  */
const FLAGSHIP_VISITOR = exports.FLAGSHIP_VISITOR = 'fs_users';
const FLAGSHIP_CONTEXT = exports.FLAGSHIP_CONTEXT = {
  [DEVICE_LOCALE]: 'string',
  [DEVICE_TYPE]: 'string',
  [DEVICE_MODEL]: 'string',
  [LOCATION_CITY]: 'string',
  [LOCATION_REGION]: 'string',
  [LOCATION_COUNTRY]: 'string',
  [LOCATION_LAT]: 'number',
  [LOCATION_LONG]: 'number',
  [IP]: 'string',
  [OS_NAME]: 'string',
  [OS_VERSION_NAME]: 'string',
  [OS_VERSION_CODE]: 'string',
  [CARRIER_NAME]: 'string',
  [INTERNET_CONNECTION]: 'string',
  [APP_VERSION_NAME]: 'string',
  [APP_VERSION_CODE]: 'string',
  [INTERFACE_NAME]: 'string',
  [FLAGSHIP_CLIENT]: 'string',
  [FLAGSHIP_VERSION]: 'string',
  [FLAGSHIP_VISITOR]: 'string'
};

/***/ }),

/***/ 7916:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var bind = __webpack_require__(6080);
var call = __webpack_require__(9565);
var toObject = __webpack_require__(8981);
var callWithSafeIterationClosing = __webpack_require__(6319);
var isArrayIteratorMethod = __webpack_require__(4209);
var isConstructor = __webpack_require__(3517);
var lengthOfArrayLike = __webpack_require__(6198);
var createProperty = __webpack_require__(4659);
var getIterator = __webpack_require__(81);
var getIteratorMethod = __webpack_require__(851);

var $Array = Array;

// `Array.from` method implementation
// https://tc39.es/ecma262/#sec-array.from
module.exports = function from(arrayLike /* , mapfn = undefined, thisArg = undefined */) {
  var O = toObject(arrayLike);
  var IS_CONSTRUCTOR = isConstructor(this);
  var argumentsLength = arguments.length;
  var mapfn = argumentsLength > 1 ? arguments[1] : undefined;
  var mapping = mapfn !== undefined;
  if (mapping) mapfn = bind(mapfn, argumentsLength > 2 ? arguments[2] : undefined);
  var iteratorMethod = getIteratorMethod(O);
  var index = 0;
  var length, result, step, iterator, next, value;
  // if the target is not iterable or it's an array with the default iterator - use a simple case
  if (iteratorMethod && !(this === $Array && isArrayIteratorMethod(iteratorMethod))) {
    result = IS_CONSTRUCTOR ? new this() : [];
    iterator = getIterator(O, iteratorMethod);
    next = iterator.next;
    for (;!(step = call(next, iterator)).done; index++) {
      value = mapping ? callWithSafeIterationClosing(iterator, mapfn, [step.value, index], true) : step.value;
      createProperty(result, index, value);
    }
  } else {
    length = lengthOfArrayLike(O);
    result = IS_CONSTRUCTOR ? new this(length) : $Array(length);
    for (;length > index; index++) {
      value = mapping ? mapfn(O[index], index) : O[index];
      createProperty(result, index, value);
    }
  }
  result.length = index;
  return result;
};


/***/ }),

/***/ 7979:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var anObject = __webpack_require__(8551);

// `RegExp.prototype.flags` getter implementation
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags
module.exports = function () {
  var that = anObject(this);
  var result = '';
  if (that.hasIndices) result += 'd';
  if (that.global) result += 'g';
  if (that.ignoreCase) result += 'i';
  if (that.multiline) result += 'm';
  if (that.dotAll) result += 's';
  if (that.unicode) result += 'u';
  if (that.unicodeSets) result += 'v';
  if (that.sticky) result += 'y';
  return result;
};


/***/ }),

/***/ 7999:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.statusChangeError = exports.FlagshipConfig = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _utils = __webpack_require__(6517);
var _TrackingManagerConfig = __webpack_require__(564);
var _sdkVersion = __webpack_require__(4116);
var _DecisionMode = __webpack_require__(8884);
const statusChangeError = exports.statusChangeError = 'statusChangedCallback must be a function';
class FlagshipConfig {
  get batchActivateHits() {
    return this._batchActivateHits;
  }
  set batchActivateHits(v) {
    this._batchActivateHits = v;
  }
  get isQAModeEnabled() {
    return this._isQAModeEnabled;
  }
  set isQAModeEnabled(v) {
    this._isQAModeEnabled = v;
  }
  get disableDeveloperUsageTracking() {
    return this._disableDeveloperUsageTracking;
  }
  set disableDeveloperUsageTracking(v) {
    this._disableDeveloperUsageTracking = v;
  }
  get fetchFlagsBufferingTime() {
    return this._fetchFlagsBufferingTime;
  }
  set fetchFlagsBufferingTime(v) {
    this._fetchFlagsBufferingTime = v;
  }
  get nextFetchConfig() {
    return this._nextFetchConfig;
  }
  set nextFetchConfig(v) {
    this._nextFetchConfig = v;
  }
  get fetchThirdPartyData() {
    return this._fetchThirdPartyData;
  }
  set fetchThirdPartyData(v) {
    this._fetchThirdPartyData = v;
  }
  get trackingManagerConfig() {
    return this._trackingManagerConfig;
  }
  get onLog() {
    return this._onLog;
  }
  set onLog(v) {
    this._onLog = v;
  }
  get onVisitorExposed() {
    return this._onVisitorExposed;
  }
  constructor(param) {
    (0, _defineProperty2.default)(this, "_batchActivateHits", false);
    const envId = param.envId,
      apiKey = param.apiKey,
      timeout = param.timeout,
      logLevel = param.logLevel,
      logManager = param.logManager,
      onSdkStatusChanged = param.onSdkStatusChanged,
      fetchNow = param.fetchNow,
      decisionMode = param.decisionMode,
      reuseVisitorIds = param.reuseVisitorIds,
      initialBucketing = param.initialBucketing,
      decisionApiUrl = param.decisionApiUrl,
      hitDeduplicationTime = param.hitDeduplicationTime,
      visitorCacheImplementation = param.visitorCacheImplementation,
      hitCacheImplementation = param.hitCacheImplementation,
      disableCache = param.disableCache,
      language = param.language,
      sdkVersion = param.sdkVersion,
      trackingManagerConfig = param.trackingManagerConfig,
      onLog = param.onLog,
      onVisitorExposed = param.onVisitorExposed,
      nextFetchConfig = param.nextFetchConfig,
      fetchFlagsBufferingTime = param.fetchFlagsBufferingTime,
      disableDeveloperUsageTracking = param.disableDeveloperUsageTracking,
      batchActivateHits = param.batchActivateHits;
    this.initQaMode();
    this.initSDKInfo(language, sdkVersion);
    if (logManager) {
      this.logManager = logManager;
    }
    this._batchActivateHits = batchActivateHits;
    this.fetchFlagsBufferingTime = fetchFlagsBufferingTime != null ? fetchFlagsBufferingTime : _index.FETCH_FLAG_BUFFERING_DEFAULT_TIME;
    this.nextFetchConfig = nextFetchConfig || {
      revalidate: 20
    };
    this._trackingManagerConfig = new _TrackingManagerConfig.TrackingManagerConfig(trackingManagerConfig || {});
    this.onLog = onLog;
    this.decisionApiUrl = decisionApiUrl || _index.BASE_API_URL;
    this._envId = envId;
    this._apiKey = apiKey;
    this.logLevel = logLevel != null ? logLevel : _index.LogLevel.INFO;
    this.timeout = timeout || _index.REQUEST_TIME_OUT;
    this.setFetchNow(fetchNow);
    this.reuseVisitorIds = typeof reuseVisitorIds === 'undefined' || reuseVisitorIds;
    this._decisionMode = decisionMode || _DecisionMode.DecisionMode.DECISION_API;
    this._initialBucketing = initialBucketing;
    this.hitDeduplicationTime = hitDeduplicationTime != null ? hitDeduplicationTime : _index.DEFAULT_DEDUPLICATION_TIME;
    this.disableCache = !!disableCache;
    this.disableDeveloperUsageTracking = disableDeveloperUsageTracking || false;
    if (visitorCacheImplementation) {
      this.visitorCacheImplementation = visitorCacheImplementation;
    }
    if (hitCacheImplementation) {
      this.hitCacheImplementation = hitCacheImplementation;
    }
    this.onSdkStatusChanged = onSdkStatusChanged;
    this._onVisitorExposed = onVisitorExposed;
  }
  setFetchNow(fetchNow) {
    if (false) {} else {
      this.fetchNow = typeof fetchNow === 'undefined' ? false : fetchNow;
    }
  }
  initQaMode() {
    if (false) {}
  }
  initSDKInfo(language, sdkVersion) {
    switch (language) {
      case 1:
        _index.SDK_INFO.name = 'ReactJS';
        _index.SDK_INFO.version = sdkVersion != null ? sdkVersion : _sdkVersion.version;
        break;
      case 2:
        _index.SDK_INFO.name = 'React-Native';
        _index.SDK_INFO.version = sdkVersion != null ? sdkVersion : _sdkVersion.version;
        break;
      default:
        _index.SDK_INFO.name = typeof window !== 'undefined' && 'Deno' in window ? 'Deno' : 'TypeScript';
        _index.SDK_INFO.version = _sdkVersion.version;
        break;
    }
  }
  get initialBucketing() {
    return this._initialBucketing;
  }
  set initialBucketing(v) {
    this._initialBucketing = v;
  }
  get reuseVisitorIds() {
    return this._reuseVisitorIds;
  }
  set reuseVisitorIds(v) {
    this._reuseVisitorIds = v;
  }
  get onBucketingUpdated() {
    return this._onBucketingUpdated;
  }
  set onBucketingUpdated(v) {
    this._onBucketingUpdated = v;
  }
  set envId(value) {
    this._envId = value;
  }
  get envId() {
    return this._envId;
  }
  set apiKey(value) {
    this._apiKey = value;
  }
  get apiKey() {
    return this._apiKey;
  }
  get decisionMode() {
    return this._decisionMode;
  }
  get timeout() {
    return this._timeout;
  }
  set timeout(value) {
    this._timeout = value;
  }
  get logLevel() {
    return this._logLevel;
  }
  set logLevel(value) {
    this._logLevel = value;
  }
  get fetchNow() {
    return this._fetchNow;
  }
  set fetchNow(v) {
    this._fetchNow = v;
  }
  get pollingInterval() {
    return this._pollingInterval;
  }
  set pollingInterval(v) {
    this._pollingInterval = v;
  }
  get hitDeduplicationTime() {
    return this._hitDeduplicationTime;
  }
  set hitDeduplicationTime(v) {
    if (typeof v !== 'number') {
      (0, _utils.logError)(this, (0, _utils.sprintf)(_index.TYPE_ERROR, 'hitDeduplicationTime', 'number'), 'hitDeduplicationTime');
      return;
    }
    this._hitDeduplicationTime = v;
  }
  get visitorCacheImplementation() {
    return this._visitorCacheImplementation;
  }
  set visitorCacheImplementation(v) {
    this._visitorCacheImplementation = v;
  }
  get hitCacheImplementation() {
    return this._hitCacheImplementation;
  }
  set hitCacheImplementation(v) {
    this._hitCacheImplementation = v;
  }
  get disableCache() {
    return this._disableCache;
  }
  set disableCache(v) {
    this._disableCache = v;
  }
  get onSdkStatusChanged() {
    return this._onSdkStatusChanged;
  }
  set onSdkStatusChanged(fn) {
    if (fn && typeof fn !== 'function') {
      (0, _utils.logError)(this, statusChangeError, 'onSdkStatusChanged');
      return;
    }
    this._onSdkStatusChanged = fn;
  }
  get logManager() {
    return this._logManager;
  }
  set logManager(value) {
    this._logManager = value;
  }
  get decisionApiUrl() {
    return this._decisionApiUrl;
  }
  set decisionApiUrl(v) {
    if (typeof v !== 'string') {
      (0, _utils.logError)(this, (0, _utils.sprintf)(_index.TYPE_ERROR, 'decisionApiUrl', 'string'), 'decisionApiUrl');
      return;
    }
    this._decisionApiUrl = v;
  }
}
exports.FlagshipConfig = FlagshipConfig;

/***/ }),

/***/ 8014:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toIntegerOrInfinity = __webpack_require__(1291);

var min = Math.min;

// `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength
module.exports = function (argument) {
  var len = toIntegerOrInfinity(argument);
  return len > 0 ? min(len, 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};


/***/ }),

/***/ 8045:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
var _FsFlags = __webpack_require__(4707);
Object.keys(_FsFlags).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FsFlags[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FsFlags[key];
    }
  });
});
var _FSFlagMetadata = __webpack_require__(8889);
Object.keys(_FSFlagMetadata).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSFlagMetadata[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSFlagMetadata[key];
    }
  });
});
var _IFSFlag = __webpack_require__(1071);
Object.keys(_IFSFlag).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _IFSFlag[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _IFSFlag[key];
    }
  });
});
var _IFSFlagCollection = __webpack_require__(6295);
Object.keys(_IFSFlagCollection).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _IFSFlagCollection[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _IFSFlagCollection[key];
    }
  });
});
var _FSFlagCollection = __webpack_require__(448);
Object.keys(_FSFlagCollection).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _FSFlagCollection[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _FSFlagCollection[key];
    }
  });
});

/***/ }),

/***/ 8156:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var $padStart = (__webpack_require__(533).start);
var WEBKIT_BUG = __webpack_require__(3063);

// `String.prototype.padStart` method
// https://tc39.es/ecma262/#sec-string.prototype.padstart
$({ target: 'String', proto: true, forced: WEBKIT_BUG }, {
  padStart: function padStart(maxLength /* , fillString = ' ' */) {
    return $padStart(this, maxLength, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ 8160:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.BatchTriggeredBy = void 0;
let BatchTriggeredBy = exports.BatchTriggeredBy = /*#__PURE__*/function (BatchTriggeredBy) {
  BatchTriggeredBy[BatchTriggeredBy["Timer"] = 0] = "Timer";
  BatchTriggeredBy[BatchTriggeredBy["BatchLength"] = 1] = "BatchLength";
  BatchTriggeredBy[BatchTriggeredBy["Flush"] = 2] = "Flush";
  BatchTriggeredBy[BatchTriggeredBy["ActivateLength"] = 3] = "ActivateLength";
  BatchTriggeredBy[BatchTriggeredBy["DirectHit"] = 4] = "DirectHit";
  BatchTriggeredBy[BatchTriggeredBy["DirectActivate"] = 5] = "DirectActivate";
  return BatchTriggeredBy;
}({});

/***/ }),

/***/ 8183:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var toIntegerOrInfinity = __webpack_require__(1291);
var toString = __webpack_require__(655);
var requireObjectCoercible = __webpack_require__(7750);

var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var stringSlice = uncurryThis(''.slice);

var createMethod = function (CONVERT_TO_STRING) {
  return function ($this, pos) {
    var S = toString(requireObjectCoercible($this));
    var position = toIntegerOrInfinity(pos);
    var size = S.length;
    var first, second;
    if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
    first = charCodeAt(S, position);
    return first < 0xD800 || first > 0xDBFF || position + 1 === size
      || (second = charCodeAt(S, position + 1)) < 0xDC00 || second > 0xDFFF
        ? CONVERT_TO_STRING
          ? charAt(S, position)
          : first
        : CONVERT_TO_STRING
          ? stringSlice(S, position, position + 2)
          : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
  };
};

module.exports = {
  // `String.prototype.codePointAt` method
  // https://tc39.es/ecma262/#sec-string.prototype.codepointat
  codeAt: createMethod(false),
  // `String.prototype.at` method
  // https://github.com/mathiasbynens/String.prototype.at
  charAt: createMethod(true)
};


/***/ }),

/***/ 8204:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(3792);
__webpack_require__(3772);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.Flagship = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
__webpack_require__(3362);
__webpack_require__(2480);
var _Visitor = __webpack_require__(3361);
var _FSSdkStatus = __webpack_require__(2137);
var _index = __webpack_require__(6701);
var _ConfigManager = __webpack_require__(8388);
var _ApiManager = __webpack_require__(82);
var _TrackingManager = __webpack_require__(5825);
var _FlagshipLogManager = __webpack_require__(5343);
var _utils = __webpack_require__(6517);
var _index2 = __webpack_require__(5394);
var _VisitorDelegate = __webpack_require__(9102);
var _BucketingManager = __webpack_require__(1844);
var _MurmurHash = __webpack_require__(1270);
var _HttpClient = __webpack_require__(5821);
var _types = __webpack_require__(7099);
var _EdgeManager = __webpack_require__(4353);
var _EdgeConfig = __webpack_require__(7406);
var _VisitorAbstract = __webpack_require__(2623);
var _BucketingSdkManager = __webpack_require__(3263);
var _EdgeSdkManager = __webpack_require__(5678);
var _ApiSdkManager = __webpack_require__(3233);
var _EmotionAI = __webpack_require__(9807);
var _VisitorProfileCache = __webpack_require__(1120);
var _DefaultVisitorCache = __webpack_require__(2358);
var _DefaultHitCache = __webpack_require__(2319);
var _SharedActionTracking = __webpack_require__(44);
var _SdkApi = __webpack_require__(1661);
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
/**
 * The `Flagship` class represents the SDK. It facilitates the initialization process and creation of new visitors.
 */
class Flagship {
  set configManager(value) {
    this._configManager = value;
  }
  get configManager() {
    return this._configManager;
  }
  constructor() {
    this.instanceId = (0, _utils.uuidV4)();
    this._status = _FSSdkStatus.FSSdkStatus.SDK_NOT_INITIALIZED;
    const extendedFlagship = Flagship;
    extendedFlagship.setVisitorProfile = function (value) {
      Flagship.visitorProfile = value;
    };
    extendedFlagship.getVisitorProfile = function () {
      return Flagship.visitorProfile;
    };
    extendedFlagship.setOnSaveVisitorProfile = function (value) {
      Flagship.onSaveVisitorProfile = value;
    };
    extendedFlagship.getOnSaveVisitorProfile = function () {
      return Flagship.onSaveVisitorProfile;
    };
    this._visitorVariationState = {};
  }
  static getInstance() {
    if (true) {
      const globalScope = globalThis;
      globalScope.__flagship_instance__ = globalScope.__flagship_instance__ || new this();
      return globalScope.__flagship_instance__;
    } else {}
  }
  setStatus(status) {
    var _this$getConfig;
    if (this._status === status) {
      return;
    }
    this._status = status;
    _VisitorAbstract.VisitorAbstract.SdkStatus = status;
    const statusChanged = (_this$getConfig = this.getConfig()) == null ? void 0 : _this$getConfig.onSdkStatusChanged;
    (0, _utils.logInfoSprintf)(this._config, _index2.PROCESS_SDK_STATUS, _index2.SDK_STATUS_CHANGED, _FSSdkStatus.FSSdkStatus[status]);
    if (this.getConfig().decisionMode !== _index.DecisionMode.BUCKETING_EDGE) {
      if (status === _FSSdkStatus.FSSdkStatus.SDK_INITIALIZED) {
        var _this$configManager;
        (_this$configManager = this.configManager) == null || (_this$configManager = _this$configManager.trackingManager) == null || _this$configManager.startBatchingLoop();
      }
      if (status === _FSSdkStatus.FSSdkStatus.SDK_NOT_INITIALIZED) {
        var _this$configManager2;
        (_this$configManager2 = this.configManager) == null || (_this$configManager2 = _this$configManager2.trackingManager) == null || _this$configManager2.stopBatchingLoop();
      }
    }
    if (statusChanged) {
      statusChanged(status);
    }
  }

  /**
   * Return current status of Flagship SDK.
   */
  static getStatus() {
    return this.getInstance()._status;
  }

  /**
   * Return current status of Flagship SDK.
   */
  getStatus() {
    return this._status;
  }

  /**
   * Return the current config set by the customer and used by the SDK.
   */
  static getConfig() {
    return this.getInstance()._config;
  }

  /**
   * Return the current config set by the customer and used by the SDK.
   */
  getConfig() {
    return this._config;
  }

  /**
   * Return the last visitor created if isNewInstance key is false. Return undefined otherwise.
   */
  getVisitor() {
    return this._visitorInstance;
  }

  /**
   * Return the last visitor created if isNewInstance key is false. Return undefined otherwise.
   */
  static getVisitor() {
    return this.getInstance().getVisitor();
  }
  buildConfig(config) {
    let newConfig;
    switch (config == null ? void 0 : config.decisionMode) {
      case _index.DecisionMode.BUCKETING:
        newConfig = new _index.BucketingConfig(config);
        break;
      case _index.DecisionMode.BUCKETING_EDGE:
        newConfig = new _EdgeConfig.EdgeConfig(config);
        break;
      default:
        newConfig = new _index.DecisionApiConfig(config);
        break;
    }
    return newConfig;
  }
  createManagers(httpClient, sdkConfig, trackingManager) {
    let sdkManager;
    switch (sdkConfig.decisionMode) {
      case _index.DecisionMode.BUCKETING:
        sdkManager = new _BucketingSdkManager.BucketingSdkManager({
          httpClient,
          sdkConfig,
          trackingManager,
          flagshipInstanceId: this.instanceId
        });
        return {
          sdkManager,
          decisionManager: new _BucketingManager.BucketingManager({
            httpClient,
            config: sdkConfig,
            murmurHash: new _MurmurHash.MurmurHash(),
            sdkManager,
            trackingManager,
            flagshipInstanceId: this.instanceId
          })
        };
      case _index.DecisionMode.BUCKETING_EDGE:
        sdkManager = new _EdgeSdkManager.EdgeSdkManager({
          httpClient,
          sdkConfig,
          trackingManager,
          flagshipInstanceId: this.instanceId
        });
        return {
          sdkManager,
          decisionManager: new _EdgeManager.EdgeManager({
            httpClient,
            config: sdkConfig,
            murmurHash: new _MurmurHash.MurmurHash(),
            sdkManager,
            trackingManager,
            flagshipInstanceId: this.instanceId
          })
        };
      default:
        sdkManager = new _ApiSdkManager.ApiSdkManager({
          httpClient,
          sdkConfig,
          trackingManager,
          flagshipInstanceId: this.instanceId
        });
        return {
          sdkManager,
          decisionManager: new _ApiManager.ApiManager({
            trackingManager,
            httpClient,
            config: sdkConfig,
            flagshipInstanceId: this.instanceId
          })
        };
    }
  }
  buildSdkApi(sharedActionTracking, sdkConfig) {
    if (false) {}
  }
  sendInitializedPostMessage() {
    if (false) {}
  }
  initializeSdk(sdkConfig) {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this$_sdkManager, _this$configManager3, _this$configManager4, _this$_sdkManager2;
      _this.setStatus(_FSSdkStatus.FSSdkStatus.SDK_INITIALIZING);
      (_this$_sdkManager = _this._sdkManager) == null || _this$_sdkManager.resetSdk();
      let sharedActionTracking = (_this$configManager3 = _this.configManager) == null ? void 0 : _this$configManager3.sharedActionTracking;
      if (false) {}
      const httpClient = new _HttpClient.HttpClient();
      const trackingManager = ((_this$configManager4 = _this.configManager) == null ? void 0 : _this$configManager4.trackingManager) || new _TrackingManager.TrackingManager(httpClient, sdkConfig, _this.instanceId, sharedActionTracking);
      const _this$createManagers = _this.createManagers(httpClient, sdkConfig, trackingManager),
        sdkManager = _this$createManagers.sdkManager,
        decisionManager = _this$createManagers.decisionManager;
      _this._sdkManager = sdkManager;
      decisionManager.statusChangedCallback(_this.setStatus.bind(_this));
      decisionManager.flagshipInstanceId = _this.instanceId;
      _this.configManager = new _ConfigManager.ConfigManager(sdkConfig, decisionManager, trackingManager, sharedActionTracking);
      yield (_this$_sdkManager2 = _this._sdkManager) == null ? void 0 : _this$_sdkManager2.initSdk();
      _this.setStatus(_FSSdkStatus.FSSdkStatus.SDK_INITIALIZED);
    })();
  }

  /**
   * Start the flagship SDK, with a custom configuration implementation
   * @param {string} envId : Environment id provided by Flagship.
   * @param {string} apiKey : Secure api key provided by Flagship.
   * @param {IFlagshipConfig} config : (optional) SDK configuration.
   */
  static start(envId, apiKey, config) {
    var _this2 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      const flagship = _this2.getInstance();
      const localConfig = flagship.buildConfig(config);
      localConfig.envId = envId;
      localConfig.apiKey = apiKey;
      flagship._config = localConfig;

      // check custom logger
      if (!localConfig.onLog && !localConfig.logManager) {
        localConfig.logManager = new _FlagshipLogManager.FlagshipLogManager();
      }
      if (!envId || !apiKey) {
        flagship.setStatus(_FSSdkStatus.FSSdkStatus.SDK_NOT_INITIALIZED);
        (0, _utils.logError)(localConfig, _index2.INITIALIZATION_PARAM_ERROR, _index2.PROCESS_INITIALIZATION);
        return flagship;
      }
      (0, _utils.logDebugSprintf)(localConfig, _index2.PROCESS_INITIALIZATION, _index2.INITIALIZATION_STARTING, _index2.SDK_INFO.version, localConfig.decisionMode, localConfig);
      if (false) {}
      yield flagship.initializeSdk(localConfig);
      (0, _utils.logInfo)(localConfig, (0, _utils.sprintf)(_index2.SDK_STARTED_INFO, _index2.SDK_INFO.version, _FSSdkStatus.FSSdkStatus[flagship._status], localConfig.decisionMode), _index2.PROCESS_INITIALIZATION);
      if (false) {}
      flagship.lastInitializationTimestamp = new Date().toISOString();
      flagship.sendInitializedPostMessage();
      return flagship;
    })();
  }

  /**
   * When called, it will batch and send all hits that are in the pool before the application is closed
   */
  close() {
    return (0, _asyncToGenerator2.default)(function* () {
      yield Flagship.close();
    })();
  }

  /**
   * When called, it will batch and send all hits that are in the pool before the application is closed
   */
  static close() {
    var _this3 = this;
    return (0, _asyncToGenerator2.default)(function* () {
      var _this3$_instance;
      yield (_this3$_instance = _this3._instance) == null || (_this3$_instance = _this3$_instance.configManager) == null || (_this3$_instance = _this3$_instance.trackingManager) == null ? void 0 : _this3$_instance.sendBatch();
    })();
  }

  /**
   * Creates a new Visitor instance.
   *
   * @param params - The parameters for creating the new Visitor.
   * @returns A new Visitor instance.
   */
  newVisitor(params) {
    return Flagship.newVisitor(params);
  }
  initializeConfigManager() {
    const config = new _index.DecisionApiConfig();
    config.logManager = new _FlagshipLogManager.FlagshipLogManager();
    const httpClient = new _HttpClient.HttpClient();
    const trackingManager = new _TrackingManager.TrackingManager(httpClient, config);
    const decisionManager = new _ApiManager.ApiManager({
      httpClient,
      config,
      trackingManager
    });
    this._config = config;
    this.configManager = new _ConfigManager.ConfigManager(config, decisionManager, trackingManager);
  }

  /**
   * Creates a new Visitor instance.
   *
   * @param params - The parameters for creating the new Visitor.
   * @returns A new Visitor instance.
   */
  static newVisitor({
    visitorId,
    context,
    isAuthenticated,
    hasConsented,
    initialCampaigns,
    initialFlagsData,
    shouldSaveInstance,
    onFlagsStatusChanged
  }) {
    var _flagship$_sdkManager;
    const saveInstance = shouldSaveInstance != null ? shouldSaveInstance : (0, _utils.isBrowser)();
    const flagship = this.getInstance();
    if (!flagship.configManager) {
      flagship.initializeConfigManager();
      (0, _utils.logError)(flagship.getConfig(), _index2.NEW_VISITOR_NOT_READY, _index2.PROCESS_NEW_VISITOR);
    }
    const sdkConfig = flagship.getConfig();
    const configManager = flagship.configManager;
    if (hasConsented === undefined) {
      (0, _utils.logWarning)(sdkConfig, _index2.CONSENT_NOT_SPECIFY_WARNING, _index2.PROCESS_NEW_VISITOR);
    }
    const emotionAi = new _EmotionAI.EmotionAI({
      sdkConfig,
      httpClient: new _HttpClient.HttpClient(),
      eAIConfig: (_flagship$_sdkManager = flagship._sdkManager) == null ? void 0 : _flagship$_sdkManager.getEAIConfig()
    });
    const visitorProfileCache = new _VisitorProfileCache.VisitorProfileCache(sdkConfig);
    _VisitorAbstract.VisitorAbstract.SdkStatus = flagship.getStatus();
    const visitorDelegate = new _VisitorDelegate.VisitorDelegate({
      visitorId,
      context: context || {},
      isAuthenticated: isAuthenticated != null ? isAuthenticated : false,
      hasConsented: hasConsented != null ? hasConsented : false,
      configManager,
      initialCampaigns,
      initialFlagsData,
      onFlagsStatusChanged,
      emotionAi,
      visitorProfileCache,
      monitoringData: {
        instanceId: this.getInstance().instanceId,
        lastInitializationTimestamp: this.getInstance().lastInitializationTimestamp,
        initialCampaigns,
        initialFlagsData
      },
      murmurHash: new _MurmurHash.MurmurHash(),
      visitorVariationState: flagship._visitorVariationState
    });
    if (false) {}
    const visitor = new _Visitor.Visitor(visitorDelegate);
    this.getInstance()._visitorInstance = saveInstance ? visitor : undefined;
    if (saveInstance) {
      (0, _utils.logDebugSprintf)(sdkConfig, _index2.PROCESS_NEW_VISITOR, _index2.SAVE_VISITOR_INSTANCE, visitor.visitorId);
    }
    if (sdkConfig.fetchNow && sdkConfig.decisionMode !== _index.DecisionMode.BUCKETING_EDGE) {
      visitor.fetchFlags();
    }
    return visitor;
  }
}
exports.Flagship = Flagship;

/***/ }),

/***/ 8227:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var shared = __webpack_require__(5745);
var hasOwn = __webpack_require__(9297);
var uid = __webpack_require__(3392);
var NATIVE_SYMBOL = __webpack_require__(4495);
var USE_SYMBOL_AS_UID = __webpack_require__(7040);

var Symbol = globalThis.Symbol;
var WellKnownSymbolsStore = shared('wks');
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol['for'] || Symbol : Symbol && Symbol.withoutSetter || uid;

module.exports = function (name) {
  if (!hasOwn(WellKnownSymbolsStore, name)) {
    WellKnownSymbolsStore[name] = NATIVE_SYMBOL && hasOwn(Symbol, name)
      ? Symbol[name]
      : createWellKnownSymbol('Symbol.' + name);
  } return WellKnownSymbolsStore[name];
};


/***/ }),

/***/ 8265:
/***/ ((module) => {

"use strict";

var Queue = function () {
  this.head = null;
  this.tail = null;
};

Queue.prototype = {
  add: function (item) {
    var entry = { item: item, next: null };
    var tail = this.tail;
    if (tail) tail.next = entry;
    else this.head = entry;
    this.tail = entry;
  },
  get: function () {
    var entry = this.head;
    if (entry) {
      var next = this.head = entry.next;
      if (next === null) this.tail = null;
      return entry.item;
    }
  }
};

module.exports = Queue;


/***/ }),

/***/ 8388:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.ConfigManager = void 0;
class ConfigManager {
  constructor(config, decisionManager, trackingManager, sharedActionTracking) {
    this._config = config;
    this._decisionManager = decisionManager;
    this._trackingManager = trackingManager;
    this._sharedActionTracking = sharedActionTracking;
  }
  get config() {
    return this._config;
  }
  set config(value) {
    this._config = value;
  }
  get decisionManager() {
    return this._decisionManager;
  }
  set decisionManager(value) {
    this._decisionManager = value;
  }
  get trackingManager() {
    return this._trackingManager;
  }
  set trackingManager(value) {
    this._trackingManager = value;
  }
  get sharedActionTracking() {
    return this._sharedActionTracking;
  }
}
exports.ConfigManager = ConfigManager;

/***/ }),

/***/ 8429:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var globalThis = __webpack_require__(2195);

// babel-minify and Closure Compiler transpiles RegExp('a', 'y') -> /a/y and it causes SyntaxError
var $RegExp = globalThis.RegExp;

var UNSUPPORTED_Y = fails(function () {
  var re = $RegExp('a', 'y');
  re.lastIndex = 2;
  return re.exec('abcd') !== null;
});

// UC Browser bug
// https://github.com/zloirock/core-js/issues/1008
var MISSED_STICKY = UNSUPPORTED_Y || fails(function () {
  return !$RegExp('a', 'y').sticky;
});

var BROKEN_CARET = UNSUPPORTED_Y || fails(function () {
  // https://bugzilla.mozilla.org/show_bug.cgi?id=773687
  var re = $RegExp('^r', 'gy');
  re.lastIndex = 2;
  return re.exec('str') !== null;
});

module.exports = {
  BROKEN_CARET: BROKEN_CARET,
  MISSED_STICKY: MISSED_STICKY,
  UNSUPPORTED_Y: UNSUPPORTED_Y
};


/***/ }),

/***/ 8480:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";

var internalObjectKeys = __webpack_require__(1828);
var enumBugKeys = __webpack_require__(8727);

var hiddenKeys = enumBugKeys.concat('length', 'prototype');

// `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es/no-object-getownpropertynames -- safe
exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
  return internalObjectKeys(O, hiddenKeys);
};


/***/ }),

/***/ 8523:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var collection = __webpack_require__(6468);
var collectionStrong = __webpack_require__(6938);

// `Map` constructor
// https://tc39.es/ecma262/#sec-map-objects
collection('Map', function (init) {
  return function Map() { return init(this, arguments.length ? arguments[0] : undefined); };
}, collectionStrong);


/***/ }),

/***/ 8551:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isObject = __webpack_require__(34);

var $String = String;
var $TypeError = TypeError;

// `Assert: Type(argument) is Object`
module.exports = function (argument) {
  if (isObject(argument)) return argument;
  throw new $TypeError($String(argument) + ' is not an object');
};


/***/ }),

/***/ 8611:
/***/ ((module) => {

"use strict";
module.exports = require("http");

/***/ }),

/***/ 8622:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var isCallable = __webpack_require__(4901);

var WeakMap = globalThis.WeakMap;

module.exports = isCallable(WeakMap) && /native code/.test(String(WeakMap));


/***/ }),

/***/ 8686:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var DESCRIPTORS = __webpack_require__(3724);
var fails = __webpack_require__(9039);

// V8 ~ Chrome 36-
// https://bugs.chromium.org/p/v8/issues/detail?id=3334
module.exports = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(function () { /* empty */ }, 'prototype', {
    value: 42,
    writable: false
  }).prototype !== 42;
});


/***/ }),

/***/ 8727:
/***/ ((module) => {

"use strict";

// IE8- don't enum bug keys
module.exports = [
  'constructor',
  'hasOwnProperty',
  'isPrototypeOf',
  'propertyIsEnumerable',
  'toLocaleString',
  'toString',
  'valueOf'
];


/***/ }),

/***/ 8745:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var NATIVE_BIND = __webpack_require__(616);

var FunctionPrototype = Function.prototype;
var apply = FunctionPrototype.apply;
var call = FunctionPrototype.call;

// eslint-disable-next-line es/no-function-prototype-bind, es/no-reflect -- safe
module.exports = typeof Reflect == 'object' && Reflect.apply || (NATIVE_BIND ? call.bind(apply) : function () {
  return call.apply(apply, arguments);
});


/***/ }),

/***/ 8773:
/***/ ((__unused_webpack_module, exports) => {

"use strict";

var $propertyIsEnumerable = {}.propertyIsEnumerable;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Nashorn ~ JDK8 bug
var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({ 1: 2 }, 1);

// `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
exports.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
  var descriptor = getOwnPropertyDescriptor(this, V);
  return !!descriptor && descriptor.enumerable;
} : $propertyIsEnumerable;


/***/ }),

/***/ 8796:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.VALUE_FIELD_ERROR = exports.Event = exports.ERROR_MESSAGE = exports.CATEGORY_ERROR = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
__webpack_require__(4423);
__webpack_require__(6034);
__webpack_require__(1699);
var _FlagshipConstant = __webpack_require__(2932);
var _HitType = __webpack_require__(6147);
var _utils = __webpack_require__(6517);
var _EventCategory = __webpack_require__(568);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'event category and event action are required';
const CATEGORY_ERROR = exports.CATEGORY_ERROR = 'The category value must be either EventCategory::ACTION_TRACKING or EventCategory::ACTION_TRACKING';
const VALUE_FIELD_ERROR = exports.VALUE_FIELD_ERROR = 'value must be an integer and be >= 0';
class Event extends _HitAbstract.HitAbstract {
  get category() {
    return this._category;
  }

  /**
   * Specify Action Tracking or User Engagement.
   */
  set category(v) {
    if (!Object.values(_EventCategory.EventCategory).includes(v)) {
      (0, _utils.logError)(this.config, CATEGORY_ERROR, 'category');
      return;
    }
    this._category = v;
  }
  get action() {
    return this._action;
  }

  /**
   * Specify Event name that will also serve as the KPI
   * that you will have inside your reporting
   */
  set action(v) {
    if (!this.isNotEmptyString(v, 'action')) {
      return;
    }
    this._action = v;
  }
  get label() {
    return this._label;
  }

  /**
   * Specify additional description of event.
   */
  set label(v) {
    if (!this.isNotEmptyString(v, 'label')) {
      return;
    }
    this._label = v;
  }
  get value() {
    return this._value;
  }

  /**
   * Specify the monetary value associated with an event
   * (e.g. you earn 10 to 100 euros depending on the quality of lead generated).
   *
   * <br/> NOTE: this value must be non-negative.
   */
  set value(v) {
    if (!Number.isInteger(v) || v < 0) {
      (0, _utils.logError)(this.config, VALUE_FIELD_ERROR, 'value');
      return;
    }
    this._value = v;
  }
  constructor(param) {
    super({
      type: _HitType.HitType.EVENT,
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode,
      isActionTrackingHit: param.isActionTrackingHit
    });
    const category = param.category,
      action = param.action,
      label = param.label,
      value = param.value;
    this.category = category;
    this.action = action;
    if (label) {
      this.label = label;
    }
    if (value) {
      this.value = value;
    }
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  toApiKeys() {
    const apiKeys = super.toApiKeys();
    apiKeys[_FlagshipConstant.EVENT_CATEGORY_API_ITEM] = this.category;
    apiKeys[_FlagshipConstant.EVENT_ACTION_API_ITEM] = this.action;
    if (this.label) {
      apiKeys[_FlagshipConstant.EVENT_LABEL_API_ITEM] = this.label;
    }
    if (this.value) {
      apiKeys[_FlagshipConstant.EVENT_VALUE_API_ITEM] = this.value;
    }
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      category: this.category,
      action: this.action,
      label: this.label,
      value: this.value
    });
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.category && this.action);
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Event = Event;

/***/ }),

/***/ 8814:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var fails = __webpack_require__(9039);
var globalThis = __webpack_require__(2195);

// babel-minify and Closure Compiler transpiles RegExp('(?<a>b)', 'g') -> /(?<a>b)/g and it causes SyntaxError
var $RegExp = globalThis.RegExp;

module.exports = fails(function () {
  var re = $RegExp('(?<a>b)', 'g');
  return re.exec('b').groups.a !== 'b' ||
    'b'.replace(re, '$<a>c') !== 'bc';
});


/***/ }),

/***/ 8858:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.HttpError = void 0;
class HttpError extends Error {
  get headers() {
    return this._headers;
  }
  get statusCode() {
    return this._statusCode;
  }
  constructor(statusCode, message, headers) {
    super(message);
    this._statusCode = statusCode;
    this._headers = headers;
  }
}
exports.HttpError = HttpError;

/***/ }),

/***/ 8884:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.DecisionMode = void 0;
let DecisionMode = exports.DecisionMode = /*#__PURE__*/function (DecisionMode) {
  /**
   *   /**
   * Flagship SDK mode decision api
   */
  DecisionMode["DECISION_API"] = "DECISION-API";
  /**
   * Flagship SDK mode bucketing
   */
  DecisionMode["BUCKETING"] = "BUCKETING";
  DecisionMode["BUCKETING_EDGE"] = "BUCKETING_EDGE";
  return DecisionMode;
}({});

/***/ }),

/***/ 8889:
/***/ ((__unused_webpack_module, exports) => {

"use strict";


exports.__esModule = true;
exports.FSFlagMetadata = void 0;
class FSFlagMetadata {
  constructor(flagMetadata) {
    const campaignId = flagMetadata.campaignId,
      variationGroupId = flagMetadata.variationGroupId,
      variationId = flagMetadata.variationId,
      isReference = flagMetadata.isReference,
      campaignType = flagMetadata.campaignType,
      slug = flagMetadata.slug,
      variationGroupName = flagMetadata.variationGroupName,
      variationName = flagMetadata.variationName,
      campaignName = flagMetadata.campaignName;
    this.campaignId = campaignId;
    this.variationGroupId = variationGroupId;
    this.variationId = variationId;
    this.isReference = isReference;
    this.campaignType = campaignType;
    this.campaignName = campaignName;
    this.variationGroupName = variationGroupName;
    this.variationName = variationName;
    this.slug = slug;
  }
  static Empty() {
    return new FSFlagMetadata({
      campaignId: '',
      campaignName: '',
      campaignType: '',
      variationId: '',
      variationName: '',
      variationGroupId: '',
      variationGroupName: '',
      isReference: false,
      slug: null
    });
  }
}
exports.FSFlagMetadata = FSFlagMetadata;

/***/ }),

/***/ 8981:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var requireObjectCoercible = __webpack_require__(7750);

var $Object = Object;

// `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject
module.exports = function (argument) {
  return $Object(requireObjectCoercible(argument));
};


/***/ }),

/***/ 9039:
/***/ ((module) => {

"use strict";

module.exports = function (exec) {
  try {
    return !!exec();
  } catch (error) {
    return true;
  }
};


/***/ }),

/***/ 9045:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

var _typeof = (__webpack_require__(3738)["default"]);
function toPrimitive(t, r) {
  if ("object" != _typeof(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != _typeof(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
module.exports = toPrimitive, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 9079:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


exports.__esModule = true;
exports.HitAbstract = void 0;
var _FlagshipConstant = __webpack_require__(2932);
var _utils = __webpack_require__(6517);
class HitAbstract {
  get isActionTrackingHit() {
    return this._isActionTrackingHit;
  }
  set isActionTrackingHit(v) {
    this._isActionTrackingHit = v;
  }
  get key() {
    return this._key;
  }
  set key(v) {
    this._key = v;
  }
  get sessionNumber() {
    return this._sessionNumber;
  }
  set sessionNumber(v) {
    this._sessionNumber = v;
  }
  get locale() {
    return this._locale;
  }
  set locale(v) {
    this._locale = v;
  }
  get screenResolution() {
    return this._screenResolution;
  }
  set screenResolution(v) {
    this._screenResolution = v;
  }
  get userIp() {
    return this._userIp;
  }
  set userIp(v) {
    this._userIp = v;
  }
  get anonymousId() {
    return this._anonymousId;
  }
  set anonymousId(v) {
    this._anonymousId = v;
  }
  get visitorId() {
    return this._visitorId;
  }
  set visitorId(v) {
    this._visitorId = v;
  }
  get ds() {
    return this._ds;
  }
  set ds(v) {
    this._ds = v;
  }
  get type() {
    return this._type;
  }
  get config() {
    return this._config;
  }
  set config(v) {
    this._config = v;
  }
  get createdAt() {
    return this._createdAt;
  }
  set createdAt(v) {
    this._createdAt = v;
  }
  get qaMode() {
    return this._qaMode;
  }
  set qaMode(v) {
    this._qaMode = v;
  }
  constructor(hit) {
    const type = hit.type,
      userIp = hit.userIp,
      screenResolution = hit.screenResolution,
      locale = hit.locale,
      sessionNumber = hit.sessionNumber,
      visitorId = hit.visitorId,
      anonymousId = hit.anonymousId,
      ds = hit.ds,
      qaMode = hit.qaMode,
      isActionTrackingHit = hit.isActionTrackingHit;
    this._type = type;
    if (userIp) {
      this.userIp = userIp;
    }
    if (screenResolution) {
      this.screenResolution = screenResolution;
    }
    if (locale) {
      this.locale = locale;
    }
    if (sessionNumber) {
      this.sessionNumber = sessionNumber;
    }
    this.visitorId = visitorId;
    this._anonymousId = anonymousId || null;
    this.createdAt = Date.now();
    this.ds = ds || _FlagshipConstant.SDK_APP;
    this.qaMode = qaMode;
    this.isActionTrackingHit = isActionTrackingHit;
  }

  /**
   * Return true if value is a string and not empty, otherwise return false
   * @param value
   * @param itemName
   * @returns
   */
  isNotEmptyString(value, itemName) {
    if (!value || typeof value !== 'string') {
      (0, _utils.logError)(this.config, (0, _utils.sprintf)(_FlagshipConstant.TYPE_ERROR, itemName, 'string'), itemName);
      return false;
    }
    return true;
  }
  isNumeric(value, itemName) {
    if (!value || typeof value !== 'number') {
      (0, _utils.logError)(this.config, (0, _utils.sprintf)(_FlagshipConstant.TYPE_ERROR, itemName, 'number'), itemName);
      return false;
    }
    return true;
  }
  isInteger(value, itemName) {
    if (!Number.isInteger(value)) {
      (0, _utils.logError)(this.config, (0, _utils.sprintf)(_FlagshipConstant.TYPE_ERROR, itemName, 'integer'), itemName);
      return false;
    }
    return true;
  }

  /**
   * Return an object with Api parameters as keys
   */

  toApiKeys() {
    var _this$config;
    const apiKeys = {
      [_FlagshipConstant.VISITOR_ID_API_ITEM]: this.visitorId,
      [_FlagshipConstant.DS_API_ITEM]: this.ds,
      [_FlagshipConstant.CUSTOMER_ENV_ID_API_ITEM]: `${(_this$config = this.config) == null ? void 0 : _this$config.envId}`,
      [_FlagshipConstant.T_API_ITEM]: this.type,
      [_FlagshipConstant.CUSTOMER_UID]: null,
      [_FlagshipConstant.QT_API_ITEM]: Date.now() - this._createdAt
    };
    if (this.qaMode) {
      apiKeys[_FlagshipConstant.QA_MODE_API_ITEM] = true;
    }
    if (this.userIp) {
      apiKeys[_FlagshipConstant.USER_IP_API_ITEM] = this.userIp;
    }
    if (this.screenResolution) {
      apiKeys[_FlagshipConstant.SCREEN_RESOLUTION_API_ITEM] = this.screenResolution;
    }
    if (this.locale) {
      apiKeys[_FlagshipConstant.USER_LANGUAGE] = this.locale;
    }
    if (this.sessionNumber) {
      apiKeys[_FlagshipConstant.SESSION_NUMBER] = this.sessionNumber;
    }
    if (this.visitorId && this.anonymousId) {
      apiKeys[_FlagshipConstant.VISITOR_ID_API_ITEM] = this.anonymousId;
      apiKeys[_FlagshipConstant.CUSTOMER_UID] = this.visitorId;
    }
    return apiKeys;
  }
  toObject() {
    return {
      key: this.key,
      visitorId: this.visitorId,
      ds: this.ds,
      type: this.type,
      userIp: this.userIp,
      screenResolution: this.screenResolution,
      locale: this.locale,
      sessionNumber: this.sessionNumber,
      anonymousId: this.anonymousId,
      createdAt: this.createdAt,
      qaMode: this.qaMode
    };
  }

  /**
   * Return true if all required attributes are given, otherwise return false
   */

  isReady(_checkParent = true) {
    return !!(this.visitorId && this.ds && this.config && this.config.envId && this.type);
  }

  /**
   * This function return the error message according to required attributes of class
   *
   */
}
exports.HitAbstract = HitAbstract;

/***/ }),

/***/ 9102:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.VisitorDelegate = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
var _VisitorAbstract = __webpack_require__(2623);
var _FsFlags = __webpack_require__(4707);
var _utils = __webpack_require__(6517);
var _FlagshipConstant = __webpack_require__(2932);
var _FSFetchStatus = __webpack_require__(1597);
var _FSFlagCollection = __webpack_require__(448);
class VisitorDelegate extends _VisitorAbstract.VisitorAbstract {
  updateContext(context, value) {
    this.getStrategy().updateContext(context, value);
    this.loadPredefinedContext();
  }
  clearContext() {
    this.getStrategy().clearContext();
  }
  getFlag(key) {
    if (this.flagsStatus.status !== _FSFetchStatus.FSFetchStatus.FETCHED && this.flagsStatus.status !== _FSFetchStatus.FSFetchStatus.FETCHING && this.flagsStatus.status !== _FSFetchStatus.FSFetchStatus.INITIAL_DATA) {
      (0, _utils.logWarningSprintf)(this.config, _FlagshipConstant.GET_FLAG, (0, _utils.visitorFlagSyncStatusMessage)(this.flagsStatus.reason), this.visitorId, key);
    }
    return new _FsFlags.FSFlag({
      key,
      visitor: this
    });
  }
  getFlags() {
    return new _FSFlagCollection.FSFlagCollection({
      visitor: this
    });
  }
  sendHit(hit) {
    return this.getStrategy().sendHit(hit);
  }
  sendHits(hits) {
    return this.getStrategy().sendHits(hits);
  }
  authenticate(visitorId) {
    this.getStrategy().authenticate(visitorId);
    this.updateCache();
  }
  unauthenticate() {
    this.getStrategy().unauthenticate();
    this.updateCache();
  }
  fetchFlags() {
    var _this = this;
    return (0, _asyncToGenerator2.default)(function* () {
      yield _this.getStrategy().fetchFlags();
    })();
  }
  visitorExposed(param) {
    return this.getStrategy().visitorExposed(param);
  }
  getFlagValue(param) {
    return this.getStrategy().getFlagValue(param);
  }
  getFlagMetadata(param) {
    return this.getStrategy().getFlagMetadata(param);
  }
}
exports.VisitorDelegate = VisitorDelegate;

/***/ }),

/***/ 9213:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var bind = __webpack_require__(6080);
var uncurryThis = __webpack_require__(9504);
var IndexedObject = __webpack_require__(7055);
var toObject = __webpack_require__(8981);
var lengthOfArrayLike = __webpack_require__(6198);
var arraySpeciesCreate = __webpack_require__(1469);

var push = uncurryThis([].push);

// `Array.prototype.{ forEach, map, filter, some, every, find, findIndex, filterReject }` methods implementation
var createMethod = function (TYPE) {
  var IS_MAP = TYPE === 1;
  var IS_FILTER = TYPE === 2;
  var IS_SOME = TYPE === 3;
  var IS_EVERY = TYPE === 4;
  var IS_FIND_INDEX = TYPE === 6;
  var IS_FILTER_REJECT = TYPE === 7;
  var NO_HOLES = TYPE === 5 || IS_FIND_INDEX;
  return function ($this, callbackfn, that, specificCreate) {
    var O = toObject($this);
    var self = IndexedObject(O);
    var length = lengthOfArrayLike(self);
    var boundFunction = bind(callbackfn, that);
    var index = 0;
    var create = specificCreate || arraySpeciesCreate;
    var target = IS_MAP ? create($this, length) : IS_FILTER || IS_FILTER_REJECT ? create($this, 0) : undefined;
    var value, result;
    for (;length > index; index++) if (NO_HOLES || index in self) {
      value = self[index];
      result = boundFunction(value, index, O);
      if (TYPE) {
        if (IS_MAP) target[index] = result; // map
        else if (result) switch (TYPE) {
          case 3: return true;              // some
          case 5: return value;             // find
          case 6: return index;             // findIndex
          case 2: push(target, value);      // filter
        } else switch (TYPE) {
          case 4: return false;             // every
          case 7: push(target, value);      // filterReject
        }
      }
    }
    return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
  };
};

module.exports = {
  // `Array.prototype.forEach` method
  // https://tc39.es/ecma262/#sec-array.prototype.foreach
  forEach: createMethod(0),
  // `Array.prototype.map` method
  // https://tc39.es/ecma262/#sec-array.prototype.map
  map: createMethod(1),
  // `Array.prototype.filter` method
  // https://tc39.es/ecma262/#sec-array.prototype.filter
  filter: createMethod(2),
  // `Array.prototype.some` method
  // https://tc39.es/ecma262/#sec-array.prototype.some
  some: createMethod(3),
  // `Array.prototype.every` method
  // https://tc39.es/ecma262/#sec-array.prototype.every
  every: createMethod(4),
  // `Array.prototype.find` method
  // https://tc39.es/ecma262/#sec-array.prototype.find
  find: createMethod(5),
  // `Array.prototype.findIndex` method
  // https://tc39.es/ecma262/#sec-array.prototype.findIndex
  findIndex: createMethod(6),
  // `Array.prototype.filterReject` method
  // https://github.com/tc39/proposal-array-filtering
  filterReject: createMethod(7)
};


/***/ }),

/***/ 9225:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var apply = __webpack_require__(8745);
var bind = __webpack_require__(6080);
var isCallable = __webpack_require__(4901);
var hasOwn = __webpack_require__(9297);
var fails = __webpack_require__(9039);
var html = __webpack_require__(397);
var arraySlice = __webpack_require__(7680);
var createElement = __webpack_require__(4055);
var validateArgumentsLength = __webpack_require__(2812);
var IS_IOS = __webpack_require__(9544);
var IS_NODE = __webpack_require__(6193);

var set = globalThis.setImmediate;
var clear = globalThis.clearImmediate;
var process = globalThis.process;
var Dispatch = globalThis.Dispatch;
var Function = globalThis.Function;
var MessageChannel = globalThis.MessageChannel;
var String = globalThis.String;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var $location, defer, channel, port;

fails(function () {
  // Deno throws a ReferenceError on `location` access without `--location` flag
  $location = globalThis.location;
});

var run = function (id) {
  if (hasOwn(queue, id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};

var runner = function (id) {
  return function () {
    run(id);
  };
};

var eventListener = function (event) {
  run(event.data);
};

var globalPostMessageDefer = function (id) {
  // old engines have not location.origin
  globalThis.postMessage(String(id), $location.protocol + '//' + $location.host);
};

// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
if (!set || !clear) {
  set = function setImmediate(handler) {
    validateArgumentsLength(arguments.length, 1);
    var fn = isCallable(handler) ? handler : Function(handler);
    var args = arraySlice(arguments, 1);
    queue[++counter] = function () {
      apply(fn, undefined, args);
    };
    defer(counter);
    return counter;
  };
  clear = function clearImmediate(id) {
    delete queue[id];
  };
  // Node.js 0.8-
  if (IS_NODE) {
    defer = function (id) {
      process.nextTick(runner(id));
    };
  // Sphere (JS game engine) Dispatch API
  } else if (Dispatch && Dispatch.now) {
    defer = function (id) {
      Dispatch.now(runner(id));
    };
  // Browsers with MessageChannel, includes WebWorkers
  // except iOS - https://github.com/zloirock/core-js/issues/624
  } else if (MessageChannel && !IS_IOS) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = eventListener;
    defer = bind(port.postMessage, port);
  // Browsers with postMessage, skip WebWorkers
  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (
    globalThis.addEventListener &&
    isCallable(globalThis.postMessage) &&
    !globalThis.importScripts &&
    $location && $location.protocol !== 'file:' &&
    !fails(globalPostMessageDefer)
  ) {
    defer = globalPostMessageDefer;
    globalThis.addEventListener('message', eventListener, false);
  // IE8-
  } else if (ONREADYSTATECHANGE in createElement('script')) {
    defer = function (id) {
      html.appendChild(createElement('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run(id);
      };
    };
  // Rest old browsers
  } else {
    defer = function (id) {
      setTimeout(runner(id), 0);
    };
  }
}

module.exports = {
  set: set,
  clear: clear
};


/***/ }),

/***/ 9228:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

// TODO: Remove from `core-js@4` since it's moved to entry points
__webpack_require__(7495);
var call = __webpack_require__(9565);
var defineBuiltIn = __webpack_require__(6840);
var regexpExec = __webpack_require__(7323);
var fails = __webpack_require__(9039);
var wellKnownSymbol = __webpack_require__(8227);
var createNonEnumerableProperty = __webpack_require__(6699);

var SPECIES = wellKnownSymbol('species');
var RegExpPrototype = RegExp.prototype;

module.exports = function (KEY, exec, FORCED, SHAM) {
  var SYMBOL = wellKnownSymbol(KEY);

  var DELEGATES_TO_SYMBOL = !fails(function () {
    // String methods call symbol-named RegExp methods
    var O = {};
    O[SYMBOL] = function () { return 7; };
    return ''[KEY](O) !== 7;
  });

  var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL && !fails(function () {
    // Symbol-named RegExp methods call .exec
    var execCalled = false;
    var re = /a/;

    if (KEY === 'split') {
      // We can't use real regex here since it causes deoptimization
      // and serious performance degradation in V8
      // https://github.com/zloirock/core-js/issues/306
      re = {};
      // RegExp[@@split] doesn't call the regex's exec method, but first creates
      // a new one. We need to return the patched regex when creating the new one.
      re.constructor = {};
      re.constructor[SPECIES] = function () { return re; };
      re.flags = '';
      re[SYMBOL] = /./[SYMBOL];
    }

    re.exec = function () {
      execCalled = true;
      return null;
    };

    re[SYMBOL]('');
    return !execCalled;
  });

  if (
    !DELEGATES_TO_SYMBOL ||
    !DELEGATES_TO_EXEC ||
    FORCED
  ) {
    var nativeRegExpMethod = /./[SYMBOL];
    var methods = exec(SYMBOL, ''[KEY], function (nativeMethod, regexp, str, arg2, forceStringMethod) {
      var $exec = regexp.exec;
      if ($exec === regexpExec || $exec === RegExpPrototype.exec) {
        if (DELEGATES_TO_SYMBOL && !forceStringMethod) {
          // The native String method already delegates to @@method (this
          // polyfilled function), leasing to infinite recursion.
          // We avoid it by directly calling the native @@method method.
          return { done: true, value: call(nativeRegExpMethod, regexp, str, arg2) };
        }
        return { done: true, value: call(nativeMethod, str, regexp, arg2) };
      }
      return { done: false };
    });

    defineBuiltIn(String.prototype, KEY, methods[0]);
    defineBuiltIn(RegExpPrototype, SYMBOL, methods[1]);
  }

  if (SHAM) createNonEnumerableProperty(RegExpPrototype[SYMBOL], 'sham', true);
};


/***/ }),

/***/ 9293:
/***/ ((module) => {

function asyncGeneratorStep(n, t, e, r, o, a, c) {
  try {
    var i = n[a](c),
      u = i.value;
  } catch (n) {
    return void e(n);
  }
  i.done ? t(u) : Promise.resolve(u).then(r, o);
}
function _asyncToGenerator(n) {
  return function () {
    var t = this,
      e = arguments;
    return new Promise(function (r, o) {
      var a = n.apply(t, e);
      function _next(n) {
        asyncGeneratorStep(a, r, o, _next, _throw, "next", n);
      }
      function _throw(n) {
        asyncGeneratorStep(a, r, o, _next, _throw, "throw", n);
      }
      _next(void 0);
    });
  };
}
module.exports = _asyncToGenerator, module.exports.__esModule = true, module.exports["default"] = module.exports;

/***/ }),

/***/ 9297:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var uncurryThis = __webpack_require__(9504);
var toObject = __webpack_require__(8981);

var hasOwnProperty = uncurryThis({}.hasOwnProperty);

// `HasOwnProperty` abstract operation
// https://tc39.es/ecma262/#sec-hasownproperty
// eslint-disable-next-line es/no-object-hasown -- safe
module.exports = Object.hasOwn || function hasOwn(it, key) {
  return hasOwnProperty(toObject(it), key);
};


/***/ }),

/***/ 9306:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var isCallable = __webpack_require__(4901);
var tryToString = __webpack_require__(6823);

var $TypeError = TypeError;

// `Assert: IsCallable(argument) is true`
module.exports = function (argument) {
  if (isCallable(argument)) return argument;
  throw new $TypeError(tryToString(argument) + ' is not a function');
};


/***/ }),

/***/ 9433:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);

// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;

module.exports = function (key, value) {
  try {
    defineProperty(globalThis, key, { value: value, configurable: true, writable: true });
  } catch (error) {
    globalThis[key] = value;
  } return value;
};


/***/ }),

/***/ 9449:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var $ = __webpack_require__(6518);
var uncurryThis = __webpack_require__(7476);
var getOwnPropertyDescriptor = (__webpack_require__(7347).f);
var toLength = __webpack_require__(8014);
var toString = __webpack_require__(655);
var notARegExp = __webpack_require__(511);
var requireObjectCoercible = __webpack_require__(7750);
var correctIsRegExpLogic = __webpack_require__(1436);
var IS_PURE = __webpack_require__(6395);

var slice = uncurryThis(''.slice);
var min = Math.min;

var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic('endsWith');
// https://github.com/zloirock/core-js/pull/702
var MDN_POLYFILL_BUG = !IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function () {
  var descriptor = getOwnPropertyDescriptor(String.prototype, 'endsWith');
  return descriptor && !descriptor.writable;
}();

// `String.prototype.endsWith` method
// https://tc39.es/ecma262/#sec-string.prototype.endswith
$({ target: 'String', proto: true, forced: !MDN_POLYFILL_BUG && !CORRECT_IS_REGEXP_LOGIC }, {
  endsWith: function endsWith(searchString /* , endPosition = @length */) {
    var that = toString(requireObjectCoercible(this));
    notARegExp(searchString);
    var endPosition = arguments.length > 1 ? arguments[1] : undefined;
    var len = that.length;
    var end = endPosition === undefined ? len : min(toLength(endPosition), len);
    var search = toString(searchString);
    return slice(that, end - search.length, end) === search;
  }
});


/***/ }),

/***/ 9463:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
// `Symbol.prototype.description` getter
// https://tc39.es/ecma262/#sec-symbol.prototype.description

var $ = __webpack_require__(6518);
var DESCRIPTORS = __webpack_require__(3724);
var globalThis = __webpack_require__(2195);
var uncurryThis = __webpack_require__(9504);
var hasOwn = __webpack_require__(9297);
var isCallable = __webpack_require__(4901);
var isPrototypeOf = __webpack_require__(1625);
var toString = __webpack_require__(655);
var defineBuiltInAccessor = __webpack_require__(2106);
var copyConstructorProperties = __webpack_require__(7740);

var NativeSymbol = globalThis.Symbol;
var SymbolPrototype = NativeSymbol && NativeSymbol.prototype;

if (DESCRIPTORS && isCallable(NativeSymbol) && (!('description' in SymbolPrototype) ||
  // Safari 12 bug
  NativeSymbol().description !== undefined
)) {
  var EmptyStringDescriptionStore = {};
  // wrap Symbol constructor for correct work with undefined description
  var SymbolWrapper = function Symbol() {
    var description = arguments.length < 1 || arguments[0] === undefined ? undefined : toString(arguments[0]);
    var result = isPrototypeOf(SymbolPrototype, this)
      // eslint-disable-next-line sonarjs/inconsistent-function-call -- ok
      ? new NativeSymbol(description)
      // in Edge 13, String(Symbol(undefined)) === 'Symbol(undefined)'
      : description === undefined ? NativeSymbol() : NativeSymbol(description);
    if (description === '') EmptyStringDescriptionStore[result] = true;
    return result;
  };

  copyConstructorProperties(SymbolWrapper, NativeSymbol);
  SymbolWrapper.prototype = SymbolPrototype;
  SymbolPrototype.constructor = SymbolWrapper;

  var NATIVE_SYMBOL = String(NativeSymbol('description detection')) === 'Symbol(description detection)';
  var thisSymbolValue = uncurryThis(SymbolPrototype.valueOf);
  var symbolDescriptiveString = uncurryThis(SymbolPrototype.toString);
  var regexp = /^Symbol\((.*)\)[^)]+$/;
  var replace = uncurryThis(''.replace);
  var stringSlice = uncurryThis(''.slice);

  defineBuiltInAccessor(SymbolPrototype, 'description', {
    configurable: true,
    get: function description() {
      var symbol = thisSymbolValue(this);
      if (hasOwn(EmptyStringDescriptionStore, symbol)) return '';
      var string = symbolDescriptiveString(symbol);
      var desc = NATIVE_SYMBOL ? stringSlice(string, 7, -1) : replace(string, regexp, '$1');
      return desc === '' ? undefined : desc;
    }
  });

  $({ global: true, constructor: true, forced: true }, {
    Symbol: SymbolWrapper
  });
}


/***/ }),

/***/ 9479:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var DESCRIPTORS = __webpack_require__(3724);
var defineBuiltInAccessor = __webpack_require__(2106);
var regExpFlags = __webpack_require__(7979);
var fails = __webpack_require__(9039);

// babel-minify and Closure Compiler transpiles RegExp('.', 'd') -> /./d and it causes SyntaxError
var RegExp = globalThis.RegExp;
var RegExpPrototype = RegExp.prototype;

var FORCED = DESCRIPTORS && fails(function () {
  var INDICES_SUPPORT = true;
  try {
    RegExp('.', 'd');
  } catch (error) {
    INDICES_SUPPORT = false;
  }

  var O = {};
  // modern V8 bug
  var calls = '';
  var expected = INDICES_SUPPORT ? 'dgimsy' : 'gimsy';

  var addGetter = function (key, chr) {
    // eslint-disable-next-line es/no-object-defineproperty -- safe
    Object.defineProperty(O, key, { get: function () {
      calls += chr;
      return true;
    } });
  };

  var pairs = {
    dotAll: 's',
    global: 'g',
    ignoreCase: 'i',
    multiline: 'm',
    sticky: 'y'
  };

  if (INDICES_SUPPORT) pairs.hasIndices = 'd';

  for (var key in pairs) addGetter(key, pairs[key]);

  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var result = Object.getOwnPropertyDescriptor(RegExpPrototype, 'flags').get.call(O);

  return result !== expected || calls !== expected;
});

// `RegExp.prototype.flags` getter
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags
if (FORCED) defineBuiltInAccessor(RegExpPrototype, 'flags', {
  configurable: true,
  get: regExpFlags
});


/***/ }),

/***/ 9504:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var NATIVE_BIND = __webpack_require__(616);

var FunctionPrototype = Function.prototype;
var call = FunctionPrototype.call;
// eslint-disable-next-line es/no-function-prototype-bind -- safe
var uncurryThisWithBind = NATIVE_BIND && FunctionPrototype.bind.bind(call, call);

module.exports = NATIVE_BIND ? uncurryThisWithBind : function (fn) {
  return function () {
    return call.apply(fn, arguments);
  };
};


/***/ }),

/***/ 9519:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var globalThis = __webpack_require__(2195);
var userAgent = __webpack_require__(2839);

var process = globalThis.process;
var Deno = globalThis.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;

if (v8) {
  match = v8.split('.');
  // in old Chrome, versions of V8 isn't V8 = Chrome / 10
  // but their correct versions are not interesting for us
  version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
}

// BrowserFS NodeJS `process` polyfill incorrectly set `.v8` to `0.0`
// so check `userAgent` even if `.v8` exists, but 0
if (!version && userAgent) {
  match = userAgent.match(/Edge\/(\d+)/);
  if (!match || match[1] >= 74) {
    match = userAgent.match(/Chrome\/(\d+)/);
    if (match) version = +match[1];
  }
}

module.exports = version;


/***/ }),

/***/ 9539:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var call = __webpack_require__(9565);
var anObject = __webpack_require__(8551);
var getMethod = __webpack_require__(5966);

module.exports = function (iterator, kind, value) {
  var innerResult, innerError;
  anObject(iterator);
  try {
    innerResult = getMethod(iterator, 'return');
    if (!innerResult) {
      if (kind === 'throw') throw value;
      return value;
    }
    innerResult = call(innerResult, iterator);
  } catch (error) {
    innerError = true;
    innerResult = error;
  }
  if (kind === 'throw') throw value;
  if (innerError) throw innerResult;
  anObject(innerResult);
  return value;
};


/***/ }),

/***/ 9544:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var userAgent = __webpack_require__(2839);

// eslint-disable-next-line redos/no-vulnerable -- safe
module.exports = /(?:ipad|iphone|ipod).*applewebkit/i.test(userAgent);


/***/ }),

/***/ 9565:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var NATIVE_BIND = __webpack_require__(616);

var call = Function.prototype.call;
// eslint-disable-next-line es/no-function-prototype-bind -- safe
module.exports = NATIVE_BIND ? call.bind(call) : function () {
  return call.apply(call, arguments);
};


/***/ }),

/***/ 9617:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";

var toIndexedObject = __webpack_require__(5397);
var toAbsoluteIndex = __webpack_require__(5610);
var lengthOfArrayLike = __webpack_require__(6198);

// `Array.prototype.{ indexOf, includes }` methods implementation
var createMethod = function (IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIndexedObject($this);
    var length = lengthOfArrayLike(O);
    if (length === 0) return !IS_INCLUDES && -1;
    var index = toAbsoluteIndex(fromIndex, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare -- NaN check
    if (IS_INCLUDES && el !== el) while (length > index) {
      value = O[index++];
      // eslint-disable-next-line no-self-compare -- NaN check
      if (value !== value) return true;
    // Array#indexOf ignores holes, Array#includes - not
    } else for (;length > index; index++) {
      if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
    } return !IS_INCLUDES && -1;
  };
};

module.exports = {
  // `Array.prototype.includes` method
  // https://tc39.es/ecma262/#sec-array.prototype.includes
  includes: createMethod(true),
  // `Array.prototype.indexOf` method
  // https://tc39.es/ecma262/#sec-array.prototype.indexof
  indexOf: createMethod(false)
};


/***/ }),

/***/ 9807:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.EmotionAI = void 0;
var _asyncToGenerator2 = _interopRequireDefault(__webpack_require__(9293));
class EmotionAI {
  constructor(_params) {
    //
  }
  init() {
    //
  }
  reportPageView() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  onEAICollectStatusChange() {
    //
  }
  startCollectingEAIData() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  reportVisitorEvent() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
  cleanup() {
    //
  }
  removeListeners() {
    //
  }
  fetchEAIScore() {
    return (0, _asyncToGenerator2.default)(function* () {
      return undefined;
    })();
  }
  collectEAIEventsAsync() {
    return (0, _asyncToGenerator2.default)(function* () {})();
  } //
}
exports.EmotionAI = EmotionAI;

/***/ }),

/***/ 9995:
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";


__webpack_require__(2008);
__webpack_require__(1278);
var _interopRequireDefault = __webpack_require__(4994);
exports.__esModule = true;
exports.ERROR_MESSAGE = exports.Activate = void 0;
var _defineProperty2 = _interopRequireDefault(__webpack_require__(3693));
var _index = __webpack_require__(5394);
var _HitAbstract = __webpack_require__(9079);
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
const ERROR_MESSAGE = exports.ERROR_MESSAGE = 'variationGroupId and variationId are required';
class Activate extends _HitAbstract.HitAbstract {
  constructor(param) {
    super({
      type: 'ACTIVATE',
      userIp: param.userIp,
      screenResolution: param.screenResolution,
      locale: param.locale,
      sessionNumber: param.sessionNumber,
      visitorId: param.visitorId,
      anonymousId: param.anonymousId,
      qaMode: param.qaMode
    });
    const variationGroupId = param.variationGroupId,
      variationId = param.variationId,
      flagKey = param.flagKey,
      flagValue = param.flagValue,
      flagDefaultValue = param.flagDefaultValue,
      flagMetadata = param.flagMetadata,
      visitorContext = param.visitorContext;
    this.variationGroupId = variationGroupId;
    this.variationId = variationId;
    this.flagKey = flagKey;
    this.flagValue = flagValue;
    this.flagDefaultValue = flagDefaultValue;
    this.flagMetadata = flagMetadata;
    this.visitorContext = visitorContext;
  }
  get variationGroupId() {
    return this._variationGroupId;
  }
  set variationGroupId(v) {
    this._variationGroupId = v;
  }
  get variationId() {
    return this._variationId;
  }
  set variationId(v) {
    this._variationId = v;
  }
  get flagKey() {
    return this._flagKey;
  }
  set flagKey(v) {
    this._flagKey = v;
  }
  get flagValue() {
    return this._flagValue;
  }
  set flagValue(v) {
    this._flagValue = v;
  }
  get flagDefaultValue() {
    return this._flagDefaultValue;
  }
  set flagDefaultValue(v) {
    this._flagDefaultValue = v;
  }
  get flagMetadata() {
    return this._flagMetadata;
  }
  set flagMetadata(v) {
    this._flagMetadata = v;
  }
  get visitorContext() {
    return this._visitorContext;
  }
  set visitorContext(v) {
    this._visitorContext = v;
  }
  isReady(checkParent = true) {
    return !!((!checkParent || super.isReady()) && this.variationGroupId && this.variationId);
  }
  toApiKeys() {
    const apiKeys = {
      [_index.VISITOR_ID_API_ITEM]: this.anonymousId || this.visitorId,
      [_index.VARIATION_ID_API_ITEM]: this.variationId,
      [_index.VARIATION_GROUP_ID_API_ITEM_ACTIVATE]: this.variationGroupId,
      [_index.CUSTOMER_ENV_ID_API_ACTIVATE]: this.config.envId,
      [_index.ANONYMOUS_ID]: null,
      [_index.QT_API_ITEM]: Date.now() - this.createdAt
    };
    if (this.visitorId && this.anonymousId) {
      apiKeys[_index.VISITOR_ID_API_ITEM] = this.visitorId;
      apiKeys[_index.ANONYMOUS_ID] = this.anonymousId;
    }
    if (this.qaMode) {
      apiKeys[_index.QA_MODE_API_ITEM] = this.qaMode;
    }
    return apiKeys;
  }
  toObject() {
    return _objectSpread(_objectSpread({}, super.toObject()), {}, {
      variationGroupId: this.variationGroupId,
      variationId: this.variationId
    });
  }
  getErrorMessage() {
    return ERROR_MESSAGE;
  }
}
exports.Activate = Activate;

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	var __webpack_exports__ = __webpack_require__(5578);
/******/ 	module.exports = __webpack_exports__;
/******/ 	
/******/ })()
;
//# sourceMappingURL=index.node.commonjs.cjs.map