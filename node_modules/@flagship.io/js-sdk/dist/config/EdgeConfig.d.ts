import { IEdgeConfig } from './IEdgeConfig';
import { FlagshipConfig } from './FlagshipConfig';
export declare class EdgeConfig extends FlagshipConfig {
    constructor(param?: Omit<IEdgeConfig, 'decisionMode'>);
}
