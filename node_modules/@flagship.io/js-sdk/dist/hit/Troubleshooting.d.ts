import { IFlagshipConfig } from '../config/index';
import { Diagnostic, IDiagnostic } from './Diagnostic';
export type TroubleshootingType = Omit<IDiagnostic & {
    config: IFlagshipConfig;
}, 'createdAt' | 'category' | 'type'>;
export declare class Troubleshooting extends Diagnostic {
    constructor(param: TroubleshootingType);
}
