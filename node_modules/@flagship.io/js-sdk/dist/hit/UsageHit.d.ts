import { IFlagshipConfig } from '../config/IFlagshipConfig';
import { Diagnostic, IDiagnostic } from './Diagnostic';
export type UsageHitType = Omit<IDiagnostic & {
    config: IFlagshipConfig;
}, 'createdAt' | 'category' | 'type'>;
export declare class UsageHit extends Diagnostic {
    constructor(param: UsageHitType);
}
